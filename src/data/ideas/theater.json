{
  "theme": {
    "slug": "theater",
    "name": "Theater & Live Performance",
    "emoji": "🎭",
    "audience": "directors, playwrights, performers, lighting and stage designers",
    "market_anchor": "the live performance market (~$30B globally) with >100K active companies"
  },
  "ideas": [
    {
      "id": "theater-scriptchain-ledger-0",
      "theme": "theater",
      "title": "ScriptChain Ledger",
      "pitch": "Securely track and timestamp script drafts through an immutable blockchain ledger.",
      "subDiscipline": "playwright collaboration",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contract ensures tamper-proof script version control and authorship verification.",
      "megaPrompt": "Build \"ScriptChain Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecurely track and timestamp script drafts through an immutable blockchain ledger.\nDiscipline: Theater & Live Performance (playwright collaboration).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contract ensures tamper-proof script version control and authorship verification.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ScriptChainLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ScriptChainLedger\n/// @notice Securely track and timestamp script drafts through an immutable blockchain ledger.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ScriptChainLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a playwright collaboration action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global digital scriptwriting tools market",
      "sam": "$200M — playwright collaboration software segment",
      "som": "$50M — early-adopting theater companies using blockchain for IP protection"
    },
    {
      "id": "theater-roleauction-platform-1",
      "theme": "theater",
      "title": "RoleAuction Platform",
      "pitch": "Decentralized audition bids let performers compete transparently for roles via smart contracts.",
      "subDiscipline": "casting marketplace",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts automate bidding and escrow without intermediaries, enhancing trust in casting.",
      "megaPrompt": "Build \"RoleAuction Platform\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nDecentralized audition bids let performers compete transparently for roles via smart contracts.\nDiscipline: Theater & Live Performance (casting marketplace).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts automate bidding and escrow without intermediaries, enhancing trust in casting.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/RoleAuctionPlatform.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title RoleAuctionPlatform\n/// @notice Decentralized audition bids let performers compete transparently for roles via smart contracts.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract RoleAuctionPlatform {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a casting marketplace action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global casting agency market",
      "sam": "$500M — online casting platforms",
      "som": "$100M — theater companies adopting blockchain casting tools"
    },
    {
      "id": "theater-stagelight-token-2",
      "theme": "theater",
      "title": "StageLight Token",
      "pitch": "Issue and trade usage rights for lighting designs using onchain tokens to protect creators.",
      "subDiscipline": "lighting rights management",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts enable verifiable licensing and royalty distribution for lighting designers.",
      "megaPrompt": "Build \"StageLight Token\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nIssue and trade usage rights for lighting designs using onchain tokens to protect creators.\nDiscipline: Theater & Live Performance (lighting rights management).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts enable verifiable licensing and royalty distribution for lighting designers.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StageLightToken.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title StageLightToken\n/// @notice Issue and trade usage rights for lighting designs using onchain tokens to protect creators.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract StageLightToken {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a lighting rights management action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — theater lighting equipment market",
      "sam": "$400M — lighting design intellectual property segment",
      "som": "$80M — digital rights management adoption in live performance"
    },
    {
      "id": "theater-promptchain-impro-3",
      "theme": "theater",
      "title": "PromptChain Impro",
      "pitch": "Generate and track custom improv prompts securely stored and shared via smart contracts.",
      "subDiscipline": "improvisation prompts",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia platform ensures provenance and fair sharing of creative prompts in improv communities.",
      "megaPrompt": "Build \"PromptChain Impro\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nGenerate and track custom improv prompts securely stored and shared via smart contracts.\nDiscipline: Theater & Live Performance (improvisation prompts).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia platform ensures provenance and fair sharing of creative prompts in improv communities.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PromptChainImpro.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title PromptChainImpro\n/// @notice Generate and track custom improv prompts securely stored and shared via smart contracts.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PromptChainImpro {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a improvisation prompts action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — improv theater training market",
      "sam": "$100M — improv prompt tools and apps",
      "som": "$20M — improv troupes experimenting with blockchain prompts"
    },
    {
      "id": "theater-tickettrust-nft-4",
      "theme": "theater",
      "title": "TicketTrust NFT",
      "pitch": "Use blockchain to mint tamper-proof NFT tickets preventing counterfeiting for live performances.",
      "subDiscipline": "ticket authenticity",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts provide secure, verifiable ownership records for live event tickets.",
      "megaPrompt": "Build \"TicketTrust NFT\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nUse blockchain to mint tamper-proof NFT tickets preventing counterfeiting for live performances.\nDiscipline: Theater & Live Performance (ticket authenticity).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts provide secure, verifiable ownership records for live event tickets.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/TicketTrustNFT.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title TicketTrustNFT\n/// @notice Use blockchain to mint tamper-proof NFT tickets preventing counterfeiting for live performances.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract TicketTrustNFT {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a ticket authenticity action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — global ticketing market",
      "sam": "$3B — digital ticketing platforms",
      "som": "$600M — blockchain ticket adoption in theaters and venues"
    },
    {
      "id": "theater-propchain-registry-5",
      "theme": "theater",
      "title": "PropChain Registry",
      "pitch": "Record and transfer ownership history of stage props transparently on a blockchain registry.",
      "subDiscipline": "prop ownership",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contract provides immutable tracking for valuable or rented props.",
      "megaPrompt": "Build \"PropChain Registry\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nRecord and transfer ownership history of stage props transparently on a blockchain registry.\nDiscipline: Theater & Live Performance (prop ownership).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contract provides immutable tracking for valuable or rented props.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PropChainRegistry.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title PropChainRegistry\n/// @notice Record and transfer ownership history of stage props transparently on a blockchain registry.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PropChainRegistry {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a prop ownership action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — theatrical prop rental and sales market",
      "sam": "$200M — prop inventory management systems",
      "som": "$40M — prop companies using blockchain asset tracking"
    },
    {
      "id": "theater-scriptroyalty-split-6",
      "theme": "theater",
      "title": "ScriptRoyalty Split",
      "pitch": "Automate fair payouts to playwrights, directors, and actors using onchain royalty splits.",
      "subDiscipline": "royalty distribution",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts ensure transparent, trustless revenue sharing from live performances.",
      "megaPrompt": "Build \"ScriptRoyalty Split\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAutomate fair payouts to playwrights, directors, and actors using onchain royalty splits.\nDiscipline: Theater & Live Performance (royalty distribution).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts ensure transparent, trustless revenue sharing from live performances.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ScriptRoyaltySplit.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ScriptRoyaltySplit\n/// @notice Automate fair payouts to playwrights, directors, and actors using onchain royalty splits.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ScriptRoyaltySplit {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a royalty distribution action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global live performance revenue",
      "sam": "$2B — royalty management software market",
      "som": "$400M — theater companies seeking automated royalty solutions"
    },
    {
      "id": "theater-stagecrew-dao-7",
      "theme": "theater",
      "title": "StageCrew DAO",
      "pitch": "Decentralize stage crew decisions and resource allocation through a transparent DAO governed by smart contracts.",
      "subDiscipline": "crew coordination",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia enables trustless voting and fund distribution among live production teams.",
      "megaPrompt": "Build \"StageCrew DAO\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nDecentralize stage crew decisions and resource allocation through a transparent DAO governed by smart contracts.\nDiscipline: Theater & Live Performance (crew coordination).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia enables trustless voting and fund distribution among live production teams.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StageCrewDAO.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title StageCrewDAO\n/// @notice Decentralize stage crew decisions and resource allocation through a transparent DAO governed by smart contracts.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract StageCrewDAO {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a crew coordination action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — stage crew management software",
      "sam": "$100M — decentralized team coordination tools",
      "som": "$25M — theater companies piloting DAO-based crew workflows"
    },
    {
      "id": "theater-playmint-nft-8",
      "theme": "theater",
      "title": "PlayMint NFT",
      "pitch": "Mint unique digital editions of scripts as NFTs to protect intellectual property and track sales.",
      "subDiscipline": "script NFTs",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts provide verifiable ownership and provenance for playwrights’ works.",
      "megaPrompt": "Build \"PlayMint NFT\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint unique digital editions of scripts as NFTs to protect intellectual property and track sales.\nDiscipline: Theater & Live Performance (script NFTs).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts provide verifiable ownership and provenance for playwrights’ works.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PlayMintNFT.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title PlayMintNFT\n/// @notice Mint unique digital editions of scripts as NFTs to protect intellectual property and track sales.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PlayMintNFT {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a script NFTs action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — digital publishing for theater",
      "sam": "$150M — NFT market for creative scripts",
      "som": "$30M — theatrical NFT experiments in IP protection"
    },
    {
      "id": "theater-actorstake-platform-9",
      "theme": "theater",
      "title": "ActorStake Platform",
      "pitch": "Allow fans to stake tokens on actors’ performances, rewarding success with blockchain incentives.",
      "subDiscipline": "performance staking",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts manage stakes and payouts transparently for audience engagement.",
      "megaPrompt": "Build \"ActorStake Platform\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAllow fans to stake tokens on actors’ performances, rewarding success with blockchain incentives.\nDiscipline: Theater & Live Performance (performance staking).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts manage stakes and payouts transparently for audience engagement.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ActorStakePlatform.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ActorStakePlatform\n/// @notice Allow fans to stake tokens on actors’ performances, rewarding success with blockchain incentives.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ActorStakePlatform {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a performance staking action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — live performance fan engagement market",
      "sam": "$400M — blockchain fan staking platforms",
      "som": "$80M — theaters exploring tokenized fan incentives"
    },
    {
      "id": "theater-lightcue-automation-10",
      "theme": "theater",
      "title": "LightCue Automation",
      "pitch": "Record and trigger lighting cues onchain to verify show timing and enable automated playback.",
      "subDiscipline": "lighting cue logs",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts guarantee immutable cue sequences and decentralize control of lighting shows.",
      "megaPrompt": "Build \"LightCue Automation\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nRecord and trigger lighting cues onchain to verify show timing and enable automated playback.\nDiscipline: Theater & Live Performance (lighting cue logs).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts guarantee immutable cue sequences and decentralize control of lighting shows.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/LightCueAutomation.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title LightCueAutomation\n/// @notice Record and trigger lighting cues onchain to verify show timing and enable automated playback.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract LightCueAutomation {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a lighting cue logs action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — live lighting control market",
      "sam": "$300M — automated lighting cue systems",
      "som": "$60M — theaters integrating blockchain control for lights"
    },
    {
      "id": "theater-scenechange-nfts-11",
      "theme": "theater",
      "title": "SceneChange NFTs",
      "pitch": "Artists mint NFT certificates of original scene designs for resale and licensing.",
      "subDiscipline": "scene ownership",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts prove scene design ownership and facilitate secondary market sales.",
      "megaPrompt": "Build \"SceneChange NFTs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nArtists mint NFT certificates of original scene designs for resale and licensing.\nDiscipline: Theater & Live Performance (scene ownership).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts prove scene design ownership and facilitate secondary market sales.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SceneChangeNFTs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SceneChangeNFTs\n/// @notice Artists mint NFT certificates of original scene designs for resale and licensing.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SceneChangeNFTs {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a scene ownership action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — stage design market",
      "sam": "$150M — digital licensing of scene concepts",
      "som": "$35M — scene designers trialing blockchain rights"
    },
    {
      "id": "theater-playbill-ledger-12",
      "theme": "theater",
      "title": "Playbill Ledger",
      "pitch": "Securely record production credits and changes transparently on a public ledger.",
      "subDiscipline": "production documentation",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts enable immutable, timestamped records of live performance production versions.",
      "megaPrompt": "Build \"Playbill Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecurely record production credits and changes transparently on a public ledger.\nDiscipline: Theater & Live Performance (production documentation).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts enable immutable, timestamped records of live performance production versions.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PlaybillLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title PlaybillLedger\n/// @notice Securely record production credits and changes transparently on a public ledger.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PlaybillLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a production documentation action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — playbill and program publishing",
      "sam": "$80M — production documentation services",
      "som": "$15M — theaters innovating version control with blockchain"
    },
    {
      "id": "theater-audiencevote-dao-13",
      "theme": "theater",
      "title": "AudienceVote DAO",
      "pitch": "Collect and tally audience votes on performances in a transparent, tamper-proof DAO system.",
      "subDiscipline": "live feedback",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contract ensures verified, unbiased audience participation results instantly.",
      "megaPrompt": "Build \"AudienceVote DAO\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCollect and tally audience votes on performances in a transparent, tamper-proof DAO system.\nDiscipline: Theater & Live Performance (live feedback).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contract ensures verified, unbiased audience participation results instantly.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/AudienceVoteDAO.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title AudienceVoteDAO\n/// @notice Collect and tally audience votes on performances in a transparent, tamper-proof DAO system.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract AudienceVoteDAO {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a live feedback action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — audience engagement tools",
      "sam": "$200M — live voting and polling platforms",
      "som": "$40M — theaters adopting blockchain audience feedback"
    },
    {
      "id": "theater-backstage-token-14",
      "theme": "theater",
      "title": "Backstage Token",
      "pitch": "Issue tokens granting secure, onchain backstage access for staff and VIPs.",
      "subDiscipline": "access control",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts provide verifiable, transferable digital credentials for event access control.",
      "megaPrompt": "Build \"Backstage Token\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nIssue tokens granting secure, onchain backstage access for staff and VIPs.\nDiscipline: Theater & Live Performance (access control).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts provide verifiable, transferable digital credentials for event access control.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/BackstageToken.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title BackstageToken\n/// @notice Issue tokens granting secure, onchain backstage access for staff and VIPs.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract BackstageToken {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a access control action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — access management market",
      "sam": "$160M — digital credentialing in live events",
      "som": "$32M — theaters piloting token-based backstage passes"
    },
    {
      "id": "theater-rehearsallog-chain-15",
      "theme": "theater",
      "title": "RehearsalLog Chain",
      "pitch": "Immutably store rehearsal attendance and notes to improve production accountability.",
      "subDiscipline": "rehearsal tracking",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts lock in rehearsal data to prevent disputes over participation.",
      "megaPrompt": "Build \"RehearsalLog Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nImmutably store rehearsal attendance and notes to improve production accountability.\nDiscipline: Theater & Live Performance (rehearsal tracking).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts lock in rehearsal data to prevent disputes over participation.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/RehearsalLogChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title RehearsalLogChain\n/// @notice Immutably store rehearsal attendance and notes to improve production accountability.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract RehearsalLogChain {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a rehearsal tracking action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$300M — rehearsal management tools",
      "sam": "$60M — production workflow tracking software",
      "som": "$10M — blockchain adoption for rehearsal logs"
    },
    {
      "id": "theater-scriptlens-dao-16",
      "theme": "theater",
      "title": "ScriptLens DAO",
      "pitch": "Enable playwrights and directors to co-own scripts via decentralized governance and revenue sharing.",
      "subDiscipline": "script co-creation",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts enable onchain voting and rights management between collaborators.",
      "megaPrompt": "Build \"ScriptLens DAO\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnable playwrights and directors to co-own scripts via decentralized governance and revenue sharing.\nDiscipline: Theater & Live Performance (script co-creation).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts enable onchain voting and rights management between collaborators.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ScriptLensDAO.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ScriptLensDAO\n/// @notice Enable playwrights and directors to co-own scripts via decentralized governance and revenue sharing.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ScriptLensDAO {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a script co-creation action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — collaborative writing software",
      "sam": "$200M — decentralized creative platforms",
      "som": "$40M — theater groups experimenting with co-creation DAOs"
    },
    {
      "id": "theater-sceneswap-marketplace-17",
      "theme": "theater",
      "title": "SceneSwap Marketplace",
      "pitch": "Trade and license scene and set designs through a verified onchain marketplace.",
      "subDiscipline": "design exchange",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts securely facilitate ownership transfer and licensing payments.",
      "megaPrompt": "Build \"SceneSwap Marketplace\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTrade and license scene and set designs through a verified onchain marketplace.\nDiscipline: Theater & Live Performance (design exchange).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts securely facilitate ownership transfer and licensing payments.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SceneSwapMarketplace.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SceneSwapMarketplace\n/// @notice Trade and license scene and set designs through a verified onchain marketplace.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SceneSwapMarketplace {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a design exchange action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — set design and rental market",
      "sam": "$250M — digital creative marketplaces",
      "som": "$50M — early adopters in blockchain design exchange"
    },
    {
      "id": "theater-performerbadge-nft-18",
      "theme": "theater",
      "title": "PerformerBadge NFT",
      "pitch": "Mint verifiable NFT badges certifying performer skills and achievements.",
      "subDiscipline": "credentials verification",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts provide immutable proof of credentials within the live performance industry.",
      "megaPrompt": "Build \"PerformerBadge NFT\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint verifiable NFT badges certifying performer skills and achievements.\nDiscipline: Theater & Live Performance (credentials verification).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts provide immutable proof of credentials within the live performance industry.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PerformerBadgeNFT.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title PerformerBadgeNFT\n/// @notice Mint verifiable NFT badges certifying performer skills and achievements.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PerformerBadgeNFT {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a credentials verification action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — talent credentialing market",
      "sam": "$140M — digital certification platforms",
      "som": "$28M — talent agencies piloting blockchain verification"
    },
    {
      "id": "theater-lightfx-tokenize-19",
      "theme": "theater",
      "title": "LightFX Tokenize",
      "pitch": "Tokenize and license stage special effects designs to productions worldwide securely.",
      "subDiscipline": "special effects licensing",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts enable transparent rights management for special effects creators.",
      "megaPrompt": "Build \"LightFX Tokenize\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTokenize and license stage special effects designs to productions worldwide securely.\nDiscipline: Theater & Live Performance (special effects licensing).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts enable transparent rights management for special effects creators.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/LightFXTokenize.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title LightFXTokenize\n/// @notice Tokenize and license stage special effects designs to productions worldwide securely.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract LightFXTokenize {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a special effects licensing action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — live performance special effects market",
      "sam": "$180M — digital licensing of effects designs",
      "som": "$36M — special effects designers adopting blockchain"
    },
    {
      "id": "theater-caststake-dao-20",
      "theme": "theater",
      "title": "CastStake DAO",
      "pitch": "Decentralize theater production funding by letting stakeholders vote and invest through DAO tokens.",
      "subDiscipline": "fundraising",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts ensure transparent disbursement and stakeholder governance.",
      "megaPrompt": "Build \"CastStake DAO\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nDecentralize theater production funding by letting stakeholders vote and invest through DAO tokens.\nDiscipline: Theater & Live Performance (fundraising).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts ensure transparent disbursement and stakeholder governance.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CastStakeDAO.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CastStakeDAO\n/// @notice Decentralize theater production funding by letting stakeholders vote and invest through DAO tokens.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CastStakeDAO {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a fundraising action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — theater production financing market",
      "sam": "$1B — crowdfunding platforms for arts",
      "som": "$200M — blockchain-powered arts fundraising pilots"
    },
    {
      "id": "theater-propstoken-swap-21",
      "theme": "theater",
      "title": "PropsToken Swap",
      "pitch": "Create a tokenized system for lending and borrowing props between theater companies.",
      "subDiscipline": "collaborative prop lending",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts automate lending agreements and track ownership transfers securely.",
      "megaPrompt": "Build \"PropsToken Swap\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate a tokenized system for lending and borrowing props between theater companies.\nDiscipline: Theater & Live Performance (collaborative prop lending).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts automate lending agreements and track ownership transfers securely.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PropsTokenSwap.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title PropsTokenSwap\n/// @notice Create a tokenized system for lending and borrowing props between theater companies.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PropsTokenSwap {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a collaborative prop lending action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — theatrical prop rental market",
      "sam": "$200M — shared asset management platforms",
      "som": "$40M — prop rental companies using blockchain tokens"
    },
    {
      "id": "theater-stagesound-dao-22",
      "theme": "theater",
      "title": "StageSound DAO",
      "pitch": "Coordinate and monetize collective sound design projects with transparent DAOs and revenue splits.",
      "subDiscipline": "sound design collaboration",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts enable trustless profit sharing and collaborative decision-making.",
      "megaPrompt": "Build \"StageSound DAO\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCoordinate and monetize collective sound design projects with transparent DAOs and revenue splits.\nDiscipline: Theater & Live Performance (sound design collaboration).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts enable trustless profit sharing and collaborative decision-making.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StageSoundDAO.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title StageSoundDAO\n/// @notice Coordinate and monetize collective sound design projects with transparent DAOs and revenue splits.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract StageSoundDAO {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a sound design collaboration action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — live sound design market",
      "sam": "$250M — collaborative music and sound platforms",
      "som": "$50M — sound designers testing blockchain collaboration"
    },
    {
      "id": "theater-encore-reward-23",
      "theme": "theater",
      "title": "Encore Reward",
      "pitch": "Tokenize audience rewards to incentivize repeat attendance and engagement at live performances.",
      "subDiscipline": "audience loyalty",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts manage transparent loyalty point issuance and redemption.",
      "megaPrompt": "Build \"Encore Reward\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTokenize audience rewards to incentivize repeat attendance and engagement at live performances.\nDiscipline: Theater & Live Performance (audience loyalty).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts manage transparent loyalty point issuance and redemption.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/EncoreReward.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title EncoreReward\n/// @notice Tokenize audience rewards to incentivize repeat attendance and engagement at live performances.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract EncoreReward {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a audience loyalty action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — audience loyalty programs in live events",
      "sam": "$600M — blockchain loyalty reward platforms",
      "som": "$120M — theaters integrating token-based rewards"
    },
    {
      "id": "theater-setpiece-provenance-24",
      "theme": "theater",
      "title": "SetPiece Provenance",
      "pitch": "Archive and authenticate historical set pieces’ origin and usage on an immutable blockchain record.",
      "subDiscipline": "historical archival",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts provide trusted provenance for valuable theatrical artifacts.",
      "megaPrompt": "Build \"SetPiece Provenance\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nArchive and authenticate historical set pieces’ origin and usage on an immutable blockchain record.\nDiscipline: Theater & Live Performance (historical archival).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts provide trusted provenance for valuable theatrical artifacts.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SetPieceProvenance.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SetPieceProvenance\n/// @notice Archive and authenticate historical set pieces’ origin and usage on an immutable blockchain record.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SetPieceProvenance {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a historical archival action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — theatrical artifact market",
      "sam": "$80M — digital archiving solutions",
      "som": "$16M — museums and theaters adopting blockchain provenance"
    },
    {
      "id": "theater-stagelight-archive-0",
      "theme": "theater",
      "title": "StageLight Archive",
      "pitch": "Pin and access dynamic stage lighting setups with permanent IPFS storage for reliable reuse and sharing.",
      "subDiscipline": "lighting design",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata's IPFS ensures lighting designs remain immutable and globally accessible for production teams.",
      "megaPrompt": "Build \"StageLight Archive\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin and access dynamic stage lighting setups with permanent IPFS storage for reliable reuse and sharing.\nDiscipline: Theater & Live Performance (lighting design).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata's IPFS ensures lighting designs remain immutable and globally accessible for production teams.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StageLightArchive.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogStageLightArchive\n/// @notice Pin and access dynamic stage lighting setups with permanent IPFS storage for reliable reuse and sharing.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogStageLightArchive {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the lighting design artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global theatrical lighting market",
      "sam": "$500M — digital lighting design software",
      "som": "$50M — niche stage lighting archival tools"
    },
    {
      "id": "theater-playscript-vault-1",
      "theme": "theater",
      "title": "PlayScript Vault",
      "pitch": "Securely save, share, and version play scripts on IPFS to preserve authorship and facilitate collaboration.",
      "subDiscipline": "playwriting",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata JWT upload guarantees immutable script versions pinned with unique CIDs for provenance.",
      "megaPrompt": "Build \"PlayScript Vault\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecurely save, share, and version play scripts on IPFS to preserve authorship and facilitate collaboration.\nDiscipline: Theater & Live Performance (playwriting).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata JWT upload guarantees immutable script versions pinned with unique CIDs for provenance.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PlayScriptVault.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogPlayScriptVault\n/// @notice Securely save, share, and version play scripts on IPFS to preserve authorship and facilitate collaboration.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogPlayScriptVault {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the playwriting artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global playwriting and publishing",
      "sam": "$700M — digital script collaboration tools",
      "som": "$70M — playwright-focused IPFS script repositories"
    },
    {
      "id": "theater-costumemood-board-2",
      "theme": "theater",
      "title": "CostumeMood Board",
      "pitch": "Pin and share costume sketches and mood boards on IPFS for collaborative costume creation.",
      "subDiscipline": "costume design",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata enables persistent, tamper-proof storage of costume design images and metadata.",
      "megaPrompt": "Build \"CostumeMood Board\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin and share costume sketches and mood boards on IPFS for collaborative costume creation.\nDiscipline: Theater & Live Performance (costume design).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata enables persistent, tamper-proof storage of costume design images and metadata.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CostumeMoodBoard.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogCostumeMoodBoard\n/// @notice Pin and share costume sketches and mood boards on IPFS for collaborative costume creation.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogCostumeMoodBoard {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the costume design artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — theatrical costume industry",
      "sam": "$400M — digital costume design platforms",
      "som": "$30M — costume-specific IPFS mood board tools"
    },
    {
      "id": "theater-actorportfolio-hub-3",
      "theme": "theater",
      "title": "ActorPortfolio Hub",
      "pitch": "Actors upload and pin portfolios including headshots and performance clips permanently on IPFS for casting accessibility.",
      "subDiscipline": "performance showcase",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS via Pinata ensures persistent availability and integrity of actor showcase content.",
      "megaPrompt": "Build \"ActorPortfolio Hub\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nActors upload and pin portfolios including headshots and performance clips permanently on IPFS for casting accessibility.\nDiscipline: Theater & Live Performance (performance showcase).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS via Pinata ensures persistent availability and integrity of actor showcase content.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ActorPortfolioHub.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogActorPortfolioHub\n/// @notice Actors upload and pin portfolios including headshots and performance clips permanently on IPFS for casting accessibility.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogActorPortfolioHub {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the performance showcase artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3.5B — global talent management",
      "sam": "$600M — digital actor portfolios",
      "som": "$40M — IPFS-based actor profile services"
    },
    {
      "id": "theater-setdesign-manifest-4",
      "theme": "theater",
      "title": "SetDesign Manifest",
      "pitch": "Store and share complex 3D set designs and manifests immutably on IPFS for production coordination.",
      "subDiscipline": "set design",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata's JWT upload pins large JSON manifests reliably to IPFS for decentralized access.",
      "megaPrompt": "Build \"SetDesign Manifest\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nStore and share complex 3D set designs and manifests immutably on IPFS for production coordination.\nDiscipline: Theater & Live Performance (set design).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata's JWT upload pins large JSON manifests reliably to IPFS for decentralized access.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SetDesignManifest.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogSetDesignManifest\n/// @notice Store and share complex 3D set designs and manifests immutably on IPFS for production coordination.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogSetDesignManifest {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the set design artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.5B — stage set production",
      "sam": "$450M — digital set design software",
      "som": "$35M — set design IPFS manifest platforms"
    },
    {
      "id": "theater-lightingcues-ledger-5",
      "theme": "theater",
      "title": "LightingCues Ledger",
      "pitch": "Save and distribute lighting cue sequences as pinned JSON on IPFS for synchronized performances.",
      "subDiscipline": "lighting cues",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata allows secure, permanent pinning of cue sequences accessible to all crew members.",
      "megaPrompt": "Build \"LightingCues Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSave and distribute lighting cue sequences as pinned JSON on IPFS for synchronized performances.\nDiscipline: Theater & Live Performance (lighting cues).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata allows secure, permanent pinning of cue sequences accessible to all crew members.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/LightingCuesLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogLightingCuesLedger\n/// @notice Save and distribute lighting cue sequences as pinned JSON on IPFS for synchronized performances.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogLightingCuesLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the lighting cues artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — theatrical lighting management",
      "sam": "$300M — digital cueing tools",
      "som": "$25M — IPFS-based lighting cue management"
    },
    {
      "id": "theater-playpromo-kit-6",
      "theme": "theater",
      "title": "PlayPromo Kit",
      "pitch": "Pin play promo images and metadata on IPFS to ensure immutable, globally accessible marketing assets.",
      "subDiscipline": "marketing collateral",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata provides permanent CID links ensuring marketing materials remain hosted reliably.",
      "megaPrompt": "Build \"PlayPromo Kit\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin play promo images and metadata on IPFS to ensure immutable, globally accessible marketing assets.\nDiscipline: Theater & Live Performance (marketing collateral).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata provides permanent CID links ensuring marketing materials remain hosted reliably.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PlayPromoKit.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogPlayPromoKit\n/// @notice Pin play promo images and metadata on IPFS to ensure immutable, globally accessible marketing assets.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogPlayPromoKit {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the marketing collateral artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — live performance marketing",
      "sam": "$800M — digital promotion platforms",
      "som": "$50M — IPFS-backed theatrical promo kits"
    },
    {
      "id": "theater-scriptrevision-chain-7",
      "theme": "theater",
      "title": "ScriptRevision Chain",
      "pitch": "Immutable version control for script edits stored and shared pinned on IPFS for playwrights and directors.",
      "subDiscipline": "script editing",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata's JWT upload guarantees tamper-proof, timestamped script versions accessible globally.",
      "megaPrompt": "Build \"ScriptRevision Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nImmutable version control for script edits stored and shared pinned on IPFS for playwrights and directors.\nDiscipline: Theater & Live Performance (script editing).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata's JWT upload guarantees tamper-proof, timestamped script versions accessible globally.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ScriptRevisionChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogScriptRevisionChain\n/// @notice Immutable version control for script edits stored and shared pinned on IPFS for playwrights and directors.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogScriptRevisionChain {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the script editing artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — script development market",
      "sam": "$600M — digital script editing",
      "som": "$45M — IPFS-based script version tracking"
    },
    {
      "id": "theater-setprops-catalog-8",
      "theme": "theater",
      "title": "SetProps Catalog",
      "pitch": "Pin and index prop images and descriptions on IPFS for collaborative rental and inventory management.",
      "subDiscipline": "props management",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata's IPFS pinning offers reliable permalinks for decentralized prop catalogs.",
      "megaPrompt": "Build \"SetProps Catalog\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin and index prop images and descriptions on IPFS for collaborative rental and inventory management.\nDiscipline: Theater & Live Performance (props management).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata's IPFS pinning offers reliable permalinks for decentralized prop catalogs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SetPropsCatalog.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogSetPropsCatalog\n/// @notice Pin and index prop images and descriptions on IPFS for collaborative rental and inventory management.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogSetPropsCatalog {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the props management artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.8B — theatrical prop rental",
      "sam": "$350M — digital prop databases",
      "som": "$20M — IPFS prop catalog solutions"
    },
    {
      "id": "theater-soundscape-archive-9",
      "theme": "theater",
      "title": "Soundscape Archive",
      "pitch": "Pin ambient and effect sound files with metadata on IPFS to ensure lasting availability and sharing.",
      "subDiscipline": "sound design",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata supports pinning large audio files and metadata immutably on IPFS for sound designers.",
      "megaPrompt": "Build \"Soundscape Archive\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin ambient and effect sound files with metadata on IPFS to ensure lasting availability and sharing.\nDiscipline: Theater & Live Performance (sound design).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata supports pinning large audio files and metadata immutably on IPFS for sound designers.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SoundscapeArchive.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogSoundscapeArchive\n/// @notice Pin ambient and effect sound files with metadata on IPFS to ensure lasting availability and sharing.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogSoundscapeArchive {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the sound design artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — live sound design",
      "sam": "$400M — digital sound libraries",
      "som": "$30M — IPFS-based sound archival tools"
    },
    {
      "id": "theater-performancereview-ledger-10",
      "theme": "theater",
      "title": "PerformanceReview Ledger",
      "pitch": "Store aggregated audience reviews as pinned JSON on IPFS guaranteeing unalterable feedback records.",
      "subDiscipline": "audience feedback",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata pins feedback data securely, ensuring transparency and trust in review storage.",
      "megaPrompt": "Build \"PerformanceReview Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nStore aggregated audience reviews as pinned JSON on IPFS guaranteeing unalterable feedback records.\nDiscipline: Theater & Live Performance (audience feedback).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata pins feedback data securely, ensuring transparency and trust in review storage.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PerformanceReviewLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogPerformanceReviewLedger\n/// @notice Store aggregated audience reviews as pinned JSON on IPFS guaranteeing unalterable feedback records.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogPerformanceReviewLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the audience feedback artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — live performance feedback",
      "sam": "$500M — digital review platforms",
      "som": "$25M — IPFS audience review services"
    },
    {
      "id": "theater-lightingpalette-exchange-11",
      "theme": "theater",
      "title": "LightingPalette Exchange",
      "pitch": "Pin and trade curated lighting color palettes as images and data on IPFS for designers to reuse.",
      "subDiscipline": "color study",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata’s JWT upload ensures palettes are permanently stored and shareable with CID references.",
      "megaPrompt": "Build \"LightingPalette Exchange\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin and trade curated lighting color palettes as images and data on IPFS for designers to reuse.\nDiscipline: Theater & Live Performance (color study).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata’s JWT upload ensures palettes are permanently stored and shareable with CID references.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/LightingPaletteExchange.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogLightingPaletteExchange\n/// @notice Pin and trade curated lighting color palettes as images and data on IPFS for designers to reuse.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogLightingPaletteExchange {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the color study artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — theatrical lighting color tools",
      "sam": "$250M — digital lighting palette apps",
      "som": "$15M — IPFS palette sharing platforms"
    },
    {
      "id": "theater-venuemap-index-12",
      "theme": "theater",
      "title": "VenueMap Index",
      "pitch": "Pin detailed venue and stage layout manifests on IPFS for directors and designers to coordinate setups.",
      "subDiscipline": "stage layout",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata supports pinning detailed JSON layout descriptors accessible for all production stakeholders.",
      "megaPrompt": "Build \"VenueMap Index\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin detailed venue and stage layout manifests on IPFS for directors and designers to coordinate setups.\nDiscipline: Theater & Live Performance (stage layout).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata supports pinning detailed JSON layout descriptors accessible for all production stakeholders.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/VenueMapIndex.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogVenueMapIndex\n/// @notice Pin detailed venue and stage layout manifests on IPFS for directors and designers to coordinate setups.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogVenueMapIndex {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the stage layout artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.3B — venue management software",
      "sam": "$400M — digital stage layout tools",
      "som": "$20M — IPFS venue mapping services"
    },
    {
      "id": "theater-monologue-archive-13",
      "theme": "theater",
      "title": "Monologue Archive",
      "pitch": "Pin curated monologue scripts and performance notes on IPFS preserving artistic intent unchangeably.",
      "subDiscipline": "performance repertoire",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata ensures monologue content is stored immutable with permanent CID links accessible worldwide.",
      "megaPrompt": "Build \"Monologue Archive\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin curated monologue scripts and performance notes on IPFS preserving artistic intent unchangeably.\nDiscipline: Theater & Live Performance (performance repertoire).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata ensures monologue content is stored immutable with permanent CID links accessible worldwide.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MonologueArchive.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogMonologueArchive\n/// @notice Pin curated monologue scripts and performance notes on IPFS preserving artistic intent unchangeably.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogMonologueArchive {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the performance repertoire artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — acting training resources",
      "sam": "$300M — digital monologue libraries",
      "som": "$18M — IPFS-based monologue archives"
    },
    {
      "id": "theater-costumetexture-bank-14",
      "theme": "theater",
      "title": "CostumeTexture Bank",
      "pitch": "Pin high-resolution fabric texture images and details on IPFS for costume designers’ resource sharing.",
      "subDiscipline": "fabric study",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata’s IPFS pinning preserves image quality and metadata for collaborative costume fabrics.",
      "megaPrompt": "Build \"CostumeTexture Bank\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin high-resolution fabric texture images and details on IPFS for costume designers’ resource sharing.\nDiscipline: Theater & Live Performance (fabric study).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata’s IPFS pinning preserves image quality and metadata for collaborative costume fabrics.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CostumeTextureBank.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogCostumeTextureBank\n/// @notice Pin high-resolution fabric texture images and details on IPFS for costume designers’ resource sharing.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogCostumeTextureBank {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the fabric study artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.8B — costume material sourcing",
      "sam": "$350M — digital fabric repositories",
      "som": "$22M — IPFS costume texture platforms"
    },
    {
      "id": "theater-propblueprint-cache-15",
      "theme": "theater",
      "title": "PropBlueprint Cache",
      "pitch": "Store and share detailed prop blueprints and assembly guides pinned immutably on IPFS for artisans.",
      "subDiscipline": "prop design",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata uploads ensure permanence and decentralized access to critical prop design files.",
      "megaPrompt": "Build \"PropBlueprint Cache\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nStore and share detailed prop blueprints and assembly guides pinned immutably on IPFS for artisans.\nDiscipline: Theater & Live Performance (prop design).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata uploads ensure permanence and decentralized access to critical prop design files.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PropBlueprintCache.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogPropBlueprintCache\n/// @notice Store and share detailed prop blueprints and assembly guides pinned immutably on IPFS for artisans.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogPropBlueprintCache {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the prop design artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.7B — prop manufacturing",
      "sam": "$320M — digital prop design platforms",
      "som": "$18M — IPFS-based blueprint archives"
    },
    {
      "id": "theater-directorstoryboard-hub-16",
      "theme": "theater",
      "title": "DirectorStoryboard Hub",
      "pitch": "Pin storyboards as image sequences on IPFS to maintain integrity and facilitate collaboration across teams.",
      "subDiscipline": "storyboarding",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata enables permanent IPFS pinning of multi-image storyboards with reliable CID referencing.",
      "megaPrompt": "Build \"DirectorStoryboard Hub\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin storyboards as image sequences on IPFS to maintain integrity and facilitate collaboration across teams.\nDiscipline: Theater & Live Performance (storyboarding).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata enables permanent IPFS pinning of multi-image storyboards with reliable CID referencing.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DirectorStoryboardHub.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogDirectorStoryboardHub\n/// @notice Pin storyboards as image sequences on IPFS to maintain integrity and facilitate collaboration across teams.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogDirectorStoryboardHub {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the storyboarding artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — theatrical directing tools",
      "sam": "$450M — digital storyboarding software",
      "som": "$25M — IPFS storyboard sharing platforms"
    },
    {
      "id": "theater-lightingfixture-db-17",
      "theme": "theater",
      "title": "LightingFixture DB",
      "pitch": "Pin detailed lighting fixture profiles and manuals on IPFS for technicians and designers to access anytime.",
      "subDiscipline": "equipment catalog",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata secures fixture data immutably with CID links ensuring decentralization and longevity.",
      "megaPrompt": "Build \"LightingFixture DB\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin detailed lighting fixture profiles and manuals on IPFS for technicians and designers to access anytime.\nDiscipline: Theater & Live Performance (equipment catalog).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata secures fixture data immutably with CID links ensuring decentralization and longevity.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/LightingFixtureDB.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogLightingFixtureDB\n/// @notice Pin detailed lighting fixture profiles and manuals on IPFS for technicians and designers to access anytime.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogLightingFixtureDB {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the equipment catalog artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — theatrical equipment databases",
      "sam": "$600M — lighting tech catalog apps",
      "som": "$40M — IPFS-backed fixture info services"
    },
    {
      "id": "theater-virtualstage-seeds-18",
      "theme": "theater",
      "title": "VirtualStage Seeds",
      "pitch": "Pin virtual stage assets and scene manifests on IPFS for seamless integration in live and hybrid performances.",
      "subDiscipline": "virtual set design",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata's IPFS pinning provides reliable decentralized storage for virtual stage asset distribution.",
      "megaPrompt": "Build \"VirtualStage Seeds\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin virtual stage assets and scene manifests on IPFS for seamless integration in live and hybrid performances.\nDiscipline: Theater & Live Performance (virtual set design).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata's IPFS pinning provides reliable decentralized storage for virtual stage asset distribution.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/VirtualStageSeeds.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogVirtualStageSeeds\n/// @notice Pin virtual stage assets and scene manifests on IPFS for seamless integration in live and hybrid performances.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogVirtualStageSeeds {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the virtual set design artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.5B — virtual performance tech",
      "sam": "$500M — virtual set design software",
      "som": "$35M — IPFS virtual staging tools"
    },
    {
      "id": "theater-lightingpattern-archive-19",
      "theme": "theater",
      "title": "LightingPattern Archive",
      "pitch": "Pin and share choreographed lighting patterns and sequences immutably on IPFS for repeatable productions.",
      "subDiscipline": "lighting choreography",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata ensures choreography data is stored permanently for consistent performance replication.",
      "megaPrompt": "Build \"LightingPattern Archive\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin and share choreographed lighting patterns and sequences immutably on IPFS for repeatable productions.\nDiscipline: Theater & Live Performance (lighting choreography).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata ensures choreography data is stored permanently for consistent performance replication.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/LightingPatternArchive.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogLightingPatternArchive\n/// @notice Pin and share choreographed lighting patterns and sequences immutably on IPFS for repeatable productions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogLightingPatternArchive {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the lighting choreography artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — theatrical lighting choreography",
      "sam": "$300M — lighting sequence software",
      "som": "$20M — IPFS-based lighting pattern archives"
    },
    {
      "id": "theater-performancemanifesto-20",
      "theme": "theater",
      "title": "PerformanceManifesto",
      "pitch": "Pin performance manifestos and artistic statements as permanent JSON files on IPFS for posterity and access.",
      "subDiscipline": "creative documentation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata’s IPFS pinning offers an immutable record for artists’ creative intent and public sharing.",
      "megaPrompt": "Build \"PerformanceManifesto\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin performance manifestos and artistic statements as permanent JSON files on IPFS for posterity and access.\nDiscipline: Theater & Live Performance (creative documentation).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata’s IPFS pinning offers an immutable record for artists’ creative intent and public sharing.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PerformanceManifesto.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogPerformanceManifesto\n/// @notice Pin performance manifestos and artistic statements as permanent JSON files on IPFS for posterity and access.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogPerformanceManifesto {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the creative documentation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — artistic documentation market",
      "sam": "$300M — digital artist statement platforms",
      "som": "$15M — IPFS manifesto hosting services"
    },
    {
      "id": "theater-stagesafety-logs-21",
      "theme": "theater",
      "title": "StageSafety Logs",
      "pitch": "Pin safety inspection reports and compliance logs on IPFS ensuring tamper-proof records for venues and crews.",
      "subDiscipline": "safety compliance",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata guarantees permanent, tamper-resistant storage of critical safety documentation on IPFS.",
      "megaPrompt": "Build \"StageSafety Logs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin safety inspection reports and compliance logs on IPFS ensuring tamper-proof records for venues and crews.\nDiscipline: Theater & Live Performance (safety compliance).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata guarantees permanent, tamper-resistant storage of critical safety documentation on IPFS.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StageSafetyLogs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogStageSafetyLogs\n/// @notice Pin safety inspection reports and compliance logs on IPFS ensuring tamper-proof records for venues and crews.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogStageSafetyLogs {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the safety compliance artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — theatrical safety management",
      "sam": "$200M — digital safety compliance tools",
      "som": "$12M — IPFS-based safety log platforms"
    },
    {
      "id": "theater-actorlinetracker-22",
      "theme": "theater",
      "title": "ActorLineTracker",
      "pitch": "Pin annotated lines and rehearsal notes as immutable JSON to IPFS for actors’ reliable study aids.",
      "subDiscipline": "line memorization",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata enables secure, permanent pinning of rehearsal content accessible across devices.",
      "megaPrompt": "Build \"ActorLineTracker\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin annotated lines and rehearsal notes as immutable JSON to IPFS for actors’ reliable study aids.\nDiscipline: Theater & Live Performance (line memorization).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata enables secure, permanent pinning of rehearsal content accessible across devices.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ActorLineTracker.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogActorLineTracker\n/// @notice Pin annotated lines and rehearsal notes as immutable JSON to IPFS for actors’ reliable study aids.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogActorLineTracker {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the line memorization artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — acting education market",
      "sam": "$250M — digital memorization tools",
      "som": "$15M — IPFS actor rehearsal aids"
    },
    {
      "id": "theater-setlighting-simulator-23",
      "theme": "theater",
      "title": "SetLighting Simulator",
      "pitch": "Pin lighting simulation data and images on IPFS for real-time, distributed production previews.",
      "subDiscipline": "previsualization",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata’s IPFS storage provides permanent hosting for complex simulation assets shared globally.",
      "megaPrompt": "Build \"SetLighting Simulator\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin lighting simulation data and images on IPFS for real-time, distributed production previews.\nDiscipline: Theater & Live Performance (previsualization).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata’s IPFS storage provides permanent hosting for complex simulation assets shared globally.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SetLightingSimulator.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogSetLightingSimulator\n/// @notice Pin lighting simulation data and images on IPFS for real-time, distributed production previews.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogSetLightingSimulator {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the previsualization artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — performance previsualization",
      "sam": "$400M — digital lighting simulator platforms",
      "som": "$25M — IPFS stage lighting simulation"
    },
    {
      "id": "theater-playbill-archive-24",
      "theme": "theater",
      "title": "PlayBill Archive",
      "pitch": "Pin historic and current playbill designs on IPFS for theatrical heritage and design reference.",
      "subDiscipline": "program design",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata offers permanent, decentralized hosting of program designs as image and JSON manifests.",
      "megaPrompt": "Build \"PlayBill Archive\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin historic and current playbill designs on IPFS for theatrical heritage and design reference.\nDiscipline: Theater & Live Performance (program design).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata offers permanent, decentralized hosting of program designs as image and JSON manifests.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PlayBillArchive.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogPlayBillArchive\n/// @notice Pin historic and current playbill designs on IPFS for theatrical heritage and design reference.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogPlayBillArchive {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the program design artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — theatrical print design",
      "sam": "$150M — digital program archives",
      "som": "$10M — IPFS playbill archival platforms"
    },
    {
      "id": "theater-smartstage-access-0",
      "theme": "theater",
      "title": "SmartStage Access",
      "pitch": "Enable seamless, gas-free ticketing and VIP experiences via social login and sponsored transactions.",
      "subDiscipline": "audience engagement",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social and sponsored tx enable hassle-free, onchain ticketing without user gas fees.",
      "megaPrompt": "Build \"SmartStage Access\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnable seamless, gas-free ticketing and VIP experiences via social login and sponsored transactions.\nDiscipline: Theater & Live Performance (audience engagement).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social and sponsored tx enable hassle-free, onchain ticketing without user gas fees.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SmartStageAccess.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogSmartStageAccess\n/// @notice Enable seamless, gas-free ticketing and VIP experiences via social login and sponsored transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogSmartStageAccess {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every audience engagement action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global live event ticketing market",
      "sam": "$2B — digital ticketing for theater performances",
      "som": "$300M — theaters adopting blockchain ticketing"
    },
    {
      "id": "theater-scriptshare-vault-1",
      "theme": "theater",
      "title": "ScriptShare Vault",
      "pitch": "Facilitate secure, collaborative script revisions with onchain identity and no-fee transactions.",
      "subDiscipline": "script collaboration",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social ensures verified users and sponsored tx enable frictionless edits.",
      "megaPrompt": "Build \"ScriptShare Vault\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nFacilitate secure, collaborative script revisions with onchain identity and no-fee transactions.\nDiscipline: Theater & Live Performance (script collaboration).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social ensures verified users and sponsored tx enable frictionless edits.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ScriptShareVault.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogScriptShareVault\n/// @notice Facilitate secure, collaborative script revisions with onchain identity and no-fee transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogScriptShareVault {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every script collaboration action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — global playwriting markets",
      "sam": "$120M — digital script collaboration tools",
      "som": "$25M — theater groups using blockchain collaboration"
    },
    {
      "id": "theater-propchain-ledger-2",
      "theme": "theater",
      "title": "PropChain Ledger",
      "pitch": "Track and verify theater prop history and ownership with private social access and free transactions.",
      "subDiscipline": "prop provenance",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social secures user access; sponsored tx keeps record updates affordable.",
      "megaPrompt": "Build \"PropChain Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTrack and verify theater prop history and ownership with private social access and free transactions.\nDiscipline: Theater & Live Performance (prop provenance).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social secures user access; sponsored tx keeps record updates affordable.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PropChainLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogPropChainLedger\n/// @notice Track and verify theater prop history and ownership with private social access and free transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogPropChainLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every prop provenance action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$300M — global theater prop markets",
      "sam": "$80M — prop rental and sales digitization",
      "som": "$15M — blockchain prop tracking adoption"
    },
    {
      "id": "theater-lightcue-sync-3",
      "theme": "theater",
      "title": "LightCue Sync",
      "pitch": "Collaborate on lighting cues live with instant, gasless onchain updates through social login.",
      "subDiscipline": "lighting design",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social streamlines designer access; sponsored transactions enable real-time updates.",
      "megaPrompt": "Build \"LightCue Sync\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCollaborate on lighting cues live with instant, gasless onchain updates through social login.\nDiscipline: Theater & Live Performance (lighting design).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social streamlines designer access; sponsored transactions enable real-time updates.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/LightCueSync.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogLightCueSync\n/// @notice Collaborate on lighting cues live with instant, gasless onchain updates through social login.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogLightCueSync {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every lighting design action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — global stage lighting equipment and design",
      "sam": "$300M — digital lighting design collaboration",
      "som": "$50M — theaters using blockchain for lighting"
    },
    {
      "id": "theater-actortrust-ledger-4",
      "theme": "theater",
      "title": "ActorTrust Ledger",
      "pitch": "Store and verify actor resumes and roles securely with social login and no gas fees.",
      "subDiscipline": "performance credentials",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social offers identity verification; sponsored tx allow easy credential updates.",
      "megaPrompt": "Build \"ActorTrust Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nStore and verify actor resumes and roles securely with social login and no gas fees.\nDiscipline: Theater & Live Performance (performance credentials).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social offers identity verification; sponsored tx allow easy credential updates.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ActorTrustLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogActorTrustLedger\n/// @notice Store and verify actor resumes and roles securely with social login and no gas fees.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogActorTrustLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every performance credentials action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — global actor management and casting market",
      "sam": "$3B — digital acting portfolios",
      "som": "$400M — digital credential verification for actors"
    },
    {
      "id": "theater-sceneswap-marketplace-5",
      "theme": "theater",
      "title": "SceneSwap Marketplace",
      "pitch": "Enable gas-free peer-to-peer trading of set designs via social wallet and sponsored tx.",
      "subDiscipline": "set design exchange",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social authenticates users; sponsored transactions enable fee-less trades.",
      "megaPrompt": "Build \"SceneSwap Marketplace\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnable gas-free peer-to-peer trading of set designs via social wallet and sponsored tx.\nDiscipline: Theater & Live Performance (set design exchange).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social authenticates users; sponsored transactions enable fee-less trades.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SceneSwapMarketplace.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogSceneSwapMarketplace\n/// @notice Enable gas-free peer-to-peer trading of set designs via social wallet and sponsored tx.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogSceneSwapMarketplace {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every set design exchange action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — global set design market",
      "sam": "$200M — online set design marketplaces",
      "som": "$30M — blockchain-enabled design exchanges"
    },
    {
      "id": "theater-playbill-nfts-6",
      "theme": "theater",
      "title": "Playbill NFTs",
      "pitch": "Create and distribute collectible digital playbills with embedded social wallet and free minting.",
      "subDiscipline": "memorabilia",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social enables easy user onboarding; sponsored tx remove minting friction.",
      "megaPrompt": "Build \"Playbill NFTs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate and distribute collectible digital playbills with embedded social wallet and free minting.\nDiscipline: Theater & Live Performance (memorabilia).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social enables easy user onboarding; sponsored tx remove minting friction.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PlaybillNFTs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogPlaybillNFTs\n/// @notice Create and distribute collectible digital playbills with embedded social wallet and free minting.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogPlaybillNFTs {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every memorabilia action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — theater memorabilia market",
      "sam": "$300M — digital collectibles sector",
      "som": "$50M — NFT playbill startups"
    },
    {
      "id": "theater-audiencevote-live-7",
      "theme": "theater",
      "title": "AudienceVote Live",
      "pitch": "Let audiences vote live on performance elements using social login and gasless onchain votes.",
      "subDiscipline": "interactive performances",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social verifies unique voters; sponsored tx ensure smooth, no-fee tallying.",
      "megaPrompt": "Build \"AudienceVote Live\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nLet audiences vote live on performance elements using social login and gasless onchain votes.\nDiscipline: Theater & Live Performance (interactive performances).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social verifies unique voters; sponsored tx ensure smooth, no-fee tallying.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/AudienceVoteLive.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogAudienceVoteLive\n/// @notice Let audiences vote live on performance elements using social login and gasless onchain votes.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogAudienceVoteLive {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every interactive performances action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global interactive theater experiences",
      "sam": "$1B — digital audience participation tech",
      "som": "$150M — blockchain-powered voting in shows"
    },
    {
      "id": "theater-backstage-pass-8",
      "theme": "theater",
      "title": "Backstage Pass",
      "pitch": "Offer backstage digital passes with seamless social wallet login and free sponsorship tx.",
      "subDiscipline": "exclusive access",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social ensures verified fans; sponsored tx keep access delivery fee-free.",
      "megaPrompt": "Build \"Backstage Pass\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nOffer backstage digital passes with seamless social wallet login and free sponsorship tx.\nDiscipline: Theater & Live Performance (exclusive access).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social ensures verified fans; sponsored tx keep access delivery fee-free.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/BackstagePass.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogBackstagePass\n/// @notice Offer backstage digital passes with seamless social wallet login and free sponsorship tx.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogBackstagePass {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every exclusive access action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — live event VIP access market",
      "sam": "$500M — digital VIP experiences",
      "som": "$80M — theaters using blockchain access passes"
    },
    {
      "id": "theater-rehearsalrecord-9",
      "theme": "theater",
      "title": "RehearsalRecord",
      "pitch": "Securely archive rehearsal footage and notes with social wallet authentication and free updates.",
      "subDiscipline": "performance archives",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social grants creator access; sponsored transactions enable unlimited archival without cost.",
      "megaPrompt": "Build \"RehearsalRecord\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecurely archive rehearsal footage and notes with social wallet authentication and free updates.\nDiscipline: Theater & Live Performance (performance archives).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social grants creator access; sponsored transactions enable unlimited archival without cost.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/RehearsalRecord.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogRehearsalRecord\n/// @notice Securely archive rehearsal footage and notes with social wallet authentication and free updates.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogRehearsalRecord {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every performance archives action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — theater production documentation market",
      "sam": "$150M — digital rehearsal tools",
      "som": "$25M — blockchain rehearsal archiving"
    },
    {
      "id": "theater-sponsorspotlight-10",
      "theme": "theater",
      "title": "SponsorSpotlight",
      "pitch": "Connect sponsors and shows with onchain contracts signed via social login and no gas fees.",
      "subDiscipline": "performance sponsorship",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social enables trust; sponsored tx streamline contract execution without user costs.",
      "megaPrompt": "Build \"SponsorSpotlight\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nConnect sponsors and shows with onchain contracts signed via social login and no gas fees.\nDiscipline: Theater & Live Performance (performance sponsorship).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social enables trust; sponsored tx streamline contract execution without user costs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SponsorSpotlight.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogSponsorSpotlight\n/// @notice Connect sponsors and shows with onchain contracts signed via social login and no gas fees.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogSponsorSpotlight {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every performance sponsorship action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global event sponsorship market",
      "sam": "$1.5B — theater sponsorship deals",
      "som": "$200M — blockchain-based sponsorships"
    },
    {
      "id": "theater-roleswap-network-11",
      "theme": "theater",
      "title": "RoleSwap Network",
      "pitch": "Facilitate secure actor role exchanges and confirmations with social wallets and sponsored transactions.",
      "subDiscipline": "cast management",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social verifies identities; sponsored tx allow gasless role swaps.",
      "megaPrompt": "Build \"RoleSwap Network\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nFacilitate secure actor role exchanges and confirmations with social wallets and sponsored transactions.\nDiscipline: Theater & Live Performance (cast management).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social verifies identities; sponsored tx allow gasless role swaps.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/RoleSwapNetwork.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogRoleSwapNetwork\n/// @notice Facilitate secure actor role exchanges and confirmations with social wallets and sponsored transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogRoleSwapNetwork {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every cast management action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global casting and personnel management",
      "sam": "$3B — digital casting platforms",
      "som": "$400M — blockchain casting exchanges"
    },
    {
      "id": "theater-stagedesign-dao-12",
      "theme": "theater",
      "title": "StageDesign DAO",
      "pitch": "Create decentralized design collectives with gasless voting and social wallet onboarding.",
      "subDiscipline": "design collaboration",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social ensures verified participation; sponsored tx enable costless DAO governance.",
      "megaPrompt": "Build \"StageDesign DAO\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate decentralized design collectives with gasless voting and social wallet onboarding.\nDiscipline: Theater & Live Performance (design collaboration).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social ensures verified participation; sponsored tx enable costless DAO governance.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StageDesignDAO.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogStageDesignDAO\n/// @notice Create decentralized design collectives with gasless voting and social wallet onboarding.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogStageDesignDAO {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every design collaboration action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — theater design collaboration",
      "sam": "$300M — digital creative DAOs",
      "som": "$40M — blockchain DAOs in live arts"
    },
    {
      "id": "theater-scripttoken-rights-13",
      "theme": "theater",
      "title": "ScriptToken Rights",
      "pitch": "Manage and transfer script rights securely via social wallet and sponsored onchain transactions.",
      "subDiscipline": "rights management",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social assures identity; sponsored tx facilitate free rights transfers.",
      "megaPrompt": "Build \"ScriptToken Rights\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nManage and transfer script rights securely via social wallet and sponsored onchain transactions.\nDiscipline: Theater & Live Performance (rights management).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social assures identity; sponsored tx facilitate free rights transfers.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ScriptTokenRights.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogScriptTokenRights\n/// @notice Manage and transfer script rights securely via social wallet and sponsored onchain transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogScriptTokenRights {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every rights management action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — global script rights market",
      "sam": "$250M — digital IP management",
      "som": "$30M — blockchain rights transfers"
    },
    {
      "id": "theater-cuepoint-tracker-14",
      "theme": "theater",
      "title": "CuePoint Tracker",
      "pitch": "Provide directors with gasless onchain cue tracking and adjustments via social login.",
      "subDiscipline": "live direction",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social manages secure access; sponsored tx enable instant, free cue updates.",
      "megaPrompt": "Build \"CuePoint Tracker\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nProvide directors with gasless onchain cue tracking and adjustments via social login.\nDiscipline: Theater & Live Performance (live direction).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social manages secure access; sponsored tx enable instant, free cue updates.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CuePointTracker.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogCuePointTracker\n/// @notice Provide directors with gasless onchain cue tracking and adjustments via social login.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogCuePointTracker {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every live direction action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — live direction tools",
      "sam": "$300M — digital stage direction software",
      "som": "$45M — blockchain-powered cue tracking"
    },
    {
      "id": "theater-actormint-ledger-15",
      "theme": "theater",
      "title": "ActorMint Ledger",
      "pitch": "Mint and distribute unique actor NFTs representing roles and performances with gasless minting.",
      "subDiscipline": "performance NFTs",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social verifies actors; sponsored tx remove minting friction.",
      "megaPrompt": "Build \"ActorMint Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint and distribute unique actor NFTs representing roles and performances with gasless minting.\nDiscipline: Theater & Live Performance (performance NFTs).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social verifies actors; sponsored tx remove minting friction.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ActorMintLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogActorMintLedger\n/// @notice Mint and distribute unique actor NFTs representing roles and performances with gasless minting.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogActorMintLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every performance NFTs action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global NFT entertainment market",
      "sam": "$500M — performance-related NFTs",
      "som": "$70M — blockchain adoption among actors"
    },
    {
      "id": "theater-setbuild-contracts-16",
      "theme": "theater",
      "title": "SetBuild Contracts",
      "pitch": "Automate set construction contracts with social wallet signing and no-fee onchain transactions.",
      "subDiscipline": "production agreements",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social ensures contract parties; sponsored tx enable cost-free contract execution.",
      "megaPrompt": "Build \"SetBuild Contracts\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAutomate set construction contracts with social wallet signing and no-fee onchain transactions.\nDiscipline: Theater & Live Performance (production agreements).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social ensures contract parties; sponsored tx enable cost-free contract execution.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SetBuildContracts.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogSetBuildContracts\n/// @notice Automate set construction contracts with social wallet signing and no-fee onchain transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogSetBuildContracts {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every production agreements action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — theater production services",
      "sam": "$600M — digital contract tools",
      "som": "$90M — blockchain contract adoption"
    },
    {
      "id": "theater-lightingtoken-rewards-17",
      "theme": "theater",
      "title": "LightingToken Rewards",
      "pitch": "Issue tokenized rewards to lighting crews with social wallet onboarding and sponsored tx.",
      "subDiscipline": "crew incentives",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social authenticates recipients; sponsored tx enable frictionless token transfers.",
      "megaPrompt": "Build \"LightingToken Rewards\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nIssue tokenized rewards to lighting crews with social wallet onboarding and sponsored tx.\nDiscipline: Theater & Live Performance (crew incentives).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social authenticates recipients; sponsored tx enable frictionless token transfers.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/LightingTokenRewards.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogLightingTokenRewards\n/// @notice Issue tokenized rewards to lighting crews with social wallet onboarding and sponsored tx.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogLightingTokenRewards {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every crew incentives action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — stage crew management",
      "sam": "$150M — digital workforce incentives",
      "som": "$20M — blockchain-driven crew rewards"
    },
    {
      "id": "theater-playwright-guild-18",
      "theme": "theater",
      "title": "Playwright Guild",
      "pitch": "Create tokenized playwright guilds with seamless social login and gasless member transactions.",
      "subDiscipline": "community membership",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social simplifies membership; sponsored tx enable costless governance.",
      "megaPrompt": "Build \"Playwright Guild\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate tokenized playwright guilds with seamless social login and gasless member transactions.\nDiscipline: Theater & Live Performance (community membership).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social simplifies membership; sponsored tx enable costless governance.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PlaywrightGuild.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogPlaywrightGuild\n/// @notice Create tokenized playwright guilds with seamless social login and gasless member transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogPlaywrightGuild {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every community membership action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — playwright associations",
      "sam": "$100M — digital community platforms",
      "som": "$15M — blockchain-based guilds"
    },
    {
      "id": "theater-audiencetip-jar-19",
      "theme": "theater",
      "title": "AudienceTip Jar",
      "pitch": "Let audiences tip performers instantly with social wallets and sponsored transaction gaslessness.",
      "subDiscipline": "fan monetization",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social facilitates fan identity; sponsored tx enable free tips.",
      "megaPrompt": "Build \"AudienceTip Jar\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nLet audiences tip performers instantly with social wallets and sponsored transaction gaslessness.\nDiscipline: Theater & Live Performance (fan monetization).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social facilitates fan identity; sponsored tx enable free tips.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/AudienceTipJar.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogAudienceTipJar\n/// @notice Let audiences tip performers instantly with social wallets and sponsored transaction gaslessness.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogAudienceTipJar {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every fan monetization action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — live performance monetization",
      "sam": "$700M — digital tipping platforms",
      "som": "$100M — blockchain tip solutions"
    },
    {
      "id": "theater-promochain-posters-20",
      "theme": "theater",
      "title": "PromoChain Posters",
      "pitch": "Distribute limited-edition promo posters as NFTs with easy social wallet minting and free tx.",
      "subDiscipline": "marketing assets",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social verifies fans; sponsored tx remove minting costs.",
      "megaPrompt": "Build \"PromoChain Posters\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nDistribute limited-edition promo posters as NFTs with easy social wallet minting and free tx.\nDiscipline: Theater & Live Performance (marketing assets).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social verifies fans; sponsored tx remove minting costs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PromoChainPosters.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogPromoChainPosters\n/// @notice Distribute limited-edition promo posters as NFTs with easy social wallet minting and free tx.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogPromoChainPosters {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every marketing assets action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — live show marketing",
      "sam": "$300M — digital promotional collectibles",
      "som": "$40M — NFT promo innovations"
    },
    {
      "id": "theater-scriptarchive-dao-21",
      "theme": "theater",
      "title": "ScriptArchive DAO",
      "pitch": "Preserve scripts onchain with collaborative DAO management and gasless transactions via social wallets.",
      "subDiscipline": "preservation",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social enables decentralized governance; sponsored tx reduce cost barriers.",
      "megaPrompt": "Build \"ScriptArchive DAO\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPreserve scripts onchain with collaborative DAO management and gasless transactions via social wallets.\nDiscipline: Theater & Live Performance (preservation).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social enables decentralized governance; sponsored tx reduce cost barriers.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ScriptArchiveDAO.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogScriptArchiveDAO\n/// @notice Preserve scripts onchain with collaborative DAO management and gasless transactions via social wallets.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogScriptArchiveDAO {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every preservation action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — script preservation",
      "sam": "$200M — digital literary archives",
      "som": "$30M — blockchain archival DAOs"
    },
    {
      "id": "theater-virtualstage-access-22",
      "theme": "theater",
      "title": "VirtualStage Access",
      "pitch": "Grant gasless access to hybrid live/virtual shows via social wallet login and sponsored tx.",
      "subDiscipline": "hybrid performances",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social simplifies ticketing; sponsored tx enable seamless access without gas.",
      "megaPrompt": "Build \"VirtualStage Access\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nGrant gasless access to hybrid live/virtual shows via social wallet login and sponsored tx.\nDiscipline: Theater & Live Performance (hybrid performances).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social simplifies ticketing; sponsored tx enable seamless access without gas.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/VirtualStageAccess.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogVirtualStageAccess\n/// @notice Grant gasless access to hybrid live/virtual shows via social wallet login and sponsored tx.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogVirtualStageAccess {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every hybrid performances action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — hybrid live/virtual performances",
      "sam": "$1.5B — digital event access",
      "som": "$200M — blockchain ticketing for hybrid shows"
    },
    {
      "id": "theater-critiquechain-feedback-23",
      "theme": "theater",
      "title": "CritiqueChain Feedback",
      "pitch": "Collect verified audience critiques onchain with social login and no-fee feedback transactions.",
      "subDiscipline": "performance reviews",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social verifies identities; sponsored tx make feedback free and easy.",
      "megaPrompt": "Build \"CritiqueChain Feedback\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCollect verified audience critiques onchain with social login and no-fee feedback transactions.\nDiscipline: Theater & Live Performance (performance reviews).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social verifies identities; sponsored tx make feedback free and easy.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CritiqueChainFeedback.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogCritiqueChainFeedback\n/// @notice Collect verified audience critiques onchain with social login and no-fee feedback transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogCritiqueChainFeedback {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every performance reviews action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — performance feedback market",
      "sam": "$200M — digital critique platforms",
      "som": "$30M — blockchain feedback adoption"
    },
    {
      "id": "theater-setinventory-nfts-24",
      "theme": "theater",
      "title": "SetInventory NFTs",
      "pitch": "Tokenize set inventory for easy rental and ownership tracking with social wallets and free tx.",
      "subDiscipline": "asset tokenization",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social authenticates owners; sponsored tx allow gasless NFT transfers.",
      "megaPrompt": "Build \"SetInventory NFTs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTokenize set inventory for easy rental and ownership tracking with social wallets and free tx.\nDiscipline: Theater & Live Performance (asset tokenization).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social authenticates owners; sponsored tx allow gasless NFT transfers.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SetInventoryNFTs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogSetInventoryNFTs\n/// @notice Tokenize set inventory for easy rental and ownership tracking with social wallets and free tx.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogSetInventoryNFTs {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every asset tokenization action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — set asset market",
      "sam": "$300M — digital asset tokenization",
      "som": "$40M — blockchain asset management"
    },
    {
      "id": "theater-scene-script-provenance-0",
      "theme": "theater",
      "title": "Scene Script Provenance",
      "pitch": "Securely mint and track original play scripts as creator-owned NFTs.",
      "subDiscipline": "playwriting",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "Minting scripts as NFTs ensures immutable proof of authorship and timestamps.",
      "megaPrompt": "Build \"Scene Script Provenance\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecurely mint and track original play scripts as creator-owned NFTs.\nDiscipline: Theater & Live Performance (playwriting).\nOnchain primitive: NFT provenance mint. Why this primitive: Minting scripts as NFTs ensures immutable proof of authorship and timestamps.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SceneScriptProvenance.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title SceneScriptProvenance\n/// @notice ERC-721 provenance for: Securely mint and track original play scripts as creator-owned NFTs.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SceneScriptProvenance is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"SceneScriptProvenance\", \"SCENES\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a playwriting artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global digital script distribution market",
      "sam": "$500M — playwrights adopting digital rights management",
      "som": "$50M — playwrights using NFT-based copyright tools"
    },
    {
      "id": "theater-lighting-cue-chain-1",
      "theme": "theater",
      "title": "Lighting Cue Chain",
      "pitch": "Record and verify lighting cue sequences as unique NFTs for live performances.",
      "subDiscipline": "lighting design",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs provide verifiable provenance for dynamic stage lighting designs.",
      "megaPrompt": "Build \"Lighting Cue Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nRecord and verify lighting cue sequences as unique NFTs for live performances.\nDiscipline: Theater & Live Performance (lighting design).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs provide verifiable provenance for dynamic stage lighting designs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/LightingCueChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title LightingCueChain\n/// @notice ERC-721 provenance for: Record and verify lighting cue sequences as unique NFTs for live performances.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract LightingCueChain is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"LightingCueChain\", \"LIGHTI\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a lighting design artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global stage lighting equipment and software market",
      "sam": "$200M — digital lighting design tools",
      "som": "$20M — NFT adoption by lighting professionals"
    },
    {
      "id": "theater-actor-rehearsal-logs-2",
      "theme": "theater",
      "title": "Actor Rehearsal Logs",
      "pitch": "Actors mint daily rehearsal videos as NFTs to prove practice and progress.",
      "subDiscipline": "performance tracking",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "Onchain minting timestamps rehearsal footage with verified ownership by performers.",
      "megaPrompt": "Build \"Actor Rehearsal Logs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nActors mint daily rehearsal videos as NFTs to prove practice and progress.\nDiscipline: Theater & Live Performance (performance tracking).\nOnchain primitive: NFT provenance mint. Why this primitive: Onchain minting timestamps rehearsal footage with verified ownership by performers.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ActorRehearsalLogs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ActorRehearsalLogs\n/// @notice ERC-721 provenance for: Actors mint daily rehearsal videos as NFTs to prove practice and progress.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ActorRehearsalLogs is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ActorRehearsalLogs\", \"ACTORR\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a performance tracking artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — actor training and rehearsal platforms",
      "sam": "$300M — digital performance coaching tools",
      "som": "$30M — NFT solutions for performance documentation"
    },
    {
      "id": "theater-set-design-archives-3",
      "theme": "theater",
      "title": "Set Design Archives",
      "pitch": "Mint 3D set designs as NFTs for secure sharing and historical provenance.",
      "subDiscipline": "scenic design",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs authenticate original scenic design assets on an immutable ledger.",
      "megaPrompt": "Build \"Set Design Archives\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint 3D set designs as NFTs for secure sharing and historical provenance.\nDiscipline: Theater & Live Performance (scenic design).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs authenticate original scenic design assets on an immutable ledger.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SetDesignArchives.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title SetDesignArchives\n/// @notice ERC-721 provenance for: Mint 3D set designs as NFTs for secure sharing and historical provenance.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SetDesignArchives is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"SetDesignArchives\", \"SETDES\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a scenic design artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — global set design and prop market",
      "sam": "$150M — digital scenic design software",
      "som": "$15M — NFT-based archival tools for set designers"
    },
    {
      "id": "theater-playwright-feedback-chain-4",
      "theme": "theater",
      "title": "Playwright Feedback Chain",
      "pitch": "Mint feedback and revisions on scripts as NFTs for transparent version control.",
      "subDiscipline": "script development",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT provenance tracks iterative script changes with verified contributor ownership.",
      "megaPrompt": "Build \"Playwright Feedback Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint feedback and revisions on scripts as NFTs for transparent version control.\nDiscipline: Theater & Live Performance (script development).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT provenance tracks iterative script changes with verified contributor ownership.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PlaywrightFeedbackChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title PlaywrightFeedbackChain\n/// @notice ERC-721 provenance for: Mint feedback and revisions on scripts as NFTs for transparent version control.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PlaywrightFeedbackChain is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"PlaywrightFeedbackChain\", \"PLAYWR\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a script development artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — script editing and collaboration tools",
      "sam": "$250M — playwright collaboration platforms",
      "som": "$25M — NFT-enabled revision management"
    },
    {
      "id": "theater-performance-nft-tickets-5",
      "theme": "theater",
      "title": "Performance NFT Tickets",
      "pitch": "Issue NFT tickets proving legitimate access and unique attendance experience.",
      "subDiscipline": "audience engagement",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT tickets provide tamper-proof proof of participation and ownership.",
      "megaPrompt": "Build \"Performance NFT Tickets\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nIssue NFT tickets proving legitimate access and unique attendance experience.\nDiscipline: Theater & Live Performance (audience engagement).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT tickets provide tamper-proof proof of participation and ownership.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PerformanceNFTTickets.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title PerformanceNFTTickets\n/// @notice ERC-721 provenance for: Issue NFT tickets proving legitimate access and unique attendance experience.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PerformanceNFTTickets is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"PerformanceNFTTickets\", \"PERFOR\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a audience engagement artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global event ticketing market",
      "sam": "$1B — digital ticketing solutions",
      "som": "$100M — NFT ticket use in live performance"
    },
    {
      "id": "theater-costume-provenance-chain-6",
      "theme": "theater",
      "title": "Costume Provenance Chain",
      "pitch": "Mint costume designs and ownership as NFTs to protect designer rights and provenance.",
      "subDiscipline": "costume design",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs secure costume intellectual property with explicit creator ownership.",
      "megaPrompt": "Build \"Costume Provenance Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint costume designs and ownership as NFTs to protect designer rights and provenance.\nDiscipline: Theater & Live Performance (costume design).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs secure costume intellectual property with explicit creator ownership.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CostumeProvenanceChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title CostumeProvenanceChain\n/// @notice ERC-721 provenance for: Mint costume designs and ownership as NFTs to protect designer rights and provenance.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CostumeProvenanceChain is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"CostumeProvenanceChain\", \"COSTUM\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a costume design artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — costume and wardrobe market",
      "sam": "$120M — digital costume design assets",
      "som": "$12M — NFT adoption in costume provenance"
    },
    {
      "id": "theater-monologue-minting-hub-7",
      "theme": "theater",
      "title": "Monologue Minting Hub",
      "pitch": "Actors mint monologues as NFTs to showcase and monetize original performances.",
      "subDiscipline": "acting",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs verify creators’ unique performance content and intellectual property.",
      "megaPrompt": "Build \"Monologue Minting Hub\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nActors mint monologues as NFTs to showcase and monetize original performances.\nDiscipline: Theater & Live Performance (acting).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs verify creators’ unique performance content and intellectual property.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MonologueMintingHub.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title MonologueMintingHub\n/// @notice ERC-721 provenance for: Actors mint monologues as NFTs to showcase and monetize original performances.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract MonologueMintingHub is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"MonologueMintingHub\", \"MONOLO\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a acting artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — actor content creation market",
      "sam": "$140M — digital acting portfolios",
      "som": "$14M — NFT monologue platforms"
    },
    {
      "id": "theater-soundscape-provenance-8",
      "theme": "theater",
      "title": "Soundscape Provenance",
      "pitch": "Mint live performance soundscapes as NFTs to authenticate original audio environments.",
      "subDiscipline": "sound design",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs provide immutable timestamps and creator proof for sound designs.",
      "megaPrompt": "Build \"Soundscape Provenance\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint live performance soundscapes as NFTs to authenticate original audio environments.\nDiscipline: Theater & Live Performance (sound design).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs provide immutable timestamps and creator proof for sound designs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SoundscapeProvenance.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title SoundscapeProvenance\n/// @notice ERC-721 provenance for: Mint live performance soundscapes as NFTs to authenticate original audio environments.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SoundscapeProvenance is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"SoundscapeProvenance\", \"SOUNDS\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a sound design artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — live sound engineering market",
      "sam": "$180M — digital sound design tools",
      "som": "$18M — NFT soundscape authentication"
    },
    {
      "id": "theater-director-s-vision-ledger-9",
      "theme": "theater",
      "title": "Director's Vision Ledger",
      "pitch": "Directors mint annotated scripts as NFTs capturing their unique vision and notes.",
      "subDiscipline": "direction",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs record the director’s creative input with provenance and ownership.",
      "megaPrompt": "Build \"Director's Vision Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nDirectors mint annotated scripts as NFTs capturing their unique vision and notes.\nDiscipline: Theater & Live Performance (direction).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs record the director’s creative input with provenance and ownership.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DirectorsVisionLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title DirectorsVisionLedger\n/// @notice ERC-721 provenance for: Directors mint annotated scripts as NFTs capturing their unique vision and notes.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract DirectorsVisionLedger is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"DirectorsVisionLedger\", \"DIRECT\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a direction artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — digital script annotation market",
      "sam": "$200M — director collaboration software",
      "som": "$20M — NFT-based vision tracking"
    },
    {
      "id": "theater-interactive-stage-nfts-10",
      "theme": "theater",
      "title": "Interactive Stage NFTs",
      "pitch": "Mint interactive performance elements as NFTs to prove creative ownership.",
      "subDiscipline": "stage interaction",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs authenticate unique interactive stage designs and technology.",
      "megaPrompt": "Build \"Interactive Stage NFTs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint interactive performance elements as NFTs to prove creative ownership.\nDiscipline: Theater & Live Performance (stage interaction).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs authenticate unique interactive stage designs and technology.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/InteractiveStageNFTs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title InteractiveStageNFTs\n/// @notice ERC-721 provenance for: Mint interactive performance elements as NFTs to prove creative ownership.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract InteractiveStageNFTs is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"InteractiveStageNFTs\", \"INTERA\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a stage interaction artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — interactive theater technology",
      "sam": "$100M — interactive stage asset markets",
      "som": "$10M — NFT use in interactive performances"
    },
    {
      "id": "theater-improv-moment-mint-11",
      "theme": "theater",
      "title": "Improv Moment Mint",
      "pitch": "Actors mint spontaneous improv performances as NFTs to preserve unique moments.",
      "subDiscipline": "improvisation",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT provenance captures ephemeral content with verifiable ownership.",
      "megaPrompt": "Build \"Improv Moment Mint\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nActors mint spontaneous improv performances as NFTs to preserve unique moments.\nDiscipline: Theater & Live Performance (improvisation).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT provenance captures ephemeral content with verifiable ownership.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ImprovMomentMint.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ImprovMomentMint\n/// @notice ERC-721 provenance for: Actors mint spontaneous improv performances as NFTs to preserve unique moments.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ImprovMomentMint is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ImprovMomentMint\", \"IMPROV\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a improvisation artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$300M — improv and live performance recording",
      "sam": "$60M — digital improv content",
      "som": "$6M — NFT improv minting platforms"
    },
    {
      "id": "theater-choreography-chain-12",
      "theme": "theater",
      "title": "Choreography Chain",
      "pitch": "Mint choreography sequences as NFTs ensuring original dance and movement rights.",
      "subDiscipline": "movement design",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs record and timestamp choreography with creator attribution.",
      "megaPrompt": "Build \"Choreography Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint choreography sequences as NFTs ensuring original dance and movement rights.\nDiscipline: Theater & Live Performance (movement design).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs record and timestamp choreography with creator attribution.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreographyChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ChoreographyChain\n/// @notice ERC-721 provenance for: Mint choreography sequences as NFTs ensuring original dance and movement rights.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ChoreographyChain is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ChoreographyChain\", \"CHOREO\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a movement design artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.8B — dance and choreography market",
      "sam": "$360M — digital choreography tools",
      "som": "$36M — NFT-based choreography protection"
    },
    {
      "id": "theater-stage-props-provenance-13",
      "theme": "theater",
      "title": "Stage Props Provenance",
      "pitch": "Mint digital twins of stage props as NFTs for ownership and rental tracking.",
      "subDiscipline": "prop management",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs track provenance and leasing history of unique stage props.",
      "megaPrompt": "Build \"Stage Props Provenance\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint digital twins of stage props as NFTs for ownership and rental tracking.\nDiscipline: Theater & Live Performance (prop management).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs track provenance and leasing history of unique stage props.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StagePropsProvenance.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title StagePropsProvenance\n/// @notice ERC-721 provenance for: Mint digital twins of stage props as NFTs for ownership and rental tracking.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract StagePropsProvenance is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"StagePropsProvenance\", \"STAGEP\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a prop management artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — theatrical prop rental and sales",
      "sam": "$140M — prop rental platforms",
      "som": "$14M — NFT prop provenance solutions"
    },
    {
      "id": "theater-script-translation-nfts-14",
      "theme": "theater",
      "title": "Script Translation NFTs",
      "pitch": "Mint translated scripts as NFTs to protect translators' intellectual property.",
      "subDiscipline": "translation",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs ensure verified ownership of individual translated script versions.",
      "megaPrompt": "Build \"Script Translation NFTs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint translated scripts as NFTs to protect translators' intellectual property.\nDiscipline: Theater & Live Performance (translation).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs ensure verified ownership of individual translated script versions.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ScriptTranslationNFTs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ScriptTranslationNFTs\n/// @notice ERC-721 provenance for: Mint translated scripts as NFTs to protect translators' intellectual property.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ScriptTranslationNFTs is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ScriptTranslationNFTs\", \"SCRIPT\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a translation artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — script translation and localization market",
      "sam": "$100M — digital translation services",
      "som": "$10M — NFT translation copyright tools"
    },
    {
      "id": "theater-audience-experience-logs-15",
      "theme": "theater",
      "title": "Audience Experience Logs",
      "pitch": "Mint audience reaction recordings as NFTs to capture genuine engagement data.",
      "subDiscipline": "audience analytics",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs provide verifiable proof of authentic audience feedback content.",
      "megaPrompt": "Build \"Audience Experience Logs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint audience reaction recordings as NFTs to capture genuine engagement data.\nDiscipline: Theater & Live Performance (audience analytics).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs provide verifiable proof of authentic audience feedback content.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/AudienceExperienceLogs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title AudienceExperienceLogs\n/// @notice ERC-721 provenance for: Mint audience reaction recordings as NFTs to capture genuine engagement data.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract AudienceExperienceLogs is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"AudienceExperienceLogs\", \"AUDIEN\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a audience analytics artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — audience engagement analytics market",
      "sam": "$160M — digital feedback tools",
      "som": "$16M — NFT feedback platforms"
    },
    {
      "id": "theater-virtual-backstage-pass-16",
      "theme": "theater",
      "title": "Virtual Backstage Pass",
      "pitch": "Issue NFT backstage passes granting exclusive digital and live experience.",
      "subDiscipline": "fan engagement",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT passes confirm exclusive access and prevent counterfeit entries.",
      "megaPrompt": "Build \"Virtual Backstage Pass\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nIssue NFT backstage passes granting exclusive digital and live experience.\nDiscipline: Theater & Live Performance (fan engagement).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT passes confirm exclusive access and prevent counterfeit entries.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/VirtualBackstagePass.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title VirtualBackstagePass\n/// @notice ERC-721 provenance for: Issue NFT backstage passes granting exclusive digital and live experience.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract VirtualBackstagePass is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"VirtualBackstagePass\", \"VIRTUA\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a fan engagement artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — fan engagement and merchandise market",
      "sam": "$600M — digital fan access platforms",
      "som": "$60M — NFT backstage pass adoption"
    },
    {
      "id": "theater-playbill-provenance-17",
      "theme": "theater",
      "title": "Playbill Provenance",
      "pitch": "Mint digital playbills as NFTs to preserve historical performance records.",
      "subDiscipline": "program design",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs store immutable playbill data with creator proof.",
      "megaPrompt": "Build \"Playbill Provenance\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint digital playbills as NFTs to preserve historical performance records.\nDiscipline: Theater & Live Performance (program design).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs store immutable playbill data with creator proof.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PlaybillProvenance.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title PlaybillProvenance\n/// @notice ERC-721 provenance for: Mint digital playbills as NFTs to preserve historical performance records.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PlaybillProvenance is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"PlaybillProvenance\", \"PLAYBI\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a program design artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — theater program and souvenir market",
      "sam": "$80M — digital playbill platforms",
      "som": "$8M — NFT archival playbill solutions"
    },
    {
      "id": "theater-set-lighting-automation-nfts-18",
      "theme": "theater",
      "title": "Set Lighting Automation NFTs",
      "pitch": "Mint automated lighting sequences as NFTs to secure ownership and resale rights.",
      "subDiscipline": "lighting tech",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs certify original lighting automation and code assets.",
      "megaPrompt": "Build \"Set Lighting Automation NFTs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint automated lighting sequences as NFTs to secure ownership and resale rights.\nDiscipline: Theater & Live Performance (lighting tech).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs certify original lighting automation and code assets.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SetLightingAutomationNFTs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title SetLightingAutomationNFTs\n/// @notice ERC-721 provenance for: Mint automated lighting sequences as NFTs to secure ownership and resale rights.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SetLightingAutomationNFTs is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"SetLightingAutomationNFTs\", \"SETLIG\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a lighting tech artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — automated stage lighting market",
      "sam": "$240M — digital lighting control software",
      "som": "$24M — NFT automation asset market"
    },
    {
      "id": "theater-drama-therapy-journals-19",
      "theme": "theater",
      "title": "Drama Therapy Journals",
      "pitch": "Mint personal drama therapy session outputs as NFTs to secure privacy and ownership.",
      "subDiscipline": "therapeutic performance",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs provide confidential, verifiable proof of therapeutic creative work.",
      "megaPrompt": "Build \"Drama Therapy Journals\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint personal drama therapy session outputs as NFTs to secure privacy and ownership.\nDiscipline: Theater & Live Performance (therapeutic performance).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs provide confidential, verifiable proof of therapeutic creative work.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DramaTherapyJournals.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title DramaTherapyJournals\n/// @notice ERC-721 provenance for: Mint personal drama therapy session outputs as NFTs to secure privacy and ownership.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract DramaTherapyJournals is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"DramaTherapyJournals\", \"DRAMAT\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a therapeutic performance artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — drama therapy and wellness market",
      "sam": "$120M — digital therapy documentation",
      "som": "$12M — NFT-secured therapy journals"
    },
    {
      "id": "theater-costume-rental-nft-20",
      "theme": "theater",
      "title": "Costume Rental NFT",
      "pitch": "Mint NFTs representing costume rental agreements to streamline transactions.",
      "subDiscipline": "rental services",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs enable trustless rental contracts with provenance and transferability.",
      "megaPrompt": "Build \"Costume Rental NFT\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint NFTs representing costume rental agreements to streamline transactions.\nDiscipline: Theater & Live Performance (rental services).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs enable trustless rental contracts with provenance and transferability.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CostumeRentalNFT.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title CostumeRentalNFT\n/// @notice ERC-721 provenance for: Mint NFTs representing costume rental agreements to streamline transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CostumeRentalNFT is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"CostumeRentalNFT\", \"COSTUM\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a rental services artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — costume rental market",
      "sam": "$180M — digital rental management platforms",
      "som": "$18M — NFT rental solutions"
    },
    {
      "id": "theater-playwright-royalty-nfts-21",
      "theme": "theater",
      "title": "Playwright Royalty NFTs",
      "pitch": "Mint royalty shares as NFTs to transparently track playwriting revenue splits.",
      "subDiscipline": "royalty management",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs allow fractionalized, verifiable royalty ownership and transfers.",
      "megaPrompt": "Build \"Playwright Royalty NFTs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint royalty shares as NFTs to transparently track playwriting revenue splits.\nDiscipline: Theater & Live Performance (royalty management).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs allow fractionalized, verifiable royalty ownership and transfers.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PlaywrightRoyaltyNFTs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title PlaywrightRoyaltyNFTs\n/// @notice ERC-721 provenance for: Mint royalty shares as NFTs to transparently track playwriting revenue splits.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PlaywrightRoyaltyNFTs is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"PlaywrightRoyaltyNFTs\", \"PLAYWR\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a royalty management artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — playwriting royalty market",
      "sam": "$300M — digital royalty tracking",
      "som": "$30M — NFT royalty platforms"
    },
    {
      "id": "theater-performance-highlight-reels-22",
      "theme": "theater",
      "title": "Performance Highlight Reels",
      "pitch": "Actors mint highlight reels as NFTs to prove originality and attract bookings.",
      "subDiscipline": "promotion",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT provenance confirms authentic content ownership for self-promotion.",
      "megaPrompt": "Build \"Performance Highlight Reels\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nActors mint highlight reels as NFTs to prove originality and attract bookings.\nDiscipline: Theater & Live Performance (promotion).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT provenance confirms authentic content ownership for self-promotion.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PerformanceHighlightReels.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title PerformanceHighlightReels\n/// @notice ERC-721 provenance for: Actors mint highlight reels as NFTs to prove originality and attract bookings.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PerformanceHighlightReels is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"PerformanceHighlightReels\", \"PERFOR\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a promotion artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — digital actor marketing market",
      "sam": "$200M — highlight reel platforms",
      "som": "$20M — NFT promotional tools"
    },
    {
      "id": "theater-virtual-set-nfts-23",
      "theme": "theater",
      "title": "Virtual Set NFTs",
      "pitch": "Mint virtual set designs as NFTs for use in augmented and virtual performances.",
      "subDiscipline": "digital scenography",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs authenticate digital stage assets for cross-platform use.",
      "megaPrompt": "Build \"Virtual Set NFTs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint virtual set designs as NFTs for use in augmented and virtual performances.\nDiscipline: Theater & Live Performance (digital scenography).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs authenticate digital stage assets for cross-platform use.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/VirtualSetNFTs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title VirtualSetNFTs\n/// @notice ERC-721 provenance for: Mint virtual set designs as NFTs for use in augmented and virtual performances.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract VirtualSetNFTs is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"VirtualSetNFTs\", \"VIRTUA\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a digital scenography artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.3B — virtual and augmented theater market",
      "sam": "$260M — digital scenography platforms",
      "som": "$26M — NFT virtual set marketplaces"
    },
    {
      "id": "theater-script-ownership-registry-24",
      "theme": "theater",
      "title": "Script Ownership Registry",
      "pitch": "Mint scripts as NFTs to create an immutable ownership registry for playwrights.",
      "subDiscipline": "legal protection",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs provide public, tamper-proof proof of script authorship.",
      "megaPrompt": "Build \"Script Ownership Registry\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint scripts as NFTs to create an immutable ownership registry for playwrights.\nDiscipline: Theater & Live Performance (legal protection).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs provide public, tamper-proof proof of script authorship.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ScriptOwnershipRegistry.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ScriptOwnershipRegistry\n/// @notice ERC-721 provenance for: Mint scripts as NFTs to create an immutable ownership registry for playwrights.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ScriptOwnershipRegistry is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ScriptOwnershipRegistry\", \"SCRIPT\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a legal protection artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global script copyright management",
      "sam": "$400M — playwright legal services",
      "som": "$40M — NFT copyright registries"
    }
  ]
}