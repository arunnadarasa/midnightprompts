{
  "theme": {
    "slug": "dance",
    "name": "Dance & Choreography",
    "emoji": "💃",
    "audience": "choreographers, dancers, dance teachers, movement directors",
    "market_anchor": "the global dance industry (~$5B; >2M studios worldwide)"
  },
  "ideas": [
    {
      "id": "dance-choreo-ledger-0",
      "theme": "dance",
      "title": "Choreo Ledger",
      "pitch": "Securely prove original choreography ownership and timestamp creations on a public ledger.",
      "subDiscipline": "movement attribution",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts provide immutable proof of choreography authorship, accessible globally and transparently.",
      "megaPrompt": "Build \"Choreo Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecurely prove original choreography ownership and timestamp creations on a public ledger.\nDiscipline: Dance & Choreography (movement attribution).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts provide immutable proof of choreography authorship, accessible globally and transparently.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreoLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ChoreoLedger\n/// @notice Securely prove original choreography ownership and timestamp creations on a public ledger.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ChoreoLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a movement attribution action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance studio software market",
      "sam": "$500M — digital dance content IP management",
      "som": "$50M — early adopters in dance choreography tech"
    },
    {
      "id": "dance-dance-nft-tickets-1",
      "theme": "dance",
      "title": "Dance NFT Tickets",
      "pitch": "Sell and verify exclusive dance event tickets as tradable NFTs on-chain to prevent fraud.",
      "subDiscipline": "event access control",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Smart contracts automate issuance and verification of NFT tickets without intermediaries.",
      "megaPrompt": "Build \"Dance NFT Tickets\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSell and verify exclusive dance event tickets as tradable NFTs on-chain to prevent fraud.\nDiscipline: Dance & Choreography (event access control).\nOnchain primitive: Sepolia smart contract. Why this primitive: Smart contracts automate issuance and verification of NFT tickets without intermediaries.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceNFTTickets.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title DanceNFTTickets\n/// @notice Sell and verify exclusive dance event tickets as tradable NFTs on-chain to prevent fraud.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract DanceNFTTickets {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a event access control action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance event and festival revenue",
      "sam": "$800M — dance ticketing digital platforms",
      "som": "$120M — NFT ticket adopters in dance festivals"
    },
    {
      "id": "dance-movecoin-rewards-2",
      "theme": "dance",
      "title": "MoveCoin Rewards",
      "pitch": "Reward dancers with crypto tokens for participation and achievements in studio classes.",
      "subDiscipline": "incentive tokenization",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Onchain tokens enable transparent, automatic reward distribution and tracking.",
      "megaPrompt": "Build \"MoveCoin Rewards\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nReward dancers with crypto tokens for participation and achievements in studio classes.\nDiscipline: Dance & Choreography (incentive tokenization).\nOnchain primitive: Sepolia smart contract. Why this primitive: Onchain tokens enable transparent, automatic reward distribution and tracking.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MoveCoinRewards.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title MoveCoinRewards\n/// @notice Reward dancers with crypto tokens for participation and achievements in studio classes.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract MoveCoinRewards {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a incentive tokenization action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance studio software",
      "sam": "$400M — dance education digital tools",
      "som": "$30M — token reward platforms in fitness and dance"
    },
    {
      "id": "dance-choreo-royalty-pool-3",
      "theme": "dance",
      "title": "Choreo Royalty Pool",
      "pitch": "Distribute royalties automatically among choreographers, dancers, and producers using smart contracts.",
      "subDiscipline": "rights management",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts transparently split and enforce royalty terms without intermediaries.",
      "megaPrompt": "Build \"Choreo Royalty Pool\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nDistribute royalties automatically among choreographers, dancers, and producers using smart contracts.\nDiscipline: Dance & Choreography (rights management).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts transparently split and enforce royalty terms without intermediaries.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreoRoyaltyPool.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ChoreoRoyaltyPool\n/// @notice Distribute royalties automatically among choreographers, dancers, and producers using smart contracts.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ChoreoRoyaltyPool {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a rights management action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance content monetization",
      "sam": "$1B — digital dance media licensing",
      "som": "$80M — royalty management platforms in dance"
    },
    {
      "id": "dance-syncstage-auction-4",
      "theme": "dance",
      "title": "SyncStage Auction",
      "pitch": "Auction live dance performance slots to global choreographers via transparent smart contracts.",
      "subDiscipline": "live collaboration",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Smart contracts enable trustless, automated auction and booking processes worldwide.",
      "megaPrompt": "Build \"SyncStage Auction\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAuction live dance performance slots to global choreographers via transparent smart contracts.\nDiscipline: Dance & Choreography (live collaboration).\nOnchain primitive: Sepolia smart contract. Why this primitive: Smart contracts enable trustless, automated auction and booking processes worldwide.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SyncStageAuction.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SyncStageAuction\n/// @notice Auction live dance performance slots to global choreographers via transparent smart contracts.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SyncStageAuction {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a live collaboration action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance performance market",
      "sam": "$600M — live dance booking platforms",
      "som": "$70M — tech-enabled dance collaboration tools"
    },
    {
      "id": "dance-moveproof-ledger-5",
      "theme": "dance",
      "title": "MoveProof Ledger",
      "pitch": "Record and verify unique dance moves proof-of-existence onchain to counter plagiarism.",
      "subDiscipline": "movement authenticity",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Immutable smart contracts provide tamper-proof timestamps for movement intellectual property.",
      "megaPrompt": "Build \"MoveProof Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nRecord and verify unique dance moves proof-of-existence onchain to counter plagiarism.\nDiscipline: Dance & Choreography (movement authenticity).\nOnchain primitive: Sepolia smart contract. Why this primitive: Immutable smart contracts provide tamper-proof timestamps for movement intellectual property.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MoveProofLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title MoveProofLedger\n/// @notice Record and verify unique dance moves proof-of-existence onchain to counter plagiarism.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract MoveProofLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a movement authenticity action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance IP management",
      "sam": "$350M — digital dance content rights",
      "som": "$25M — early adopters of authenticity verification"
    },
    {
      "id": "dance-tokenized-choreo-kits-6",
      "theme": "dance",
      "title": "Tokenized Choreo Kits",
      "pitch": "Sell bundled choreography sequences as tokenized assets for easier licensing and resale.",
      "subDiscipline": "content packaging",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts facilitate fractional ownership and transparent transfers.",
      "megaPrompt": "Build \"Tokenized Choreo Kits\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSell bundled choreography sequences as tokenized assets for easier licensing and resale.\nDiscipline: Dance & Choreography (content packaging).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts facilitate fractional ownership and transparent transfers.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/TokenizedChoreoKits.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title TokenizedChoreoKits\n/// @notice Sell bundled choreography sequences as tokenized assets for easier licensing and resale.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract TokenizedChoreoKits {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a content packaging action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance content licensing",
      "sam": "$450M — digital choreography sales",
      "som": "$40M — platforms enabling dance content resale"
    },
    {
      "id": "dance-dancedao-collective-7",
      "theme": "dance",
      "title": "DanceDAO Collective",
      "pitch": "Enable dance groups to govern studios and projects democratically via onchain voting.",
      "subDiscipline": "community governance",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Smart contracts ensure transparent, tamper-proof decentralized decision-making.",
      "megaPrompt": "Build \"DanceDAO Collective\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnable dance groups to govern studios and projects democratically via onchain voting.\nDiscipline: Dance & Choreography (community governance).\nOnchain primitive: Sepolia smart contract. Why this primitive: Smart contracts ensure transparent, tamper-proof decentralized decision-making.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceDAOCollective.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title DanceDAOCollective\n/// @notice Enable dance groups to govern studios and projects democratically via onchain voting.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract DanceDAOCollective {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a community governance action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance studios management",
      "sam": "$300M — dance organization software",
      "som": "$20M — DAO-powered dance communities"
    },
    {
      "id": "dance-nft-move-badges-8",
      "theme": "dance",
      "title": "NFT Move Badges",
      "pitch": "Issue unique NFT badges for dance milestones and skills to motivate learners.",
      "subDiscipline": "achievement tracking",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Onchain issuance guarantees badge uniqueness and verifiability worldwide.",
      "megaPrompt": "Build \"NFT Move Badges\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nIssue unique NFT badges for dance milestones and skills to motivate learners.\nDiscipline: Dance & Choreography (achievement tracking).\nOnchain primitive: Sepolia smart contract. Why this primitive: Onchain issuance guarantees badge uniqueness and verifiability worldwide.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/NFTMoveBadges.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title NFTMoveBadges\n/// @notice Issue unique NFT badges for dance milestones and skills to motivate learners.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract NFTMoveBadges {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a achievement tracking action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance education market",
      "sam": "$250M — digital learning credentials",
      "som": "$15M — NFT-based achievement tracking"
    },
    {
      "id": "dance-choreo-licensing-hub-9",
      "theme": "dance",
      "title": "Choreo Licensing Hub",
      "pitch": "Create a decentralized marketplace for buying and selling choreography licenses directly onchain.",
      "subDiscipline": "rights marketplace",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Smart contracts automate license agreements and transparent payments without middlemen.",
      "megaPrompt": "Build \"Choreo Licensing Hub\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate a decentralized marketplace for buying and selling choreography licenses directly onchain.\nDiscipline: Dance & Choreography (rights marketplace).\nOnchain primitive: Sepolia smart contract. Why this primitive: Smart contracts automate license agreements and transparent payments without middlemen.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreoLicensingHub.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ChoreoLicensingHub\n/// @notice Create a decentralized marketplace for buying and selling choreography licenses directly onchain.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ChoreoLicensingHub {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a rights marketplace action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance IP licensing",
      "sam": "$700M — choreography rights platforms",
      "som": "$55M — blockchain-enabled license marketplaces"
    },
    {
      "id": "dance-dancemove-provenance-10",
      "theme": "dance",
      "title": "DanceMove Provenance",
      "pitch": "Trace and display the lineage and remix history of dance moves on an immutable ledger.",
      "subDiscipline": "creative lineage",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts track ownership and modification history transparently and permanently.",
      "megaPrompt": "Build \"DanceMove Provenance\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTrace and display the lineage and remix history of dance moves on an immutable ledger.\nDiscipline: Dance & Choreography (creative lineage).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts track ownership and modification history transparently and permanently.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceMoveProvenance.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title DanceMoveProvenance\n/// @notice Trace and display the lineage and remix history of dance moves on an immutable ledger.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract DanceMoveProvenance {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a creative lineage action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance creative IP",
      "sam": "$400M — dance content provenance tools",
      "som": "$30M — provenance tracking adopters"
    },
    {
      "id": "dance-livemotion-payments-11",
      "theme": "dance",
      "title": "LiveMotion Payments",
      "pitch": "Settle payments instantly for live dance services and gigs through smart contracts.",
      "subDiscipline": "instant settlement",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Onchain escrow and release ensures trustless, real-time transaction finality.",
      "megaPrompt": "Build \"LiveMotion Payments\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSettle payments instantly for live dance services and gigs through smart contracts.\nDiscipline: Dance & Choreography (instant settlement).\nOnchain primitive: Sepolia smart contract. Why this primitive: Onchain escrow and release ensures trustless, real-time transaction finality.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/LiveMotionPayments.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title LiveMotionPayments\n/// @notice Settle payments instantly for live dance services and gigs through smart contracts.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract LiveMotionPayments {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a instant settlement action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance gig economy",
      "sam": "$600M — dance freelance payment platforms",
      "som": "$50M — blockchain payment adopters"
    },
    {
      "id": "dance-choreocrowd-fund-12",
      "theme": "dance",
      "title": "ChoreoCrowd Fund",
      "pitch": "Enable transparent onchain fundraising for dance projects with milestones and payout automation.",
      "subDiscipline": "project crowdfunding",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Smart contracts enforce conditions and ensure proper fund distribution to creators.",
      "megaPrompt": "Build \"ChoreoCrowd Fund\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnable transparent onchain fundraising for dance projects with milestones and payout automation.\nDiscipline: Dance & Choreography (project crowdfunding).\nOnchain primitive: Sepolia smart contract. Why this primitive: Smart contracts enforce conditions and ensure proper fund distribution to creators.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreoCrowdFund.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ChoreoCrowdFund\n/// @notice Enable transparent onchain fundraising for dance projects with milestones and payout automation.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ChoreoCrowdFund {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a project crowdfunding action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance production funding",
      "sam": "$200M — dance crowdfunding platforms",
      "som": "$18M — blockchain crowdfunding adopters"
    },
    {
      "id": "dance-dancestep-identity-13",
      "theme": "dance",
      "title": "DanceStep Identity",
      "pitch": "Create verifiable digital identities for dancers' skills and experiences stored onchain.",
      "subDiscipline": "digital identity",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Immutable smart contracts secure skill records accessible globally by studios and agents.",
      "megaPrompt": "Build \"DanceStep Identity\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate verifiable digital identities for dancers' skills and experiences stored onchain.\nDiscipline: Dance & Choreography (digital identity).\nOnchain primitive: Sepolia smart contract. Why this primitive: Immutable smart contracts secure skill records accessible globally by studios and agents.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceStepIdentity.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title DanceStepIdentity\n/// @notice Create verifiable digital identities for dancers' skills and experiences stored onchain.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract DanceStepIdentity {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a digital identity action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance workforce management",
      "sam": "$220M — digital talent verification",
      "som": "$20M — digital identity adopters in dance"
    },
    {
      "id": "dance-choreoswap-marketplace-14",
      "theme": "dance",
      "title": "ChoreoSwap Marketplace",
      "pitch": "Swap choreography clips and movement motifs peer-to-peer as tokenized assets onchain.",
      "subDiscipline": "content exchange",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Smart contracts enable trustless, instant exchange and ownership transfer without intermediaries.",
      "megaPrompt": "Build \"ChoreoSwap Marketplace\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSwap choreography clips and movement motifs peer-to-peer as tokenized assets onchain.\nDiscipline: Dance & Choreography (content exchange).\nOnchain primitive: Sepolia smart contract. Why this primitive: Smart contracts enable trustless, instant exchange and ownership transfer without intermediaries.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreoSwapMarketplace.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ChoreoSwapMarketplace\n/// @notice Swap choreography clips and movement motifs peer-to-peer as tokenized assets onchain.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ChoreoSwapMarketplace {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a content exchange action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance content exchange",
      "sam": "$480M — digital choreography marketplaces",
      "som": "$35M — blockchain content trading users"
    },
    {
      "id": "dance-movemint-workshops-15",
      "theme": "dance",
      "title": "MoveMint Workshops",
      "pitch": "Mint limited access tokens to exclusive dance workshops and masterclasses as NFTs.",
      "subDiscipline": "tokenized education",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Onchain NFT tickets guarantee authenticity and scarcity for premium educational content.",
      "megaPrompt": "Build \"MoveMint Workshops\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint limited access tokens to exclusive dance workshops and masterclasses as NFTs.\nDiscipline: Dance & Choreography (tokenized education).\nOnchain primitive: Sepolia smart contract. Why this primitive: Onchain NFT tickets guarantee authenticity and scarcity for premium educational content.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MoveMintWorkshops.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title MoveMintWorkshops\n/// @notice Mint limited access tokens to exclusive dance workshops and masterclasses as NFTs.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract MoveMintWorkshops {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a tokenized education action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance education market",
      "sam": "$350M — digital workshop platforms",
      "som": "$25M — NFT ticket users in dance education"
    },
    {
      "id": "dance-stepchain-voting-16",
      "theme": "dance",
      "title": "StepChain Voting",
      "pitch": "Let audiences vote transparently and fairly on dance competition outcomes via smart contracts.",
      "subDiscipline": "event curation",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts provide immutable, verifiable vote tallying without manipulation.",
      "megaPrompt": "Build \"StepChain Voting\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nLet audiences vote transparently and fairly on dance competition outcomes via smart contracts.\nDiscipline: Dance & Choreography (event curation).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts provide immutable, verifiable vote tallying without manipulation.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StepChainVoting.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title StepChainVoting\n/// @notice Let audiences vote transparently and fairly on dance competition outcomes via smart contracts.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract StepChainVoting {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a event curation action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance event market",
      "sam": "$300M — digital event management tools",
      "som": "$22M — blockchain voting adopters"
    },
    {
      "id": "dance-choreoproof-licensing-17",
      "theme": "dance",
      "title": "ChoreoProof Licensing",
      "pitch": "Automate licensing agreements for choreography use with onchain smart contract enforcement.",
      "subDiscipline": "automated contracts",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Smart contracts reduce disputes by executing terms exactly as coded without intermediaries.",
      "megaPrompt": "Build \"ChoreoProof Licensing\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAutomate licensing agreements for choreography use with onchain smart contract enforcement.\nDiscipline: Dance & Choreography (automated contracts).\nOnchain primitive: Sepolia smart contract. Why this primitive: Smart contracts reduce disputes by executing terms exactly as coded without intermediaries.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreoProofLicensing.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ChoreoProofLicensing\n/// @notice Automate licensing agreements for choreography use with onchain smart contract enforcement.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ChoreoProofLicensing {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a automated contracts action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance IP licensing",
      "sam": "$650M — contract automation platforms",
      "som": "$45M — onchain licensing adopters"
    },
    {
      "id": "dance-dancetoken-staking-18",
      "theme": "dance",
      "title": "DanceToken Staking",
      "pitch": "Stake tokens to support favorite dancers and receive exclusive perks or content rewards.",
      "subDiscipline": "community incentives",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Onchain staking programs enable transparent, programmable community funding and rewards.",
      "megaPrompt": "Build \"DanceToken Staking\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nStake tokens to support favorite dancers and receive exclusive perks or content rewards.\nDiscipline: Dance & Choreography (community incentives).\nOnchain primitive: Sepolia smart contract. Why this primitive: Onchain staking programs enable transparent, programmable community funding and rewards.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceTokenStaking.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title DanceTokenStaking\n/// @notice Stake tokens to support favorite dancers and receive exclusive perks or content rewards.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract DanceTokenStaking {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a community incentives action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance fan engagement market",
      "sam": "$270M — digital fan token economies",
      "som": "$20M — token staking communities in dance"
    },
    {
      "id": "dance-stepswap-royalties-19",
      "theme": "dance",
      "title": "StepSwap Royalties",
      "pitch": "Track and enforce royalty payments on resales of choreography NFTs via smart contracts.",
      "subDiscipline": "resale tracking",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts automatically route royalties ensuring creator revenue continuity.",
      "megaPrompt": "Build \"StepSwap Royalties\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTrack and enforce royalty payments on resales of choreography NFTs via smart contracts.\nDiscipline: Dance & Choreography (resale tracking).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts automatically route royalties ensuring creator revenue continuity.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StepSwapRoyalties.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title StepSwapRoyalties\n/// @notice Track and enforce royalty payments on resales of choreography NFTs via smart contracts.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract StepSwapRoyalties {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a resale tracking action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — digital dance IP market",
      "sam": "$500M — NFT resale royalty management",
      "som": "$40M — active royalty enforcement users"
    },
    {
      "id": "dance-danceledger-archives-20",
      "theme": "dance",
      "title": "DanceLedger Archives",
      "pitch": "Archive dance event records and performances permanently on a public blockchain ledger.",
      "subDiscipline": "historical record",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Immutable data storage preserves cultural heritage accessible globally forever.",
      "megaPrompt": "Build \"DanceLedger Archives\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nArchive dance event records and performances permanently on a public blockchain ledger.\nDiscipline: Dance & Choreography (historical record).\nOnchain primitive: Sepolia smart contract. Why this primitive: Immutable data storage preserves cultural heritage accessible globally forever.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceLedgerArchives.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title DanceLedgerArchives\n/// @notice Archive dance event records and performances permanently on a public blockchain ledger.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract DanceLedgerArchives {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a historical record action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance media archives",
      "sam": "$300M — digital archival solutions",
      "som": "$25M — blockchain archival adopters"
    },
    {
      "id": "dance-choreominting-platform-21",
      "theme": "dance",
      "title": "ChoreoMinting Platform",
      "pitch": "Empower dancers to mint and sell unique choreography NFTs directly from studios.",
      "subDiscipline": "NFT creation",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Onchain minting supports decentralized content ownership and monetization by creators.",
      "megaPrompt": "Build \"ChoreoMinting Platform\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEmpower dancers to mint and sell unique choreography NFTs directly from studios.\nDiscipline: Dance & Choreography (NFT creation).\nOnchain primitive: Sepolia smart contract. Why this primitive: Onchain minting supports decentralized content ownership and monetization by creators.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreoMintingPlatform.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ChoreoMintingPlatform\n/// @notice Empower dancers to mint and sell unique choreography NFTs directly from studios.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ChoreoMintingPlatform {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a NFT creation action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — digital dance content market",
      "sam": "$600M — NFT creation tools for dance",
      "som": "$50M — dance NFT creators"
    },
    {
      "id": "dance-tokenmove-sponsorship-22",
      "theme": "dance",
      "title": "TokenMove Sponsorship",
      "pitch": "Enable brands to sponsor dancers directly through programmable tokens and onchain contracts.",
      "subDiscipline": "brand engagement",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Smart contracts transparently automate sponsor payments and benefits with auditability.",
      "megaPrompt": "Build \"TokenMove Sponsorship\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnable brands to sponsor dancers directly through programmable tokens and onchain contracts.\nDiscipline: Dance & Choreography (brand engagement).\nOnchain primitive: Sepolia smart contract. Why this primitive: Smart contracts transparently automate sponsor payments and benefits with auditability.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/TokenMoveSponsorship.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title TokenMoveSponsorship\n/// @notice Enable brands to sponsor dancers directly through programmable tokens and onchain contracts.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract TokenMoveSponsorship {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a brand engagement action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance sponsorship market",
      "sam": "$400M — dance-brand partnership platforms",
      "som": "$30M — blockchain-enabled sponsorship users"
    },
    {
      "id": "dance-crowddance-licensing-23",
      "theme": "dance",
      "title": "CrowdDance Licensing",
      "pitch": "License remixed choreography created by crowdsourced dancers with onchain rights management.",
      "subDiscipline": "mass collaboration",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Smart contracts coordinate multi-owner rights and revenue splitting efficiently and fairly.",
      "megaPrompt": "Build \"CrowdDance Licensing\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nLicense remixed choreography created by crowdsourced dancers with onchain rights management.\nDiscipline: Dance & Choreography (mass collaboration).\nOnchain primitive: Sepolia smart contract. Why this primitive: Smart contracts coordinate multi-owner rights and revenue splitting efficiently and fairly.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CrowdDanceLicensing.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CrowdDanceLicensing\n/// @notice License remixed choreography created by crowdsourced dancers with onchain rights management.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CrowdDanceLicensing {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a mass collaboration action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — collaborative dance content market",
      "sam": "$350M — group licensing platforms",
      "som": "$28M — blockchain group rights users"
    },
    {
      "id": "dance-danceswap-collaborations-24",
      "theme": "dance",
      "title": "DanceSwap Collaborations",
      "pitch": "Facilitate peer-to-peer choreography collaboration and revenue sharing via smart contracts.",
      "subDiscipline": "peer collaboration",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Onchain contracts ensure trustless cooperation and fair profit distribution among artists.",
      "megaPrompt": "Build \"DanceSwap Collaborations\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nFacilitate peer-to-peer choreography collaboration and revenue sharing via smart contracts.\nDiscipline: Dance & Choreography (peer collaboration).\nOnchain primitive: Sepolia smart contract. Why this primitive: Onchain contracts ensure trustless cooperation and fair profit distribution among artists.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceSwapCollaborations.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title DanceSwapCollaborations\n/// @notice Facilitate peer-to-peer choreography collaboration and revenue sharing via smart contracts.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract DanceSwapCollaborations {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a peer collaboration action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance collaboration market",
      "sam": "$400M — collaborative creation platforms",
      "som": "$30M — blockchain collaboration adopters"
    },
    {
      "id": "dance-choreo-sequence-vault-0",
      "theme": "dance",
      "title": "Choreo Sequence Vault",
      "pitch": "Securely store and share choreographic sequences as immutable IPFS manifests for global access.",
      "subDiscipline": "dance notation archiving",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS ensures permanent, tamper-proof storage of choreographic data accessible anywhere.",
      "megaPrompt": "Build \"Choreo Sequence Vault\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecurely store and share choreographic sequences as immutable IPFS manifests for global access.\nDiscipline: Dance & Choreography (dance notation archiving).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS ensures permanent, tamper-proof storage of choreographic data accessible anywhere.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreoSequenceVault.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogChoreoSequenceVault\n/// @notice Securely store and share choreographic sequences as immutable IPFS manifests for global access.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogChoreoSequenceVault {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the dance notation archiving artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance studio software",
      "sam": "$1B — choreography archiving tools",
      "som": "$100M — cloud storage for dance educators"
    },
    {
      "id": "dance-movement-moodboard-1",
      "theme": "dance",
      "title": "Movement Moodboard",
      "pitch": "Create and pin curated dance inspiration boards with images and JSON metadata on IPFS.",
      "subDiscipline": "dance inspiration boards",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata JWT uploads enable decentralized and permanent preservation of moodboards.",
      "megaPrompt": "Build \"Movement Moodboard\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate and pin curated dance inspiration boards with images and JSON metadata on IPFS.\nDiscipline: Dance & Choreography (dance inspiration boards).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata JWT uploads enable decentralized and permanent preservation of moodboards.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MovementMoodboard.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogMovementMoodboard\n/// @notice Create and pin curated dance inspiration boards with images and JSON metadata on IPFS.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogMovementMoodboard {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the dance inspiration boards artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance studio software",
      "sam": "$300M — digital moodboard platforms",
      "som": "$30M — dance teacher resources"
    },
    {
      "id": "dance-dance-nft-gallery-2",
      "theme": "dance",
      "title": "Dance NFT Gallery",
      "pitch": "Artists mint and pin digital dance art portfolios as NFTs stored forever on IPFS.",
      "subDiscipline": "digital dance art",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata’s IPFS pinning guarantees permanent availability of NFT metadata and artworks.",
      "megaPrompt": "Build \"Dance NFT Gallery\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nArtists mint and pin digital dance art portfolios as NFTs stored forever on IPFS.\nDiscipline: Dance & Choreography (digital dance art).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata’s IPFS pinning guarantees permanent availability of NFT metadata and artworks.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceNFTGallery.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogDanceNFTGallery\n/// @notice Artists mint and pin digital dance art portfolios as NFTs stored forever on IPFS.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogDanceNFTGallery {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the digital dance art artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry digital products",
      "sam": "$500M — dance-related NFTs",
      "som": "$50M — niche dance digital collectibles"
    },
    {
      "id": "dance-choreographer-s-ledger-3",
      "theme": "dance",
      "title": "Choreographer’s Ledger",
      "pitch": "Record choreography permissions and usage rights as JSON manifests pinned on IPFS.",
      "subDiscipline": "IP rights management",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS immutability ensures proof of IP ownership and permission records.",
      "megaPrompt": "Build \"Choreographer’s Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nRecord choreography permissions and usage rights as JSON manifests pinned on IPFS.\nDiscipline: Dance & Choreography (IP rights management).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS immutability ensures proof of IP ownership and permission records.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreographersLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogChoreographersLedger\n/// @notice Record choreography permissions and usage rights as JSON manifests pinned on IPFS.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogChoreographersLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the IP rights management artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance IP management",
      "sam": "$800M — choreography licensing software",
      "som": "$70M — dance rights tracking"
    },
    {
      "id": "dance-technique-archive-hub-4",
      "theme": "dance",
      "title": "Technique Archive Hub",
      "pitch": "Pin and share detailed dance technique manuals and videos as permanent IPFS content.",
      "subDiscipline": "dance training materials",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS provides a censorship-resistant platform for long-term educational content storage.",
      "megaPrompt": "Build \"Technique Archive Hub\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin and share detailed dance technique manuals and videos as permanent IPFS content.\nDiscipline: Dance & Choreography (dance training materials).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS provides a censorship-resistant platform for long-term educational content storage.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/TechniqueArchiveHub.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogTechniqueArchiveHub\n/// @notice Pin and share detailed dance technique manuals and videos as permanent IPFS content.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogTechniqueArchiveHub {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the dance training materials artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance education market",
      "sam": "$600M — online dance training resources",
      "som": "$50M — technique tutorial distribution"
    },
    {
      "id": "dance-movement-metadata-mapper-5",
      "theme": "dance",
      "title": "Movement Metadata Mapper",
      "pitch": "Attach rich JSON metadata to dance performance images pinned via IPFS for enhanced searchability.",
      "subDiscipline": "dance data annotation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS stores immutable annotated datasets accessible globally without central servers.",
      "megaPrompt": "Build \"Movement Metadata Mapper\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAttach rich JSON metadata to dance performance images pinned via IPFS for enhanced searchability.\nDiscipline: Dance & Choreography (dance data annotation).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS stores immutable annotated datasets accessible globally without central servers.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MovementMetadataMapper.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogMovementMetadataMapper\n/// @notice Attach rich JSON metadata to dance performance images pinned via IPFS for enhanced searchability.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogMovementMetadataMapper {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the dance data annotation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance analytics services",
      "sam": "$200M — metadata management tools",
      "som": "$20M — dance performance data platforms"
    },
    {
      "id": "dance-global-dance-archive-6",
      "theme": "dance",
      "title": "Global Dance Archive",
      "pitch": "Digitize and pin cultural dance archives on IPFS preserving global dance heritage forever.",
      "subDiscipline": "dance heritage preservation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata’s IPFS ensures permanent, decentralized custody of invaluable dance history data.",
      "megaPrompt": "Build \"Global Dance Archive\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nDigitize and pin cultural dance archives on IPFS preserving global dance heritage forever.\nDiscipline: Dance & Choreography (dance heritage preservation).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata’s IPFS ensures permanent, decentralized custody of invaluable dance history data.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GlobalDanceArchive.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogGlobalDanceArchive\n/// @notice Digitize and pin cultural dance archives on IPFS preserving global dance heritage forever.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogGlobalDanceArchive {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the dance heritage preservation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — cultural heritage digitization",
      "sam": "$400M — dance archive initiatives",
      "som": "$40M — dance museum digital collections"
    },
    {
      "id": "dance-collaborative-choreo-file-7",
      "theme": "dance",
      "title": "Collaborative Choreo File",
      "pitch": "Teams pin evolving choreography JSON manifests on IPFS for real-time, decentralized collaboration.",
      "subDiscipline": "group choreography workflow",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS allows versioned, immutable storage accessible to all team members without downtime.",
      "megaPrompt": "Build \"Collaborative Choreo File\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTeams pin evolving choreography JSON manifests on IPFS for real-time, decentralized collaboration.\nDiscipline: Dance & Choreography (group choreography workflow).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS allows versioned, immutable storage accessible to all team members without downtime.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CollaborativeChoreoFile.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogCollaborativeChoreoFile\n/// @notice Teams pin evolving choreography JSON manifests on IPFS for real-time, decentralized collaboration.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogCollaborativeChoreoFile {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the group choreography workflow artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance collaboration tools",
      "sam": "$150M — workflow management apps",
      "som": "$15M — choreographer team software"
    },
    {
      "id": "dance-dance-move-tokens-8",
      "theme": "dance",
      "title": "Dance Move Tokens",
      "pitch": "Create, pin, and trade unique dance moves with JSON metadata on IPFS representing rights.",
      "subDiscipline": "movement licensing",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata facilitates permanent off-chain storage of token metadata ensuring rights transparency.",
      "megaPrompt": "Build \"Dance Move Tokens\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate, pin, and trade unique dance moves with JSON metadata on IPFS representing rights.\nDiscipline: Dance & Choreography (movement licensing).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata facilitates permanent off-chain storage of token metadata ensuring rights transparency.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceMoveTokens.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogDanceMoveTokens\n/// @notice Create, pin, and trade unique dance moves with JSON metadata on IPFS representing rights.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogDanceMoveTokens {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the movement licensing artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance IP monetization",
      "sam": "$400M — dance move marketplaces",
      "som": "$35M — digital dance asset trading"
    },
    {
      "id": "dance-virtual-rehearsal-logs-9",
      "theme": "dance",
      "title": "Virtual Rehearsal Logs",
      "pitch": "Pin JSON logs of rehearsal notes and images on IPFS for permanent, shareable practice records.",
      "subDiscipline": "practice session tracking",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS ensures immutable, censorship-resistant rehearsal documentation accessible anytime.",
      "megaPrompt": "Build \"Virtual Rehearsal Logs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin JSON logs of rehearsal notes and images on IPFS for permanent, shareable practice records.\nDiscipline: Dance & Choreography (practice session tracking).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS ensures immutable, censorship-resistant rehearsal documentation accessible anytime.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/VirtualRehearsalLogs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogVirtualRehearsalLogs\n/// @notice Pin JSON logs of rehearsal notes and images on IPFS for permanent, shareable practice records.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogVirtualRehearsalLogs {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the practice session tracking artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance training tools",
      "sam": "$100M — rehearsal management software",
      "som": "$10M — dancer practice tracking"
    },
    {
      "id": "dance-interactive-dance-maps-10",
      "theme": "dance",
      "title": "Interactive Dance Maps",
      "pitch": "Pin visual and JSON data of choreography spatial maps on IPFS for decentralized access and reuse.",
      "subDiscipline": "movement spatialization",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS stores complex multimedia choreography spatializations without central failures.",
      "megaPrompt": "Build \"Interactive Dance Maps\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin visual and JSON data of choreography spatial maps on IPFS for decentralized access and reuse.\nDiscipline: Dance & Choreography (movement spatialization).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS stores complex multimedia choreography spatializations without central failures.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/InteractiveDanceMaps.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogInteractiveDanceMaps\n/// @notice Pin visual and JSON data of choreography spatial maps on IPFS for decentralized access and reuse.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogInteractiveDanceMaps {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the movement spatialization artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance technology market",
      "sam": "$180M — spatial choreography tools",
      "som": "$18M — movement mapping software"
    },
    {
      "id": "dance-ai-dance-dataset-repository-11",
      "theme": "dance",
      "title": "AI Dance Dataset Repository",
      "pitch": "Pin curated datasets of labeled dance images and JSON metadata on IPFS for AI training.",
      "subDiscipline": "machine learning data",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS guarantees permanent, verifiable storage of valuable AI training data.",
      "megaPrompt": "Build \"AI Dance Dataset Repository\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin curated datasets of labeled dance images and JSON metadata on IPFS for AI training.\nDiscipline: Dance & Choreography (machine learning data).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS guarantees permanent, verifiable storage of valuable AI training data.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/AIDanceDatasetRepository.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogAIDanceDatasetRepository\n/// @notice Pin curated datasets of labeled dance images and JSON metadata on IPFS for AI training.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogAIDanceDatasetRepository {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the machine learning data artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — AI dance innovation",
      "sam": "$250M — dance AI datasets",
      "som": "$25M — machine learning data hubs"
    },
    {
      "id": "dance-dance-festival-archive-12",
      "theme": "dance",
      "title": "Dance Festival Archive",
      "pitch": "Pin photos and JSON manifests of live dance festival performances on IPFS for posterity and access.",
      "subDiscipline": "event documentation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata enables permanent, decentralized storage preventing loss of live event records.",
      "megaPrompt": "Build \"Dance Festival Archive\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin photos and JSON manifests of live dance festival performances on IPFS for posterity and access.\nDiscipline: Dance & Choreography (event documentation).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata enables permanent, decentralized storage preventing loss of live event records.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceFestivalArchive.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogDanceFestivalArchive\n/// @notice Pin photos and JSON manifests of live dance festival performances on IPFS for posterity and access.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogDanceFestivalArchive {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the event documentation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance event market",
      "sam": "$300M — festival media archiving",
      "som": "$30M — digital dance event libraries"
    },
    {
      "id": "dance-costume-design-catalog-13",
      "theme": "dance",
      "title": "Costume Design Catalog",
      "pitch": "Pin images and JSON design manifests of dance costumes on IPFS for long-term design preservation.",
      "subDiscipline": "dance wardrobe archives",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS provides immutable, decentralized storage ensuring no loss of costume records.",
      "megaPrompt": "Build \"Costume Design Catalog\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin images and JSON design manifests of dance costumes on IPFS for long-term design preservation.\nDiscipline: Dance & Choreography (dance wardrobe archives).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS provides immutable, decentralized storage ensuring no loss of costume records.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CostumeDesignCatalog.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogCostumeDesignCatalog\n/// @notice Pin images and JSON design manifests of dance costumes on IPFS for long-term design preservation.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogCostumeDesignCatalog {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the dance wardrobe archives artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance production resources",
      "sam": "$150M — costume design archives",
      "som": "$12M — dance wardrobe databases"
    },
    {
      "id": "dance-movement-emotion-tags-14",
      "theme": "dance",
      "title": "Movement Emotion Tags",
      "pitch": "Pin emotionally tagged dance movement metadata on IPFS to enhance creative interpretative tools.",
      "subDiscipline": "affective annotation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS stores richly annotated JSON ensuring permanent availability for emotion-based indexing.",
      "megaPrompt": "Build \"Movement Emotion Tags\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin emotionally tagged dance movement metadata on IPFS to enhance creative interpretative tools.\nDiscipline: Dance & Choreography (affective annotation).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS stores richly annotated JSON ensuring permanent availability for emotion-based indexing.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MovementEmotionTags.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogMovementEmotionTags\n/// @notice Pin emotionally tagged dance movement metadata on IPFS to enhance creative interpretative tools.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogMovementEmotionTags {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the affective annotation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance tech innovation",
      "sam": "$100M — affective computing tools",
      "som": "$11M — emotion annotation tools"
    },
    {
      "id": "dance-dance-challenge-repository-15",
      "theme": "dance",
      "title": "Dance Challenge Repository",
      "pitch": "Pin community dance challenge videos and JSON instructions on IPFS for global participation.",
      "subDiscipline": "community choreography",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS enables permanent, censorship-resistant hosting of viral dance challenges globally.",
      "megaPrompt": "Build \"Dance Challenge Repository\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin community dance challenge videos and JSON instructions on IPFS for global participation.\nDiscipline: Dance & Choreography (community choreography).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS enables permanent, censorship-resistant hosting of viral dance challenges globally.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceChallengeRepository.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogDanceChallengeRepository\n/// @notice Pin community dance challenge videos and JSON instructions on IPFS for global participation.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogDanceChallengeRepository {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the community choreography artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — social dance platforms",
      "sam": "$350M — online dance communities",
      "som": "$40M — challenge content hosting"
    },
    {
      "id": "dance-movement-style-index-16",
      "theme": "dance",
      "title": "Movement Style Index",
      "pitch": "Pin genre-classified dance move datasets with JSON metadata on IPFS for style machine parsing.",
      "subDiscipline": "genre classification",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS stores large stylistic datasets immutably accessible for genre recognition algorithms.",
      "megaPrompt": "Build \"Movement Style Index\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin genre-classified dance move datasets with JSON metadata on IPFS for style machine parsing.\nDiscipline: Dance & Choreography (genre classification).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS stores large stylistic datasets immutably accessible for genre recognition algorithms.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MovementStyleIndex.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogMovementStyleIndex\n/// @notice Pin genre-classified dance move datasets with JSON metadata on IPFS for style machine parsing.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogMovementStyleIndex {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the genre classification artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance analytics",
      "sam": "$220M — style classification tools",
      "som": "$22M — dance data indexing"
    },
    {
      "id": "dance-live-choreo-snapshot-17",
      "theme": "dance",
      "title": "Live Choreo Snapshot",
      "pitch": "Pin image and JSON snapshots of live choreography moments on IPFS for historical timestamping.",
      "subDiscipline": "performance capture",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS ensures permanent, verifiable storage of live performance digital artifacts.",
      "megaPrompt": "Build \"Live Choreo Snapshot\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin image and JSON snapshots of live choreography moments on IPFS for historical timestamping.\nDiscipline: Dance & Choreography (performance capture).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS ensures permanent, verifiable storage of live performance digital artifacts.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/LiveChoreoSnapshot.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogLiveChoreoSnapshot\n/// @notice Pin image and JSON snapshots of live choreography moments on IPFS for historical timestamping.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogLiveChoreoSnapshot {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the performance capture artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — live performance tech",
      "sam": "$180M — performance capture tools",
      "som": "$15M — dance show archiving"
    },
    {
      "id": "dance-dance-studio-portfolio-18",
      "theme": "dance",
      "title": "Dance Studio Portfolio",
      "pitch": "Studios pin portfolios of images and JSON class manifests on IPFS to showcase offerings forever.",
      "subDiscipline": "business marketing",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS secures studio marketing materials with permanent, decentralized hosting.",
      "megaPrompt": "Build \"Dance Studio Portfolio\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nStudios pin portfolios of images and JSON class manifests on IPFS to showcase offerings forever.\nDiscipline: Dance & Choreography (business marketing).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS secures studio marketing materials with permanent, decentralized hosting.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceStudioPortfolio.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogDanceStudioPortfolio\n/// @notice Studios pin portfolios of images and JSON class manifests on IPFS to showcase offerings forever.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogDanceStudioPortfolio {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the business marketing artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance studio software",
      "sam": "$500M — studio marketing platforms",
      "som": "$50M — dance business portfolios"
    },
    {
      "id": "dance-choreography-remix-log-19",
      "theme": "dance",
      "title": "Choreography Remix Log",
      "pitch": "Pin JSON manifests of choreographic remixes and images on IPFS to document creative lineage.",
      "subDiscipline": "creative versioning",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS allows immutable, traceable version histories of choreography remixes.",
      "megaPrompt": "Build \"Choreography Remix Log\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin JSON manifests of choreographic remixes and images on IPFS to document creative lineage.\nDiscipline: Dance & Choreography (creative versioning).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS allows immutable, traceable version histories of choreography remixes.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreographyRemixLog.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogChoreographyRemixLog\n/// @notice Pin JSON manifests of choreographic remixes and images on IPFS to document creative lineage.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogChoreographyRemixLog {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the creative versioning artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance creative tools",
      "sam": "$200M — choreography version control",
      "som": "$18M — remix documentation"
    },
    {
      "id": "dance-dance-props-inventory-20",
      "theme": "dance",
      "title": "Dance Props Inventory",
      "pitch": "Pin images and JSON manifests of dance prop inventories to IPFS for decentralized sharing.",
      "subDiscipline": "production resource management",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS guarantees permanent, accessible storage of production resource data.",
      "megaPrompt": "Build \"Dance Props Inventory\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin images and JSON manifests of dance prop inventories to IPFS for decentralized sharing.\nDiscipline: Dance & Choreography (production resource management).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS guarantees permanent, accessible storage of production resource data.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DancePropsInventory.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogDancePropsInventory\n/// @notice Pin images and JSON manifests of dance prop inventories to IPFS for decentralized sharing.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogDancePropsInventory {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the production resource management artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance production management",
      "sam": "$120M — prop rental platforms",
      "som": "$10M — studio inventory systems"
    },
    {
      "id": "dance-movement-annotation-tool-21",
      "theme": "dance",
      "title": "Movement Annotation Tool",
      "pitch": "Pin annotated movement images and JSON metadata on IPFS to support global dance research.",
      "subDiscipline": "dance research",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS provides immutable, globally accessible data for academic and artistic analysis.",
      "megaPrompt": "Build \"Movement Annotation Tool\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin annotated movement images and JSON metadata on IPFS to support global dance research.\nDiscipline: Dance & Choreography (dance research).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS provides immutable, globally accessible data for academic and artistic analysis.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MovementAnnotationTool.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogMovementAnnotationTool\n/// @notice Pin annotated movement images and JSON metadata on IPFS to support global dance research.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogMovementAnnotationTool {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the dance research artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance research tools",
      "sam": "$80M — annotation software",
      "som": "$8M — academic dance databases"
    },
    {
      "id": "dance-dance-therapy-logs-22",
      "theme": "dance",
      "title": "Dance Therapy Logs",
      "pitch": "Pin JSON and image records of dance therapy sessions on IPFS for secure patient histories.",
      "subDiscipline": "health documentation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS ensures confidential, permanent, and decentralized health session storage.",
      "megaPrompt": "Build \"Dance Therapy Logs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin JSON and image records of dance therapy sessions on IPFS for secure patient histories.\nDiscipline: Dance & Choreography (health documentation).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS ensures confidential, permanent, and decentralized health session storage.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceTherapyLogs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogDanceTherapyLogs\n/// @notice Pin JSON and image records of dance therapy sessions on IPFS for secure patient histories.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogDanceTherapyLogs {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the health documentation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance therapy market",
      "sam": "$90M — therapy practice software",
      "som": "$9M — patient session archiving"
    },
    {
      "id": "dance-interactive-step-manuals-23",
      "theme": "dance",
      "title": "Interactive Step Manuals",
      "pitch": "Pin interactive dance step image guides plus JSON instructions on IPFS for teacher-student use.",
      "subDiscipline": "teaching aids",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS supports permanent, decentralized hosting of teaching content accessible offline.",
      "megaPrompt": "Build \"Interactive Step Manuals\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin interactive dance step image guides plus JSON instructions on IPFS for teacher-student use.\nDiscipline: Dance & Choreography (teaching aids).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS supports permanent, decentralized hosting of teaching content accessible offline.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/InteractiveStepManuals.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogInteractiveStepManuals\n/// @notice Pin interactive dance step image guides plus JSON instructions on IPFS for teacher-student use.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogInteractiveStepManuals {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the teaching aids artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance education",
      "sam": "$400M — teaching resource platforms",
      "som": "$35M — dance manual distribution"
    },
    {
      "id": "dance-movement-patent-registry-24",
      "theme": "dance",
      "title": "Movement Patent Registry",
      "pitch": "Pin JSON manifests documenting novel dance moves on IPFS to certify original creation dates.",
      "subDiscipline": "innovation tracking",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS provides immutable timestamped records critical for innovation proofing.",
      "megaPrompt": "Build \"Movement Patent Registry\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin JSON manifests documenting novel dance moves on IPFS to certify original creation dates.\nDiscipline: Dance & Choreography (innovation tracking).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS provides immutable timestamped records critical for innovation proofing.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MovementPatentRegistry.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogMovementPatentRegistry\n/// @notice Pin JSON manifests documenting novel dance moves on IPFS to certify original creation dates.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogMovementPatentRegistry {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the innovation tracking artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance IP protection",
      "sam": "$300M — innovation registries",
      "som": "$28M — dance patent recordkeeping"
    },
    {
      "id": "dance-gasless-dance-battles-0",
      "theme": "dance",
      "title": "Gasless Dance Battles",
      "pitch": "Enable dancers to join and sponsor dance battles without paying gas fees.",
      "subDiscipline": "competitive choreography",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social with sponsored tx removes friction of blockchain fees in quick peer competitions.",
      "megaPrompt": "Build \"Gasless Dance Battles\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnable dancers to join and sponsor dance battles without paying gas fees.\nDiscipline: Dance & Choreography (competitive choreography).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social with sponsored tx removes friction of blockchain fees in quick peer competitions.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GaslessDanceBattles.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogGaslessDanceBattles\n/// @notice Enable dancers to join and sponsor dance battles without paying gas fees.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogGaslessDanceBattles {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every competitive choreography action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry revenue",
      "sam": "$600M — online dance battle platforms",
      "som": "$30M — active dance battle participants paying digitally"
    },
    {
      "id": "dance-choreo-nft-vault-1",
      "theme": "dance",
      "title": "Choreo NFT Vault",
      "pitch": "Securely store and share choreography as NFTs without users handling gas payments.",
      "subDiscipline": "digital choreography ownership",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet with Google sign-in and sponsored tx eases NFT minting experience.",
      "megaPrompt": "Build \"Choreo NFT Vault\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecurely store and share choreography as NFTs without users handling gas payments.\nDiscipline: Dance & Choreography (digital choreography ownership).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet with Google sign-in and sponsored tx eases NFT minting experience.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreoNFTVault.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogChoreoNFTVault\n/// @notice Securely store and share choreography as NFTs without users handling gas payments.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogChoreoNFTVault {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every digital choreography ownership action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance content monetization",
      "sam": "$800M — dance NFT market segment",
      "som": "$50M — choreographers minting digital rights"
    },
    {
      "id": "dance-studio-access-club-2",
      "theme": "dance",
      "title": "Studio Access Club",
      "pitch": "Create token-gated memberships for dance studios with seamless gas-free onboarding.",
      "subDiscipline": "membership management",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Embedded wallet and sponsored transactions simplify user entry and retention.",
      "megaPrompt": "Build \"Studio Access Club\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate token-gated memberships for dance studios with seamless gas-free onboarding.\nDiscipline: Dance & Choreography (membership management).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Embedded wallet and sponsored transactions simplify user entry and retention.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StudioAccessClub.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogStudioAccessClub\n/// @notice Create token-gated memberships for dance studios with seamless gas-free onboarding.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogStudioAccessClub {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every membership management action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance studio operations",
      "sam": "$1B — dance studio subscription services",
      "som": "$60M — studios adopting digital memberships"
    },
    {
      "id": "dance-movement-royalty-tracker-3",
      "theme": "dance",
      "title": "Movement Royalty Tracker",
      "pitch": "Automatically track and reward choreographers when their moves get used commercially.",
      "subDiscipline": "usage rights",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social plus sponsored tx enable transparent royalty distribution without gas burden.",
      "megaPrompt": "Build \"Movement Royalty Tracker\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAutomatically track and reward choreographers when their moves get used commercially.\nDiscipline: Dance & Choreography (usage rights).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social plus sponsored tx enable transparent royalty distribution without gas burden.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MovementRoyaltyTracker.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogMovementRoyaltyTracker\n/// @notice Automatically track and reward choreographers when their moves get used commercially.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogMovementRoyaltyTracker {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every usage rights action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance content licensing",
      "sam": "$700M — choreography rights market",
      "som": "$40M — royalty payments automated"
    },
    {
      "id": "dance-gas-free-dance-voting-4",
      "theme": "dance",
      "title": "Gas-Free Dance Voting",
      "pitch": "Host dance community polls and awards with zero gas cost for voters.",
      "subDiscipline": "community decision-making",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Google sign-in and sponsored tx streamline inclusive community governance.",
      "megaPrompt": "Build \"Gas-Free Dance Voting\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nHost dance community polls and awards with zero gas cost for voters.\nDiscipline: Dance & Choreography (community decision-making).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Google sign-in and sponsored tx streamline inclusive community governance.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GasFreeDanceVoting.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogGasFreeDanceVoting\n/// @notice Host dance community polls and awards with zero gas cost for voters.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogGasFreeDanceVoting {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every community decision-making action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry engagement",
      "sam": "$500M — digital dance community activities",
      "som": "$25M — community voting participants"
    },
    {
      "id": "dance-choreo-collaboration-hub-5",
      "theme": "dance",
      "title": "Choreo Collaboration Hub",
      "pitch": "Facilitate collaborative dance projects with blockchain-secured contribution records, gas-free.",
      "subDiscipline": "joint choreography creation",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet and sponsored tx enable seamless multi-user editing without crypto hurdles.",
      "megaPrompt": "Build \"Choreo Collaboration Hub\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nFacilitate collaborative dance projects with blockchain-secured contribution records, gas-free.\nDiscipline: Dance & Choreography (joint choreography creation).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet and sponsored tx enable seamless multi-user editing without crypto hurdles.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreoCollaborationHub.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogChoreoCollaborationHub\n/// @notice Facilitate collaborative dance projects with blockchain-secured contribution records, gas-free.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogChoreoCollaborationHub {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every joint choreography creation action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance creative projects",
      "sam": "$650M — collaborative dance platforms",
      "som": "$35M — choreographers collaborating online"
    },
    {
      "id": "dance-dance-event-tickets-6",
      "theme": "dance",
      "title": "Dance Event Tickets",
      "pitch": "Sell and distribute dance event tickets as NFTs with no gas for buyers.",
      "subDiscipline": "ticketing system",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Embedded wallet bootstrapped and sponsored tx reduce friction in ticket ownership.",
      "megaPrompt": "Build \"Dance Event Tickets\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSell and distribute dance event tickets as NFTs with no gas for buyers.\nDiscipline: Dance & Choreography (ticketing system).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Embedded wallet bootstrapped and sponsored tx reduce friction in ticket ownership.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceEventTickets.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogDanceEventTickets\n/// @notice Sell and distribute dance event tickets as NFTs with no gas for buyers.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogDanceEventTickets {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every ticketing system action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance live events",
      "sam": "$900M — digital event ticket sales",
      "som": "$70M — NFT ticket adopters in dance"
    },
    {
      "id": "dance-gasless-dance-royalties-7",
      "theme": "dance",
      "title": "Gasless Dance Royalties",
      "pitch": "Pay royalties to dancers and choreographers instantly with no transaction fees.",
      "subDiscipline": "automatic payments",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Sponsored transactions ensure fast, costless royalty distribution via Privy wallet.",
      "megaPrompt": "Build \"Gasless Dance Royalties\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPay royalties to dancers and choreographers instantly with no transaction fees.\nDiscipline: Dance & Choreography (automatic payments).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Sponsored transactions ensure fast, costless royalty distribution via Privy wallet.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GaslessDanceRoyalties.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogGaslessDanceRoyalties\n/// @notice Pay royalties to dancers and choreographers instantly with no transaction fees.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogGaslessDanceRoyalties {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every automatic payments action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance content monetization",
      "sam": "$750M — royalty payment platforms",
      "som": "$45M — royalty payees using blockchain"
    },
    {
      "id": "dance-dance-move-provenance-8",
      "theme": "dance",
      "title": "Dance Move Provenance",
      "pitch": "Verify authenticity and origin of iconic dance moves onchain, gaslessly.",
      "subDiscipline": "authenticity verification",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social plus sponsored tx allow users to prove originality without gas costs.",
      "megaPrompt": "Build \"Dance Move Provenance\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nVerify authenticity and origin of iconic dance moves onchain, gaslessly.\nDiscipline: Dance & Choreography (authenticity verification).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social plus sponsored tx allow users to prove originality without gas costs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceMoveProvenance.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogDanceMoveProvenance\n/// @notice Verify authenticity and origin of iconic dance moves onchain, gaslessly.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogDanceMoveProvenance {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every authenticity verification action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance IP protection",
      "sam": "$550M — digital dance authenticity tools",
      "som": "$28M — verified dance creators"
    },
    {
      "id": "dance-gas-free-dance-workshops-9",
      "theme": "dance",
      "title": "Gas-Free Dance Workshops",
      "pitch": "Enroll and attend dance workshops with blockchain-secured certificates, no gas fees.",
      "subDiscipline": "online learning",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet and sponsored tx improve user onboarding and certificate issuance.",
      "megaPrompt": "Build \"Gas-Free Dance Workshops\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnroll and attend dance workshops with blockchain-secured certificates, no gas fees.\nDiscipline: Dance & Choreography (online learning).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet and sponsored tx improve user onboarding and certificate issuance.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GasFreeDanceWorkshops.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogGasFreeDanceWorkshops\n/// @notice Enroll and attend dance workshops with blockchain-secured certificates, no gas fees.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogGasFreeDanceWorkshops {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every online learning action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance education market",
      "sam": "$1.2B — online dance classes",
      "som": "$80M — blockchain-certified workshop attendees"
    },
    {
      "id": "dance-choreo-token-rewards-10",
      "theme": "dance",
      "title": "Choreo Token Rewards",
      "pitch": "Reward choreographers with tokens for popular routines without requiring gas payments.",
      "subDiscipline": "creator incentives",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Embedded wallet and sponsored transactions streamline token distribution to creators.",
      "megaPrompt": "Build \"Choreo Token Rewards\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nReward choreographers with tokens for popular routines without requiring gas payments.\nDiscipline: Dance & Choreography (creator incentives).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Embedded wallet and sponsored transactions streamline token distribution to creators.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreoTokenRewards.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogChoreoTokenRewards\n/// @notice Reward choreographers with tokens for popular routines without requiring gas payments.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogChoreoTokenRewards {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every creator incentives action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance creator economy",
      "sam": "$600M — dance tokenized incentives",
      "som": "$35M — rewarded choreographers"
    },
    {
      "id": "dance-dance-gear-marketplace-11",
      "theme": "dance",
      "title": "Dance Gear Marketplace",
      "pitch": "Buy and sell dance apparel and gear as NFTs with gasless transaction experience.",
      "subDiscipline": "digital commerce",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social wallet and sponsored tx enable smooth gas-free commerce.",
      "megaPrompt": "Build \"Dance Gear Marketplace\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nBuy and sell dance apparel and gear as NFTs with gasless transaction experience.\nDiscipline: Dance & Choreography (digital commerce).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social wallet and sponsored tx enable smooth gas-free commerce.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceGearMarketplace.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogDanceGearMarketplace\n/// @notice Buy and sell dance apparel and gear as NFTs with gasless transaction experience.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogDanceGearMarketplace {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every digital commerce action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance retail and merchandise",
      "sam": "$700M — digital dance gear sales",
      "som": "$40M — NFT marketplace participants"
    },
    {
      "id": "dance-movement-data-wallet-12",
      "theme": "dance",
      "title": "Movement Data Wallet",
      "pitch": "Securely store and share movement data NFTs with no blockchain fees for dancers.",
      "subDiscipline": "performance analytics",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy embedded wallet and sponsored tx ensure easy access and sharing of data.",
      "megaPrompt": "Build \"Movement Data Wallet\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecurely store and share movement data NFTs with no blockchain fees for dancers.\nDiscipline: Dance & Choreography (performance analytics).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy embedded wallet and sponsored tx ensure easy access and sharing of data.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MovementDataWallet.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogMovementDataWallet\n/// @notice Securely store and share movement data NFTs with no blockchain fees for dancers.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogMovementDataWallet {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every performance analytics action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance tech analytics market",
      "sam": "$400M — movement analytics services",
      "som": "$22M — dancers using data NFTs"
    },
    {
      "id": "dance-gasless-dance-challenges-13",
      "theme": "dance",
      "title": "Gasless Dance Challenges",
      "pitch": "Launch viral dance challenges with onchain proof and no gas cost for participants.",
      "subDiscipline": "social engagement",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet and sponsored transactions maximize participation by eliminating fees.",
      "megaPrompt": "Build \"Gasless Dance Challenges\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nLaunch viral dance challenges with onchain proof and no gas cost for participants.\nDiscipline: Dance & Choreography (social engagement).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet and sponsored transactions maximize participation by eliminating fees.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GaslessDanceChallenges.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogGaslessDanceChallenges\n/// @notice Launch viral dance challenges with onchain proof and no gas cost for participants.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogGaslessDanceChallenges {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every social engagement action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance social media influence",
      "sam": "$500M — dance challenge platforms",
      "som": "$30M — active challenge participants"
    },
    {
      "id": "dance-dance-nft-gallery-14",
      "theme": "dance",
      "title": "Dance NFT Gallery",
      "pitch": "Host curated dance NFT galleries that users can explore without paying gas.",
      "subDiscipline": "exhibition curation",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Google sign-in based wallet plus sponsored tx simplify visitor experience.",
      "megaPrompt": "Build \"Dance NFT Gallery\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nHost curated dance NFT galleries that users can explore without paying gas.\nDiscipline: Dance & Choreography (exhibition curation).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Google sign-in based wallet plus sponsored tx simplify visitor experience.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceNFTGallery.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogDanceNFTGallery\n/// @notice Host curated dance NFT galleries that users can explore without paying gas.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogDanceNFTGallery {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every exhibition curation action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance digital art exhibitions",
      "sam": "$450M — online dance galleries",
      "som": "$27M — NFT gallery visitors"
    },
    {
      "id": "dance-choreography-licensing-hub-15",
      "theme": "dance",
      "title": "Choreography Licensing Hub",
      "pitch": "License dance routines with transparent blockchain contracts and gasless transactions.",
      "subDiscipline": "rights management",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy embedded wallet with sponsored tx reduces licensing friction.",
      "megaPrompt": "Build \"Choreography Licensing Hub\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nLicense dance routines with transparent blockchain contracts and gasless transactions.\nDiscipline: Dance & Choreography (rights management).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy embedded wallet with sponsored tx reduces licensing friction.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreographyLicensingHub.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogChoreographyLicensingHub\n/// @notice License dance routines with transparent blockchain contracts and gasless transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogChoreographyLicensingHub {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every rights management action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance licensing market",
      "sam": "$800M — digital choreography licenses",
      "som": "$50M — licensees using platform"
    },
    {
      "id": "dance-gas-free-dance-nfts-16",
      "theme": "dance",
      "title": "Gas-Free Dance NFTs",
      "pitch": "Mint and trade unique dance moment NFTs without gas fees for fans and artists.",
      "subDiscipline": "blockchain collectibles",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet and sponsored transactions eliminate gas barriers for casual users.",
      "megaPrompt": "Build \"Gas-Free Dance NFTs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint and trade unique dance moment NFTs without gas fees for fans and artists.\nDiscipline: Dance & Choreography (blockchain collectibles).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet and sponsored transactions eliminate gas barriers for casual users.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GasFreeDanceNFTs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogGasFreeDanceNFTs\n/// @notice Mint and trade unique dance moment NFTs without gas fees for fans and artists.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogGasFreeDanceNFTs {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every blockchain collectibles action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance NFT collectibles",
      "sam": "$700M — dance-related NFT volume",
      "som": "$40M — active dance NFT collectors"
    },
    {
      "id": "dance-virtual-dance-studios-17",
      "theme": "dance",
      "title": "Virtual Dance Studios",
      "pitch": "Access and customize virtual dance studios with gasless token gating.",
      "subDiscipline": "metaverse dance spaces",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social wallet and sponsored tx make metaverse entry seamless.",
      "megaPrompt": "Build \"Virtual Dance Studios\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAccess and customize virtual dance studios with gasless token gating.\nDiscipline: Dance & Choreography (metaverse dance spaces).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social wallet and sponsored tx make metaverse entry seamless.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/VirtualDanceStudios.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogVirtualDanceStudios\n/// @notice Access and customize virtual dance studios with gasless token gating.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogVirtualDanceStudios {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every metaverse dance spaces action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance virtual reality market",
      "sam": "$900M — virtual dance studio platforms",
      "som": "$60M — active virtual studio users"
    },
    {
      "id": "dance-performance-ticket-nfts-18",
      "theme": "dance",
      "title": "Performance Ticket NFTs",
      "pitch": "Issue and verify dance performance tickets as NFTs with no gas costs for attendees.",
      "subDiscipline": "ticketing and access",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy embedded wallet and sponsored tx simplify ticket ownership experience.",
      "megaPrompt": "Build \"Performance Ticket NFTs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nIssue and verify dance performance tickets as NFTs with no gas costs for attendees.\nDiscipline: Dance & Choreography (ticketing and access).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy embedded wallet and sponsored tx simplify ticket ownership experience.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PerformanceTicketNFTs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogPerformanceTicketNFTs\n/// @notice Issue and verify dance performance tickets as NFTs with no gas costs for attendees.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogPerformanceTicketNFTs {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every ticketing and access action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance live performance revenue",
      "sam": "$850M — NFT ticketing market",
      "som": "$55M — NFT ticket users"
    },
    {
      "id": "dance-gasless-dance-socials-19",
      "theme": "dance",
      "title": "Gasless Dance Socials",
      "pitch": "Create dance-focused social networks with token rewards and no gas transaction fees.",
      "subDiscipline": "community building",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet and sponsored tx drive engagement with zero cost actions.",
      "megaPrompt": "Build \"Gasless Dance Socials\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate dance-focused social networks with token rewards and no gas transaction fees.\nDiscipline: Dance & Choreography (community building).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet and sponsored tx drive engagement with zero cost actions.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GaslessDanceSocials.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogGaslessDanceSocials\n/// @notice Create dance-focused social networks with token rewards and no gas transaction fees.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogGaslessDanceSocials {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every community building action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance social media market",
      "sam": "$600M — dance community platforms",
      "som": "$33M — active social network users"
    },
    {
      "id": "dance-choreographer-profiles-20",
      "theme": "dance",
      "title": "Choreographer Profiles",
      "pitch": "Build verified onchain profiles for choreographers with gasless updates and endorsements.",
      "subDiscipline": "professional branding",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social wallet plus sponsored tx streamline identity management.",
      "megaPrompt": "Build \"Choreographer Profiles\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nBuild verified onchain profiles for choreographers with gasless updates and endorsements.\nDiscipline: Dance & Choreography (professional branding).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social wallet plus sponsored tx streamline identity management.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreographerProfiles.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogChoreographerProfiles\n/// @notice Build verified onchain profiles for choreographers with gasless updates and endorsements.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogChoreographerProfiles {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every professional branding action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance professional services",
      "sam": "$650M — online dancer portfolios",
      "som": "$36M — choreographer profile users"
    },
    {
      "id": "dance-dance-nft-streaming-21",
      "theme": "dance",
      "title": "Dance NFT Streaming",
      "pitch": "Stream exclusive dance content NFTs to fans without requiring gas payments.",
      "subDiscipline": "content distribution",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet and sponsored tx allow smooth access to paid streams.",
      "megaPrompt": "Build \"Dance NFT Streaming\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nStream exclusive dance content NFTs to fans without requiring gas payments.\nDiscipline: Dance & Choreography (content distribution).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet and sponsored tx allow smooth access to paid streams.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceNFTStreaming.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogDanceNFTStreaming\n/// @notice Stream exclusive dance content NFTs to fans without requiring gas payments.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogDanceNFTStreaming {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every content distribution action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance digital media market",
      "sam": "$700M — dance NFT streaming",
      "som": "$42M — paying stream viewers"
    },
    {
      "id": "dance-movement-token-tips-22",
      "theme": "dance",
      "title": "Movement Token Tips",
      "pitch": "Let fans tip dancers instantly with tokens and no gas fees.",
      "subDiscipline": "fan support",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy embedded wallet and sponsored tx remove barriers to microtransactions.",
      "megaPrompt": "Build \"Movement Token Tips\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nLet fans tip dancers instantly with tokens and no gas fees.\nDiscipline: Dance & Choreography (fan support).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy embedded wallet and sponsored tx remove barriers to microtransactions.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MovementTokenTips.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogMovementTokenTips\n/// @notice Let fans tip dancers instantly with tokens and no gas fees.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogMovementTokenTips {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every fan support action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance fan economy",
      "sam": "$550M — digital tipping platforms",
      "som": "$30M — active tip recipients"
    },
    {
      "id": "dance-gasless-dance-analytics-23",
      "theme": "dance",
      "title": "Gasless Dance Analytics",
      "pitch": "Access detailed dance performance analytics with blockchain-backed data, no gas required.",
      "subDiscipline": "performance insights",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet plus sponsored tx facilitate data queries at zero user cost.",
      "megaPrompt": "Build \"Gasless Dance Analytics\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAccess detailed dance performance analytics with blockchain-backed data, no gas required.\nDiscipline: Dance & Choreography (performance insights).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet plus sponsored tx facilitate data queries at zero user cost.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GaslessDanceAnalytics.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogGaslessDanceAnalytics\n/// @notice Access detailed dance performance analytics with blockchain-backed data, no gas required.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogGaslessDanceAnalytics {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every performance insights action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance performance tech",
      "sam": "$450M — analytics subscription services",
      "som": "$25M — users accessing data analytics"
    },
    {
      "id": "dance-nft-dance-collectives-24",
      "theme": "dance",
      "title": "NFT Dance Collectives",
      "pitch": "Form dance groups managing collective NFTs and revenue with gasless transactions.",
      "subDiscipline": "group ownership",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social and sponsored tx enable easy group wallet management.",
      "megaPrompt": "Build \"NFT Dance Collectives\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nForm dance groups managing collective NFTs and revenue with gasless transactions.\nDiscipline: Dance & Choreography (group ownership).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social and sponsored tx enable easy group wallet management.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/NFTDanceCollectives.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogNFTDanceCollectives\n/// @notice Form dance groups managing collective NFTs and revenue with gasless transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogNFTDanceCollectives {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every group ownership action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — dance collaborative economy",
      "sam": "$500M — NFT group ownership market",
      "som": "$28M — active dance collectives"
    },
    {
      "id": "dance-choreochain-ledger-0",
      "theme": "dance",
      "title": "ChoreoChain Ledger",
      "pitch": "Securely mint and prove original dance moves as NFTs to protect choreographers' intellectual property.",
      "subDiscipline": "movement cataloging",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT provenance ensures immutable proof of original creation stored onchain.",
      "megaPrompt": "Build \"ChoreoChain Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecurely mint and prove original dance moves as NFTs to protect choreographers' intellectual property.\nDiscipline: Dance & Choreography (movement cataloging).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT provenance ensures immutable proof of original creation stored onchain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreoChainLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ChoreoChainLedger\n/// @notice ERC-721 provenance for: Securely mint and prove original dance moves as NFTs to protect choreographers' intellectual property.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ChoreoChainLedger is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ChoreoChainLedger\", \"CHOREO\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a movement cataloging artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global dance industry encompassing studios and creators",
      "sam": "$500M — digital choreography rights management sector",
      "som": "$50M — early adopters of blockchain dance IP tools worldwide"
    },
    {
      "id": "dance-flowmotion-archive-1",
      "theme": "dance",
      "title": "FlowMotion Archive",
      "pitch": "Mint NFTs representing detailed dance notations for preservation and easy sharing among dancers.",
      "subDiscipline": "dance notation",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs anchor IPFS-stored notation files with verifiable onchain ownership.",
      "megaPrompt": "Build \"FlowMotion Archive\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint NFTs representing detailed dance notations for preservation and easy sharing among dancers.\nDiscipline: Dance & Choreography (dance notation).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs anchor IPFS-stored notation files with verifiable onchain ownership.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/FlowMotionArchive.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title FlowMotionArchive\n/// @notice ERC-721 provenance for: Mint NFTs representing detailed dance notations for preservation and easy sharing among dancers.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract FlowMotionArchive is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"FlowMotionArchive\", \"FLOWMO\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a dance notation artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — dance education and archival resources market",
      "sam": "$120M — digital dance instruction content delivery",
      "som": "$15M — NFT-based educational resource users in dance"
    },
    {
      "id": "dance-beatsync-provenance-2",
      "theme": "dance",
      "title": "BeatSync Provenance",
      "pitch": "Authenticate unique dance beat patterns as NFTs for royalty-tracked reuse in choreography.",
      "subDiscipline": "rhythmic innovation",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 tokens link beats to creators with transparent ownership history.",
      "megaPrompt": "Build \"BeatSync Provenance\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAuthenticate unique dance beat patterns as NFTs for royalty-tracked reuse in choreography.\nDiscipline: Dance & Choreography (rhythmic innovation).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 tokens link beats to creators with transparent ownership history.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/BeatSyncProvenance.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title BeatSyncProvenance\n/// @notice ERC-721 provenance for: Authenticate unique dance beat patterns as NFTs for royalty-tracked reuse in choreography.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract BeatSyncProvenance is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"BeatSyncProvenance\", \"BEATSY\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a rhythmic innovation artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global dance music and rhythm licensing",
      "sam": "$300M — digital dance beat production and licensing",
      "som": "$30M — blockchain-savvy dance music producers"
    },
    {
      "id": "dance-posemint-studio-3",
      "theme": "dance",
      "title": "PoseMint Studio",
      "pitch": "Create NFT collections of signature dance poses to license and showcase unique styles.",
      "subDiscipline": "pose collections",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT minting guarantees original pose ownership and provenance onchain.",
      "megaPrompt": "Build \"PoseMint Studio\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate NFT collections of signature dance poses to license and showcase unique styles.\nDiscipline: Dance & Choreography (pose collections).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT minting guarantees original pose ownership and provenance onchain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PoseMintStudio.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title PoseMintStudio\n/// @notice ERC-721 provenance for: Create NFT collections of signature dance poses to license and showcase unique styles.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PoseMintStudio is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"PoseMintStudio\", \"POSEMI\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a pose collections artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global market for dance teaching and pose references",
      "sam": "$250M — digital pose libraries and tools",
      "som": "$25M — NFT collectors and dance educators"
    },
    {
      "id": "dance-steptrace-rights-4",
      "theme": "dance",
      "title": "StepTrace Rights",
      "pitch": "Mint entire step sequences as NFTs to secure choreographic rights and facilitate licensing.",
      "subDiscipline": "step sequence rights",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs provide immutable records of step sequences with creator proofs.",
      "megaPrompt": "Build \"StepTrace Rights\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint entire step sequences as NFTs to secure choreographic rights and facilitate licensing.\nDiscipline: Dance & Choreography (step sequence rights).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs provide immutable records of step sequences with creator proofs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StepTraceRights.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title StepTraceRights\n/// @notice ERC-721 provenance for: Mint entire step sequences as NFTs to secure choreographic rights and facilitate licensing.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract StepTraceRights is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"StepTraceRights\", \"STEPTR\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a step sequence rights artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — dance choreography licensing market",
      "sam": "$400M — digital choreography licensing platforms",
      "som": "$40M — blockchain-based choreography licensing adopters"
    },
    {
      "id": "dance-mirrormove-vault-5",
      "theme": "dance",
      "title": "MirrorMove Vault",
      "pitch": "NFT dance movement tokens enable secure sharing and replication tracking of signature moves.",
      "subDiscipline": "movement replication",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs offer tamper-proof proof of movement origin and transfer onchain.",
      "megaPrompt": "Build \"MirrorMove Vault\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nNFT dance movement tokens enable secure sharing and replication tracking of signature moves.\nDiscipline: Dance & Choreography (movement replication).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs offer tamper-proof proof of movement origin and transfer onchain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MirrorMoveVault.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title MirrorMoveVault\n/// @notice ERC-721 provenance for: NFT dance movement tokens enable secure sharing and replication tracking of signature moves.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract MirrorMoveVault is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"MirrorMoveVault\", \"MIRROR\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a movement replication artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3.5B — global dance movement sharing platforms",
      "sam": "$350M — digital dance content licensing",
      "som": "$35M — early NFT movement replicators"
    },
    {
      "id": "dance-rhythmroots-nft-6",
      "theme": "dance",
      "title": "RhythmRoots NFT",
      "pitch": "Preserve cultural dances as NFT-verified IPFS multimedia packages for global education.",
      "subDiscipline": "cultural dance preservation",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT provenance protects cultural IP and ensures authenticity and respect.",
      "megaPrompt": "Build \"RhythmRoots NFT\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPreserve cultural dances as NFT-verified IPFS multimedia packages for global education.\nDiscipline: Dance & Choreography (cultural dance preservation).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT provenance protects cultural IP and ensures authenticity and respect.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/RhythmRootsNFT.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title RhythmRootsNFT\n/// @notice ERC-721 provenance for: Preserve cultural dances as NFT-verified IPFS multimedia packages for global education.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract RhythmRootsNFT is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"RhythmRootsNFT\", \"RHYTHM\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a cultural dance preservation artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — dance heritage and education market globally",
      "sam": "$200M — cultural dance digital archiving",
      "som": "$20M — NFT cultural heritage educators and advocates"
    },
    {
      "id": "dance-motionflow-tokens-7",
      "theme": "dance",
      "title": "MotionFlow Tokens",
      "pitch": "Mint NFTs of continuous dance flows that authenticate choreography progression and originality.",
      "subDiscipline": "dance flow sequences",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs create unalterable records of flow sequences onchain for proof of authorship.",
      "megaPrompt": "Build \"MotionFlow Tokens\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint NFTs of continuous dance flows that authenticate choreography progression and originality.\nDiscipline: Dance & Choreography (dance flow sequences).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs create unalterable records of flow sequences onchain for proof of authorship.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MotionFlowTokens.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title MotionFlowTokens\n/// @notice ERC-721 provenance for: Mint NFTs of continuous dance flows that authenticate choreography progression and originality.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract MotionFlowTokens is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"MotionFlowTokens\", \"MOTION\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a dance flow sequences artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — dance choreography software and platforms",
      "sam": "$300M — dance flow digital content monetization",
      "som": "$30M — NFT-savvy choreographers"
    },
    {
      "id": "dance-echodance-ledger-8",
      "theme": "dance",
      "title": "EchoDance Ledger",
      "pitch": "Tokenize remix rights of dance routines as NFTs to facilitate fair use and royalties.",
      "subDiscipline": "dance remix rights",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT provenance enables transparent chain of rights and remix permissions.",
      "megaPrompt": "Build \"EchoDance Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTokenize remix rights of dance routines as NFTs to facilitate fair use and royalties.\nDiscipline: Dance & Choreography (dance remix rights).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT provenance enables transparent chain of rights and remix permissions.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/EchoDanceLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title EchoDanceLedger\n/// @notice ERC-721 provenance for: Tokenize remix rights of dance routines as NFTs to facilitate fair use and royalties.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract EchoDanceLedger is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"EchoDanceLedger\", \"ECHODA\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a dance remix rights artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4.5B — global dance remix and derivative market",
      "sam": "$450M — digital dance remix licensing",
      "som": "$45M — remixers using blockchain rights management"
    },
    {
      "id": "dance-stepsync-collectibles-9",
      "theme": "dance",
      "title": "StepSync Collectibles",
      "pitch": "Create collectible NFTs of iconic dance steps for fans and teaching accreditation.",
      "subDiscipline": "signature step NFTs",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "Immutable NFTs certify ownership and authenticity of iconic dance moves.",
      "megaPrompt": "Build \"StepSync Collectibles\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate collectible NFTs of iconic dance steps for fans and teaching accreditation.\nDiscipline: Dance & Choreography (signature step NFTs).\nOnchain primitive: NFT provenance mint. Why this primitive: Immutable NFTs certify ownership and authenticity of iconic dance moves.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StepSyncCollectibles.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title StepSyncCollectibles\n/// @notice ERC-721 provenance for: Create collectible NFTs of iconic dance steps for fans and teaching accreditation.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract StepSyncCollectibles is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"StepSyncCollectibles\", \"STEPSY\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a signature step NFTs artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.5B — dance fan merchandise and collectibles",
      "sam": "$250M — digital collectible dance steps",
      "som": "$25M — NFT dance collectibles market"
    },
    {
      "id": "dance-choreochain-badge-10",
      "theme": "dance",
      "title": "ChoreoChain Badge",
      "pitch": "Award NFT badges for verified choreography skills and professional credentials.",
      "subDiscipline": "certified choreography",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs provide tamperproof certification linked to creator identity and Etherscan verification.",
      "megaPrompt": "Build \"ChoreoChain Badge\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAward NFT badges for verified choreography skills and professional credentials.\nDiscipline: Dance & Choreography (certified choreography).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs provide tamperproof certification linked to creator identity and Etherscan verification.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreoChainBadge.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ChoreoChainBadge\n/// @notice ERC-721 provenance for: Award NFT badges for verified choreography skills and professional credentials.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ChoreoChainBadge is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ChoreoChainBadge\", \"CHOREO\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a certified choreography artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.8B — dance training and professional certification market",
      "sam": "$180M — digital certification platforms for dance",
      "som": "$18M — blockchain-based skill verification users"
    },
    {
      "id": "dance-dancedna-registry-11",
      "theme": "dance",
      "title": "DanceDNA Registry",
      "pitch": "Mint NFTs encoding a dancer’s unique style ‘DNA’ for branding and licensing.",
      "subDiscipline": "personal style encoding",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs link personal style data stored on IPFS to a verifiable creator token.",
      "megaPrompt": "Build \"DanceDNA Registry\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint NFTs encoding a dancer’s unique style ‘DNA’ for branding and licensing.\nDiscipline: Dance & Choreography (personal style encoding).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs link personal style data stored on IPFS to a verifiable creator token.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceDNARegistry.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title DanceDNARegistry\n/// @notice ERC-721 provenance for: Mint NFTs encoding a dancer’s unique style ‘DNA’ for branding and licensing.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract DanceDNARegistry is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"DanceDNARegistry\", \"DANCED\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a personal style encoding artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global dance branding and personal IP market",
      "sam": "$200M — digital dance personal brand assets",
      "som": "$20M — blockchain-style trademark adopters"
    },
    {
      "id": "dance-pulseproof-archives-12",
      "theme": "dance",
      "title": "PulseProof Archives",
      "pitch": "Create immutable NFT records of historical dance footage for rights and preservation.",
      "subDiscipline": "historical dance archives",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs anchor archival IPFS content with verifiable creator provenance onchain.",
      "megaPrompt": "Build \"PulseProof Archives\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate immutable NFT records of historical dance footage for rights and preservation.\nDiscipline: Dance & Choreography (historical dance archives).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs anchor archival IPFS content with verifiable creator provenance onchain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PulseProofArchives.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title PulseProofArchives\n/// @notice ERC-721 provenance for: Create immutable NFT records of historical dance footage for rights and preservation.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PulseProofArchives is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"PulseProofArchives\", \"PULSEP\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a historical dance archives artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — dance archival and historic preservation market",
      "sam": "$150M — digital archival licensing platforms",
      "som": "$15M — NFT archival holders"
    },
    {
      "id": "dance-spinmint-marketplace-13",
      "theme": "dance",
      "title": "SpinMint Marketplace",
      "pitch": "Tokenize 360° recorded dance moves as NFTs to share with immersive digital experiences.",
      "subDiscipline": "360 dance moves",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs guarantee ownership of complex multimedia dance assets stored on IPFS.",
      "megaPrompt": "Build \"SpinMint Marketplace\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTokenize 360° recorded dance moves as NFTs to share with immersive digital experiences.\nDiscipline: Dance & Choreography (360 dance moves).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs guarantee ownership of complex multimedia dance assets stored on IPFS.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SpinMintMarketplace.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title SpinMintMarketplace\n/// @notice ERC-721 provenance for: Tokenize 360° recorded dance moves as NFTs to share with immersive digital experiences.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SpinMintMarketplace is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"SpinMintMarketplace\", \"SPINMI\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a 360 dance moves artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.8B — immersive dance and digital experience market",
      "sam": "$280M — 360° digital dance content licensing",
      "som": "$28M — NFT immersive dance collectors"
    },
    {
      "id": "dance-movemark-licensing-14",
      "theme": "dance",
      "title": "MoveMark Licensing",
      "pitch": "License choreography sequences securely as NFTs to streamline usage tracking and payments.",
      "subDiscipline": "choreography licensing",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT provenance ensures transparent onchain tracking of licensing rights and transfers.",
      "megaPrompt": "Build \"MoveMark Licensing\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nLicense choreography sequences securely as NFTs to streamline usage tracking and payments.\nDiscipline: Dance & Choreography (choreography licensing).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT provenance ensures transparent onchain tracking of licensing rights and transfers.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MoveMarkLicensing.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title MoveMarkLicensing\n/// @notice ERC-721 provenance for: License choreography sequences securely as NFTs to streamline usage tracking and payments.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract MoveMarkLicensing is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"MoveMarkLicensing\", \"MOVEMA\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a choreography licensing artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4.2B — choreography licensing global market",
      "sam": "$420M — digital choreography licensing platforms",
      "som": "$42M — NFT licensing adopters in dance"
    },
    {
      "id": "dance-freezeframe-tokens-15",
      "theme": "dance",
      "title": "FreezeFrame Tokens",
      "pitch": "Mint NFTs capturing iconic frozen dance frames for fan engagement and merchandising.",
      "subDiscipline": "iconic frozen poses",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs provide proof of authenticity and scarcity of digitally minted poses.",
      "megaPrompt": "Build \"FreezeFrame Tokens\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint NFTs capturing iconic frozen dance frames for fan engagement and merchandising.\nDiscipline: Dance & Choreography (iconic frozen poses).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs provide proof of authenticity and scarcity of digitally minted poses.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/FreezeFrameTokens.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title FreezeFrameTokens\n/// @notice ERC-721 provenance for: Mint NFTs capturing iconic frozen dance frames for fan engagement and merchandising.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract FreezeFrameTokens is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"FreezeFrameTokens\", \"FREEZE\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a iconic frozen poses artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.3B — dance fan merchandise and digital collectibles",
      "sam": "$230M — frozen pose digital collectibles market",
      "som": "$23M — NFT pose collectors"
    },
    {
      "id": "dance-stepsync-remix-16",
      "theme": "dance",
      "title": "StepSync Remix",
      "pitch": "Mint and license remixable dance steps as NFTs to promote collaborative creation.",
      "subDiscipline": "dance step sampling",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs track sample provenance and usage rights transparently onchain.",
      "megaPrompt": "Build \"StepSync Remix\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint and license remixable dance steps as NFTs to promote collaborative creation.\nDiscipline: Dance & Choreography (dance step sampling).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs track sample provenance and usage rights transparently onchain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StepSyncRemix.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title StepSyncRemix\n/// @notice ERC-721 provenance for: Mint and license remixable dance steps as NFTs to promote collaborative creation.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract StepSyncRemix is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"StepSyncRemix\", \"STEPSY\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a dance step sampling artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3.7B — dance sampling and remix culture market",
      "sam": "$370M — digital dance sample licensing",
      "som": "$37M — NFT-based step remix users"
    },
    {
      "id": "dance-choreocraft-tokens-17",
      "theme": "dance",
      "title": "ChoreoCraft Tokens",
      "pitch": "Create and sell NFT-backed custom choreography kits for teachers and studios.",
      "subDiscipline": "custom choreography kits",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT minting verifies ownership and uniqueness of choreography kit IP packages.",
      "megaPrompt": "Build \"ChoreoCraft Tokens\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate and sell NFT-backed custom choreography kits for teachers and studios.\nDiscipline: Dance & Choreography (custom choreography kits).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT minting verifies ownership and uniqueness of choreography kit IP packages.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ChoreoCraftTokens.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ChoreoCraftTokens\n/// @notice ERC-721 provenance for: Create and sell NFT-backed custom choreography kits for teachers and studios.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ChoreoCraftTokens is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ChoreoCraftTokens\", \"CHOREO\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a custom choreography kits artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — dance education content creation market",
      "sam": "$300M — digital choreography toolkits",
      "som": "$30M — NFT choreography kit buyers"
    },
    {
      "id": "dance-dancetrail-provenance-18",
      "theme": "dance",
      "title": "DanceTrail Provenance",
      "pitch": "Record and mint NFT trails of live performances to authenticate originality and authorship.",
      "subDiscipline": "performance recording",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs link performance data stored on IPFS with immutable onchain proof.",
      "megaPrompt": "Build \"DanceTrail Provenance\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nRecord and mint NFT trails of live performances to authenticate originality and authorship.\nDiscipline: Dance & Choreography (performance recording).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs link performance data stored on IPFS with immutable onchain proof.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DanceTrailProvenance.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title DanceTrailProvenance\n/// @notice ERC-721 provenance for: Record and mint NFT trails of live performances to authenticate originality and authorship.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract DanceTrailProvenance is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"DanceTrailProvenance\", \"DANCET\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a performance recording artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — live dance performance recording market",
      "sam": "$400M — digital live performance monetization",
      "som": "$40M — NFT authenticated performance users"
    },
    {
      "id": "dance-rhythmripple-chain-19",
      "theme": "dance",
      "title": "RhythmRipple Chain",
      "pitch": "Tokenize rhythm sequences as NFTs for secure ownership and licensing in dance shows.",
      "subDiscipline": "beat choreography",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT provenance ensures trusted beat IP management and transfer.",
      "megaPrompt": "Build \"RhythmRipple Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTokenize rhythm sequences as NFTs for secure ownership and licensing in dance shows.\nDiscipline: Dance & Choreography (beat choreography).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT provenance ensures trusted beat IP management and transfer.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/RhythmRippleChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title RhythmRippleChain\n/// @notice ERC-721 provenance for: Tokenize rhythm sequences as NFTs for secure ownership and licensing in dance shows.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract RhythmRippleChain is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"RhythmRippleChain\", \"RHYTHM\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a beat choreography artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3.2B — dance show music and beat licensing",
      "sam": "$320M — digital beat choreography tools",
      "som": "$32M — NFT beat owners and licensors"
    },
    {
      "id": "dance-movemint-studio-20",
      "theme": "dance",
      "title": "MoveMint Studio",
      "pitch": "Choreographers mint unique dance moves as NFTs to monetize and protect their creations.",
      "subDiscipline": "original move minting",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 minting enables secure creator ownership and provenance verification.",
      "megaPrompt": "Build \"MoveMint Studio\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nChoreographers mint unique dance moves as NFTs to monetize and protect their creations.\nDiscipline: Dance & Choreography (original move minting).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 minting enables secure creator ownership and provenance verification.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MoveMintStudio.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title MoveMintStudio\n/// @notice ERC-721 provenance for: Choreographers mint unique dance moves as NFTs to monetize and protect their creations.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract MoveMintStudio is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"MoveMintStudio\", \"MOVEMI\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a original move minting artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global choreography creation industry",
      "sam": "$500M — digital choreography tools and licensing",
      "som": "$50M — NFT dance move creators and collectors"
    },
    {
      "id": "dance-posechain-gallery-21",
      "theme": "dance",
      "title": "PoseChain Gallery",
      "pitch": "Create galleries of NFT-verified dance poses to showcase and sell digital art collections.",
      "subDiscipline": "dance pose exhibitions",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT provenance ensures uniqueness and rightful ownership onchain.",
      "megaPrompt": "Build \"PoseChain Gallery\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate galleries of NFT-verified dance poses to showcase and sell digital art collections.\nDiscipline: Dance & Choreography (dance pose exhibitions).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT provenance ensures uniqueness and rightful ownership onchain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PoseChainGallery.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title PoseChainGallery\n/// @notice ERC-721 provenance for: Create galleries of NFT-verified dance poses to showcase and sell digital art collections.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PoseChainGallery is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"PoseChainGallery\", \"POSECH\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a dance pose exhibitions artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — digital dance art and gallery market",
      "sam": "$200M — NFT-based dance art sales",
      "som": "$20M — digital dance art collectors"
    },
    {
      "id": "dance-glideproof-tokens-22",
      "theme": "dance",
      "title": "GlideProof Tokens",
      "pitch": "Mint NFTs encoding fluidity metrics of dance sequences for analytics and coaching.",
      "subDiscipline": "fluidity measurement",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs timestamp and verify data stored offchain, linked to creator identities.",
      "megaPrompt": "Build \"GlideProof Tokens\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint NFTs encoding fluidity metrics of dance sequences for analytics and coaching.\nDiscipline: Dance & Choreography (fluidity measurement).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs timestamp and verify data stored offchain, linked to creator identities.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GlideProofTokens.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title GlideProofTokens\n/// @notice ERC-721 provenance for: Mint NFTs encoding fluidity metrics of dance sequences for analytics and coaching.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract GlideProofTokens is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"GlideProofTokens\", \"GLIDEP\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a fluidity measurement artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — dance analytics and coaching technology",
      "sam": "$120M — digital dance performance data licensing",
      "som": "$12M — NFT-based coaching tool users"
    },
    {
      "id": "dance-shadowstep-registry-23",
      "theme": "dance",
      "title": "ShadowStep Registry",
      "pitch": "Catalog silent or lip-synced dance movements as NFTs for licensing and crediting.",
      "subDiscipline": "silent movement catalog",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT provenance provides clear IP proof for silent choreography assets.",
      "megaPrompt": "Build \"ShadowStep Registry\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCatalog silent or lip-synced dance movements as NFTs for licensing and crediting.\nDiscipline: Dance & Choreography (silent movement catalog).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT provenance provides clear IP proof for silent choreography assets.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ShadowStepRegistry.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ShadowStepRegistry\n/// @notice ERC-721 provenance for: Catalog silent or lip-synced dance movements as NFTs for licensing and crediting.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ShadowStepRegistry is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ShadowStepRegistry\", \"SHADOW\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a silent movement catalog artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.7B — dance movement licensing and cataloging",
      "sam": "$270M — digital silent dance content market",
      "som": "$27M — NFT silent movement adopters"
    },
    {
      "id": "dance-leapchain-ledger-24",
      "theme": "dance",
      "title": "LeapChain Ledger",
      "pitch": "Mint NFTs of signature leaps to prove originality and enable commercial licensing.",
      "subDiscipline": "leaps and jumps",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFTs anchor leap moves with immutable creator provenance onchain.",
      "megaPrompt": "Build \"LeapChain Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint NFTs of signature leaps to prove originality and enable commercial licensing.\nDiscipline: Dance & Choreography (leaps and jumps).\nOnchain primitive: NFT provenance mint. Why this primitive: NFTs anchor leap moves with immutable creator provenance onchain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/LeapChainLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title LeapChainLedger\n/// @notice ERC-721 provenance for: Mint NFTs of signature leaps to prove originality and enable commercial licensing.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract LeapChainLedger is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"LeapChainLedger\", \"LEAPCH\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a leaps and jumps artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.5B — dance instructional and rights management",
      "sam": "$250M — digital move-specific licensing",
      "som": "$25M — NFT leap move rights holders"
    }
  ]
}