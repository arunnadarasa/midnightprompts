{
  "theme": {
    "slug": "games",
    "name": "Game Design & Interactive Media",
    "emoji": "🎮",
    "audience": "game designers, interactive artists, XR creators",
    "market_anchor": "the game industry (~$200B) and >3M indie developers"
  },
  "ideas": [
    {
      "id": "games-onchain-quest-ledger-0",
      "theme": "games",
      "title": "Onchain Quest Ledger",
      "pitch": "Securely log player quest progress and achievements on Ethereum Sepolia to prove in-game milestones.",
      "subDiscipline": "quest tracking",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a quest tracking demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Onchain Quest Ledger\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nSecurely log player quest progress and achievements on Ethereum Sepolia to prove in-game milestones.\nDiscipline: Game Design & Interactive Media (quest tracking).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a quest tracking demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/OnchainQuestLedger.compact\n// Securely log player quest progress and achievements on Ethereum Sepolia to prove in-game milestones.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"onchain_quest_ledger:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"quest tracking\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — global RPG market valuing player progression features",
      "sam": "$300M — blockchain-enhanced gaming platforms",
      "som": "$25M — indie RPG developers integrating onchain tracking"
    },
    {
      "id": "games-tokenized-loot-drops-1",
      "theme": "games",
      "title": "Tokenized Loot Drops",
      "pitch": "Distribute rare, verifiable loot as tokens directly through Sepolia smart contracts to guarantee authenticity.",
      "subDiscipline": "item distribution",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a item distribution demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Tokenized Loot Drops\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nDistribute rare, verifiable loot as tokens directly through Sepolia smart contracts to guarantee authenticity.\nDiscipline: Game Design & Interactive Media (item distribution).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a item distribution demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/TokenizedLootDrops.compact\n// Distribute rare, verifiable loot as tokens directly through Sepolia smart contracts to guarantee authenticity.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"tokenized_loot_drops:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"item distribution\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.5B — global loot box and item trading market",
      "sam": "$800M — NFT-powered gaming assets",
      "som": "$40M — indie multiplayer games with tokenized economies"
    },
    {
      "id": "games-player-reputation-ledger-2",
      "theme": "games",
      "title": "Player Reputation Ledger",
      "pitch": "Create immutable player reputation scores recorded on Sepolia to foster fair matchmaking and collaboration.",
      "subDiscipline": "community trust",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a community trust demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Player Reputation Ledger\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate immutable player reputation scores recorded on Sepolia to foster fair matchmaking and collaboration.\nDiscipline: Game Design & Interactive Media (community trust).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a community trust demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PlayerReputationLedger.compact\n// Create immutable player reputation scores recorded on Sepolia to foster fair matchmaking and collaboration.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"player_reputation_ledger:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"community trust\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — global online multiplayer market needing trust systems",
      "sam": "$200M — blockchain reputation management in games",
      "som": "$15M — indie developers building social trust features"
    },
    {
      "id": "games-onchain-puzzle-locks-3",
      "theme": "games",
      "title": "Onchain Puzzle Locks",
      "pitch": "Design puzzles whose solutions must be proven onchain via Sepolia smart contracts to unlock game content.",
      "subDiscipline": "interactive puzzles",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a interactive puzzles demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Onchain Puzzle Locks\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nDesign puzzles whose solutions must be proven onchain via Sepolia smart contracts to unlock game content.\nDiscipline: Game Design & Interactive Media (interactive puzzles).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a interactive puzzles demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/OnchainPuzzleLocks.compact\n// Design puzzles whose solutions must be proven onchain via Sepolia smart contracts to unlock game content.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"onchain_puzzle_locks:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"interactive puzzles\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — global puzzle game revenue including ARGs",
      "sam": "$180M — blockchain-integrated puzzle experiences",
      "som": "$12M — indie puzzle creators exploring cryptographic challenges"
    },
    {
      "id": "games-immutable-scoreboards-4",
      "theme": "games",
      "title": "Immutable Scoreboards",
      "pitch": "Store game scores on Sepolia to prevent cheating and create permanent leaderboards.",
      "subDiscipline": "competitive scoring",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a competitive scoring demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Immutable Scoreboards\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nStore game scores on Sepolia to prevent cheating and create permanent leaderboards.\nDiscipline: Game Design & Interactive Media (competitive scoring).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a competitive scoring demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ImmutableScoreboards.compact\n// Store game scores on Sepolia to prevent cheating and create permanent leaderboards.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"immutable_scoreboards:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"competitive scoring\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — esports and competitive gaming markets",
      "sam": "$350M — blockchain-based scoreboard solutions",
      "som": "$22M — indie competitive game developers"
    },
    {
      "id": "games-collectible-avatars-5",
      "theme": "games",
      "title": "Collectible Avatars",
      "pitch": "Mint avatar parts as unique tokens on Sepolia, enabling player-owned, tradable character features.",
      "subDiscipline": "character customization",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a character customization demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Collectible Avatars\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint avatar parts as unique tokens on Sepolia, enabling player-owned, tradable character features.\nDiscipline: Game Design & Interactive Media (character customization).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a character customization demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/CollectibleAvatars.compact\n// Mint avatar parts as unique tokens on Sepolia, enabling player-owned, tradable character features.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"collectible_avatars:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"character customization\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global avatar accessory market",
      "sam": "$1B — blockchain-customizable in-game assets",
      "som": "$60M — indie games specializing in avatar economies"
    },
    {
      "id": "games-onchain-narrative-branches-6",
      "theme": "games",
      "title": "Onchain Narrative Branches",
      "pitch": "Record player story choices on Sepolia to create transparent, shared branching narratives.",
      "subDiscipline": "storytelling",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a storytelling demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Onchain Narrative Branches\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nRecord player story choices on Sepolia to create transparent, shared branching narratives.\nDiscipline: Game Design & Interactive Media (storytelling).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a storytelling demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/OnchainNarrativeBranches.compact\n// Record player story choices on Sepolia to create transparent, shared branching narratives.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"onchain_narrative_branches:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"storytelling\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — interactive story-driven games market",
      "sam": "$250M — blockchain-enabled narrative games",
      "som": "$18M — indie narrative game developers"
    },
    {
      "id": "games-decentralized-game-voting-7",
      "theme": "games",
      "title": "Decentralized Game Voting",
      "pitch": "Empower players to vote on game decisions using Sepolia smart contracts for transparent results.",
      "subDiscipline": "community governance",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a community governance demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Decentralized Game Voting\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nEmpower players to vote on game decisions using Sepolia smart contracts for transparent results.\nDiscipline: Game Design & Interactive Media (community governance).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a community governance demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/DecentralizedGameVoting.compact\n// Empower players to vote on game decisions using Sepolia smart contracts for transparent results.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"decentralized_game_voting:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"community governance\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — player-driven game feature development market",
      "sam": "$150M — blockchain governance in games",
      "som": "$10M — indie developers using onchain voting"
    },
    {
      "id": "games-proof-of-play-events-8",
      "theme": "games",
      "title": "Proof-of-Play Events",
      "pitch": "Authenticate player participation in timed events on Sepolia for exclusive rewards and recognition.",
      "subDiscipline": "event verification",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a event verification demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Proof-of-Play Events\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nAuthenticate player participation in timed events on Sepolia for exclusive rewards and recognition.\nDiscipline: Game Design & Interactive Media (event verification).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a event verification demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ProofofPlayEvents.compact\n// Authenticate player participation in timed events on Sepolia for exclusive rewards and recognition.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"proof_of_play_events:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"event verification\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$450M — global online event gaming market",
      "sam": "$120M — blockchain-based event verification",
      "som": "$8M — indie event-centric games"
    },
    {
      "id": "games-onchain-multiplayer-matchmaking-9",
      "theme": "games",
      "title": "Onchain Multiplayer Matchmaking",
      "pitch": "Use Sepolia to transparently pair players based on verified criteria ensuring fairness.",
      "subDiscipline": "match coordination",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a match coordination demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Onchain Multiplayer Matchmaking\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nUse Sepolia to transparently pair players based on verified criteria ensuring fairness.\nDiscipline: Game Design & Interactive Media (match coordination).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a match coordination demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/OnchainMultiplayerMatchmaking.compact\n// Use Sepolia to transparently pair players based on verified criteria ensuring fairness.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"onchain_multiplayer_matchmaking:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"match coordination\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global online multiplayer market",
      "sam": "$600M — blockchain-enhanced matchmaking solutions",
      "som": "$30M — indie multiplayer developers exploring onchain matching"
    },
    {
      "id": "games-rare-achievement-tokens-10",
      "theme": "games",
      "title": "Rare Achievement Tokens",
      "pitch": "Mint unique achievement tokens on Sepolia when players hit milestones to prove in-game skills.",
      "subDiscipline": "achievement systems",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a achievement systems demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Rare Achievement Tokens\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint unique achievement tokens on Sepolia when players hit milestones to prove in-game skills.\nDiscipline: Game Design & Interactive Media (achievement systems).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a achievement systems demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/RareAchievementTokens.compact\n// Mint unique achievement tokens on Sepolia when players hit milestones to prove in-game skills.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"rare_achievement_tokens:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"achievement systems\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — game achievements and rewards market",
      "sam": "$400M — blockchain achievement token markets",
      "som": "$25M — indie developers integrating unique achievements"
    },
    {
      "id": "games-onchain-virtual-economies-11",
      "theme": "games",
      "title": "Onchain Virtual Economies",
      "pitch": "Manage in-game currency and item economies via Sepolia smart contracts for secure, transparent transactions.",
      "subDiscipline": "economy management",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a economy management demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Onchain Virtual Economies\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nManage in-game currency and item economies via Sepolia smart contracts for secure, transparent transactions.\nDiscipline: Game Design & Interactive Media (economy management).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a economy management demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/OnchainVirtualEconomies.compact\n// Manage in-game currency and item economies via Sepolia smart contracts for secure, transparent transactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"onchain_virtual_economies:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"economy management\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global in-game economy market",
      "sam": "$1.5B — blockchain virtual economy platforms",
      "som": "$75M — indie developers building decentralized economies"
    },
    {
      "id": "games-dynamic-nft-game-assets-12",
      "theme": "games",
      "title": "Dynamic NFT Game Assets",
      "pitch": "Allow game assets to evolve on Sepolia based on gameplay, creating unique player-owned NFTs.",
      "subDiscipline": "asset evolution",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a asset evolution demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Dynamic NFT Game Assets\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nAllow game assets to evolve on Sepolia based on gameplay, creating unique player-owned NFTs.\nDiscipline: Game Design & Interactive Media (asset evolution).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a asset evolution demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/DynamicNFTGameAssets.compact\n// Allow game assets to evolve on Sepolia based on gameplay, creating unique player-owned NFTs.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"dynamic_nft_game_assets:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"asset evolution\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — NFT gaming assets market",
      "sam": "$700M — dynamic NFT implementations",
      "som": "$35M — indie NFT game studios"
    },
    {
      "id": "games-onchain-scavenger-hunts-13",
      "theme": "games",
      "title": "Onchain Scavenger Hunts",
      "pitch": "Create geolocated scavenger hunts tracked and verified on Sepolia to reward player discoveries.",
      "subDiscipline": "interactive exploration",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a interactive exploration demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Onchain Scavenger Hunts\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate geolocated scavenger hunts tracked and verified on Sepolia to reward player discoveries.\nDiscipline: Game Design & Interactive Media (interactive exploration).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a interactive exploration demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/OnchainScavengerHunts.compact\n// Create geolocated scavenger hunts tracked and verified on Sepolia to reward player discoveries.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"onchain_scavenger_hunts:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"interactive exploration\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — augmented reality game market",
      "sam": "$180M — blockchain location-based games",
      "som": "$15M — indie AR game creators"
    },
    {
      "id": "games-transparent-rng-mechanics-14",
      "theme": "games",
      "title": "Transparent RNG Mechanics",
      "pitch": "Implement verifiable random number generation on Sepolia to ensure fair game outcomes.",
      "subDiscipline": "game fairness",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a game fairness demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Transparent RNG Mechanics\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nImplement verifiable random number generation on Sepolia to ensure fair game outcomes.\nDiscipline: Game Design & Interactive Media (game fairness).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a game fairness demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/TransparentRNGMechanics.compact\n// Implement verifiable random number generation on Sepolia to ensure fair game outcomes.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"transparent_rng_mechanics:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"game fairness\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.3B — global gaming fairness and anti-cheat market",
      "sam": "$400M — blockchain RNG services",
      "som": "$28M — indie developers needing trustworthy RNG"
    },
    {
      "id": "games-onchain-skill-trees-15",
      "theme": "games",
      "title": "Onchain Skill Trees",
      "pitch": "Record and validate player skill tree choices on Sepolia ensuring unique character builds are guaranteed.",
      "subDiscipline": "character progression",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a character progression demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Onchain Skill Trees\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nRecord and validate player skill tree choices on Sepolia ensuring unique character builds are guaranteed.\nDiscipline: Game Design & Interactive Media (character progression).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a character progression demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/OnchainSkillTrees.compact\n// Record and validate player skill tree choices on Sepolia ensuring unique character builds are guaranteed.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"onchain_skill_trees:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"character progression\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — skill progression system market",
      "sam": "$350M — blockchain-based progression tracking",
      "som": "$20M — indie RPG and strategy developers"
    },
    {
      "id": "games-decentralized-story-co-creation-16",
      "theme": "games",
      "title": "Decentralized Story Co-Creation",
      "pitch": "Facilitate community-driven story creation by recording contributions on Sepolia for transparent authorship.",
      "subDiscipline": "collaborative narrative",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a collaborative narrative demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Decentralized Story Co-Creation\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nFacilitate community-driven story creation by recording contributions on Sepolia for transparent authorship.\nDiscipline: Game Design & Interactive Media (collaborative narrative).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a collaborative narrative demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/DecentralizedStoryCoCreation.compact\n// Facilitate community-driven story creation by recording contributions on Sepolia for transparent authorship.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"decentralized_story_co_creation:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"collaborative narrative\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — interactive storytelling market",
      "sam": "$220M — blockchain narrative collaboration",
      "som": "$14M — indie interactive media creators"
    },
    {
      "id": "games-onchain-game-jam-scores-17",
      "theme": "games",
      "title": "Onchain Game Jam Scores",
      "pitch": "Publish and verify game jam scores on Sepolia to ensure fairness and public recognition.",
      "subDiscipline": "competition scoring",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a competition scoring demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Onchain Game Jam Scores\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPublish and verify game jam scores on Sepolia to ensure fairness and public recognition.\nDiscipline: Game Design & Interactive Media (competition scoring).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a competition scoring demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/OnchainGameJamScores.compact\n// Publish and verify game jam scores on Sepolia to ensure fairness and public recognition.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"onchain_game_jam_scores:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"competition scoring\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — game jam and indie competition market",
      "sam": "$90M — blockchain-based competition platforms",
      "som": "$7M — indie game jam organizers"
    },
    {
      "id": "games-player-created-onchain-items-18",
      "theme": "games",
      "title": "Player-Created Onchain Items",
      "pitch": "Allow players to mint and trade custom items on Sepolia smart contracts within games.",
      "subDiscipline": "user-generated content",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a user-generated content demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Player-Created Onchain Items\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nAllow players to mint and trade custom items on Sepolia smart contracts within games.\nDiscipline: Game Design & Interactive Media (user-generated content).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a user-generated content demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PlayerCreatedOnchainItems.compact\n// Allow players to mint and trade custom items on Sepolia smart contracts within games.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"player_created_onchain_items:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"user-generated content\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.7B — user-generated content monetization",
      "sam": "$500M — blockchain-based content marketplaces",
      "som": "$30M — indie games with UGC economies"
    },
    {
      "id": "games-onchain-vr-experience-logs-19",
      "theme": "games",
      "title": "Onchain VR Experience Logs",
      "pitch": "Record user interactions in VR on Sepolia for secure session histories and experience sharing.",
      "subDiscipline": "immersive interaction",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a immersive interaction demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Onchain VR Experience Logs\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nRecord user interactions in VR on Sepolia for secure session histories and experience sharing.\nDiscipline: Game Design & Interactive Media (immersive interaction).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a immersive interaction demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/OnchainVRExperienceLogs.compact\n// Record user interactions in VR on Sepolia for secure session histories and experience sharing.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"onchain_vr_experience_logs:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"immersive interaction\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — VR software and content market",
      "sam": "$450M — blockchain-enhanced VR applications",
      "som": "$25M — indie VR creators needing trustable logs"
    },
    {
      "id": "games-blockchain-dialogue-trees-20",
      "theme": "games",
      "title": "Blockchain Dialogue Trees",
      "pitch": "Store branching dialogue choices on Sepolia to enable transparent player-driven storytelling.",
      "subDiscipline": "interactive dialogue",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a interactive dialogue demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Blockchain Dialogue Trees\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nStore branching dialogue choices on Sepolia to enable transparent player-driven storytelling.\nDiscipline: Game Design & Interactive Media (interactive dialogue).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a interactive dialogue demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/BlockchainDialogueTrees.compact\n// Store branching dialogue choices on Sepolia to enable transparent player-driven storytelling.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"blockchain_dialogue_trees:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"interactive dialogue\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$850M — interactive narrative dialogue market",
      "sam": "$300M — blockchain dialogue integration",
      "som": "$16M — indie narrative game developers"
    },
    {
      "id": "games-onchain-speedrun-verification-21",
      "theme": "games",
      "title": "Onchain Speedrun Verification",
      "pitch": "Verify and timestamp speedrun attempts on Sepolia smart contracts to prevent cheating.",
      "subDiscipline": "challenge validation",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a challenge validation demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Onchain Speedrun Verification\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nVerify and timestamp speedrun attempts on Sepolia smart contracts to prevent cheating.\nDiscipline: Game Design & Interactive Media (challenge validation).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a challenge validation demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/OnchainSpeedrunVerification.compact\n// Verify and timestamp speedrun attempts on Sepolia smart contracts to prevent cheating.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"onchain_speedrun_verification:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"challenge validation\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — speedrunning and challenge gaming market",
      "sam": "$150M — blockchain-based verification services",
      "som": "$9M — indie speedrun community tools"
    },
    {
      "id": "games-decentralized-game-mods-22",
      "theme": "games",
      "title": "Decentralized Game Mods",
      "pitch": "Distribute and verify mods using Sepolia smart contracts for secure, community-driven content sharing.",
      "subDiscipline": "mod distribution",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a mod distribution demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Decentralized Game Mods\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nDistribute and verify mods using Sepolia smart contracts for secure, community-driven content sharing.\nDiscipline: Game Design & Interactive Media (mod distribution).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a mod distribution demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/DecentralizedGameMods.compact\n// Distribute and verify mods using Sepolia smart contracts for secure, community-driven content sharing.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"decentralized_game_mods:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"mod distribution\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — game modding market",
      "sam": "$350M — blockchain-based content distribution",
      "som": "$22M — indie modding tool developers"
    },
    {
      "id": "games-token-gated-game-access-23",
      "theme": "games",
      "title": "Token-Gated Game Access",
      "pitch": "Use Sepolia smart contracts to restrict game content access via ownership of specific tokens.",
      "subDiscipline": "access control",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a access control demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Token-Gated Game Access\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nUse Sepolia smart contracts to restrict game content access via ownership of specific tokens.\nDiscipline: Game Design & Interactive Media (access control).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a access control demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/TokenGatedGameAccess.compact\n// Use Sepolia smart contracts to restrict game content access via ownership of specific tokens.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"token_gated_game_access:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"access control\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.2B — premium and gated game content market",
      "sam": "$700M — blockchain access control solutions",
      "som": "$40M — indie developers experimenting with token gating"
    },
    {
      "id": "games-onchain-interactive-artpieces-24",
      "theme": "games",
      "title": "Onchain Interactive Artpieces",
      "pitch": "Create interactive artworks whose states and ownership are controlled via Sepolia smart contracts.",
      "subDiscipline": "generative art",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a generative art demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Onchain Interactive Artpieces\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate interactive artworks whose states and ownership are controlled via Sepolia smart contracts.\nDiscipline: Game Design & Interactive Media (generative art).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a generative art demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/OnchainInteractiveArtpieces.compact\n// Create interactive artworks whose states and ownership are controlled via Sepolia smart contracts.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"onchain_interactive_artpieces:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"generative art\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — digital and interactive art market",
      "sam": "$200M — blockchain-enabled generative art",
      "som": "$12M — indie interactive media artists"
    },
    {
      "id": "games-pixel-lore-vault-0",
      "theme": "games",
      "title": "Pixel Lore Vault",
      "pitch": "Permanently store and share evolving game storylines and lore across player communities.",
      "subDiscipline": "game narrative archiving",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a game narrative archiving demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Pixel Lore Vault\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPermanently store and share evolving game storylines and lore across player communities.\nDiscipline: Game Design & Interactive Media (game narrative archiving).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a game narrative archiving demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"game narrative archiving\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PixelLoreVault.compact\n// Permanently store and share evolving game storylines and lore across player communities.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a game narrative archiving artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — narrative tools for game studios globally",
      "sam": "$200M — indie narrative design tools market",
      "som": "$10M — early adopters in community-driven storytelling"
    },
    {
      "id": "games-avatar-trait-forge-1",
      "theme": "games",
      "title": "Avatar Trait Forge",
      "pitch": "Save and share unique character traits and skins securely and permanently on IPFS.",
      "subDiscipline": "character customization data",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a character customization data demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Avatar Trait Forge\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nSave and share unique character traits and skins securely and permanently on IPFS.\nDiscipline: Game Design & Interactive Media (character customization data).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a character customization data demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"character customization data\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/AvatarTraitForge.compact\n// Save and share unique character traits and skins securely and permanently on IPFS.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a character customization data artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — avatar customization market in gaming",
      "sam": "$400M — indie avatar tool users",
      "som": "$25M — avatar trait NFT collectors"
    },
    {
      "id": "games-quest-chain-archive-2",
      "theme": "games",
      "title": "Quest Chain Archive",
      "pitch": "Distribute permanent quest data and decision trees for replayable and modifiable game adventures.",
      "subDiscipline": "interactive quest design",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a interactive quest design demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Quest Chain Archive\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nDistribute permanent quest data and decision trees for replayable and modifiable game adventures.\nDiscipline: Game Design & Interactive Media (interactive quest design).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a interactive quest design demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"interactive quest design\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/QuestChainArchive.compact\n// Distribute permanent quest data and decision trees for replayable and modifiable game adventures.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a interactive quest design artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — global interactive quest tools",
      "sam": "$120M — indie game narrative tools",
      "som": "$8M — experimental quest designers"
    },
    {
      "id": "games-xr-scene-snapshot-3",
      "theme": "games",
      "title": "XR Scene Snapshot",
      "pitch": "Pin immersive XR scenes and metadata permanently for cross-platform sharing and reuse.",
      "subDiscipline": "extended reality content storage",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a extended reality content storage demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"XR Scene Snapshot\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin immersive XR scenes and metadata permanently for cross-platform sharing and reuse.\nDiscipline: Game Design & Interactive Media (extended reality content storage).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a extended reality content storage demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"extended reality content storage\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/XRSceneSnapshot.compact\n// Pin immersive XR scenes and metadata permanently for cross-platform sharing and reuse.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a extended reality content storage artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — XR content creation software",
      "sam": "$350M — indie XR artists",
      "som": "$20M — XR scene archivists"
    },
    {
      "id": "games-procedural-map-depository-4",
      "theme": "games",
      "title": "Procedural Map Depository",
      "pitch": "Securely archive and share procedurally generated game maps for community use and collaboration.",
      "subDiscipline": "map generation and sharing",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a map generation and sharing demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Procedural Map Depository\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nSecurely archive and share procedurally generated game maps for community use and collaboration.\nDiscipline: Game Design & Interactive Media (map generation and sharing).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a map generation and sharing demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"map generation and sharing\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ProceduralMapDepository.compact\n// Securely archive and share procedurally generated game maps for community use and collaboration.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a map generation and sharing artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — game level design tools",
      "sam": "$250M — indie map creators",
      "som": "$15M — collaborative map builders"
    },
    {
      "id": "games-artifact-provenance-ledger-5",
      "theme": "games",
      "title": "Artifact Provenance Ledger",
      "pitch": "Track and preserve full history of rare game artifacts immutably for authentic ownership proof.",
      "subDiscipline": "in-game item history tracking",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a in-game item history tracking demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Artifact Provenance Ledger\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nTrack and preserve full history of rare game artifacts immutably for authentic ownership proof.\nDiscipline: Game Design & Interactive Media (in-game item history tracking).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a in-game item history tracking demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"in-game item history tracking\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ArtifactProvenanceLedger.compact\n// Track and preserve full history of rare game artifacts immutably for authentic ownership proof.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a in-game item history tracking artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — virtual item marketplaces",
      "sam": "$500M — indie collectible creators",
      "som": "$35M — artifact provenance enthusiasts"
    },
    {
      "id": "games-audio-loop-library-6",
      "theme": "games",
      "title": "Audio Loop Library",
      "pitch": "Curate and permanently store unique game audio loops for easy reuse and remixing.",
      "subDiscipline": "sound asset curation",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a sound asset curation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Audio Loop Library\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCurate and permanently store unique game audio loops for easy reuse and remixing.\nDiscipline: Game Design & Interactive Media (sound asset curation).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a sound asset curation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"sound asset curation\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/AudioLoopLibrary.compact\n// Curate and permanently store unique game audio loops for easy reuse and remixing.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a sound asset curation artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — game audio markets worldwide",
      "sam": "$180M — indie audio designers",
      "som": "$12M — loop remixers"
    },
    {
      "id": "games-interactive-comic-archive-7",
      "theme": "games",
      "title": "Interactive Comic Archive",
      "pitch": "Pin interactive comic panels and scripts securely to enable decentralized reader-driven narratives.",
      "subDiscipline": "visual storytelling",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a visual storytelling demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Interactive Comic Archive\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin interactive comic panels and scripts securely to enable decentralized reader-driven narratives.\nDiscipline: Game Design & Interactive Media (visual storytelling).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a visual storytelling demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"visual storytelling\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/InteractiveComicArchive.compact\n// Pin interactive comic panels and scripts securely to enable decentralized reader-driven narratives.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a visual storytelling artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — interactive storytelling market",
      "sam": "$130M — indie comic creators",
      "som": "$7M — narrative experimenters"
    },
    {
      "id": "games-ai-npc-memory-hub-8",
      "theme": "games",
      "title": "AI NPC Memory Hub",
      "pitch": "Store AI NPC dialogue and memory states permanently for persistent, evolving game worlds.",
      "subDiscipline": "non-player character data",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a non-player character data demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"AI NPC Memory Hub\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nStore AI NPC dialogue and memory states permanently for persistent, evolving game worlds.\nDiscipline: Game Design & Interactive Media (non-player character data).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a non-player character data demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"non-player character data\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/AINPCMemoryHub.compact\n// Store AI NPC dialogue and memory states permanently for persistent, evolving game worlds.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a non-player character data artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — AI-driven game content",
      "sam": "$300M — indie AI game devs",
      "som": "$18M — persistent world builders"
    },
    {
      "id": "games-glyph-pattern-codex-9",
      "theme": "games",
      "title": "Glyph Pattern Codex",
      "pitch": "Archive unique glyphs and icon sets on IPFS for universal use in games and media.",
      "subDiscipline": "symbol and icon design",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a symbol and icon design demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Glyph Pattern Codex\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nArchive unique glyphs and icon sets on IPFS for universal use in games and media.\nDiscipline: Game Design & Interactive Media (symbol and icon design).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a symbol and icon design demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"symbol and icon design\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GlyphPatternCodex.compact\n// Archive unique glyphs and icon sets on IPFS for universal use in games and media.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a symbol and icon design artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — UI/UX design assets",
      "sam": "$90M — indie icon artists",
      "som": "$6M — symbol reuse communities"
    },
    {
      "id": "games-mod-manifest-vault-10",
      "theme": "games",
      "title": "Mod Manifest Vault",
      "pitch": "Pin and share mod manifests permanently to enable decentralized mod discovery and trust.",
      "subDiscipline": "game modification metadata",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a game modification metadata demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Mod Manifest Vault\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin and share mod manifests permanently to enable decentralized mod discovery and trust.\nDiscipline: Game Design & Interactive Media (game modification metadata).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a game modification metadata demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"game modification metadata\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ModManifestVault.compact\n// Pin and share mod manifests permanently to enable decentralized mod discovery and trust.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a game modification metadata artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — game modding ecosystem",
      "sam": "$220M — indie mod developers",
      "som": "$14M — mod distribution networks"
    },
    {
      "id": "games-dynamic-ui-blueprint-11",
      "theme": "games",
      "title": "Dynamic UI Blueprint",
      "pitch": "Save and distribute dynamic game UI states and settings with permanent accessibility.",
      "subDiscipline": "interface state storage",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a interface state storage demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Dynamic UI Blueprint\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nSave and distribute dynamic game UI states and settings with permanent accessibility.\nDiscipline: Game Design & Interactive Media (interface state storage).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a interface state storage demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"interface state storage\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/DynamicUIBlueprint.compact\n// Save and distribute dynamic game UI states and settings with permanent accessibility.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a interface state storage artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — game UI tool market",
      "sam": "$150M — indie UI designers",
      "som": "$9M — dynamic interface users"
    },
    {
      "id": "games-lore-collaboration-board-12",
      "theme": "games",
      "title": "Lore Collaboration Board",
      "pitch": "Enable community-driven lore contributions to be pinned permanently, ensuring collective narrative ownership.",
      "subDiscipline": "community storytelling",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a community storytelling demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Lore Collaboration Board\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nEnable community-driven lore contributions to be pinned permanently, ensuring collective narrative ownership.\nDiscipline: Game Design & Interactive Media (community storytelling).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a community storytelling demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"community storytelling\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/LoreCollaborationBoard.compact\n// Enable community-driven lore contributions to be pinned permanently, ensuring collective narrative ownership.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a community storytelling artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — collaborative content platforms",
      "sam": "$100M — indie lore builders",
      "som": "$5M — fan-driven stories"
    },
    {
      "id": "games-playable-prop-archive-13",
      "theme": "games",
      "title": "Playable Prop Archive",
      "pitch": "Permanently store props and interactive assets for reusable, verifiable game object libraries.",
      "subDiscipline": "asset permanence",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a asset permanence demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Playable Prop Archive\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPermanently store props and interactive assets for reusable, verifiable game object libraries.\nDiscipline: Game Design & Interactive Media (asset permanence).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a asset permanence demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"asset permanence\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PlayablePropArchive.compact\n// Permanently store props and interactive assets for reusable, verifiable game object libraries.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a asset permanence artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — 3D asset marketplace",
      "sam": "$400M — indie asset creators",
      "som": "$28M — reusable prop users"
    },
    {
      "id": "games-speedrun-data-ledger-14",
      "theme": "games",
      "title": "Speedrun Data Ledger",
      "pitch": "Pin detailed speedrun metadata and replay JSONs permanently for competitive integrity and sharing.",
      "subDiscipline": "game performance recording",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a game performance recording demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Speedrun Data Ledger\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin detailed speedrun metadata and replay JSONs permanently for competitive integrity and sharing.\nDiscipline: Game Design & Interactive Media (game performance recording).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a game performance recording demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"game performance recording\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SpeedrunDataLedger.compact\n// Pin detailed speedrun metadata and replay JSONs permanently for competitive integrity and sharing.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a game performance recording artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$300M — esports and speedrunning communities",
      "sam": "$50M — indie speedrun organizers",
      "som": "$3M — verified leaderboard users"
    },
    {
      "id": "games-virtual-stage-archives-15",
      "theme": "games",
      "title": "Virtual Stage Archives",
      "pitch": "Store and preserve virtual stage designs and performance manifests for XR and live events.",
      "subDiscipline": "interactive performance spaces",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a interactive performance spaces demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Virtual Stage Archives\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nStore and preserve virtual stage designs and performance manifests for XR and live events.\nDiscipline: Game Design & Interactive Media (interactive performance spaces).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a interactive performance spaces demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"interactive performance spaces\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/VirtualStageArchives.compact\n// Store and preserve virtual stage designs and performance manifests for XR and live events.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a interactive performance spaces artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.8B — virtual events and XR",
      "sam": "$320M — indie event creators",
      "som": "$22M — archived stage designers"
    },
    {
      "id": "games-emotion-filter-pack-16",
      "theme": "games",
      "title": "Emotion Filter Pack",
      "pitch": "Pin and share unique emotion-driven visual filters permanently for character and scene enhancement.",
      "subDiscipline": "visual effect assets",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a visual effect assets demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Emotion Filter Pack\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin and share unique emotion-driven visual filters permanently for character and scene enhancement.\nDiscipline: Game Design & Interactive Media (visual effect assets).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a visual effect assets demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"visual effect assets\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/EmotionFilterPack.compact\n// Pin and share unique emotion-driven visual filters permanently for character and scene enhancement.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a visual effect assets artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — game visual effects market",
      "sam": "$140M — indie effect designers",
      "som": "$9M — emotional filter users"
    },
    {
      "id": "games-puzzle-logic-cache-17",
      "theme": "games",
      "title": "Puzzle Logic Cache",
      "pitch": "Store complex puzzle logic and solution paths immutably for community puzzle sharing and remixing.",
      "subDiscipline": "game mechanic storage",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a game mechanic storage demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Puzzle Logic Cache\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nStore complex puzzle logic and solution paths immutably for community puzzle sharing and remixing.\nDiscipline: Game Design & Interactive Media (game mechanic storage).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a game mechanic storage demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"game mechanic storage\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PuzzleLogicCache.compact\n// Store complex puzzle logic and solution paths immutably for community puzzle sharing and remixing.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a game mechanic storage artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — casual puzzle games market",
      "sam": "$85M — indie puzzle creators",
      "som": "$6M — puzzle sharing communities"
    },
    {
      "id": "games-lore-token-index-18",
      "theme": "games",
      "title": "Lore Token Index",
      "pitch": "Create permanent indexes linking lore tokens to their corresponding IPFS-hosted metadata manifests.",
      "subDiscipline": "metadata indexing",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a metadata indexing demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Lore Token Index\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate permanent indexes linking lore tokens to their corresponding IPFS-hosted metadata manifests.\nDiscipline: Game Design & Interactive Media (metadata indexing).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a metadata indexing demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"metadata indexing\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/LoreTokenIndex.compact\n// Create permanent indexes linking lore tokens to their corresponding IPFS-hosted metadata manifests.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a metadata indexing artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — game NFT ecosystems",
      "sam": "$450M — indie token creators",
      "som": "$30M — lore-token collectors"
    },
    {
      "id": "games-multiplayer-mod-sync-19",
      "theme": "games",
      "title": "Multiplayer Mod Sync",
      "pitch": "Pin mods and game asset manifests to IPFS for real-time multiplayer mod synchronization.",
      "subDiscipline": "cross-user asset distribution",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a cross-user asset distribution demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Multiplayer Mod Sync\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin mods and game asset manifests to IPFS for real-time multiplayer mod synchronization.\nDiscipline: Game Design & Interactive Media (cross-user asset distribution).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a cross-user asset distribution demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"cross-user asset distribution\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/MultiplayerModSync.compact\n// Pin mods and game asset manifests to IPFS for real-time multiplayer mod synchronization.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a cross-user asset distribution artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — multiplayer game tools",
      "sam": "$350M — indie multiplayer devs",
      "som": "$25M — synchronized mod users"
    },
    {
      "id": "games-interactive-soundscapes-20",
      "theme": "games",
      "title": "Interactive Soundscapes",
      "pitch": "Pin rich, layered ambient soundscapes permanently for immersive and customizable game atmospheres.",
      "subDiscipline": "ambient audio curation",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a ambient audio curation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Interactive Soundscapes\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin rich, layered ambient soundscapes permanently for immersive and customizable game atmospheres.\nDiscipline: Game Design & Interactive Media (ambient audio curation).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a ambient audio curation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"ambient audio curation\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/InteractiveSoundscapes.compact\n// Pin rich, layered ambient soundscapes permanently for immersive and customizable game atmospheres.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a ambient audio curation artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — game audio ambient market",
      "sam": "$170M — indie sound designers",
      "som": "$11M — immersive audio users"
    },
    {
      "id": "games-immersive-narrative-nodes-21",
      "theme": "games",
      "title": "Immersive Narrative Nodes",
      "pitch": "Store branching narrative nodes permanently to enable expansive, decentralized interactive storytelling.",
      "subDiscipline": "branching story data",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a branching story data demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Immersive Narrative Nodes\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nStore branching narrative nodes permanently to enable expansive, decentralized interactive storytelling.\nDiscipline: Game Design & Interactive Media (branching story data).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a branching story data demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"branching story data\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ImmersiveNarrativeNodes.compact\n// Store branching narrative nodes permanently to enable expansive, decentralized interactive storytelling.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a branching story data artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — interactive fiction market",
      "sam": "$200M — indie narrative devs",
      "som": "$13M — branching story authors"
    },
    {
      "id": "games-xr-artifact-library-22",
      "theme": "games",
      "title": "XR Artifact Library",
      "pitch": "Archive and share 3D XR artifacts via IPFS, ensuring permanent access to virtual museum pieces.",
      "subDiscipline": "3D object preservation",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a 3D object preservation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"XR Artifact Library\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nArchive and share 3D XR artifacts via IPFS, ensuring permanent access to virtual museum pieces.\nDiscipline: Game Design & Interactive Media (3D object preservation).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a 3D object preservation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"3D object preservation\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/XRArtifactLibrary.compact\n// Archive and share 3D XR artifacts via IPFS, ensuring permanent access to virtual museum pieces.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a 3D object preservation artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — XR content archive market",
      "sam": "$300M — indie XR artists",
      "som": "$20M — virtual artifact curators"
    },
    {
      "id": "games-game-jam-showcase-23",
      "theme": "games",
      "title": "Game Jam Showcase",
      "pitch": "Permanently pin game jam project assets and manifests to showcase creative works indefinitely.",
      "subDiscipline": "project preservation",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a project preservation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Game Jam Showcase\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPermanently pin game jam project assets and manifests to showcase creative works indefinitely.\nDiscipline: Game Design & Interactive Media (project preservation).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a project preservation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"project preservation\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GameJamShowcase.compact\n// Permanently pin game jam project assets and manifests to showcase creative works indefinitely.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a project preservation artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — game jam ecosystem",
      "sam": "$120M — indie participants",
      "som": "$7M — showcased projects"
    },
    {
      "id": "games-tokenized-lore-boards-24",
      "theme": "games",
      "title": "Tokenized Lore Boards",
      "pitch": "Create permanent, token-linked lore boards for collaborative and credentialed story creation.",
      "subDiscipline": "community content curation",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a community content curation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Tokenized Lore Boards\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate permanent, token-linked lore boards for collaborative and credentialed story creation.\nDiscipline: Game Design & Interactive Media (community content curation).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a community content curation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"community content curation\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/TokenizedLoreBoards.compact\n// Create permanent, token-linked lore boards for collaborative and credentialed story creation.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a community content curation artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.3B — tokenized creative platforms",
      "sam": "$320M — indie storytellers",
      "som": "$18M — collaborative lore communities"
    },
    {
      "id": "games-gasless-guilds-0",
      "theme": "games",
      "title": "Gasless Guilds",
      "pitch": "Seamlessly create and join guilds with gas-free onboarding and instant member transactions.",
      "subDiscipline": "multiplayer coordination",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a multiplayer coordination demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Gasless Guilds\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nSeamlessly create and join guilds with gas-free onboarding and instant member transactions.\nDiscipline: Game Design & Interactive Media (multiplayer coordination).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a multiplayer coordination demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GaslessGuilds.compact\n// Seamlessly create and join guilds with gas-free onboarding and instant member transactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"gasless_guilds:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every multiplayer coordination action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global MMOG social features market",
      "sam": "$1.5B — guild and clan management tools",
      "som": "$300M — gasless onboarding solutions for indie MMOGs"
    },
    {
      "id": "games-sponsored-loot-drops-1",
      "theme": "games",
      "title": "Sponsored Loot Drops",
      "pitch": "Distribute in-game rewards directly to players' wallets without any gas fees.",
      "subDiscipline": "reward distribution",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a reward distribution demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Sponsored Loot Drops\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nDistribute in-game rewards directly to players' wallets without any gas fees.\nDiscipline: Game Design & Interactive Media (reward distribution).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a reward distribution demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SponsoredLootDrops.compact\n// Distribute in-game rewards directly to players' wallets without any gas fees.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"sponsored_loot_drops:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every reward distribution action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global game rewards market",
      "sam": "$2.8B — indie game token economies",
      "som": "$600M — gasless reward systems for digital collectibles"
    },
    {
      "id": "games-privy-vr-lobby-2",
      "theme": "games",
      "title": "Privy VR Lobby",
      "pitch": "Enter virtual lobbies with gas-free wallet login and seamless social interactions.",
      "subDiscipline": "XR social spaces",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a XR social spaces demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Privy VR Lobby\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nEnter virtual lobbies with gas-free wallet login and seamless social interactions.\nDiscipline: Game Design & Interactive Media (XR social spaces).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a XR social spaces demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PrivyVRLobby.compact\n// Enter virtual lobbies with gas-free wallet login and seamless social interactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"privy_vr_lobby:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every XR social spaces action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$7B — XR social platform market",
      "sam": "$1.2B — immersive multiplayer spaces",
      "som": "$250M — gasless onboarding in XR games"
    },
    {
      "id": "games-onchain-avatar-store-3",
      "theme": "games",
      "title": "Onchain Avatar Store",
      "pitch": "Buy and customize avatars with zero gas fees using embedded wallets and sponsored transactions.",
      "subDiscipline": "digital fashion",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a digital fashion demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Onchain Avatar Store\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nBuy and customize avatars with zero gas fees using embedded wallets and sponsored transactions.\nDiscipline: Game Design & Interactive Media (digital fashion).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a digital fashion demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/OnchainAvatarStore.compact\n// Buy and customize avatars with zero gas fees using embedded wallets and sponsored transactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"onchain_avatar_store:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every digital fashion action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — virtual fashion and avatar market",
      "sam": "$900M — indie avatar customization",
      "som": "$200M — gasless avatar item transactions"
    },
    {
      "id": "games-social-quest-chains-4",
      "theme": "games",
      "title": "Social Quest Chains",
      "pitch": "Collaborate on story-driven quests with gasless wallet sign-in and transaction sponsorship.",
      "subDiscipline": "interactive storytelling",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a interactive storytelling demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Social Quest Chains\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCollaborate on story-driven quests with gasless wallet sign-in and transaction sponsorship.\nDiscipline: Game Design & Interactive Media (interactive storytelling).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a interactive storytelling demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SocialQuestChains.compact\n// Collaborate on story-driven quests with gasless wallet sign-in and transaction sponsorship.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"social_quest_chains:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every interactive storytelling action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — narrative-driven gaming market",
      "sam": "$750M — collaborative storytelling tools",
      "som": "$150M — gasless transaction systems for quests"
    },
    {
      "id": "games-gasless-xr-art-swap-5",
      "theme": "games",
      "title": "Gasless XR Art Swap",
      "pitch": "Trade XR art pieces effortlessly with embedded wallets and sponsored transactions.",
      "subDiscipline": "interactive art exchange",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a interactive art exchange demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Gasless XR Art Swap\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nTrade XR art pieces effortlessly with embedded wallets and sponsored transactions.\nDiscipline: Game Design & Interactive Media (interactive art exchange).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a interactive art exchange demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GaslessXRArtSwap.compact\n// Trade XR art pieces effortlessly with embedded wallets and sponsored transactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"gasless_xr_art_swap:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every interactive art exchange action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — XR art marketplace",
      "sam": "$600M — indie interactive art platforms",
      "som": "$120M — gasless NFT art swaps"
    },
    {
      "id": "games-privy-game-jam-6",
      "theme": "games",
      "title": "Privy Game Jam",
      "pitch": "Host game jams with onchain identity and gas-free participation transactions.",
      "subDiscipline": "developer community",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a developer community demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Privy Game Jam\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nHost game jams with onchain identity and gas-free participation transactions.\nDiscipline: Game Design & Interactive Media (developer community).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a developer community demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PrivyGameJam.compact\n// Host game jams with onchain identity and gas-free participation transactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"privy_game_jam:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every developer community action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — game development event market",
      "sam": "$400M — indie game jam platforms",
      "som": "$80M — gasless participant transactions"
    },
    {
      "id": "games-sponsored-beta-access-7",
      "theme": "games",
      "title": "Sponsored Beta Access",
      "pitch": "Grant beta testing access through gasless wallet authentication and sponsored invites.",
      "subDiscipline": "user testing",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a user testing demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Sponsored Beta Access\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nGrant beta testing access through gasless wallet authentication and sponsored invites.\nDiscipline: Game Design & Interactive Media (user testing).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a user testing demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SponsoredBetaAccess.compact\n// Grant beta testing access through gasless wallet authentication and sponsored invites.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"sponsored_beta_access:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every user testing action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — game beta testing services",
      "sam": "$300M — indie testing platforms",
      "som": "$60M — gasless invite systems"
    },
    {
      "id": "games-privy-scoreboard-8",
      "theme": "games",
      "title": "Privy Scoreboard",
      "pitch": "Display and update player rankings with gas-free wallet sign-in and transaction updates.",
      "subDiscipline": "competitive ranking",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a competitive ranking demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Privy Scoreboard\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nDisplay and update player rankings with gas-free wallet sign-in and transaction updates.\nDiscipline: Game Design & Interactive Media (competitive ranking).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a competitive ranking demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PrivyScoreboard.compact\n// Display and update player rankings with gas-free wallet sign-in and transaction updates.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"privy_scoreboard:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every competitive ranking action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4.5B — esports and leaderboard market",
      "sam": "$850M — indie competitive platforms",
      "som": "$170M — gasless leaderboard update tools"
    },
    {
      "id": "games-instant-nft-trades-9",
      "theme": "games",
      "title": "Instant NFT Trades",
      "pitch": "Buy and sell game NFTs instantly with embedded wallets and zero gas fees.",
      "subDiscipline": "marketplace integration",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a marketplace integration demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Instant NFT Trades\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nBuy and sell game NFTs instantly with embedded wallets and zero gas fees.\nDiscipline: Game Design & Interactive Media (marketplace integration).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a marketplace integration demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/InstantNFTTrades.compact\n// Buy and sell game NFTs instantly with embedded wallets and zero gas fees.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"instant_nft_trades:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every marketplace integration action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$20B — global NFT gaming market",
      "sam": "$4B — indie NFT marketplaces",
      "som": "$800M — gasless NFT trading services"
    },
    {
      "id": "games-gasless-avatar-gifting-10",
      "theme": "games",
      "title": "Gasless Avatar Gifting",
      "pitch": "Send avatar skins and items as gifts without any gas fees.",
      "subDiscipline": "social gifting",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a social gifting demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Gasless Avatar Gifting\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nSend avatar skins and items as gifts without any gas fees.\nDiscipline: Game Design & Interactive Media (social gifting).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a social gifting demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GaslessAvatarGifting.compact\n// Send avatar skins and items as gifts without any gas fees.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"gasless_avatar_gifting:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every social gifting action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3.5B — virtual gifting market",
      "sam": "$700M — indie avatar economies",
      "som": "$140M — gasless social gifting platforms"
    },
    {
      "id": "games-sponsored-skill-boosts-11",
      "theme": "games",
      "title": "Sponsored Skill Boosts",
      "pitch": "Grant skill boosts and power-ups via gasless wallet transactions.",
      "subDiscipline": "in-game perks",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a in-game perks demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Sponsored Skill Boosts\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nGrant skill boosts and power-ups via gasless wallet transactions.\nDiscipline: Game Design & Interactive Media (in-game perks).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a in-game perks demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SponsoredSkillBoosts.compact\n// Grant skill boosts and power-ups via gasless wallet transactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"sponsored_skill_boosts:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every in-game perks action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — in-game purchase market",
      "sam": "$1.1B — indie game power-ups",
      "som": "$220M — gasless boost delivery systems"
    },
    {
      "id": "games-privy-story-worlds-12",
      "theme": "games",
      "title": "Privy Story Worlds",
      "pitch": "Create and share story-driven worlds with gasless wallet logins and sponsored content publishing.",
      "subDiscipline": "user-generated content",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a user-generated content demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Privy Story Worlds\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate and share story-driven worlds with gasless wallet logins and sponsored content publishing.\nDiscipline: Game Design & Interactive Media (user-generated content).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a user-generated content demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PrivyStoryWorlds.compact\n// Create and share story-driven worlds with gasless wallet logins and sponsored content publishing.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"privy_story_worlds:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every user-generated content action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — UGC game market",
      "sam": "$850M — indie narrative platforms",
      "som": "$170M — gasless content publishing"
    },
    {
      "id": "games-gas-free-co-op-play-13",
      "theme": "games",
      "title": "Gas-Free Co-op Play",
      "pitch": "Join cooperative matches instantly with gasless wallet onboarding and sponsored sync transactions.",
      "subDiscipline": "multiplayer mechanics",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a multiplayer mechanics demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Gas-Free Co-op Play\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nJoin cooperative matches instantly with gasless wallet onboarding and sponsored sync transactions.\nDiscipline: Game Design & Interactive Media (multiplayer mechanics).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a multiplayer mechanics demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GasFreeCoopPlay.compact\n// Join cooperative matches instantly with gasless wallet onboarding and sponsored sync transactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"gas_free_co_op_play:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every multiplayer mechanics action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — co-op game market",
      "sam": "$2B — indie multiplayer titles",
      "som": "$400M — gasless multiplayer entry systems"
    },
    {
      "id": "games-sponsored-xr-avatars-14",
      "theme": "games",
      "title": "Sponsored XR Avatars",
      "pitch": "Equip XR avatars with sponsored token transactions and easy wallet sign-in.",
      "subDiscipline": "virtual identity",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a virtual identity demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Sponsored XR Avatars\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nEquip XR avatars with sponsored token transactions and easy wallet sign-in.\nDiscipline: Game Design & Interactive Media (virtual identity).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a virtual identity demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SponsoredXRAvatars.compact\n// Equip XR avatars with sponsored token transactions and easy wallet sign-in.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"sponsored_xr_avatars:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every virtual identity action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — XR avatar market",
      "sam": "$1B — indie XR developers",
      "som": "$200M — gasless avatar upgrades"
    },
    {
      "id": "games-privy-puzzle-rewards-15",
      "theme": "games",
      "title": "Privy Puzzle Rewards",
      "pitch": "Earn puzzle completion rewards with gas-free wallet sign-in and transaction sponsorship.",
      "subDiscipline": "casual game incentives",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a casual game incentives demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Privy Puzzle Rewards\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nEarn puzzle completion rewards with gas-free wallet sign-in and transaction sponsorship.\nDiscipline: Game Design & Interactive Media (casual game incentives).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a casual game incentives demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PrivyPuzzleRewards.compact\n// Earn puzzle completion rewards with gas-free wallet sign-in and transaction sponsorship.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"privy_puzzle_rewards:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every casual game incentives action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — casual gaming reward market",
      "sam": "$600M — indie puzzle games",
      "som": "$120M — gasless reward claim platforms"
    },
    {
      "id": "games-gasless-avatar-battles-16",
      "theme": "games",
      "title": "Gasless Avatar Battles",
      "pitch": "Compete in avatar battles with gas-free wallet authentication and sponsored match transactions.",
      "subDiscipline": "competitive avatars",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a competitive avatars demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Gasless Avatar Battles\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCompete in avatar battles with gas-free wallet authentication and sponsored match transactions.\nDiscipline: Game Design & Interactive Media (competitive avatars).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a competitive avatars demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GaslessAvatarBattles.compact\n// Compete in avatar battles with gas-free wallet authentication and sponsored match transactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"gasless_avatar_battles:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every competitive avatars action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$7B — avatar battle games market",
      "sam": "$1.3B — indie competitive avatars",
      "som": "$260M — gasless battle transaction systems"
    },
    {
      "id": "games-sponsored-xr-exhibits-17",
      "theme": "games",
      "title": "Sponsored XR Exhibits",
      "pitch": "Host XR galleries that visitors explore with gasless wallet login and sponsored interaction tx.",
      "subDiscipline": "virtual galleries",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a virtual galleries demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Sponsored XR Exhibits\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nHost XR galleries that visitors explore with gasless wallet login and sponsored interaction tx.\nDiscipline: Game Design & Interactive Media (virtual galleries).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a virtual galleries demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SponsoredXRExhibits.compact\n// Host XR galleries that visitors explore with gasless wallet login and sponsored interaction tx.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"sponsored_xr_exhibits:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every virtual galleries action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.5B — virtual exhibit market",
      "sam": "$500M — indie XR galleries",
      "som": "$100M — gasless visitor interaction tools"
    },
    {
      "id": "games-privy-crafting-market-18",
      "theme": "games",
      "title": "Privy Crafting Market",
      "pitch": "Trade crafted game items with gasless wallet sign-in and sponsored trades.",
      "subDiscipline": "player economies",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a player economies demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Privy Crafting Market\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nTrade crafted game items with gasless wallet sign-in and sponsored trades.\nDiscipline: Game Design & Interactive Media (player economies).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a player economies demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PrivyCraftingMarket.compact\n// Trade crafted game items with gasless wallet sign-in and sponsored trades.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"privy_crafting_market:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every player economies action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — in-game crafting markets",
      "sam": "$1.2B — indie player economies",
      "som": "$240M — gasless item trading platforms"
    },
    {
      "id": "games-gasless-streaming-rewards-19",
      "theme": "games",
      "title": "Gasless Streaming Rewards",
      "pitch": "Reward game stream viewers with gas-free wallet transactions sponsored by creators.",
      "subDiscipline": "viewer incentives",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a viewer incentives demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Gasless Streaming Rewards\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nReward game stream viewers with gas-free wallet transactions sponsored by creators.\nDiscipline: Game Design & Interactive Media (viewer incentives).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a viewer incentives demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GaslessStreamingRewards.compact\n// Reward game stream viewers with gas-free wallet transactions sponsored by creators.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"gasless_streaming_rewards:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every viewer incentives action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — game streaming economy",
      "sam": "$1.6B — indie streamer rewards",
      "som": "$320M — gasless viewer tipping platforms"
    },
    {
      "id": "games-sponsored-fan-tokens-20",
      "theme": "games",
      "title": "Sponsored Fan Tokens",
      "pitch": "Issue fan engagement tokens to gamers using gasless wallet onboarding and sponsored mints.",
      "subDiscipline": "community engagement",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a community engagement demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Sponsored Fan Tokens\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nIssue fan engagement tokens to gamers using gasless wallet onboarding and sponsored mints.\nDiscipline: Game Design & Interactive Media (community engagement).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a community engagement demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SponsoredFanTokens.compact\n// Issue fan engagement tokens to gamers using gasless wallet onboarding and sponsored mints.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"sponsored_fan_tokens:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every community engagement action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — fan token market",
      "sam": "$1B — indie community tokens",
      "som": "$200M — gasless token issuance"
    },
    {
      "id": "games-privy-challenge-leaderboards-21",
      "theme": "games",
      "title": "Privy Challenge Leaderboards",
      "pitch": "Track daily challenges and leaderboards with gasless wallet logins and sponsored updates.",
      "subDiscipline": "competitive tracking",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a competitive tracking demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Privy Challenge Leaderboards\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nTrack daily challenges and leaderboards with gasless wallet logins and sponsored updates.\nDiscipline: Game Design & Interactive Media (competitive tracking).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a competitive tracking demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PrivyChallengeLeaderboards.compact\n// Track daily challenges and leaderboards with gasless wallet logins and sponsored updates.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"privy_challenge_leaderboards:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every competitive tracking action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — challenge-based gaming market",
      "sam": "$800M — indie competitive platforms",
      "som": "$160M — gasless leaderboard maintenance"
    },
    {
      "id": "games-gas-free-level-sharing-22",
      "theme": "games",
      "title": "Gas-Free Level Sharing",
      "pitch": "Share custom levels with friends through gasless wallet sign-in and sponsored access transactions.",
      "subDiscipline": "user content",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a user content demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Gas-Free Level Sharing\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nShare custom levels with friends through gasless wallet sign-in and sponsored access transactions.\nDiscipline: Game Design & Interactive Media (user content).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a user content demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GasFreeLevelSharing.compact\n// Share custom levels with friends through gasless wallet sign-in and sponsored access transactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"gas_free_level_sharing:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every user content action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — user-generated level market",
      "sam": "$600M — indie game modding",
      "som": "$120M — gasless content distribution"
    },
    {
      "id": "games-sponsored-multiplayer-drops-23",
      "theme": "games",
      "title": "Sponsored Multiplayer Drops",
      "pitch": "Distribute gasless event drops during multiplayer games with embedded wallets and sponsored tx.",
      "subDiscipline": "event rewards",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a event rewards demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Sponsored Multiplayer Drops\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nDistribute gasless event drops during multiplayer games with embedded wallets and sponsored tx.\nDiscipline: Game Design & Interactive Media (event rewards).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a event rewards demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SponsoredMultiplayerDrops.compact\n// Distribute gasless event drops during multiplayer games with embedded wallets and sponsored tx.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"sponsored_multiplayer_drops:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every event rewards action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$7B — live multiplayer events market",
      "sam": "$1.3B — indie multiplayer events",
      "som": "$260M — gasless event reward systems"
    },
    {
      "id": "games-privy-interactive-ads-24",
      "theme": "games",
      "title": "Privy Interactive Ads",
      "pitch": "Engage users with interactive ads that deliver gasless rewards via embedded wallets.",
      "subDiscipline": "ad integration",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a ad integration demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Privy Interactive Ads\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nEngage users with interactive ads that deliver gasless rewards via embedded wallets.\nDiscipline: Game Design & Interactive Media (ad integration).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a ad integration demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PrivyInteractiveAds.compact\n// Engage users with interactive ads that deliver gasless rewards via embedded wallets.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"privy_interactive_ads:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every ad integration action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — in-game advertising market",
      "sam": "$2B — indie ad reward platforms",
      "som": "$400M — gasless ad reward distribution"
    },
    {
      "id": "games-provenance-playbooks-0",
      "theme": "games",
      "title": "Provenance Playbooks",
      "pitch": "Create branching story games with verifiable original story assets minted onchain.",
      "subDiscipline": "game narrative design",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a game narrative design demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Provenance Playbooks\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate branching story games with verifiable original story assets minted onchain.\nDiscipline: Game Design & Interactive Media (game narrative design).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a game narrative design demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ProvenancePlaybooks.compact\n// Create branching story games with verifiable original story assets minted onchain.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove game narrative design\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — narrative-driven game market",
      "sam": "$500M — indie narrative game developers",
      "som": "$50M — studios adopting blockchain for storytelling"
    },
    {
      "id": "games-pixel-provenance-1",
      "theme": "games",
      "title": "Pixel Provenance",
      "pitch": "Mint and verify original pixel art sprites as unique NFTs for games and assets.",
      "subDiscipline": "pixel art creation",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a pixel art creation demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Pixel Provenance\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint and verify original pixel art sprites as unique NFTs for games and assets.\nDiscipline: Game Design & Interactive Media (pixel art creation).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a pixel art creation demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PixelProvenance.compact\n// Mint and verify original pixel art sprites as unique NFTs for games and assets.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove pixel art creation\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — digital art assets in gaming",
      "sam": "$300M — indie pixel art creators",
      "som": "$30M — blockchain-enabled art marketplaces"
    },
    {
      "id": "games-sound-slice-chain-2",
      "theme": "games",
      "title": "Sound Slice Chain",
      "pitch": "Securely mint and track original sound samples for interactive game soundtracks.",
      "subDiscipline": "game audio sampling",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a game audio sampling demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Sound Slice Chain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nSecurely mint and track original sound samples for interactive game soundtracks.\nDiscipline: Game Design & Interactive Media (game audio sampling).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a game audio sampling demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SoundSliceChain.compact\n// Securely mint and track original sound samples for interactive game soundtracks.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove game audio sampling\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — game sound asset market",
      "sam": "$250M — independent sound designers",
      "som": "$25M — blockchain-based audio licensing"
    },
    {
      "id": "games-vr-provenance-hub-3",
      "theme": "games",
      "title": "VR Provenance Hub",
      "pitch": "Mint immersive VR environments as NFTs to ensure creator authenticity and ownership.",
      "subDiscipline": "XR environment design",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a XR environment design demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"VR Provenance Hub\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint immersive VR environments as NFTs to ensure creator authenticity and ownership.\nDiscipline: Game Design & Interactive Media (XR environment design).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a XR environment design demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/VRProvenanceHub.compact\n// Mint immersive VR environments as NFTs to ensure creator authenticity and ownership.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove XR environment design\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — VR content creation",
      "sam": "$2B — XR indie developers",
      "som": "$200M — blockchain-verified VR experiences"
    },
    {
      "id": "games-avatar-dna-chain-4",
      "theme": "games",
      "title": "Avatar DNA Chain",
      "pitch": "Create unique avatars with provenance-tracked traits minted as ERC-721 tokens.",
      "subDiscipline": "custom avatar creation",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a custom avatar creation demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Avatar DNA Chain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate unique avatars with provenance-tracked traits minted as ERC-721 tokens.\nDiscipline: Game Design & Interactive Media (custom avatar creation).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a custom avatar creation demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/AvatarDNAChain.compact\n// Create unique avatars with provenance-tracked traits minted as ERC-721 tokens.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove custom avatar creation\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — avatar customization market",
      "sam": "$600M — indie avatar designers",
      "som": "$60M — blockchain avatar platforms"
    },
    {
      "id": "games-procedural-provenance-5",
      "theme": "games",
      "title": "Procedural Provenance",
      "pitch": "Mint procedurally generated game art as NFTs linked to creator algorithms onchain.",
      "subDiscipline": "algorithmic game art",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a algorithmic game art demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Procedural Provenance\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint procedurally generated game art as NFTs linked to creator algorithms onchain.\nDiscipline: Game Design & Interactive Media (algorithmic game art).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a algorithmic game art demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ProceduralProvenance.compact\n// Mint procedurally generated game art as NFTs linked to creator algorithms onchain.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove algorithmic game art\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — procedural art in games",
      "sam": "$150M — indie procedural artists",
      "som": "$15M — blockchain procedural asset sales"
    },
    {
      "id": "games-interactive-comics-chain-6",
      "theme": "games",
      "title": "Interactive Comics Chain",
      "pitch": "Securely mint interactive comic panels as unique NFTs with onchain provenance.",
      "subDiscipline": "interactive storytelling",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a interactive storytelling demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Interactive Comics Chain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nSecurely mint interactive comic panels as unique NFTs with onchain provenance.\nDiscipline: Game Design & Interactive Media (interactive storytelling).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a interactive storytelling demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/InteractiveComicsChain.compact\n// Securely mint interactive comic panels as unique NFTs with onchain provenance.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove interactive storytelling\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — interactive comics market",
      "sam": "$350M — indie comic creators",
      "som": "$35M — blockchain-backed story platforms"
    },
    {
      "id": "games-game-mod-provenance-7",
      "theme": "games",
      "title": "Game Mod Provenance",
      "pitch": "Mint unique game mods with provenance to authenticate original creator contributions.",
      "subDiscipline": "modding community tools",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a modding community tools demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Game Mod Provenance\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint unique game mods with provenance to authenticate original creator contributions.\nDiscipline: Game Design & Interactive Media (modding community tools).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a modding community tools demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GameModProvenance.compact\n// Mint unique game mods with provenance to authenticate original creator contributions.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove modding community tools\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — game mod market",
      "sam": "$500M — indie modders",
      "som": "$50M — blockchain mod distribution"
    },
    {
      "id": "games-level-chain-creator-8",
      "theme": "games",
      "title": "Level Chain Creator",
      "pitch": "Mint custom game levels as NFTs with proof of originality and ownership onchain.",
      "subDiscipline": "game level design",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a game level design demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Level Chain Creator\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint custom game levels as NFTs with proof of originality and ownership onchain.\nDiscipline: Game Design & Interactive Media (game level design).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a game level design demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/LevelChainCreator.compact\n// Mint custom game levels as NFTs with proof of originality and ownership onchain.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove game level design\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.5B — user-generated game levels",
      "sam": "$400M — indie level designers",
      "som": "$40M — blockchain level marketplaces"
    },
    {
      "id": "games-replay-provenance-9",
      "theme": "games",
      "title": "Replay Provenance",
      "pitch": "Mint verified game replays as NFTs to prove originality and preserve gameplay moments.",
      "subDiscipline": "game replay sharing",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a game replay sharing demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Replay Provenance\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint verified game replays as NFTs to prove originality and preserve gameplay moments.\nDiscipline: Game Design & Interactive Media (game replay sharing).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a game replay sharing demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ReplayProvenance.compact\n// Mint verified game replays as NFTs to prove originality and preserve gameplay moments.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove game replay sharing\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — game streaming and replay",
      "sam": "$200M — replay content creators",
      "som": "$20M — blockchain replay archives"
    },
    {
      "id": "games-xr-gesture-tokens-10",
      "theme": "games",
      "title": "XR Gesture Tokens",
      "pitch": "Mint original XR gesture captures as NFTs proving creator authorship.",
      "subDiscipline": "motion capture NFTs",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a motion capture NFTs demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"XR Gesture Tokens\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint original XR gesture captures as NFTs proving creator authorship.\nDiscipline: Game Design & Interactive Media (motion capture NFTs).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a motion capture NFTs demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/XRGestureTokens.compact\n// Mint original XR gesture captures as NFTs proving creator authorship.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove motion capture NFTs\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — XR motion capture market",
      "sam": "$150M — indie motion artists",
      "som": "$15M — blockchain gesture asset sales"
    },
    {
      "id": "games-lore-chain-artifacts-11",
      "theme": "games",
      "title": "Lore Chain Artifacts",
      "pitch": "Mint in-game lore entries as NFTs to guarantee original creation and provenance.",
      "subDiscipline": "game lore documentation",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a game lore documentation demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Lore Chain Artifacts\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint in-game lore entries as NFTs to guarantee original creation and provenance.\nDiscipline: Game Design & Interactive Media (game lore documentation).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a game lore documentation demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/LoreChainArtifacts.compact\n// Mint in-game lore entries as NFTs to guarantee original creation and provenance.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove game lore documentation\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — game lore markets",
      "sam": "$200M — indie lore creators",
      "som": "$20M — blockchain lore collectibles"
    },
    {
      "id": "games-ar-filter-provenance-12",
      "theme": "games",
      "title": "AR Filter Provenance",
      "pitch": "Create and mint AR filters as NFTs ensuring verified creator ownership.",
      "subDiscipline": "augmented reality effects",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a augmented reality effects demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"AR Filter Provenance\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate and mint AR filters as NFTs ensuring verified creator ownership.\nDiscipline: Game Design & Interactive Media (augmented reality effects).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a augmented reality effects demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ARFilterProvenance.compact\n// Create and mint AR filters as NFTs ensuring verified creator ownership.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove augmented reality effects\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — AR filter market",
      "sam": "$400M — indie AR creators",
      "som": "$40M — blockchain AR asset sales"
    },
    {
      "id": "games-interactive-music-nfts-13",
      "theme": "games",
      "title": "Interactive Music NFTs",
      "pitch": "Mint unique interactive game soundtrack remixes with onchain provenance.",
      "subDiscipline": "game soundtrack remixing",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a game soundtrack remixing demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Interactive Music NFTs\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint unique interactive game soundtrack remixes with onchain provenance.\nDiscipline: Game Design & Interactive Media (game soundtrack remixing).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a game soundtrack remixing demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/InteractiveMusicNFTs.compact\n// Mint unique interactive game soundtrack remixes with onchain provenance.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove game soundtrack remixing\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — game music remix market",
      "sam": "$300M — indie remix artists",
      "som": "$30M — blockchain music licensing"
    },
    {
      "id": "games-tokenized-game-guides-14",
      "theme": "games",
      "title": "Tokenized Game Guides",
      "pitch": "Mint original game strategy guides as NFTs to prove and reward authorship.",
      "subDiscipline": "strategy content",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a strategy content demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Tokenized Game Guides\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint original game strategy guides as NFTs to prove and reward authorship.\nDiscipline: Game Design & Interactive Media (strategy content).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a strategy content demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/TokenizedGameGuides.compact\n// Mint original game strategy guides as NFTs to prove and reward authorship.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove strategy content\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — game walkthrough market",
      "sam": "$250M — independent guide authors",
      "som": "$25M — blockchain guide sales"
    },
    {
      "id": "games-dynamic-npc-tokens-15",
      "theme": "games",
      "title": "Dynamic NPC Tokens",
      "pitch": "Mint dynamic NPCs as NFTs with provable creator-generated traits and backstories.",
      "subDiscipline": "procedural character design",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a procedural character design demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Dynamic NPC Tokens\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint dynamic NPCs as NFTs with provable creator-generated traits and backstories.\nDiscipline: Game Design & Interactive Media (procedural character design).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a procedural character design demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/DynamicNPCTokens.compact\n// Mint dynamic NPCs as NFTs with provable creator-generated traits and backstories.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove procedural character design\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — NPC asset market",
      "sam": "$350M — indie character designers",
      "som": "$35M — blockchain NPC marketplaces"
    },
    {
      "id": "games-haptic-asset-chain-16",
      "theme": "games",
      "title": "Haptic Asset Chain",
      "pitch": "Mint original haptic feedback patterns for XR as provenance-verified NFTs.",
      "subDiscipline": "tactile feedback design",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a tactile feedback design demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Haptic Asset Chain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint original haptic feedback patterns for XR as provenance-verified NFTs.\nDiscipline: Game Design & Interactive Media (tactile feedback design).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a tactile feedback design demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/HapticAssetChain.compact\n// Mint original haptic feedback patterns for XR as provenance-verified NFTs.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove tactile feedback design\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — haptic feedback market",
      "sam": "$120M — indie haptic designers",
      "som": "$12M — blockchain haptic asset sales"
    },
    {
      "id": "games-ai-art-provenance-17",
      "theme": "games",
      "title": "AI Art Provenance",
      "pitch": "Mint AI-generated game art NFTs with provenance proving creator guidance originality.",
      "subDiscipline": "AI-generated game art",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a AI-generated game art demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"AI Art Provenance\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint AI-generated game art NFTs with provenance proving creator guidance originality.\nDiscipline: Game Design & Interactive Media (AI-generated game art).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a AI-generated game art demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/AIArtProvenance.compact\n// Mint AI-generated game art NFTs with provenance proving creator guidance originality.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove AI-generated game art\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.8B — AI art in gaming",
      "sam": "$300M — indie AI artists",
      "som": "$30M — blockchain AI art sales"
    },
    {
      "id": "games-multiplayer-skin-chain-18",
      "theme": "games",
      "title": "Multiplayer Skin Chain",
      "pitch": "Mint unique multiplayer game skins as NFTs with verifiable creator provenance.",
      "subDiscipline": "cosmetic item creation",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a cosmetic item creation demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Multiplayer Skin Chain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint unique multiplayer game skins as NFTs with verifiable creator provenance.\nDiscipline: Game Design & Interactive Media (cosmetic item creation).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a cosmetic item creation demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/MultiplayerSkinChain.compact\n// Mint unique multiplayer game skins as NFTs with verifiable creator provenance.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove cosmetic item creation\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — cosmetic skin market",
      "sam": "$700M — indie skin designers",
      "som": "$70M — blockchain skin marketplaces"
    },
    {
      "id": "games-puzzle-chain-creator-19",
      "theme": "games",
      "title": "Puzzle Chain Creator",
      "pitch": "Mint original interactive puzzles as NFTs proving unique creator ownership.",
      "subDiscipline": "interactive puzzle design",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a interactive puzzle design demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Puzzle Chain Creator\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint original interactive puzzles as NFTs proving unique creator ownership.\nDiscipline: Game Design & Interactive Media (interactive puzzle design).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a interactive puzzle design demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PuzzleChainCreator.compact\n// Mint original interactive puzzles as NFTs proving unique creator ownership.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove interactive puzzle design\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — puzzle game market",
      "sam": "$200M — indie puzzle designers",
      "som": "$20M — blockchain puzzle asset sales"
    },
    {
      "id": "games-narrative-voice-tokens-20",
      "theme": "games",
      "title": "Narrative Voice Tokens",
      "pitch": "Mint original voiceover clips for games as provenance-verified NFTs.",
      "subDiscipline": "voice acting assets",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a voice acting assets demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Narrative Voice Tokens\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint original voiceover clips for games as provenance-verified NFTs.\nDiscipline: Game Design & Interactive Media (voice acting assets).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a voice acting assets demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/NarrativeVoiceTokens.compact\n// Mint original voiceover clips for games as provenance-verified NFTs.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove voice acting assets\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.3B — voice asset market",
      "sam": "$220M — indie voice actors",
      "som": "$22M — blockchain voice clip sales"
    },
    {
      "id": "games-interactive-map-nfts-21",
      "theme": "games",
      "title": "Interactive Map NFTs",
      "pitch": "Mint original interactive game maps as NFTs with verified creator provenance.",
      "subDiscipline": "game world mapping",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a game world mapping demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Interactive Map NFTs\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint original interactive game maps as NFTs with verified creator provenance.\nDiscipline: Game Design & Interactive Media (game world mapping).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a game world mapping demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/InteractiveMapNFTs.compact\n// Mint original interactive game maps as NFTs with verified creator provenance.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove game world mapping\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.7B — game map asset market",
      "sam": "$280M — indie map creators",
      "som": "$28M — blockchain map marketplaces"
    },
    {
      "id": "games-collectible-lore-cards-22",
      "theme": "games",
      "title": "Collectible Lore Cards",
      "pitch": "Mint unique lore cards as NFTs proving authentic creator provenance.",
      "subDiscipline": "digital collectible design",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a digital collectible design demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Collectible Lore Cards\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint unique lore cards as NFTs proving authentic creator provenance.\nDiscipline: Game Design & Interactive Media (digital collectible design).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a digital collectible design demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/CollectibleLoreCards.compact\n// Mint unique lore cards as NFTs proving authentic creator provenance.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove digital collectible design\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — collectible card market",
      "sam": "$400M — indie collectible designers",
      "som": "$40M — blockchain card marketplaces"
    },
    {
      "id": "games-gesture-control-assets-23",
      "theme": "games",
      "title": "Gesture-Control Assets",
      "pitch": "Mint unique XR gesture control assets as NFTs ensuring original creator rights.",
      "subDiscipline": "XR gesture interaction",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a XR gesture interaction demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Gesture-Control Assets\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint unique XR gesture control assets as NFTs ensuring original creator rights.\nDiscipline: Game Design & Interactive Media (XR gesture interaction).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a XR gesture interaction demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GestureControlAssets.compact\n// Mint unique XR gesture control assets as NFTs ensuring original creator rights.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove XR gesture interaction\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — XR interaction assets",
      "sam": "$150M — indie gesture creators",
      "som": "$15M — blockchain gesture sales"
    },
    {
      "id": "games-game-font-provenance-24",
      "theme": "games",
      "title": "Game Font Provenance",
      "pitch": "Mint unique game fonts as NFTs proving original designer ownership.",
      "subDiscipline": "typography for games",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a typography for games demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Game Font Provenance\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint unique game fonts as NFTs proving original designer ownership.\nDiscipline: Game Design & Interactive Media (typography for games).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a typography for games demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GameFontProvenance.compact\n// Mint unique game fonts as NFTs proving original designer ownership.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove typography for games\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucets: Preview  https://midnight-tmnight-preview.nethermind.dev/  ·  Preprod  https://midnight-tmnight-preprod.nethermind.dev/  (dispense tNIGHT — generate tDUST in Lace)\nExplorer: Preview  https://preview.midnightexplorer.com/  ·  Preprod  https://preprod.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — game font market",
      "sam": "$100M — indie typographers",
      "som": "$10M — blockchain font sales"
    }
  ]
}