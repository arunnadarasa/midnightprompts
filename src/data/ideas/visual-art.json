{
  "theme": {
    "slug": "visual-art",
    "name": "Visual Art",
    "emoji": "🎨",
    "audience": "painters, illustrators, generative artists, gallerists",
    "market_anchor": "the global art market (~$65B; >300K working visual artists)"
  },
  "ideas": [
    {
      "id": "visual-art-provenance-palette-0",
      "theme": "visual-art",
      "title": "Provenance Palette",
      "pitch": "Securely track and prove the ownership lineage of paintings with tamper-proof blockchain records.",
      "subDiscipline": "art ownership tracking",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a art ownership tracking demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Provenance Palette\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nSecurely track and prove the ownership lineage of paintings with tamper-proof blockchain records.\nDiscipline: Visual Art (art ownership tracking).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a art ownership tracking demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ProvenancePalette.compact\n// Securely track and prove the ownership lineage of paintings with tamper-proof blockchain records.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"provenance_palette:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"art ownership tracking\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market needing ownership verification",
      "sam": "$10B — marketplace for authenticated paintings worldwide",
      "som": "$500M — early adopters using blockchain provenance solutions"
    },
    {
      "id": "visual-art-generative-mint-studio-1",
      "theme": "visual-art",
      "title": "Generative Mint Studio",
      "pitch": "Artists can mint unique generative art pieces directly onto Ethereum Sepolia with automated smart contracts.",
      "subDiscipline": "generative art minting",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a generative art minting demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Generative Mint Studio\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nArtists can mint unique generative art pieces directly onto Ethereum Sepolia with automated smart contracts.\nDiscipline: Visual Art (generative art minting).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a generative art minting demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GenerativeMintStudio.compact\n// Artists can mint unique generative art pieces directly onto Ethereum Sepolia with automated smart contracts.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"generative_mint_studio:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"generative art minting\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — blockchain-based generative art market",
      "sam": "$150M — active generative artists minting NFTs",
      "som": "$20M — users adopting Sepolia for minting"
    },
    {
      "id": "visual-art-exhibit-chain-ledger-2",
      "theme": "visual-art",
      "title": "Exhibit Chain Ledger",
      "pitch": "Record every artwork's exhibition history securely and transparently onchain for galleries and collectors.",
      "subDiscipline": "gallery exhibition tracking",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a gallery exhibition tracking demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Exhibit Chain Ledger\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nRecord every artwork's exhibition history securely and transparently onchain for galleries and collectors.\nDiscipline: Visual Art (gallery exhibition tracking).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a gallery exhibition tracking demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ExhibitChainLedger.compact\n// Record every artwork's exhibition history securely and transparently onchain for galleries and collectors.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"exhibit_chain_ledger:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"gallery exhibition tracking\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global gallery and exhibition market",
      "sam": "$1B — art galleries integrating digital tracking",
      "som": "$100M — digital provenance adopters in galleries"
    },
    {
      "id": "visual-art-colorcode-certifier-3",
      "theme": "visual-art",
      "title": "ColorCode Certifier",
      "pitch": "Certify and verify exact color compositions of artworks using blockchain color code hashes.",
      "subDiscipline": "color authenticity validation",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a color authenticity validation demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"ColorCode Certifier\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCertify and verify exact color compositions of artworks using blockchain color code hashes.\nDiscipline: Visual Art (color authenticity validation).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a color authenticity validation demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ColorCodeCertifier.compact\n// Certify and verify exact color compositions of artworks using blockchain color code hashes.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"colorcode_certifier:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"color authenticity validation\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — art market reliant on color fidelity",
      "sam": "$500M — artists and collectors valuing color authenticity",
      "som": "$30M — initial blockchain-based color certification users"
    },
    {
      "id": "visual-art-auction-trust-chain-4",
      "theme": "visual-art",
      "title": "Auction Trust Chain",
      "pitch": "Enable transparent, verifiable auction processes and bids for visual art onchain.",
      "subDiscipline": "art auction transparency",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a art auction transparency demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Auction Trust Chain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nEnable transparent, verifiable auction processes and bids for visual art onchain.\nDiscipline: Visual Art (art auction transparency).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a art auction transparency demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/AuctionTrustChain.compact\n// Enable transparent, verifiable auction processes and bids for visual art onchain.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"auction_trust_chain:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"art auction transparency\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — global art auction market volume",
      "sam": "$3B — online art auction platforms",
      "som": "$250M — blockchain auction services adoption"
    },
    {
      "id": "visual-art-brushstroke-timestamp-5",
      "theme": "visual-art",
      "title": "Brushstroke Timestamp",
      "pitch": "Document and timestamp artists’ brushstroke progress live onchain for proof of creation.",
      "subDiscipline": "creative process timestamping",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a creative process timestamping demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Brushstroke Timestamp\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nDocument and timestamp artists’ brushstroke progress live onchain for proof of creation.\nDiscipline: Visual Art (creative process timestamping).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a creative process timestamping demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/BrushstrokeTimestamp.compact\n// Document and timestamp artists’ brushstroke progress live onchain for proof of creation.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"brushstroke_timestamp:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"creative process timestamping\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — art market valuing originality proof",
      "sam": "$200M — artists interested in process authentication",
      "som": "$10M — early userbase for onchain creation logs"
    },
    {
      "id": "visual-art-licensing-ledger-6",
      "theme": "visual-art",
      "title": "Licensing Ledger",
      "pitch": "Manage and enforce artwork licensing rights transparently with blockchain smart contracts.",
      "subDiscipline": "artwork license management",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a artwork license management demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Licensing Ledger\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nManage and enforce artwork licensing rights transparently with blockchain smart contracts.\nDiscipline: Visual Art (artwork license management).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a artwork license management demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/LicensingLedger.compact\n// Manage and enforce artwork licensing rights transparently with blockchain smart contracts.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"licensing_ledger:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"artwork license management\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global art licensing market",
      "sam": "$500M — digital art licensing platforms",
      "som": "$50M — blockchain-based licensing solutions"
    },
    {
      "id": "visual-art-interactive-canvas-dao-7",
      "theme": "visual-art",
      "title": "Interactive Canvas DAO",
      "pitch": "Enable collective decision-making on collaborative visual art projects via decentralized onchain voting.",
      "subDiscipline": "collaborative art governance",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a collaborative art governance demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Interactive Canvas DAO\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nEnable collective decision-making on collaborative visual art projects via decentralized onchain voting.\nDiscipline: Visual Art (collaborative art governance).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a collaborative art governance demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/InteractiveCanvasDAO.compact\n// Enable collective decision-making on collaborative visual art projects via decentralized onchain voting.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"interactive_canvas_dao:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"collaborative art governance\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$100M — collaborative digital art initiatives",
      "sam": "$30M — artists using decentralized governance tools",
      "som": "$5M — DAO experiment adopters in art"
    },
    {
      "id": "visual-art-art-swap-protocol-8",
      "theme": "visual-art",
      "title": "Art Swap Protocol",
      "pitch": "Create a trustless platform for artists and collectors to swap artworks instantly onchain.",
      "subDiscipline": "peer-to-peer art exchange",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a peer-to-peer art exchange demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Art Swap Protocol\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate a trustless platform for artists and collectors to swap artworks instantly onchain.\nDiscipline: Visual Art (peer-to-peer art exchange).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a peer-to-peer art exchange demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ArtSwapProtocol.compact\n// Create a trustless platform for artists and collectors to swap artworks instantly onchain.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"art_swap_protocol:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"peer-to-peer art exchange\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — peer-to-peer art trading market",
      "sam": "$200M — active visual art traders",
      "som": "$25M — early users adopting blockchain swaps"
    },
    {
      "id": "visual-art-edition-tracker-9",
      "theme": "visual-art",
      "title": "Edition Tracker",
      "pitch": "Track edition numbers and print runs of limited artworks securely and transparently onchain.",
      "subDiscipline": "limited print tracking",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a limited print tracking demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Edition Tracker\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nTrack edition numbers and print runs of limited artworks securely and transparently onchain.\nDiscipline: Visual Art (limited print tracking).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a limited print tracking demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/EditionTracker.compact\n// Track edition numbers and print runs of limited artworks securely and transparently onchain.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"edition_tracker:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"limited print tracking\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — limited edition print market",
      "sam": "$1.2B — collectors focusing on print authenticity",
      "som": "$80M — users transitioning to blockchain edition tracking"
    },
    {
      "id": "visual-art-art-commission-chain-10",
      "theme": "visual-art",
      "title": "Art Commission Chain",
      "pitch": "Streamline and secure commission contracts and payments between artists and patrons using smart contracts.",
      "subDiscipline": "commission workflow management",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a commission workflow management demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Art Commission Chain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nStreamline and secure commission contracts and payments between artists and patrons using smart contracts.\nDiscipline: Visual Art (commission workflow management).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a commission workflow management demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ArtCommissionChain.compact\n// Streamline and secure commission contracts and payments between artists and patrons using smart contracts.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"art_commission_chain:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"commission workflow management\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global art commission market",
      "sam": "$800M — platforms facilitating digital commissions",
      "som": "$60M — early blockchain adoption for commissions"
    },
    {
      "id": "visual-art-dynamic-frame-nfts-11",
      "theme": "visual-art",
      "title": "Dynamic Frame NFTs",
      "pitch": "Create NFTs that change frames or borders based on onchain conditions or events.",
      "subDiscipline": "programmable artwork framing",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a programmable artwork framing demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Dynamic Frame NFTs\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate NFTs that change frames or borders based on onchain conditions or events.\nDiscipline: Visual Art (programmable artwork framing).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a programmable artwork framing demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/DynamicFrameNFTs.compact\n// Create NFTs that change frames or borders based on onchain conditions or events.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"dynamic_frame_nfts:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"programmable artwork framing\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — programmable art NFT market",
      "sam": "$150M — collectors seeking interactive art experiences",
      "som": "$20M — initial adopters of dynamic NFTs"
    },
    {
      "id": "visual-art-gallery-access-token-12",
      "theme": "visual-art",
      "title": "Gallery Access Token",
      "pitch": "Issue access tokens for exclusive gallery events and exhibitions secured on blockchain.",
      "subDiscipline": "event admission tokens",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a event admission tokens demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Gallery Access Token\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nIssue access tokens for exclusive gallery events and exhibitions secured on blockchain.\nDiscipline: Visual Art (event admission tokens).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a event admission tokens demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GalleryAccessToken.compact\n// Issue access tokens for exclusive gallery events and exhibitions secured on blockchain.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"gallery_access_token:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"event admission tokens\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global gallery event ticket market",
      "sam": "$600M — digital ticketing for art events",
      "som": "$40M — blockchain-enabled ticket users"
    },
    {
      "id": "visual-art-art-critique-chain-13",
      "theme": "visual-art",
      "title": "Art Critique Chain",
      "pitch": "Record artist peer critiques and feedback permanently and transparently onchain.",
      "subDiscipline": "peer review logging",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a peer review logging demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Art Critique Chain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nRecord artist peer critiques and feedback permanently and transparently onchain.\nDiscipline: Visual Art (peer review logging).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a peer review logging demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ArtCritiqueChain.compact\n// Record artist peer critiques and feedback permanently and transparently onchain.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"art_critique_chain:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"peer review logging\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — peer review platforms globally",
      "sam": "$120M — digital visual art communities",
      "som": "$10M — blockchain critique tool users"
    },
    {
      "id": "visual-art-signature-stamp-14",
      "theme": "visual-art",
      "title": "Signature Stamp",
      "pitch": "Cryptographically verify artist signatures on digital and physical artworks using blockchain stamps.",
      "subDiscipline": "artist signature authentication",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a artist signature authentication demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Signature Stamp\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCryptographically verify artist signatures on digital and physical artworks using blockchain stamps.\nDiscipline: Visual Art (artist signature authentication).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a artist signature authentication demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SignatureStamp.compact\n// Cryptographically verify artist signatures on digital and physical artworks using blockchain stamps.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"signature_stamp:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"artist signature authentication\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — art authentication services",
      "sam": "$2B — signature verification market",
      "som": "$100M — blockchain adoption for signature validation"
    },
    {
      "id": "visual-art-interactive-auction-game-15",
      "theme": "visual-art",
      "title": "Interactive Auction Game",
      "pitch": "Turn art auctions into engaging games with transparent rules enforced by onchain smart contracts.",
      "subDiscipline": "gamified art auctions",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a gamified art auctions demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Interactive Auction Game\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nTurn art auctions into engaging games with transparent rules enforced by onchain smart contracts.\nDiscipline: Visual Art (gamified art auctions).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a gamified art auctions demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/InteractiveAuctionGame.compact\n// Turn art auctions into engaging games with transparent rules enforced by onchain smart contracts.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"interactive_auction_game:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"gamified art auctions\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — art auction global market",
      "sam": "$500M — gamified auction platforms",
      "som": "$40M — users engaging in blockchain auctions"
    },
    {
      "id": "visual-art-material-usage-ledger-16",
      "theme": "visual-art",
      "title": "Material Usage Ledger",
      "pitch": "Track and verify the ethical sourcing and usage of materials in visual artworks onchain.",
      "subDiscipline": "supply chain transparency",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a supply chain transparency demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Material Usage Ledger\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nTrack and verify the ethical sourcing and usage of materials in visual artworks onchain.\nDiscipline: Visual Art (supply chain transparency).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a supply chain transparency demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/MaterialUsageLedger.compact\n// Track and verify the ethical sourcing and usage of materials in visual artworks onchain.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"material_usage_ledger:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"supply chain transparency\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — ethical art material market",
      "sam": "$700M — collectors prioritizing ethical sourcing",
      "som": "$30M — blockchain material traceability adopters"
    },
    {
      "id": "visual-art-visual-royalties-manager-17",
      "theme": "visual-art",
      "title": "Visual Royalties Manager",
      "pitch": "Automate royalty payments to artists via smart contracts every time artwork changes hands.",
      "subDiscipline": "artist royalty automation",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a artist royalty automation demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Visual Royalties Manager\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nAutomate royalty payments to artists via smart contracts every time artwork changes hands.\nDiscipline: Visual Art (artist royalty automation).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a artist royalty automation demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/VisualRoyaltiesManager.compact\n// Automate royalty payments to artists via smart contracts every time artwork changes hands.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"visual_royalties_manager:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"artist royalty automation\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global art royalty market",
      "sam": "$400M — digital art sales platforms",
      "som": "$50M — blockchain royalty users"
    },
    {
      "id": "visual-art-moodboard-dao-18",
      "theme": "visual-art",
      "title": "MoodBoard DAO",
      "pitch": "Artists collaboratively curate and govern mood boards for projects using decentralized governance.",
      "subDiscipline": "collective inspiration curation",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a collective inspiration curation demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"MoodBoard DAO\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nArtists collaboratively curate and govern mood boards for projects using decentralized governance.\nDiscipline: Visual Art (collective inspiration curation).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a collective inspiration curation demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/MoodBoardDAO.compact\n// Artists collaboratively curate and govern mood boards for projects using decentralized governance.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"moodboard_dao:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"collective inspiration curation\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$120M — collaborative art projects",
      "sam": "$25M — digital collaboration tools for artists",
      "som": "$3M — DAO tools adoption in art"
    },
    {
      "id": "visual-art-sketch-snapshot-chain-19",
      "theme": "visual-art",
      "title": "Sketch Snapshot Chain",
      "pitch": "Instantly archive and timestamp digital sketches onchain for secure creative journaling.",
      "subDiscipline": "digital sketch archiving",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a digital sketch archiving demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Sketch Snapshot Chain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nInstantly archive and timestamp digital sketches onchain for secure creative journaling.\nDiscipline: Visual Art (digital sketch archiving).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a digital sketch archiving demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SketchSnapshotChain.compact\n// Instantly archive and timestamp digital sketches onchain for secure creative journaling.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"sketch_snapshot_chain:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"digital sketch archiving\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — digital art tools market",
      "sam": "$200M — digital sketch app users",
      "som": "$15M — early blockchain integration adopters"
    },
    {
      "id": "visual-art-canvas-rental-token-20",
      "theme": "visual-art",
      "title": "Canvas Rental Token",
      "pitch": "Tokenize and manage temporary rental of studio or gallery spaces for artists seamlessly onchain.",
      "subDiscipline": "art space access",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a art space access demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Canvas Rental Token\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nTokenize and manage temporary rental of studio or gallery spaces for artists seamlessly onchain.\nDiscipline: Visual Art (art space access).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a art space access demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/CanvasRentalToken.compact\n// Tokenize and manage temporary rental of studio or gallery spaces for artists seamlessly onchain.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"canvas_rental_token:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"art space access\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — global artist workspace market",
      "sam": "$120M — short term art space rentals",
      "som": "$8M — blockchain-based rental platform users"
    },
    {
      "id": "visual-art-nft-curation-index-21",
      "theme": "visual-art",
      "title": "NFT Curation Index",
      "pitch": "Create and manage curated NFT visual art collections with transparent onchain voting and tracking.",
      "subDiscipline": "curated NFT collections",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a curated NFT collections demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"NFT Curation Index\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate and manage curated NFT visual art collections with transparent onchain voting and tracking.\nDiscipline: Visual Art (curated NFT collections).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a curated NFT collections demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/NFTCurationIndex.compact\n// Create and manage curated NFT visual art collections with transparent onchain voting and tracking.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"nft_curation_index:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"curated NFT collections\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — NFT art collectible market",
      "sam": "$600M — active curators and collectors",
      "som": "$50M — users onchain curation platforms"
    },
    {
      "id": "visual-art-brushstroke-dao-vault-22",
      "theme": "visual-art",
      "title": "Brushstroke DAO Vault",
      "pitch": "Pool resources in decentralized vaults to fund promising visual art projects transparently.",
      "subDiscipline": "collective art funding",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a collective art funding demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Brushstroke DAO Vault\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPool resources in decentralized vaults to fund promising visual art projects transparently.\nDiscipline: Visual Art (collective art funding).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a collective art funding demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/BrushstrokeDAOVault.compact\n// Pool resources in decentralized vaults to fund promising visual art projects transparently.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"brushstroke_dao_vault:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"collective art funding\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — art crowd-funding market",
      "sam": "$150M — decentralized funding adopters",
      "som": "$12M — early DAO art fund users"
    },
    {
      "id": "visual-art-art-swap-escrow-23",
      "theme": "visual-art",
      "title": "Art Swap Escrow",
      "pitch": "Allow artists and collectors to trade artworks using blockchain escrow ensuring transaction safety.",
      "subDiscipline": "secure art trades",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a secure art trades demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Art Swap Escrow\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nAllow artists and collectors to trade artworks using blockchain escrow ensuring transaction safety.\nDiscipline: Visual Art (secure art trades).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a secure art trades demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ArtSwapEscrow.compact\n// Allow artists and collectors to trade artworks using blockchain escrow ensuring transaction safety.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"art_swap_escrow:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"secure art trades\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — art trading market",
      "sam": "$300M — online art trade platforms",
      "som": "$30M — blockchain escrow adopters in art"
    },
    {
      "id": "visual-art-visual-art-rewards-24",
      "theme": "visual-art",
      "title": "Visual Art Rewards",
      "pitch": "Reward artists and illustrators for community engagement or milestones via onchain token incentives.",
      "subDiscipline": "artist incentivization",
      "quantumHook": "Compact ZK contract",
      "quantumHookId": "compact-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "This idea fits Compact ZK contract because onchain logic is exactly what a artist incentivization demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...",
      "megaPrompt": "Build \"Visual Art Rewards\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nReward artists and illustrators for community engagement or milestones via onchain token incentives.\nDiscipline: Visual Art (artist incentivization).\nOnchain primitive: Compact ZK contract (onchain logic). Why this primitive: This idea fits Compact ZK contract because onchain logic is exactly what a artist incentivization demo needs: a Compact `.compact` contract compiled to ZK proving/verifying keys and deployed to the Midnight preview testnet, with p...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/VisualArtRewards.compact\n// Reward artists and illustrators for community engagement or milestones via onchain token incentives.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"visual_art_rewards:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. Land on page → 'Connect Lace' button → poll `window.midnight` → `connect('preview')`.\n2. Show shielded address, tDUST reminder (\"Get testnet DUST → {VITE_FAUCET_URL}\"), and Deploy button.\n3. On Deploy: import `deployContract` from `@midnight-ntwrk/midnight-js-contracts`, pass witnesses\n   ({ localSecretKey: async () => sk }) where `sk` is a 32-byte value persisted in localStorage.\n4. Show a \"Proving…\" spinner for up to 120s while the local proof server crunches.\n5. On success, render the deployed address + a Midnight explorer link and persist it to\n   `src/data/midnight-contract.json` so the app boots straight into it next time.\n6. The \"artist incentivization\" action calls `appendEntry(payload)` — same flow: prove, submit, refresh feed.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200M — artist monetization platforms",
      "sam": "$50M — community-driven artist platforms",
      "som": "$5M — initial blockchain reward users"
    },
    {
      "id": "visual-art-permacolor-archive-0",
      "theme": "visual-art",
      "title": "PermaColor Archive",
      "pitch": "Secure and share your unique color palettes as permanent IPFS references.",
      "subDiscipline": "color palette curation",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a color palette curation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"PermaColor Archive\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nSecure and share your unique color palettes as permanent IPFS references.\nDiscipline: Visual Art (color palette curation).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a color palette curation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"color palette curation\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PermaColorArchive.compact\n// Secure and share your unique color palettes as permanent IPFS references.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a color palette curation artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global digital artist tools market",
      "sam": "$250M — color-centric art resource platforms",
      "som": "$50M — paid subscriptions for curated palette archives"
    },
    {
      "id": "visual-art-generative-art-vault-1",
      "theme": "visual-art",
      "title": "Generative Art Vault",
      "pitch": "Pin generative art scripts and outputs permanently to preserve creative provenance.",
      "subDiscipline": "algorithmic artwork storage",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a algorithmic artwork storage demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Generative Art Vault\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin generative art scripts and outputs permanently to preserve creative provenance.\nDiscipline: Visual Art (algorithmic artwork storage).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a algorithmic artwork storage demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"algorithmic artwork storage\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GenerativeArtVault.compact\n// Pin generative art scripts and outputs permanently to preserve creative provenance.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a algorithmic artwork storage artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global generative art market",
      "sam": "$700M — platforms hosting generative art",
      "som": "$150M — premium archival and licensing services"
    },
    {
      "id": "visual-art-gallery-manifest-hub-2",
      "theme": "visual-art",
      "title": "Gallery Manifest Hub",
      "pitch": "Create and pin permanent digital manifests for art gallery exhibitions with immutable records.",
      "subDiscipline": "exhibition cataloging",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a exhibition cataloging demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Gallery Manifest Hub\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate and pin permanent digital manifests for art gallery exhibitions with immutable records.\nDiscipline: Visual Art (exhibition cataloging).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a exhibition cataloging demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"exhibition cataloging\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GalleryManifestHub.compact\n// Create and pin permanent digital manifests for art gallery exhibitions with immutable records.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a exhibition cataloging artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global gallery management software",
      "sam": "$1.2B — digital exhibition catalog platforms",
      "som": "$300M — galleries adopting blockchain provenance"
    },
    {
      "id": "visual-art-illustrator-portfolio-lock-3",
      "theme": "visual-art",
      "title": "Illustrator Portfolio Lock",
      "pitch": "Permanently store and share your illustrator portfolios with verifiable IPFS links.",
      "subDiscipline": "portfolio archiving",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a portfolio archiving demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Illustrator Portfolio Lock\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPermanently store and share your illustrator portfolios with verifiable IPFS links.\nDiscipline: Visual Art (portfolio archiving).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a portfolio archiving demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"portfolio archiving\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/IllustratorPortfolioLock.compact\n// Permanently store and share your illustrator portfolios with verifiable IPFS links.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a portfolio archiving artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — digital portfolio platforms",
      "sam": "$400M — illustrator-specific portfolio tools",
      "som": "$80M — paid portfolio permanence services"
    },
    {
      "id": "visual-art-digital-canvas-ledger-4",
      "theme": "visual-art",
      "title": "Digital Canvas Ledger",
      "pitch": "Record and pin painted artwork metadata to IPFS for permanent, tamper-proof provenance.",
      "subDiscipline": "painting provenance tracking",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a painting provenance tracking demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Digital Canvas Ledger\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nRecord and pin painted artwork metadata to IPFS for permanent, tamper-proof provenance.\nDiscipline: Visual Art (painting provenance tracking).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a painting provenance tracking demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"painting provenance tracking\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/DigitalCanvasLedger.compact\n// Record and pin painted artwork metadata to IPFS for permanent, tamper-proof provenance.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a painting provenance tracking artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global art provenance solutions",
      "sam": "$2.7B — provenance platforms for painters",
      "som": "$500M — subscription provenance verification"
    },
    {
      "id": "visual-art-nft-manifest-generator-5",
      "theme": "visual-art",
      "title": "NFT Manifest Generator",
      "pitch": "Bundle images and metadata into permanent IPFS manifests to guarantee NFT authenticity.",
      "subDiscipline": "digital asset packaging",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a digital asset packaging demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"NFT Manifest Generator\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nBundle images and metadata into permanent IPFS manifests to guarantee NFT authenticity.\nDiscipline: Visual Art (digital asset packaging).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a digital asset packaging demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"digital asset packaging\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/NFTManifestGenerator.compact\n// Bundle images and metadata into permanent IPFS manifests to guarantee NFT authenticity.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a digital asset packaging artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$7B — NFT marketplace infrastructure",
      "sam": "$1.6B — NFT metadata management tools",
      "som": "$350M — NFT authenticity services"
    },
    {
      "id": "visual-art-interactive-art-pinning-6",
      "theme": "visual-art",
      "title": "Interactive Art Pinning",
      "pitch": "Pin interactive art assets as JSON and media files, preserving experience forever on IPFS.",
      "subDiscipline": "multimedia art preservation",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a multimedia art preservation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Interactive Art Pinning\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin interactive art assets as JSON and media files, preserving experience forever on IPFS.\nDiscipline: Visual Art (multimedia art preservation).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a multimedia art preservation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"multimedia art preservation\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/InteractiveArtPinning.compact\n// Pin interactive art assets as JSON and media files, preserving experience forever on IPFS.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a multimedia art preservation artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — multimedia art platforms",
      "sam": "$200M — interactive digital art tools",
      "som": "$45M — premium preservation subscriptions"
    },
    {
      "id": "visual-art-exhibit-provenance-chain-7",
      "theme": "visual-art",
      "title": "Exhibit Provenance Chain",
      "pitch": "Pin exhibit documents and artwork proofs to guarantee authenticity for global exhibitions.",
      "subDiscipline": "art exposition verification",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a art exposition verification demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Exhibit Provenance Chain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin exhibit documents and artwork proofs to guarantee authenticity for global exhibitions.\nDiscipline: Visual Art (art exposition verification).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a art exposition verification demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"art exposition verification\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ExhibitProvenanceChain.compact\n// Pin exhibit documents and artwork proofs to guarantee authenticity for global exhibitions.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a art exposition verification artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global exhibition services market",
      "sam": "$1B — digital exhibit verification tools",
      "som": "$250M — paid provenance verification services"
    },
    {
      "id": "visual-art-painter-s-time-capsule-8",
      "theme": "visual-art",
      "title": "Painter’s Time Capsule",
      "pitch": "Permanently archive each painting stage as images and notes pinned immutably on IPFS.",
      "subDiscipline": "artistic process documentation",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a artistic process documentation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Painter’s Time Capsule\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPermanently archive each painting stage as images and notes pinned immutably on IPFS.\nDiscipline: Visual Art (artistic process documentation).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a artistic process documentation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"artistic process documentation\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PaintersTimeCapsule.compact\n// Permanently archive each painting stage as images and notes pinned immutably on IPFS.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a artistic process documentation artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — artist education and documentation",
      "sam": "$150M — process archiving platforms",
      "som": "$30M — paid archival service usage"
    },
    {
      "id": "visual-art-visual-art-supply-chain-9",
      "theme": "visual-art",
      "title": "Visual Art Supply Chain",
      "pitch": "Pin supplier certificates and provenance of materials to IPFS for buyer transparency.",
      "subDiscipline": "art materials tracking",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a art materials tracking demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Visual Art Supply Chain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin supplier certificates and provenance of materials to IPFS for buyer transparency.\nDiscipline: Visual Art (art materials tracking).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a art materials tracking demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"art materials tracking\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/VisualArtSupplyChain.compact\n// Pin supplier certificates and provenance of materials to IPFS for buyer transparency.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a art materials tracking artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$850M — art supplier certification market",
      "sam": "$210M — digital supply chain traceability",
      "som": "$40M — transparent certification subscriptions"
    },
    {
      "id": "visual-art-auto-manifest-builder-10",
      "theme": "visual-art",
      "title": "Auto-Manifest Builder",
      "pitch": "Automatically generate and pin complete IPFS manifests for artists’ works with minimal effort.",
      "subDiscipline": "metadata automation",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a metadata automation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Auto-Manifest Builder\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nAutomatically generate and pin complete IPFS manifests for artists’ works with minimal effort.\nDiscipline: Visual Art (metadata automation).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a metadata automation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"metadata automation\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/AutoManifestBuilder.compact\n// Automatically generate and pin complete IPFS manifests for artists’ works with minimal effort.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a metadata automation artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — creative automation software",
      "sam": "$500M — metadata management solutions",
      "som": "$100M — paid automation feature adoption"
    },
    {
      "id": "visual-art-collaborative-art-archive-11",
      "theme": "visual-art",
      "title": "Collaborative Art Archive",
      "pitch": "Pin shared artworks and process files to IPFS for permanent collaborative history storage.",
      "subDiscipline": "group project documentation",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a group project documentation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Collaborative Art Archive\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin shared artworks and process files to IPFS for permanent collaborative history storage.\nDiscipline: Visual Art (group project documentation).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a group project documentation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"group project documentation\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/CollaborativeArtArchive.compact\n// Pin shared artworks and process files to IPFS for permanent collaborative history storage.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a group project documentation artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — collaborative art tool market",
      "sam": "$350M — team project archiving platforms",
      "som": "$75M — subscription for collaborative archiving"
    },
    {
      "id": "visual-art-style-transfer-keeper-12",
      "theme": "visual-art",
      "title": "Style Transfer Keeper",
      "pitch": "Pin AI-generated style files and outputs permanently to safeguard unique artistic styles.",
      "subDiscipline": "AI style preservation",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a AI style preservation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Style Transfer Keeper\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin AI-generated style files and outputs permanently to safeguard unique artistic styles.\nDiscipline: Visual Art (AI style preservation).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a AI style preservation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"AI style preservation\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/StyleTransferKeeper.compact\n// Pin AI-generated style files and outputs permanently to safeguard unique artistic styles.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a AI style preservation artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — AI art tools market",
      "sam": "$220M — style transfer application users",
      "som": "$45M — paid style preservation services"
    },
    {
      "id": "visual-art-art-fair-digital-catalog-13",
      "theme": "visual-art",
      "title": "Art Fair Digital Catalog",
      "pitch": "Create and pin permanent digital catalogs for art fairs accessible globally.",
      "subDiscipline": "event art listings",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a event art listings demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Art Fair Digital Catalog\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate and pin permanent digital catalogs for art fairs accessible globally.\nDiscipline: Visual Art (event art listings).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a event art listings demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"event art listings\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ArtFairDigitalCatalog.compact\n// Create and pin permanent digital catalogs for art fairs accessible globally.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a event art listings artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global art event management",
      "sam": "$750M — digital catalog solutions",
      "som": "$160M — event catalog subscription usage"
    },
    {
      "id": "visual-art-generative-token-gallery-14",
      "theme": "visual-art",
      "title": "Generative Token Gallery",
      "pitch": "Pin generative art manifests and images for permanent token-backed gallery display.",
      "subDiscipline": "tokenized generative art",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a tokenized generative art demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Generative Token Gallery\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin generative art manifests and images for permanent token-backed gallery display.\nDiscipline: Visual Art (tokenized generative art).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a tokenized generative art demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"tokenized generative art\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GenerativeTokenGallery.compact\n// Pin generative art manifests and images for permanent token-backed gallery display.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a tokenized generative art artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — tokenized art market",
      "sam": "$1B — token gallery platforms",
      "som": "$210M — paid generative token services"
    },
    {
      "id": "visual-art-illustration-rights-ledger-15",
      "theme": "visual-art",
      "title": "Illustration Rights Ledger",
      "pitch": "Pin illustration licenses and usage terms permanently for transparent rights management.",
      "subDiscipline": "digital rights management",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a digital rights management demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Illustration Rights Ledger\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin illustration licenses and usage terms permanently for transparent rights management.\nDiscipline: Visual Art (digital rights management).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a digital rights management demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"digital rights management\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/IllustrationRightsLedger.compact\n// Pin illustration licenses and usage terms permanently for transparent rights management.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a digital rights management artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.8B — digital rights platforms",
      "sam": "$450M — illustration rights management",
      "som": "$90M — subscription licensing verification"
    },
    {
      "id": "visual-art-augmented-art-archive-16",
      "theme": "visual-art",
      "title": "Augmented Art Archive",
      "pitch": "Pin AR textures, markers, and metadata permanently to IPFS for reliable augmented experiences.",
      "subDiscipline": "AR art content storage",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a AR art content storage demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Augmented Art Archive\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin AR textures, markers, and metadata permanently to IPFS for reliable augmented experiences.\nDiscipline: Visual Art (AR art content storage).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a AR art content storage demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"AR art content storage\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/AugmentedArtArchive.compact\n// Pin AR textures, markers, and metadata permanently to IPFS for reliable augmented experiences.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a AR art content storage artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.5B — AR content creation market",
      "sam": "$600M — AR art publishing",
      "som": "$130M — paid AR archival"
    },
    {
      "id": "visual-art-permanent-sketchbook-hub-17",
      "theme": "visual-art",
      "title": "Permanent Sketchbook Hub",
      "pitch": "Pin your sketchbook pages as immutable IPFS entries to build a lifelong visual diary.",
      "subDiscipline": "digital sketch archiving",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a digital sketch archiving demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Permanent Sketchbook Hub\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin your sketchbook pages as immutable IPFS entries to build a lifelong visual diary.\nDiscipline: Visual Art (digital sketch archiving).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a digital sketch archiving demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"digital sketch archiving\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PermanentSketchbookHub.compact\n// Pin your sketchbook pages as immutable IPFS entries to build a lifelong visual diary.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a digital sketch archiving artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — digital artist journaling tools",
      "sam": "$270M — sketch archiving apps",
      "som": "$60M — paid archival storage"
    },
    {
      "id": "visual-art-visual-storyboard-chain-18",
      "theme": "visual-art",
      "title": "Visual Storyboard Chain",
      "pitch": "Pin storyboards and frame metadata permanently for narrative visual art projects.",
      "subDiscipline": "narrative art sequencing",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a narrative art sequencing demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Visual Storyboard Chain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin storyboards and frame metadata permanently for narrative visual art projects.\nDiscipline: Visual Art (narrative art sequencing).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a narrative art sequencing demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"narrative art sequencing\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/VisualStoryboardChain.compact\n// Pin storyboards and frame metadata permanently for narrative visual art projects.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a narrative art sequencing artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$850M — visual storytelling tools",
      "sam": "$210M — storyboard digital platforms",
      "som": "$45M — subscriptions for sequence permanence"
    },
    {
      "id": "visual-art-curator-s-immutable-log-19",
      "theme": "visual-art",
      "title": "Curator’s Immutable Log",
      "pitch": "Pin curator notes, selections, and art metadata immutably to IPFS ensuring exhibition integrity.",
      "subDiscipline": "art exhibition curation",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a art exhibition curation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Curator’s Immutable Log\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin curator notes, selections, and art metadata immutably to IPFS ensuring exhibition integrity.\nDiscipline: Visual Art (art exhibition curation).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a art exhibition curation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"art exhibition curation\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/CuratorsImmutableLog.compact\n// Pin curator notes, selections, and art metadata immutably to IPFS ensuring exhibition integrity.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a art exhibition curation artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — digital curation software",
      "sam": "$320M — online exhibition tools",
      "som": "$70M — curator service subscriptions"
    },
    {
      "id": "visual-art-visual-remix-repository-20",
      "theme": "visual-art",
      "title": "Visual Remix Repository",
      "pitch": "Pin remixed artworks and source attribution information permanently for remix transparency.",
      "subDiscipline": "art remix documentation",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a art remix documentation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Visual Remix Repository\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin remixed artworks and source attribution information permanently for remix transparency.\nDiscipline: Visual Art (art remix documentation).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a art remix documentation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"art remix documentation\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/VisualRemixRepository.compact\n// Pin remixed artworks and source attribution information permanently for remix transparency.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a art remix documentation artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — remix art marketplaces",
      "sam": "$280M — remix attribution tools",
      "som": "$55M — paid transparency services"
    },
    {
      "id": "visual-art-unique-print-provenance-21",
      "theme": "visual-art",
      "title": "Unique Print Provenance",
      "pitch": "Pin limited print metadata and certificates immutably to prove authenticity for collectors.",
      "subDiscipline": "limited edition prints",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a limited edition prints demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Unique Print Provenance\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin limited print metadata and certificates immutably to prove authenticity for collectors.\nDiscipline: Visual Art (limited edition prints).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a limited edition prints demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"limited edition prints\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/UniquePrintProvenance.compact\n// Pin limited print metadata and certificates immutably to prove authenticity for collectors.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a limited edition prints artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — global print art market",
      "sam": "$1.5B — print provenance platforms",
      "som": "$350M — certification and archival subscriptions"
    },
    {
      "id": "visual-art-artistic-collaboration-chain-22",
      "theme": "visual-art",
      "title": "Artistic Collaboration Chain",
      "pitch": "Pin and permanently store co-created artworks and contribution metadata for fair crediting.",
      "subDiscipline": "joint creation records",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a joint creation records demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Artistic Collaboration Chain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin and permanently store co-created artworks and contribution metadata for fair crediting.\nDiscipline: Visual Art (joint creation records).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a joint creation records demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"joint creation records\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ArtisticCollaborationChain.compact\n// Pin and permanently store co-created artworks and contribution metadata for fair crediting.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a joint creation records artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.4B — collaborative art tools",
      "sam": "$360M — joint creative project platforms",
      "som": "$75M — paid collaboration archiving"
    },
    {
      "id": "visual-art-visual-rights-archive-23",
      "theme": "visual-art",
      "title": "Visual Rights Archive",
      "pitch": "Pin copyright registration data and proofs permanently for artists’ legal protection.",
      "subDiscipline": "copyright preservation",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a copyright preservation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Visual Rights Archive\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin copyright registration data and proofs permanently for artists’ legal protection.\nDiscipline: Visual Art (copyright preservation).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a copyright preservation demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"copyright preservation\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/VisualRightsArchive.compact\n// Pin copyright registration data and proofs permanently for artists’ legal protection.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a copyright preservation artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — copyright registration market",
      "sam": "$800M — digital copyright services",
      "som": "$180M — subscription copyright storage"
    },
    {
      "id": "visual-art-dynamic-exhibit-snapshot-24",
      "theme": "visual-art",
      "title": "Dynamic Exhibit Snapshot",
      "pitch": "Pin snapshots of changing exhibitions and arrangements permanently for historical records.",
      "subDiscipline": "exhibit state recording",
      "quantumHook": "IPFS content + on-chain CID",
      "quantumHookId": "ipfs-content",
      "quantumTag": "decentralized storage",
      "quantumRationale": "This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a exhibit state recording demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...",
      "megaPrompt": "Build \"Dynamic Exhibit Snapshot\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPin snapshots of changing exhibitions and arrangements permanently for historical records.\nDiscipline: Visual Art (exhibit state recording).\nOnchain primitive: IPFS content + on-chain CID (decentralized storage). Why this primitive: This idea fits IPFS content + on-chain CID because decentralized storage is exactly what a exhibit state recording demo needs: pin creative artefacts to IPFS via Pinata, then commit the CID to the public ledger through the Compact contract so prov...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nPINATA STEP (src/lib/pinata.ts):\n```ts\nexport async function pinToIPFS(file: Blob, name = \"exhibit state recording\") {\n  const fd = new FormData(); fd.append(\"file\", file, name);\n  const r = await fetch(\"https://api.pinata.cloud/pinning/pinFileToIPFS\", {\n    method: \"POST\", headers: { Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` },\n    body: fd,\n  });\n  return (await r.json()).IpfsHash as string;\n}\n```\n\nCONTRACT\n```compact\n// contracts/DynamicExhibitSnapshot.compact\n// Pin snapshots of changing exhibitions and arrangements permanently for historical records.  (pin-to-IPFS-then-commit-CID pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger cid_count: Counter;\nexport ledger last_cid: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { cid_count.increment(1); last_cid = disclose(\"(empty)\"); }\n\nexport circuit commitCid(cid: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = cid_count as Field as Bytes<32>;\n  last_author_commitment = disclose(\n    persistentHash<Vector<3, Bytes<32>>>([pad(32, \"cid:author:\"), seq, sk])\n  );\n  last_cid = disclose(cid);\n  cid_count.increment(1);\n}\n```\n\nFRONTEND FLOW\n1. User creates a exhibit state recording artefact in the UI (upload / draw / record / generate).\n2. `const cid = await pinToIPFS(blob)` → show `https://gateway.pinata.cloud/ipfs/<cid>`.\n3. `commitCid(cid)` via `findContractInstance(...).callTx.commitCid(witnesses, cid)`.\n4. Proving spinner (30–120s) → confirmation → append `{cid, tx, at}` to the on-screen feed.\n5. Feed is hydrated from the Indexer (`readLedger`), so refreshes work with just the wallet disconnected.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — exhibit documentation solutions",
      "sam": "$260M — art show documentation tools",
      "som": "$55M — paid exhibit snapshot services"
    },
    {
      "id": "visual-art-canvas-collaborate-0",
      "theme": "visual-art",
      "title": "Canvas Collaborate",
      "pitch": "Enable painters to co-create artworks with seamless onchain identity and gasless contributions.",
      "subDiscipline": "collaborative painting",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a collaborative painting demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Canvas Collaborate\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nEnable painters to co-create artworks with seamless onchain identity and gasless contributions.\nDiscipline: Visual Art (collaborative painting).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a collaborative painting demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/CanvasCollaborate.compact\n// Enable painters to co-create artworks with seamless onchain identity and gasless contributions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"canvas_collaborate:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every collaborative painting action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global collaborative art platforms",
      "sam": "$2B — digital collaboration tools for painters",
      "som": "$100M — early adopters in gasless art collaboration"
    },
    {
      "id": "visual-art-illustrator-guild-1",
      "theme": "visual-art",
      "title": "Illustrator Guild",
      "pitch": "Create a hosted network for illustrators to share, sell, and verify work with easy login and zero gas fees.",
      "subDiscipline": "illustrator community",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a illustrator community demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Illustrator Guild\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate a hosted network for illustrators to share, sell, and verify work with easy login and zero gas fees.\nDiscipline: Visual Art (illustrator community).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a illustrator community demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/IllustratorGuild.compact\n// Create a hosted network for illustrators to share, sell, and verify work with easy login and zero gas fees.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"illustrator_guild:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every illustrator community action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global illustration market",
      "sam": "$1B — digital sales of illustrations",
      "som": "$50M — networks enabling gasless art transactions"
    },
    {
      "id": "visual-art-generative-canvas-2",
      "theme": "visual-art",
      "title": "Generative Canvas",
      "pitch": "Let generative artists deploy and monetize code-driven art with instant gasless onboarding and transactions.",
      "subDiscipline": "generative art",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a generative art demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Generative Canvas\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nLet generative artists deploy and monetize code-driven art with instant gasless onboarding and transactions.\nDiscipline: Visual Art (generative art).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a generative art demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GenerativeCanvas.compact\n// Let generative artists deploy and monetize code-driven art with instant gasless onboarding and transactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"generative_canvas:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every generative art action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — generative digital art sales",
      "sam": "$800M — code-based art platforms",
      "som": "$40M — gasless onboarding for generative artists"
    },
    {
      "id": "visual-art-gallery-ledger-3",
      "theme": "visual-art",
      "title": "Gallery Ledger",
      "pitch": "Provide gallerists with gasless, onchain tools to prove artwork provenance and authenticity effortlessly.",
      "subDiscipline": "art provenance tracking",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a art provenance tracking demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Gallery Ledger\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nProvide gallerists with gasless, onchain tools to prove artwork provenance and authenticity effortlessly.\nDiscipline: Visual Art (art provenance tracking).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a art provenance tracking demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GalleryLedger.compact\n// Provide gallerists with gasless, onchain tools to prove artwork provenance and authenticity effortlessly.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"gallery_ledger:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every art provenance tracking action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — global art provenance solutions",
      "sam": "$4B — provenance tools for galleries",
      "som": "$200M — gasless provenance management users"
    },
    {
      "id": "visual-art-palette-swap-4",
      "theme": "visual-art",
      "title": "Palette Swap",
      "pitch": "Allow painters and illustrators to exchange and license color palettes with transparent, gasless transactions.",
      "subDiscipline": "color exchange",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a color exchange demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Palette Swap\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nAllow painters and illustrators to exchange and license color palettes with transparent, gasless transactions.\nDiscipline: Visual Art (color exchange).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a color exchange demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PaletteSwap.compact\n// Allow painters and illustrators to exchange and license color palettes with transparent, gasless transactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"palette_swap:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every color exchange action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — digital color licensing market",
      "sam": "$120M — palette trading platforms",
      "som": "$10M — early palette exchange adopters"
    },
    {
      "id": "visual-art-sketchstream-5",
      "theme": "visual-art",
      "title": "SketchStream",
      "pitch": "Stream live sketches with integrated onchain tips and gasless wallet logins for artist-audience engagement.",
      "subDiscipline": "live drawing streams",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a live drawing streams demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"SketchStream\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nStream live sketches with integrated onchain tips and gasless wallet logins for artist-audience engagement.\nDiscipline: Visual Art (live drawing streams).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a live drawing streams demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SketchStream.compact\n// Stream live sketches with integrated onchain tips and gasless wallet logins for artist-audience engagement.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"sketchstream:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every live drawing streams action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — live streaming art markets",
      "sam": "$400M — live tip-enabled drawing platforms",
      "som": "$25M — gasless tipping users"
    },
    {
      "id": "visual-art-nft-splitter-6",
      "theme": "visual-art",
      "title": "NFT Splitter",
      "pitch": "Enable painters and collectors to split artwork ownership onchain with easy social login and no gas fees.",
      "subDiscipline": "fractional art ownership",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a fractional art ownership demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"NFT Splitter\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nEnable painters and collectors to split artwork ownership onchain with easy social login and no gas fees.\nDiscipline: Visual Art (fractional art ownership).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a fractional art ownership demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/NFTSplitter.compact\n// Enable painters and collectors to split artwork ownership onchain with easy social login and no gas fees.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"nft_splitter:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every fractional art ownership action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — art fractionalization market",
      "sam": "$3B — digital sharing of artwork equity",
      "som": "$150M — gasless split ownership users"
    },
    {
      "id": "visual-art-artswap-social-7",
      "theme": "visual-art",
      "title": "ArtSwap Social",
      "pitch": "Build a social marketplace for illustrators to swap artworks and services without gas concerns.",
      "subDiscipline": "art barter communities",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a art barter communities demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"ArtSwap Social\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nBuild a social marketplace for illustrators to swap artworks and services without gas concerns.\nDiscipline: Visual Art (art barter communities).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a art barter communities demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ArtSwapSocial.compact\n// Build a social marketplace for illustrators to swap artworks and services without gas concerns.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"artswap_social:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every art barter communities action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global art barter economy",
      "sam": "$350M — illustrator barter platforms",
      "som": "$20M — gasless transaction swap users"
    },
    {
      "id": "visual-art-sketchvault-8",
      "theme": "visual-art",
      "title": "SketchVault",
      "pitch": "Offer painters a gasless, onchain-secured vault for storing and sharing preliminary sketches with social login.",
      "subDiscipline": "secure sketch storage",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a secure sketch storage demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"SketchVault\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nOffer painters a gasless, onchain-secured vault for storing and sharing preliminary sketches with social login.\nDiscipline: Visual Art (secure sketch storage).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a secure sketch storage demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SketchVault.compact\n// Offer painters a gasless, onchain-secured vault for storing and sharing preliminary sketches with social login.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"sketchvault:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every secure sketch storage action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — secure digital art storage",
      "sam": "$200M — sketch archival platforms",
      "som": "$12M — gasless storage adopters"
    },
    {
      "id": "visual-art-brushstroke-rights-9",
      "theme": "visual-art",
      "title": "Brushstroke Rights",
      "pitch": "Help illustrators manage and transfer licenses onchain with simple Google sign-in and zero gas.",
      "subDiscipline": "digital rights management",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a digital rights management demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Brushstroke Rights\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nHelp illustrators manage and transfer licenses onchain with simple Google sign-in and zero gas.\nDiscipline: Visual Art (digital rights management).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a digital rights management demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/BrushstrokeRights.compact\n// Help illustrators manage and transfer licenses onchain with simple Google sign-in and zero gas.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"brushstroke_rights:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every digital rights management action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — digital art licensing market",
      "sam": "$1.5B — licensing platforms for illustrators",
      "som": "$80M — gasless DRM users"
    },
    {
      "id": "visual-art-artchain-auctions-10",
      "theme": "visual-art",
      "title": "ArtChain Auctions",
      "pitch": "Create gasless onchain auctions for visual artists with fast wallet onboarding and sponsored bids.",
      "subDiscipline": "auction facilitation",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a auction facilitation demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"ArtChain Auctions\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate gasless onchain auctions for visual artists with fast wallet onboarding and sponsored bids.\nDiscipline: Visual Art (auction facilitation).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a auction facilitation demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ArtChainAuctions.compact\n// Create gasless onchain auctions for visual artists with fast wallet onboarding and sponsored bids.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"artchain_auctions:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every auction facilitation action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global art auctions",
      "sam": "$3.5B — digital art auction platforms",
      "som": "$180M — gasless auction users"
    },
    {
      "id": "visual-art-framefi-marketplace-11",
      "theme": "visual-art",
      "title": "FrameFi Marketplace",
      "pitch": "Enable illustrators to sell artworks with embedded gasless framing add-ons and easy wallet login.",
      "subDiscipline": "digital framing services",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a digital framing services demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"FrameFi Marketplace\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nEnable illustrators to sell artworks with embedded gasless framing add-ons and easy wallet login.\nDiscipline: Visual Art (digital framing services).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a digital framing services demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/FrameFiMarketplace.compact\n// Enable illustrators to sell artworks with embedded gasless framing add-ons and easy wallet login.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"framefi_marketplace:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every digital framing services action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — digital art framing market",
      "sam": "$150M — framing add-on buyers",
      "som": "$10M — gasless framing service users"
    },
    {
      "id": "visual-art-mural-mesh-12",
      "theme": "visual-art",
      "title": "Mural Mesh",
      "pitch": "Coordinate large-scale public murals with painters via gasless onchain collaboration and social login.",
      "subDiscipline": "public mural collaboration",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a public mural collaboration demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Mural Mesh\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCoordinate large-scale public murals with painters via gasless onchain collaboration and social login.\nDiscipline: Visual Art (public mural collaboration).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a public mural collaboration demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/MuralMesh.compact\n// Coordinate large-scale public murals with painters via gasless onchain collaboration and social login.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"mural_mesh:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every public mural collaboration action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — public mural funding",
      "sam": "$300M — collaborative mural initiatives",
      "som": "$18M — gasless onchain mural participants"
    },
    {
      "id": "visual-art-colorstory-13",
      "theme": "visual-art",
      "title": "ColorStory",
      "pitch": "Illustrators share evolving color-based stories secured onchain with gasless updates and Google sign-in.",
      "subDiscipline": "color narrative art",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a color narrative art demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"ColorStory\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nIllustrators share evolving color-based stories secured onchain with gasless updates and Google sign-in.\nDiscipline: Visual Art (color narrative art).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a color narrative art demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ColorStory.compact\n// Illustrators share evolving color-based stories secured onchain with gasless updates and Google sign-in.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"colorstory:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every color narrative art action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$350M — digital narrative art market",
      "sam": "$90M — color story platforms",
      "som": "$7M — gasless narrative update users"
    },
    {
      "id": "visual-art-token-tapestry-14",
      "theme": "visual-art",
      "title": "Token Tapestry",
      "pitch": "Artists tokenize and sell unique textile patterns with gasless wallet integration and sponsored transactions.",
      "subDiscipline": "textile pattern art",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a textile pattern art demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Token Tapestry\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nArtists tokenize and sell unique textile patterns with gasless wallet integration and sponsored transactions.\nDiscipline: Visual Art (textile pattern art).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a textile pattern art demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/TokenTapestry.compact\n// Artists tokenize and sell unique textile patterns with gasless wallet integration and sponsored transactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"token_tapestry:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every textile pattern art action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — digital textile art market",
      "sam": "$500M — pattern tokenization",
      "som": "$30M — gasless textile pattern buyers"
    },
    {
      "id": "visual-art-gallery-ghost-15",
      "theme": "visual-art",
      "title": "Gallery Ghost",
      "pitch": "Gallerists create virtual galleries with gasless onchain ticketing and Google-based wallet onboarding.",
      "subDiscipline": "virtual gallery hosting",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a virtual gallery hosting demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Gallery Ghost\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nGallerists create virtual galleries with gasless onchain ticketing and Google-based wallet onboarding.\nDiscipline: Visual Art (virtual gallery hosting).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a virtual gallery hosting demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GalleryGhost.compact\n// Gallerists create virtual galleries with gasless onchain ticketing and Google-based wallet onboarding.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"gallery_ghost:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every virtual gallery hosting action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — virtual gallery ticket sales",
      "sam": "$1.2B — digital gallery hosting platforms",
      "som": "$70M — gasless ticket user base"
    },
    {
      "id": "visual-art-brushbot-16",
      "theme": "visual-art",
      "title": "BrushBot",
      "pitch": "Integrate AI painting tools powered by gasless onchain user verification and transactions.",
      "subDiscipline": "AI-assisted painting",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a AI-assisted painting demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"BrushBot\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nIntegrate AI painting tools powered by gasless onchain user verification and transactions.\nDiscipline: Visual Art (AI-assisted painting).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a AI-assisted painting demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/BrushBot.compact\n// Integrate AI painting tools powered by gasless onchain user verification and transactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"brushbot:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every AI-assisted painting action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3.5B — AI art tool market",
      "sam": "$900M — AI painter subscriptions",
      "som": "$45M — gasless AI tool users"
    },
    {
      "id": "visual-art-sketchset-swap-17",
      "theme": "visual-art",
      "title": "SketchSet Swap",
      "pitch": "Illustrators trade and license brush presets with zero gas fees and easy Google sign-in wallets.",
      "subDiscipline": "brush preset exchange",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a brush preset exchange demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"SketchSet Swap\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nIllustrators trade and license brush presets with zero gas fees and easy Google sign-in wallets.\nDiscipline: Visual Art (brush preset exchange).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a brush preset exchange demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SketchSetSwap.compact\n// Illustrators trade and license brush presets with zero gas fees and easy Google sign-in wallets.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"sketchset_swap:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every brush preset exchange action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — digital brush market",
      "sam": "$100M — brush preset trading",
      "som": "$8M — gasless brush exchange users"
    },
    {
      "id": "visual-art-artchain-critique-18",
      "theme": "visual-art",
      "title": "ArtChain Critique",
      "pitch": "Painters share work for peer feedback with onchain reputation and gasless login/transactions.",
      "subDiscipline": "peer review platform",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a peer review platform demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"ArtChain Critique\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPainters share work for peer feedback with onchain reputation and gasless login/transactions.\nDiscipline: Visual Art (peer review platform).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a peer review platform demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ArtChainCritique.compact\n// Painters share work for peer feedback with onchain reputation and gasless login/transactions.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"artchain_critique:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every peer review platform action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — digital art critique services",
      "sam": "$180M — peer review platforms for artists",
      "som": "$12M — gasless critique users"
    },
    {
      "id": "visual-art-illustrate-impact-19",
      "theme": "visual-art",
      "title": "Illustrate Impact",
      "pitch": "Facilitate gasless donations and sponsorships for illustrators supporting social causes via easy login.",
      "subDiscipline": "art philanthropy",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a art philanthropy demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Illustrate Impact\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nFacilitate gasless donations and sponsorships for illustrators supporting social causes via easy login.\nDiscipline: Visual Art (art philanthropy).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a art philanthropy demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/IllustrateImpact.compact\n// Facilitate gasless donations and sponsorships for illustrators supporting social causes via easy login.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"illustrate_impact:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every art philanthropy action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.5B — art philanthropy market",
      "sam": "$600M — donations to illustrators",
      "som": "$35M — gasless charity transaction users"
    },
    {
      "id": "visual-art-colorchain-auctions-20",
      "theme": "visual-art",
      "title": "Colorchain Auctions",
      "pitch": "Host auctions of color-driven artworks with gasless bidding and quick wallet onboarding.",
      "subDiscipline": "color-themed art auctions",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a color-themed art auctions demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Colorchain Auctions\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nHost auctions of color-driven artworks with gasless bidding and quick wallet onboarding.\nDiscipline: Visual Art (color-themed art auctions).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a color-themed art auctions demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ColorchainAuctions.compact\n// Host auctions of color-driven artworks with gasless bidding and quick wallet onboarding.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"colorchain_auctions:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every color-themed art auctions action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — color art auction market",
      "sam": "$250M — color themed auction platforms",
      "som": "$15M — gasless bidders"
    },
    {
      "id": "visual-art-illustrator-ink-21",
      "theme": "visual-art",
      "title": "Illustrator Ink",
      "pitch": "Support ink artists selling limited editions with gasless minting and sponsored transaction flows.",
      "subDiscipline": "digital ink art",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a digital ink art demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Illustrator Ink\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nSupport ink artists selling limited editions with gasless minting and sponsored transaction flows.\nDiscipline: Visual Art (digital ink art).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a digital ink art demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/IllustratorInk.compact\n// Support ink artists selling limited editions with gasless minting and sponsored transaction flows.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"illustrator_ink:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every digital ink art action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — digital ink art market",
      "sam": "$400M — limited edition ink artworks",
      "som": "$22M — gasless minting users"
    },
    {
      "id": "visual-art-generative-gallery-22",
      "theme": "visual-art",
      "title": "Generative Gallery",
      "pitch": "Curate generative art exhibits featuring gasless ticketing and social login onboarding.",
      "subDiscipline": "algorithmic art exhibition",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a algorithmic art exhibition demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"Generative Gallery\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCurate generative art exhibits featuring gasless ticketing and social login onboarding.\nDiscipline: Visual Art (algorithmic art exhibition).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a algorithmic art exhibition demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GenerativeGallery.compact\n// Curate generative art exhibits featuring gasless ticketing and social login onboarding.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"generative_gallery:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every algorithmic art exhibition action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.8B — digital generative art events",
      "sam": "$500M — ticketing for generative exhibits",
      "som": "$28M — gasless event participants"
    },
    {
      "id": "visual-art-artswap-network-23",
      "theme": "visual-art",
      "title": "ArtSwap Network",
      "pitch": "Enable painters and illustrators to swap artworks onchain with gasless wallet bootstrapped by social login.",
      "subDiscipline": "visual art trades",
      "quantumHook": "Lace wallet + tDUST",
      "quantumHookId": "lace-wallet",
      "quantumTag": "wallet UX",
      "quantumRationale": "This idea fits Lace wallet + tDUST because wallet UX is exactly what a visual art trades demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...",
      "megaPrompt": "Build \"ArtSwap Network\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nEnable painters and illustrators to swap artworks onchain with gasless wallet bootstrapped by social login.\nDiscipline: Visual Art (visual art trades).\nOnchain primitive: Lace wallet + tDUST (wallet UX). Why this primitive: This idea fits Lace wallet + tDUST because wallet UX is exactly what a visual art trades demo needs: detect `window.midnight`, call `initialAPI.connect('preview')`, let Lace balance the transaction with tDUST and submit i...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ArtSwapNetwork.compact\n// Enable painters and illustrators to swap artworks onchain with gasless wallet bootstrapped by social login.\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\n// Public ledger state — visible to everyone via the Indexer\nexport ledger entry_count: Counter;\nexport ledger last_message: Opaque<\"string\">;\nexport ledger last_author_commitment: Bytes<32>;\n\n// Private callback wired from TypeScript; the returned bytes never touch chain\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() {\n  entry_count.increment(1);\n  last_message = disclose(\"(empty)\");\n}\n\nexport circuit authorCommitment(sk: Bytes<32>, seq: Bytes<32>): Bytes<32> {\n  return persistentHash<Vector<3, Bytes<32>>>(\n    [pad(32, \"artswap_network:author:\"), seq, sk]\n  );\n}\n\nexport circuit appendEntry(newMessage: Opaque<\"string\">): [] {\n  const sk = localSecretKey();\n  const seq = entry_count as Field as Bytes<32>;\n  last_author_commitment = disclose(authorCommitment(sk, seq));\n  last_message = disclose(newMessage);      // disclose is REQUIRED before writing to ledger\n  entry_count.increment(1);\n}\n```\n\nFRONTEND FLOW — Lace is the whole auth story\n1. Landing page shows one button: \"Connect Lace\". No email, no password, no OAuth.\n2. On click: `initProviders()` → shielded address becomes the identity.\n3. tDUST balance strip: `const bal = await connectedAPI.balanceAndProofOfBalance();` render it.\n4. Every visual art trades action = `contract.callTx.appendEntry(witnesses, payload)`. Show status:\n   `Proving → Balancing (Lace adds tDUST fees) → Submitting → Confirmed` with a Midnight explorer link.\n5. If `waitForLace` rejects, render the exact install URL for Lace with a friendly nudge —\n   no fake \"click here to install\" placeholder. This is the single biggest onboarding drop-off.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global art swap market",
      "sam": "$550M — digital artwork trade platforms",
      "som": "$33M — gasless swap users"
    },
    {
      "id": "visual-art-canvas-chronicles-0",
      "theme": "visual-art",
      "title": "Canvas Chronicles",
      "pitch": "Securely mint and track authentic painting portfolios with verifiable creation history.",
      "subDiscipline": "painting portfolios",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a painting portfolios demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Canvas Chronicles\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nSecurely mint and track authentic painting portfolios with verifiable creation history.\nDiscipline: Visual Art (painting portfolios).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a painting portfolios demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/CanvasChronicles.compact\n// Securely mint and track authentic painting portfolios with verifiable creation history.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove painting portfolios\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$2.5B — online artist portfolio services",
      "som": "$50M — NFT-based artist portfolio adopters"
    },
    {
      "id": "visual-art-sketch-stamp-1",
      "theme": "visual-art",
      "title": "Sketch Stamp",
      "pitch": "Authenticate and timestamp early illustration drafts to prove originality and creative process.",
      "subDiscipline": "illustration drafts",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a illustration drafts demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Sketch Stamp\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nAuthenticate and timestamp early illustration drafts to prove originality and creative process.\nDiscipline: Visual Art (illustration drafts).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a illustration drafts demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SketchStamp.compact\n// Authenticate and timestamp early illustration drafts to prove originality and creative process.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove illustration drafts\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$800M — digital illustration sector",
      "som": "$10M — provenance-tracked illustration sales"
    },
    {
      "id": "visual-art-generative-genesis-2",
      "theme": "visual-art",
      "title": "Generative Genesis",
      "pitch": "Track and mint each generative art iteration as unique collectible tokens.",
      "subDiscipline": "generative art iterations",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a generative art iterations demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Generative Genesis\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nTrack and mint each generative art iteration as unique collectible tokens.\nDiscipline: Visual Art (generative art iterations).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a generative art iterations demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GenerativeGenesis.compact\n// Track and mint each generative art iteration as unique collectible tokens.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove generative art iterations\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$400M — generative art niche",
      "som": "$5M — NFT generative art collectors"
    },
    {
      "id": "visual-art-gallery-gatekeeper-3",
      "theme": "visual-art",
      "title": "Gallery Gatekeeper",
      "pitch": "Enable galleries to mint and verify provenance of physical artwork displayed or sold.",
      "subDiscipline": "gallery asset curation",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a gallery asset curation demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Gallery Gatekeeper\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nEnable galleries to mint and verify provenance of physical artwork displayed or sold.\nDiscipline: Visual Art (gallery asset curation).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a gallery asset curation demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GalleryGatekeeper.compact\n// Enable galleries to mint and verify provenance of physical artwork displayed or sold.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove gallery asset curation\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$7B — global gallery sales",
      "som": "$100M — digital provenance certified artworks"
    },
    {
      "id": "visual-art-color-ledger-4",
      "theme": "visual-art",
      "title": "Color Ledger",
      "pitch": "Mint unique NFTs capturing and securing artist-specific color palettes and schemes.",
      "subDiscipline": "color palette preservation",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a color palette preservation demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Color Ledger\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint unique NFTs capturing and securing artist-specific color palettes and schemes.\nDiscipline: Visual Art (color palette preservation).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a color palette preservation demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ColorLedger.compact\n// Mint unique NFTs capturing and securing artist-specific color palettes and schemes.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove color palette preservation\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$300M — digital art toolsets",
      "som": "$4M — color-palette licensing via NFTs"
    },
    {
      "id": "visual-art-brushstroke-chain-5",
      "theme": "visual-art",
      "title": "Brushstroke Chain",
      "pitch": "Record and mint signature brushstroke styles as NFTs for artist branding and licensing.",
      "subDiscipline": "brush technique archives",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a brush technique archives demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Brushstroke Chain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nRecord and mint signature brushstroke styles as NFTs for artist branding and licensing.\nDiscipline: Visual Art (brush technique archives).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a brush technique archives demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/BrushstrokeChain.compact\n// Record and mint signature brushstroke styles as NFTs for artist branding and licensing.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove brush technique archives\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$150M — visual art teaching content",
      "som": "$3M — brushstroke NFT sales"
    },
    {
      "id": "visual-art-minted-murals-6",
      "theme": "visual-art",
      "title": "Minted Murals",
      "pitch": "Authenticate and immortalize public murals with creator-owned NFT provenance tokens.",
      "subDiscipline": "public mural documentation",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a public mural documentation demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Minted Murals\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nAuthenticate and immortalize public murals with creator-owned NFT provenance tokens.\nDiscipline: Visual Art (public mural documentation).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a public mural documentation demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/MintedMurals.compact\n// Authenticate and immortalize public murals with creator-owned NFT provenance tokens.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove public mural documentation\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$500M — public art projects",
      "som": "$8M — NFT mural provenance fees"
    },
    {
      "id": "visual-art-framechain-7",
      "theme": "visual-art",
      "title": "FrameChain",
      "pitch": "Control and verify reproduction rights of artworks via mintable provenance NFTs.",
      "subDiscipline": "art reproduction control",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a art reproduction control demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"FrameChain\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nControl and verify reproduction rights of artworks via mintable provenance NFTs.\nDiscipline: Visual Art (art reproduction control).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a art reproduction control demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/FrameChain.compact\n// Control and verify reproduction rights of artworks via mintable provenance NFTs.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove art reproduction control\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$3B — art reproduction licensing",
      "som": "$50M — provenance NFT reproduction licenses"
    },
    {
      "id": "visual-art-illustrator-s-imprint-8",
      "theme": "visual-art",
      "title": "Illustrator’s Imprint",
      "pitch": "Mint signed digital prints with verifiable creator provenance directly on the blockchain.",
      "subDiscipline": "signed digital prints",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a signed digital prints demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Illustrator’s Imprint\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint signed digital prints with verifiable creator provenance directly on the blockchain.\nDiscipline: Visual Art (signed digital prints).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a signed digital prints demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/IllustratorsImprint.compact\n// Mint signed digital prints with verifiable creator provenance directly on the blockchain.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove signed digital prints\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$1B — digital print sales",
      "som": "$15M — NFT print provenance market"
    },
    {
      "id": "visual-art-provenance-palette-9",
      "theme": "visual-art",
      "title": "Provenance Palette",
      "pitch": "Digitally certify and mint provenance tokens for unique physical artist palettes.",
      "subDiscipline": "artist palette provenance",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a artist palette provenance demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Provenance Palette\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nDigitally certify and mint provenance tokens for unique physical artist palettes.\nDiscipline: Visual Art (artist palette provenance).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a artist palette provenance demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ProvenancePalette.compact\n// Digitally certify and mint provenance tokens for unique physical artist palettes.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove artist palette provenance\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$50M — artist tools & collectibles",
      "som": "$1.5M — NFT-verified palette auctions"
    },
    {
      "id": "visual-art-pixel-provenance-10",
      "theme": "visual-art",
      "title": "Pixel Provenance",
      "pitch": "Securely mint pixel art pieces with immutable creator provenance and IPFS metadata.",
      "subDiscipline": "pixel art authentication",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a pixel art authentication demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Pixel Provenance\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nSecurely mint pixel art pieces with immutable creator provenance and IPFS metadata.\nDiscipline: Visual Art (pixel art authentication).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a pixel art authentication demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/PixelProvenance.compact\n// Securely mint pixel art pieces with immutable creator provenance and IPFS metadata.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove pixel art authentication\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$250M — pixel art market",
      "som": "$5M — NFT pixel art provenance"
    },
    {
      "id": "visual-art-exhibit-echo-11",
      "theme": "visual-art",
      "title": "Exhibit Echo",
      "pitch": "Preserve virtual gallery exhibits with creator-owned NFT provenance linked to IPFS.",
      "subDiscipline": "virtual exhibition records",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a virtual exhibition records demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Exhibit Echo\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nPreserve virtual gallery exhibits with creator-owned NFT provenance linked to IPFS.\nDiscipline: Visual Art (virtual exhibition records).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a virtual exhibition records demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ExhibitEcho.compact\n// Preserve virtual gallery exhibits with creator-owned NFT provenance linked to IPFS.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove virtual exhibition records\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$1B — virtual exhibition revenues",
      "som": "$12M — NFT exhibit provenance buyers"
    },
    {
      "id": "visual-art-storyboard-stamp-12",
      "theme": "visual-art",
      "title": "Storyboard Stamp",
      "pitch": "Mint sequential NFTs to authenticate evolving concept art storyboards and ideas.",
      "subDiscipline": "concept art progression",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a concept art progression demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Storyboard Stamp\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint sequential NFTs to authenticate evolving concept art storyboards and ideas.\nDiscipline: Visual Art (concept art progression).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a concept art progression demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/StoryboardStamp.compact\n// Mint sequential NFTs to authenticate evolving concept art storyboards and ideas.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove concept art progression\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$350M — concept art services",
      "som": "$6M — provenance NFT storyboards"
    },
    {
      "id": "visual-art-masterstroke-mint-13",
      "theme": "visual-art",
      "title": "Masterstroke Mint",
      "pitch": "Create immutable certificates of authenticity for signature artworks via NFT provenance mint.",
      "subDiscipline": "signature artwork certs",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a signature artwork certs demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Masterstroke Mint\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate immutable certificates of authenticity for signature artworks via NFT provenance mint.\nDiscipline: Visual Art (signature artwork certs).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a signature artwork certs demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/MasterstrokeMint.compact\n// Create immutable certificates of authenticity for signature artworks via NFT provenance mint.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove signature artwork certs\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$9B — art certification services",
      "som": "$100M — NFT-based authenticity certificates"
    },
    {
      "id": "visual-art-layer-ledger-14",
      "theme": "visual-art",
      "title": "Layer Ledger",
      "pitch": "Track and mint individual painting layers proving creative process and ownership.",
      "subDiscipline": "digital painting layers",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a digital painting layers demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Layer Ledger\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nTrack and mint individual painting layers proving creative process and ownership.\nDiscipline: Visual Art (digital painting layers).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a digital painting layers demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/LayerLedger.compact\n// Track and mint individual painting layers proving creative process and ownership.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove digital painting layers\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$400M — digital art creation tools",
      "som": "$7M — provenance NFTs of layers"
    },
    {
      "id": "visual-art-artchain-auction-15",
      "theme": "visual-art",
      "title": "Artchain Auction",
      "pitch": "Integrate provenance minting into art auctions to verify authenticity and creator royalties.",
      "subDiscipline": "secondary art sales",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a secondary art sales demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Artchain Auction\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nIntegrate provenance minting into art auctions to verify authenticity and creator royalties.\nDiscipline: Visual Art (secondary art sales).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a secondary art sales demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ArtchainAuction.compact\n// Integrate provenance minting into art auctions to verify authenticity and creator royalties.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove secondary art sales\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$15B — secondary art market",
      "som": "$300M — NFT provenance auction fees"
    },
    {
      "id": "visual-art-color-code-vault-16",
      "theme": "visual-art",
      "title": "Color Code Vault",
      "pitch": "Securely store and prove origin of proprietary digital color codes with mintable NFTs.",
      "subDiscipline": "digital color codes",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a digital color codes demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Color Code Vault\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nSecurely store and prove origin of proprietary digital color codes with mintable NFTs.\nDiscipline: Visual Art (digital color codes).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a digital color codes demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ColorCodeVault.compact\n// Securely store and prove origin of proprietary digital color codes with mintable NFTs.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove digital color codes\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$200M — digital color tool market",
      "som": "$3M — NFT color code licenses"
    },
    {
      "id": "visual-art-art-mentor-mark-17",
      "theme": "visual-art",
      "title": "Art Mentor Mark",
      "pitch": "Mint NFTs to certify mentorship lineage and creative influence among artists.",
      "subDiscipline": "art mentorship proof",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a art mentorship proof demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Art Mentor Mark\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint NFTs to certify mentorship lineage and creative influence among artists.\nDiscipline: Visual Art (art mentorship proof).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a art mentorship proof demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ArtMentorMark.compact\n// Mint NFTs to certify mentorship lineage and creative influence among artists.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove art mentorship proof\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$500M — art education platforms",
      "som": "$8M — NFT mentorship certificates"
    },
    {
      "id": "visual-art-signature-seal-18",
      "theme": "visual-art",
      "title": "Signature Seal",
      "pitch": "Allow artists to mint unique digital signatures as NFT proof of authorship and authenticity.",
      "subDiscipline": "digital signature minting",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a digital signature minting demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Signature Seal\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nAllow artists to mint unique digital signatures as NFT proof of authorship and authenticity.\nDiscipline: Visual Art (digital signature minting).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a digital signature minting demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/SignatureSeal.compact\n// Allow artists to mint unique digital signatures as NFT proof of authorship and authenticity.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove digital signature minting\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$1B — digital signature services",
      "som": "$12M — NFT signature deployments"
    },
    {
      "id": "visual-art-nft-mirror-19",
      "theme": "visual-art",
      "title": "NFT Mirror",
      "pitch": "Mint paired NFTs representing original and reflected artworks to showcase creative duality.",
      "subDiscipline": "artistic reflection sets",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a artistic reflection sets demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"NFT Mirror\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint paired NFTs representing original and reflected artworks to showcase creative duality.\nDiscipline: Visual Art (artistic reflection sets).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a artistic reflection sets demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/NFTMirror.compact\n// Mint paired NFTs representing original and reflected artworks to showcase creative duality.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove artistic reflection sets\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$250M — contemporary art niche",
      "som": "$4M — NFT paired artwork sales"
    },
    {
      "id": "visual-art-art-rewind-20",
      "theme": "visual-art",
      "title": "Art Rewind",
      "pitch": "Mint stepwise NFTs documenting artwork creation stages for replay and provenance.",
      "subDiscipline": "creative process playback",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a creative process playback demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Art Rewind\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint stepwise NFTs documenting artwork creation stages for replay and provenance.\nDiscipline: Visual Art (creative process playback).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a creative process playback demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ArtRewind.compact\n// Mint stepwise NFTs documenting artwork creation stages for replay and provenance.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove creative process playback\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$350M — digital art tutorials",
      "som": "$6M — NFT creative process records"
    },
    {
      "id": "visual-art-gallery-provenance-21",
      "theme": "visual-art",
      "title": "Gallery Provenance",
      "pitch": "Digitally certify artwork origin and gallery exhibition history onchain with NFTs.",
      "subDiscipline": "physical gallery tracking",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a physical gallery tracking demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Gallery Provenance\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nDigitally certify artwork origin and gallery exhibition history onchain with NFTs.\nDiscipline: Visual Art (physical gallery tracking).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a physical gallery tracking demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/GalleryProvenance.compact\n// Digitally certify artwork origin and gallery exhibition history onchain with NFTs.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove physical gallery tracking\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$6B — gallery exhibited artwork",
      "som": "$90M — NFT gallery provenance usage"
    },
    {
      "id": "visual-art-nft-colorgram-22",
      "theme": "visual-art",
      "title": "NFT Colorgram",
      "pitch": "Create NFTs representing exclusive color-themed artworks with immutable provenance.",
      "subDiscipline": "chromatic art NFTs",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a chromatic art NFTs demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"NFT Colorgram\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nCreate NFTs representing exclusive color-themed artworks with immutable provenance.\nDiscipline: Visual Art (chromatic art NFTs).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a chromatic art NFTs demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/NFTColorgram.compact\n// Create NFTs representing exclusive color-themed artworks with immutable provenance.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove chromatic art NFTs\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$500M — color-focused art collectors",
      "som": "$7M — NFT color art trading"
    },
    {
      "id": "visual-art-motion-mint-23",
      "theme": "visual-art",
      "title": "Motion Mint",
      "pitch": "Mint creator-owned NFTs for animated illustrations backed by IPFS-stored assets.",
      "subDiscipline": "animated illustration ownership",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a animated illustration ownership demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Motion Mint\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nMint creator-owned NFTs for animated illustrations backed by IPFS-stored assets.\nDiscipline: Visual Art (animated illustration ownership).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a animated illustration ownership demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/MotionMint.compact\n// Mint creator-owned NFTs for animated illustrations backed by IPFS-stored assets.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove animated illustration ownership\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$600M — animated illustration market",
      "som": "$9M — NFT animated art sales"
    },
    {
      "id": "visual-art-provenance-palette-24",
      "theme": "visual-art",
      "title": "Provenance Palette",
      "pitch": "Digitize and mint physical palette images as NFTs preserving unique artist color mixes.",
      "subDiscipline": "physical palette digitization",
      "quantumHook": "Private witness proof",
      "quantumHookId": "private-witness",
      "quantumTag": "zero-knowledge",
      "quantumRationale": "This idea fits Private witness proof because zero-knowledge is exactly what a physical palette digitization demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...",
      "megaPrompt": "Build \"Provenance Palette\" in ONE Lovable message. Single-page Midnight ZK demo.\n\nCONCEPT\nDigitize and mint physical palette images as NFTs preserving unique artist color mixes.\nDiscipline: Visual Art (physical palette digitization).\nOnchain primitive: Private witness proof (zero-knowledge). Why this primitive: This idea fits Private witness proof because zero-knowledge is exactly what a physical palette digitization demo needs: a Compact `witness` callback that keeps a user secret local while a ZK proof asserts a property of it — the proof lands ...\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page Vite + React app. No router, no Lovable Cloud, no database, no server-side auth.\n- ONE Compact contract, ≤80 lines, deployed to Midnight preview testnet.\n- Lace wallet is the auth + tx layer. `window.midnight` is polled; the shielded address is the identity.\n- A locally-run proof server (Docker port 6300) is REQUIRED for any tx submit; the UI must show a\n  \"Proving… this can take 30–120s\" state and stay usable while proofs generate.\n- Pinata / IPFS only if the idea genuinely stores a file or artefact — then the CID is committed on-chain.\n- At most ONE AI call per user action (Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (index route only).\n- Midnight preview testnet. Compact language 0.23. MidnightJS SDK 4.1.1.\n- Lace wallet is the sole auth surface — no Privy, no MetaMask, no OAuth.\n- Local proof server (Docker port 6300) does all ZK proving. The UI shows Proving state.\n- No SSR. All MidnightJS imports live behind `<ClientOnly>` + `useEffect`.\n\nPACKAGES (all pinned to the versions Midnight ships together):\n- @midnight-ntwrk/dapp-connector-api@4.0.1\n- @midnight-ntwrk/midnight-js-contracts@4.1.1\n- @midnight-ntwrk/midnight-js-types@4.1.1\n- @midnight-ntwrk/midnight-js-protocol@4.1.1\n- @midnight-ntwrk/midnight-js-network-id@4.1.1\n- @midnight-ntwrk/midnight-js-fetch-zk-config-provider@4.1.1\n- @midnight-ntwrk/midnight-js-http-client-proof-provider@4.1.1\n- @midnight-ntwrk/midnight-js-indexer-public-data-provider@4.1.1\n- @midnight-ntwrk/midnight-js-utils@4.1.1\n- @midnight-ntwrk/compact-runtime@0.16.0\n- rxjs fp-ts semver buffer pino\n- vite-plugin-wasm  vite-plugin-top-level-await  (dev)\n\nCOMPACT TOOLCHAIN (one-time setup — the human runs this in a terminal, not Lovable):\n```bash\ncurl --proto '=https' --tlsv1.2 -LsSf \\\n  https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh\nsource ~/.bashrc && compact update\ncompact compile contracts/YourContract.compact contracts/managed/your-contract\ncp -r contracts/managed/your-contract/keys public/keys\ncp -r contracts/managed/your-contract/zkir public/zkir\ndocker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v\n```\n\nVITE CONFIG (vite.config.ts) — WASM + top-level await are MANDATORY for MidnightJS:\n```ts\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport wasm from 'vite-plugin-wasm';\nimport topLevelAwait from 'vite-plugin-top-level-await';\nexport default defineConfig({\n  build: { target: 'esnext', commonjsOptions: { transformMixedEsModules: true, extensions: ['.js','.cjs'] } },\n  plugins: [react(), wasm(), topLevelAwait()],\n  optimizeDeps: {\n    esbuildOptions: { target: 'esnext', supported: { 'top-level-await': true } },\n    include: ['@midnight-ntwrk/compact-runtime'],\n    exclude: ['@midnight-ntwrk/onchain-runtime-v3',\n              '@midnight-ntwrk/onchain-runtime-v3/midnight_onchain_runtime_wasm_bg.wasm'],\n  },\n});\n```\n\nSSR RULE: never import a `@midnight-ntwrk/*` package at module scope of a route file — it uses\nNode Buffer + browser globals + WASM top-level await and crashes SSR. Load providers behind\n`useEffect` or a dynamic `import()` inside a `<ClientOnly>` boundary.\n\nWALLET DETECT (src/lib/lace.ts) — poll window.midnight up to 5s:\n```ts\nimport type { InitialAPI } from '@midnight-ntwrk/dapp-connector-api';\nimport semver from 'semver';\nexport async function waitForLace(timeoutMs = 5000): Promise<InitialAPI> {\n  return new Promise((resolve, reject) => {\n    const start = Date.now();\n    const t = setInterval(() => {\n      const m = (window as any).midnight ?? {};\n      const w = Object.values(m).find((x: any) =>\n        x && typeof x === 'object' && 'apiVersion' in x &&\n        semver.satisfies(x.apiVersion, '4.x')) as InitialAPI | undefined;\n      if (w) { clearInterval(t); resolve(w); return; }\n      if (Date.now() - start > timeoutMs) { clearInterval(t);\n        reject(new Error('Lace Midnight wallet not found. Install it: https://www.lace.io/')); }\n    }, 100);\n  });\n}\n```\n\nBUFFER POLYFILL (src/main.tsx, MUST be the very first line):\n```ts\nimport { Buffer } from 'buffer'; (globalThis as any).Buffer = Buffer;\n```\n\nPROVIDERS (src/lib/providers.ts) — chain Lace + proof server + indexer:\n```ts\nimport { setNetworkId } from '@midnight-ntwrk/midnight-js-network-id';\nimport { FetchZkConfigProvider } from '@midnight-ntwrk/midnight-js-fetch-zk-config-provider';\nimport { httpClientProofProvider } from '@midnight-ntwrk/midnight-js-http-client-proof-provider';\nimport { indexerPublicDataProvider } from '@midnight-ntwrk/midnight-js-indexer-public-data-provider';\nimport { waitForLace } from './lace';\n\nexport async function initProviders() {\n  setNetworkId(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const lace = await waitForLace();\n  const connectedAPI = await lace.connect(import.meta.env.VITE_NETWORK_ID ?? 'preview');\n  const cfg = await connectedAPI.getConfiguration();\n  const zk = new FetchZkConfigProvider(window.location.origin, fetch.bind(window));\n  return {\n    connectedAPI,\n    zkConfigProvider: zk,\n    proofProvider: httpClientProofProvider(cfg.proverServerUri ?? import.meta.env.VITE_PROOF_SERVER_URL, zk),\n    publicDataProvider: indexerPublicDataProvider(cfg.indexerUri, cfg.indexerWsUri),\n  };\n}\n```\n\nREAD-ONLY LEDGER FETCH (no wallet needed — great for public feeds):\n```ts\nconst INDEXER = import.meta.env.VITE_INDEXER_URL;\nexport async function readLedger(address: string) {\n  const r = await fetch(INDEXER, {\n    method: 'POST', headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      query: `query($a:HexEncoded!){ contractAction(address:$a){ state } }`,\n      variables: { address },\n    }),\n  });\n  return (await r.json()).data?.contractAction?.state as string | null;\n}\n```\n\nCONTRACT\n```compact\n// contracts/ProvenancePalette.compact\n// Digitize and mint physical palette images as NFTs preserving unique artist color mixes.  (private-witness pattern)\n// Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\npragma language_version 0.23;\n\nimport CompactStandardLibrary;\n\nexport ledger enrolled_commitments: Set<Bytes<32>>;   // public: which commitments are valid\nexport ledger last_action_count: Counter;\n\nwitness localSecretKey(): Bytes<32>;\n\nconstructor() { last_action_count.increment(1); }\n\nexport circuit enroll(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  enrolled_commitments.insert(disclose(c));            // sk stays local; only c goes public\n}\n\nexport circuit proveOwnership(commitmentTag: Bytes<32>): [] {\n  const sk = localSecretKey();\n  const c = persistentHash<Vector<2, Bytes<32>>>([commitmentTag, sk]);\n  assert(enrolled_commitments.member(c), \"not enrolled\");\n  last_action_count.increment(1);\n}\n```\n\nFRONTEND FLOW — the secret NEVER leaves the browser\n1. On first visit, generate a 32-byte secret with `crypto.getRandomValues(new Uint8Array(32))`\n   and persist it base64-encoded in localStorage. Warn users that clearing storage revokes proofs.\n2. The witness callback returns that secret to the Compact circuit — it is used inside the ZK proof\n   but never appears in the transaction that goes on chain.\n3. \"Enroll\" action → `enroll(commitmentTag)` — commits `hash(tag, sk)` on chain.\n4. \"Prove physical palette digitization\" action → `proveOwnership(commitmentTag)` — the circuit asserts the commitment\n   is in `enrolled_commitments` for THIS wallet's secret. UI shows a green \"proved without revealing\" chip.\n5. Anyone reading the Indexer sees only commitments and a bumped counter — they cannot link\n   two proofs to the same user because the secret never appeared on chain.\n\nRED FLAGS — DO NOT ATTEMPT:\n- No bridging to Ethereum / any EVM chain. Midnight is a standalone L1; there is no bridge.\n- No oracle / external HTTP data inside a circuit. Circuits are bounded and cannot do I/O.\n- No sub-second finality UX. Proofs for k=14 circuits take 30–120s — build for that latency.\n- No recursion in Compact. Loops must be bounded by compile-time constants.\n- No SSR. MidnightJS uses browser globals + WASM + top-level-await; ClientOnly is mandatory.\n\nREQUIRED SECRETS (Lovable → Project Settings → Secrets):\n- VITE_NETWORK_ID           preview   (Midnight preview testnet; set to `preprod` for the more stable network)\n- VITE_INDEXER_URL          https://indexer.preview.midnight.network/api/v4/graphql\n- VITE_INDEXER_WS_URL       wss://indexer.preview.midnight.network/api/v4/graphql/ws\n- VITE_PROOF_SERVER_URL     http://localhost:6300   (run `docker run -p 6300:6300 midnightntwrk/proof-server:latest midnight-proof-server -v`)\n- VITE_DEFAULT_CONTRACT     the hex address printed by your first deploy — paste it here so users skip the deploy step\n\nFaucet:  https://cloud.google.com/application/web3/faucet/midnight/testnet  (tDUST — pays proof / balance fees)\nExplorer: https://preview.midnightexplorer.com/\nDocs:    https://docs.midnight.network/llms-full.txt\n\nCREDIT (must appear in UI footer AND as a header comment on every Compact contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$50M — art collector memorabilia",
      "som": "$1.2M — NFT palette collector sales"
    }
  ]
}