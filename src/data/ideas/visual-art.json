{
  "theme": {
    "slug": "visual-art",
    "name": "Visual Art",
    "emoji": "🎨",
    "audience": "painters, illustrators, generative artists, gallerists",
    "market_anchor": "the global art market (~$65B; >300K working visual artists)"
  },
  "ideas": [
    {
      "id": "visual-art-provenance-palette-0",
      "theme": "visual-art",
      "title": "Provenance Palette",
      "pitch": "Securely track and prove the ownership lineage of paintings with tamper-proof blockchain records.",
      "subDiscipline": "art ownership tracking",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts provide immutable provenance records verified on-chain.",
      "megaPrompt": "Build \"Provenance Palette\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecurely track and prove the ownership lineage of paintings with tamper-proof blockchain records.\nDiscipline: Visual Art (art ownership tracking).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts provide immutable provenance records verified on-chain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ProvenancePalette.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ProvenancePalette\n/// @notice Securely track and prove the ownership lineage of paintings with tamper-proof blockchain records.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ProvenancePalette {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a art ownership tracking action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global art market needing ownership verification",
      "sam": "$10B — marketplace for authenticated paintings worldwide",
      "som": "$500M — early adopters using blockchain provenance solutions"
    },
    {
      "id": "visual-art-generative-mint-studio-1",
      "theme": "visual-art",
      "title": "Generative Mint Studio",
      "pitch": "Artists can mint unique generative art pieces directly onto Ethereum Sepolia with automated smart contracts.",
      "subDiscipline": "generative art minting",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts enable onchain minting with verifiable randomness and ownership.",
      "megaPrompt": "Build \"Generative Mint Studio\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nArtists can mint unique generative art pieces directly onto Ethereum Sepolia with automated smart contracts.\nDiscipline: Visual Art (generative art minting).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts enable onchain minting with verifiable randomness and ownership.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GenerativeMintStudio.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title GenerativeMintStudio\n/// @notice Artists can mint unique generative art pieces directly onto Ethereum Sepolia with automated smart contracts.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract GenerativeMintStudio {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a generative art minting action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — blockchain-based generative art market",
      "sam": "$150M — active generative artists minting NFTs",
      "som": "$20M — users adopting Sepolia for minting"
    },
    {
      "id": "visual-art-exhibit-chain-ledger-2",
      "theme": "visual-art",
      "title": "Exhibit Chain Ledger",
      "pitch": "Record every artwork's exhibition history securely and transparently onchain for galleries and collectors.",
      "subDiscipline": "gallery exhibition tracking",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts provide decentralized, timestamped exhibition records.",
      "megaPrompt": "Build \"Exhibit Chain Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nRecord every artwork's exhibition history securely and transparently onchain for galleries and collectors.\nDiscipline: Visual Art (gallery exhibition tracking).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts provide decentralized, timestamped exhibition records.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ExhibitChainLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ExhibitChainLedger\n/// @notice Record every artwork's exhibition history securely and transparently onchain for galleries and collectors.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ExhibitChainLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a gallery exhibition tracking action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global gallery and exhibition market",
      "sam": "$1B — art galleries integrating digital tracking",
      "som": "$100M — digital provenance adopters in galleries"
    },
    {
      "id": "visual-art-colorcode-certifier-3",
      "theme": "visual-art",
      "title": "ColorCode Certifier",
      "pitch": "Certify and verify exact color compositions of artworks using blockchain color code hashes.",
      "subDiscipline": "color authenticity validation",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts guarantee immutable storage of color code data linked to artist hashes.",
      "megaPrompt": "Build \"ColorCode Certifier\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCertify and verify exact color compositions of artworks using blockchain color code hashes.\nDiscipline: Visual Art (color authenticity validation).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts guarantee immutable storage of color code data linked to artist hashes.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ColorCodeCertifier.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ColorCodeCertifier\n/// @notice Certify and verify exact color compositions of artworks using blockchain color code hashes.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ColorCodeCertifier {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a color authenticity validation action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — art market reliant on color fidelity",
      "sam": "$500M — artists and collectors valuing color authenticity",
      "som": "$30M — initial blockchain-based color certification users"
    },
    {
      "id": "visual-art-auction-trust-chain-4",
      "theme": "visual-art",
      "title": "Auction Trust Chain",
      "pitch": "Enable transparent, verifiable auction processes and bids for visual art onchain.",
      "subDiscipline": "art auction transparency",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts enforce fair bid logic and record bids immutably.",
      "megaPrompt": "Build \"Auction Trust Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnable transparent, verifiable auction processes and bids for visual art onchain.\nDiscipline: Visual Art (art auction transparency).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts enforce fair bid logic and record bids immutably.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/AuctionTrustChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title AuctionTrustChain\n/// @notice Enable transparent, verifiable auction processes and bids for visual art onchain.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract AuctionTrustChain {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a art auction transparency action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — global art auction market volume",
      "sam": "$3B — online art auction platforms",
      "som": "$250M — blockchain auction services adoption"
    },
    {
      "id": "visual-art-brushstroke-timestamp-5",
      "theme": "visual-art",
      "title": "Brushstroke Timestamp",
      "pitch": "Document and timestamp artists’ brushstroke progress live onchain for proof of creation.",
      "subDiscipline": "creative process timestamping",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts provide reliable, immutable time-stamped events during creation.",
      "megaPrompt": "Build \"Brushstroke Timestamp\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nDocument and timestamp artists’ brushstroke progress live onchain for proof of creation.\nDiscipline: Visual Art (creative process timestamping).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts provide reliable, immutable time-stamped events during creation.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/BrushstrokeTimestamp.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title BrushstrokeTimestamp\n/// @notice Document and timestamp artists’ brushstroke progress live onchain for proof of creation.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract BrushstrokeTimestamp {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a creative process timestamping action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — art market valuing originality proof",
      "sam": "$200M — artists interested in process authentication",
      "som": "$10M — early userbase for onchain creation logs"
    },
    {
      "id": "visual-art-licensing-ledger-6",
      "theme": "visual-art",
      "title": "Licensing Ledger",
      "pitch": "Manage and enforce artwork licensing rights transparently with blockchain smart contracts.",
      "subDiscipline": "artwork license management",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts automate license terms and enforce royalties securely onchain.",
      "megaPrompt": "Build \"Licensing Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nManage and enforce artwork licensing rights transparently with blockchain smart contracts.\nDiscipline: Visual Art (artwork license management).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts automate license terms and enforce royalties securely onchain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/LicensingLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title LicensingLedger\n/// @notice Manage and enforce artwork licensing rights transparently with blockchain smart contracts.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract LicensingLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a artwork license management action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global art licensing market",
      "sam": "$500M — digital art licensing platforms",
      "som": "$50M — blockchain-based licensing solutions"
    },
    {
      "id": "visual-art-interactive-canvas-dao-7",
      "theme": "visual-art",
      "title": "Interactive Canvas DAO",
      "pitch": "Enable collective decision-making on collaborative visual art projects via decentralized onchain voting.",
      "subDiscipline": "collaborative art governance",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts support secure DAO governance with transparent vote tallying.",
      "megaPrompt": "Build \"Interactive Canvas DAO\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnable collective decision-making on collaborative visual art projects via decentralized onchain voting.\nDiscipline: Visual Art (collaborative art governance).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts support secure DAO governance with transparent vote tallying.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/InteractiveCanvasDAO.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title InteractiveCanvasDAO\n/// @notice Enable collective decision-making on collaborative visual art projects via decentralized onchain voting.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract InteractiveCanvasDAO {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a collaborative art governance action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$100M — collaborative digital art initiatives",
      "sam": "$30M — artists using decentralized governance tools",
      "som": "$5M — DAO experiment adopters in art"
    },
    {
      "id": "visual-art-art-swap-protocol-8",
      "theme": "visual-art",
      "title": "Art Swap Protocol",
      "pitch": "Create a trustless platform for artists and collectors to swap artworks instantly onchain.",
      "subDiscipline": "peer-to-peer art exchange",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts enable atomic swaps guaranteeing secure, simultaneous asset exchanges.",
      "megaPrompt": "Build \"Art Swap Protocol\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate a trustless platform for artists and collectors to swap artworks instantly onchain.\nDiscipline: Visual Art (peer-to-peer art exchange).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts enable atomic swaps guaranteeing secure, simultaneous asset exchanges.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ArtSwapProtocol.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ArtSwapProtocol\n/// @notice Create a trustless platform for artists and collectors to swap artworks instantly onchain.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ArtSwapProtocol {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a peer-to-peer art exchange action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — peer-to-peer art trading market",
      "sam": "$200M — active visual art traders",
      "som": "$25M — early users adopting blockchain swaps"
    },
    {
      "id": "visual-art-edition-tracker-9",
      "theme": "visual-art",
      "title": "Edition Tracker",
      "pitch": "Track edition numbers and print runs of limited artworks securely and transparently onchain.",
      "subDiscipline": "limited print tracking",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts provide immutable tracking of edition metadata and supply limits.",
      "megaPrompt": "Build \"Edition Tracker\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTrack edition numbers and print runs of limited artworks securely and transparently onchain.\nDiscipline: Visual Art (limited print tracking).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts provide immutable tracking of edition metadata and supply limits.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/EditionTracker.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title EditionTracker\n/// @notice Track edition numbers and print runs of limited artworks securely and transparently onchain.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract EditionTracker {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a limited print tracking action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — limited edition print market",
      "sam": "$1.2B — collectors focusing on print authenticity",
      "som": "$80M — users transitioning to blockchain edition tracking"
    },
    {
      "id": "visual-art-art-commission-chain-10",
      "theme": "visual-art",
      "title": "Art Commission Chain",
      "pitch": "Streamline and secure commission contracts and payments between artists and patrons using smart contracts.",
      "subDiscipline": "commission workflow management",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts automate escrow and milestone payments trustlessly.",
      "megaPrompt": "Build \"Art Commission Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nStreamline and secure commission contracts and payments between artists and patrons using smart contracts.\nDiscipline: Visual Art (commission workflow management).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts automate escrow and milestone payments trustlessly.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ArtCommissionChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ArtCommissionChain\n/// @notice Streamline and secure commission contracts and payments between artists and patrons using smart contracts.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ArtCommissionChain {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a commission workflow management action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global art commission market",
      "sam": "$800M — platforms facilitating digital commissions",
      "som": "$60M — early blockchain adoption for commissions"
    },
    {
      "id": "visual-art-dynamic-frame-nfts-11",
      "theme": "visual-art",
      "title": "Dynamic Frame NFTs",
      "pitch": "Create NFTs that change frames or borders based on onchain conditions or events.",
      "subDiscipline": "programmable artwork framing",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts enable dynamic metadata updates triggering visual changes in real-time.",
      "megaPrompt": "Build \"Dynamic Frame NFTs\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate NFTs that change frames or borders based on onchain conditions or events.\nDiscipline: Visual Art (programmable artwork framing).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts enable dynamic metadata updates triggering visual changes in real-time.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DynamicFrameNFTs.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title DynamicFrameNFTs\n/// @notice Create NFTs that change frames or borders based on onchain conditions or events.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract DynamicFrameNFTs {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a programmable artwork framing action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — programmable art NFT market",
      "sam": "$150M — collectors seeking interactive art experiences",
      "som": "$20M — initial adopters of dynamic NFTs"
    },
    {
      "id": "visual-art-gallery-access-token-12",
      "theme": "visual-art",
      "title": "Gallery Access Token",
      "pitch": "Issue access tokens for exclusive gallery events and exhibitions secured on blockchain.",
      "subDiscipline": "event admission tokens",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts provide verifiable, non-transferable access control tokens onchain.",
      "megaPrompt": "Build \"Gallery Access Token\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nIssue access tokens for exclusive gallery events and exhibitions secured on blockchain.\nDiscipline: Visual Art (event admission tokens).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts provide verifiable, non-transferable access control tokens onchain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GalleryAccessToken.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title GalleryAccessToken\n/// @notice Issue access tokens for exclusive gallery events and exhibitions secured on blockchain.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract GalleryAccessToken {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a event admission tokens action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global gallery event ticket market",
      "sam": "$600M — digital ticketing for art events",
      "som": "$40M — blockchain-enabled ticket users"
    },
    {
      "id": "visual-art-art-critique-chain-13",
      "theme": "visual-art",
      "title": "Art Critique Chain",
      "pitch": "Record artist peer critiques and feedback permanently and transparently onchain.",
      "subDiscipline": "peer review logging",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts ensure immutable and censorship-resistant critique logs.",
      "megaPrompt": "Build \"Art Critique Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nRecord artist peer critiques and feedback permanently and transparently onchain.\nDiscipline: Visual Art (peer review logging).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts ensure immutable and censorship-resistant critique logs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ArtCritiqueChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ArtCritiqueChain\n/// @notice Record artist peer critiques and feedback permanently and transparently onchain.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ArtCritiqueChain {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a peer review logging action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — peer review platforms globally",
      "sam": "$120M — digital visual art communities",
      "som": "$10M — blockchain critique tool users"
    },
    {
      "id": "visual-art-signature-stamp-14",
      "theme": "visual-art",
      "title": "Signature Stamp",
      "pitch": "Cryptographically verify artist signatures on digital and physical artworks using blockchain stamps.",
      "subDiscipline": "artist signature authentication",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts store immutable signature proofs linked to artworks.",
      "megaPrompt": "Build \"Signature Stamp\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCryptographically verify artist signatures on digital and physical artworks using blockchain stamps.\nDiscipline: Visual Art (artist signature authentication).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts store immutable signature proofs linked to artworks.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SignatureStamp.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SignatureStamp\n/// @notice Cryptographically verify artist signatures on digital and physical artworks using blockchain stamps.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SignatureStamp {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a artist signature authentication action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — art authentication services",
      "sam": "$2B — signature verification market",
      "som": "$100M — blockchain adoption for signature validation"
    },
    {
      "id": "visual-art-interactive-auction-game-15",
      "theme": "visual-art",
      "title": "Interactive Auction Game",
      "pitch": "Turn art auctions into engaging games with transparent rules enforced by onchain smart contracts.",
      "subDiscipline": "gamified art auctions",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts enable trustless, rule-based game mechanics for auctions.",
      "megaPrompt": "Build \"Interactive Auction Game\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTurn art auctions into engaging games with transparent rules enforced by onchain smart contracts.\nDiscipline: Visual Art (gamified art auctions).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts enable trustless, rule-based game mechanics for auctions.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/InteractiveAuctionGame.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title InteractiveAuctionGame\n/// @notice Turn art auctions into engaging games with transparent rules enforced by onchain smart contracts.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract InteractiveAuctionGame {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a gamified art auctions action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — art auction global market",
      "sam": "$500M — gamified auction platforms",
      "som": "$40M — users engaging in blockchain auctions"
    },
    {
      "id": "visual-art-material-usage-ledger-16",
      "theme": "visual-art",
      "title": "Material Usage Ledger",
      "pitch": "Track and verify the ethical sourcing and usage of materials in visual artworks onchain.",
      "subDiscipline": "supply chain transparency",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts provide immutable supply chain transparency for art materials.",
      "megaPrompt": "Build \"Material Usage Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTrack and verify the ethical sourcing and usage of materials in visual artworks onchain.\nDiscipline: Visual Art (supply chain transparency).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts provide immutable supply chain transparency for art materials.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MaterialUsageLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title MaterialUsageLedger\n/// @notice Track and verify the ethical sourcing and usage of materials in visual artworks onchain.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract MaterialUsageLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a supply chain transparency action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — ethical art material market",
      "sam": "$700M — collectors prioritizing ethical sourcing",
      "som": "$30M — blockchain material traceability adopters"
    },
    {
      "id": "visual-art-visual-royalties-manager-17",
      "theme": "visual-art",
      "title": "Visual Royalties Manager",
      "pitch": "Automate royalty payments to artists via smart contracts every time artwork changes hands.",
      "subDiscipline": "artist royalty automation",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts enforce automatic royalty splits transparently and reliably.",
      "megaPrompt": "Build \"Visual Royalties Manager\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAutomate royalty payments to artists via smart contracts every time artwork changes hands.\nDiscipline: Visual Art (artist royalty automation).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts enforce automatic royalty splits transparently and reliably.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/VisualRoyaltiesManager.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title VisualRoyaltiesManager\n/// @notice Automate royalty payments to artists via smart contracts every time artwork changes hands.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract VisualRoyaltiesManager {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a artist royalty automation action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — global art royalty market",
      "sam": "$400M — digital art sales platforms",
      "som": "$50M — blockchain royalty users"
    },
    {
      "id": "visual-art-moodboard-dao-18",
      "theme": "visual-art",
      "title": "MoodBoard DAO",
      "pitch": "Artists collaboratively curate and govern mood boards for projects using decentralized governance.",
      "subDiscipline": "collective inspiration curation",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts facilitate transparent proposal voting and curation rewards.",
      "megaPrompt": "Build \"MoodBoard DAO\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nArtists collaboratively curate and govern mood boards for projects using decentralized governance.\nDiscipline: Visual Art (collective inspiration curation).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts facilitate transparent proposal voting and curation rewards.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MoodBoardDAO.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title MoodBoardDAO\n/// @notice Artists collaboratively curate and govern mood boards for projects using decentralized governance.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract MoodBoardDAO {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a collective inspiration curation action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$120M — collaborative art projects",
      "sam": "$25M — digital collaboration tools for artists",
      "som": "$3M — DAO tools adoption in art"
    },
    {
      "id": "visual-art-sketch-snapshot-chain-19",
      "theme": "visual-art",
      "title": "Sketch Snapshot Chain",
      "pitch": "Instantly archive and timestamp digital sketches onchain for secure creative journaling.",
      "subDiscipline": "digital sketch archiving",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts provide immutable and decentralized snapshot storage proofs.",
      "megaPrompt": "Build \"Sketch Snapshot Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nInstantly archive and timestamp digital sketches onchain for secure creative journaling.\nDiscipline: Visual Art (digital sketch archiving).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts provide immutable and decentralized snapshot storage proofs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SketchSnapshotChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SketchSnapshotChain\n/// @notice Instantly archive and timestamp digital sketches onchain for secure creative journaling.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SketchSnapshotChain {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a digital sketch archiving action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — digital art tools market",
      "sam": "$200M — digital sketch app users",
      "som": "$15M — early blockchain integration adopters"
    },
    {
      "id": "visual-art-canvas-rental-token-20",
      "theme": "visual-art",
      "title": "Canvas Rental Token",
      "pitch": "Tokenize and manage temporary rental of studio or gallery spaces for artists seamlessly onchain.",
      "subDiscipline": "art space access",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts enable secure timed access with programmable expiration and transfers.",
      "megaPrompt": "Build \"Canvas Rental Token\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTokenize and manage temporary rental of studio or gallery spaces for artists seamlessly onchain.\nDiscipline: Visual Art (art space access).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts enable secure timed access with programmable expiration and transfers.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CanvasRentalToken.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CanvasRentalToken\n/// @notice Tokenize and manage temporary rental of studio or gallery spaces for artists seamlessly onchain.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CanvasRentalToken {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a art space access action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — global artist workspace market",
      "sam": "$120M — short term art space rentals",
      "som": "$8M — blockchain-based rental platform users"
    },
    {
      "id": "visual-art-nft-curation-index-21",
      "theme": "visual-art",
      "title": "NFT Curation Index",
      "pitch": "Create and manage curated NFT visual art collections with transparent onchain voting and tracking.",
      "subDiscipline": "curated NFT collections",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts enable decentralized curation and provenance validation.",
      "megaPrompt": "Build \"NFT Curation Index\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate and manage curated NFT visual art collections with transparent onchain voting and tracking.\nDiscipline: Visual Art (curated NFT collections).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts enable decentralized curation and provenance validation.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/NFTCurationIndex.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title NFTCurationIndex\n/// @notice Create and manage curated NFT visual art collections with transparent onchain voting and tracking.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract NFTCurationIndex {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a curated NFT collections action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — NFT art collectible market",
      "sam": "$600M — active curators and collectors",
      "som": "$50M — users onchain curation platforms"
    },
    {
      "id": "visual-art-brushstroke-dao-vault-22",
      "theme": "visual-art",
      "title": "Brushstroke DAO Vault",
      "pitch": "Pool resources in decentralized vaults to fund promising visual art projects transparently.",
      "subDiscipline": "collective art funding",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts allow secure fund management and voting by contributors.",
      "megaPrompt": "Build \"Brushstroke DAO Vault\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPool resources in decentralized vaults to fund promising visual art projects transparently.\nDiscipline: Visual Art (collective art funding).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts allow secure fund management and voting by contributors.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/BrushstrokeDAOVault.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title BrushstrokeDAOVault\n/// @notice Pool resources in decentralized vaults to fund promising visual art projects transparently.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract BrushstrokeDAOVault {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a collective art funding action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — art crowd-funding market",
      "sam": "$150M — decentralized funding adopters",
      "som": "$12M — early DAO art fund users"
    },
    {
      "id": "visual-art-art-swap-escrow-23",
      "theme": "visual-art",
      "title": "Art Swap Escrow",
      "pitch": "Allow artists and collectors to trade artworks using blockchain escrow ensuring transaction safety.",
      "subDiscipline": "secure art trades",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia contracts provide trustless escrow with automatic release upon conditions met.",
      "megaPrompt": "Build \"Art Swap Escrow\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAllow artists and collectors to trade artworks using blockchain escrow ensuring transaction safety.\nDiscipline: Visual Art (secure art trades).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia contracts provide trustless escrow with automatic release upon conditions met.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ArtSwapEscrow.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title ArtSwapEscrow\n/// @notice Allow artists and collectors to trade artworks using blockchain escrow ensuring transaction safety.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ArtSwapEscrow {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a secure art trades action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — art trading market",
      "sam": "$300M — online art trade platforms",
      "som": "$30M — blockchain escrow adopters in art"
    },
    {
      "id": "visual-art-visual-art-rewards-24",
      "theme": "visual-art",
      "title": "Visual Art Rewards",
      "pitch": "Reward artists and illustrators for community engagement or milestones via onchain token incentives.",
      "subDiscipline": "artist incentivization",
      "quantumHook": "Sepolia smart contract",
      "quantumHookId": "sepolia-deploy",
      "quantumTag": "onchain logic",
      "quantumRationale": "Sepolia smart contracts manage transparent and automated reward distributions.",
      "megaPrompt": "Build \"Visual Art Rewards\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nReward artists and illustrators for community engagement or milestones via onchain token incentives.\nDiscipline: Visual Art (artist incentivization).\nOnchain primitive: Sepolia smart contract. Why this primitive: Sepolia smart contracts manage transparent and automated reward distributions.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/VisualArtRewards.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title VisualArtRewards\n/// @notice Reward artists and illustrators for community engagement or milestones via onchain token incentives.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract VisualArtRewards {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. User performs a artist incentivization action; the app calls `log(payload)` on the contract via Privy sponsored tx and shows the Etherscan link as proof.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$200M — artist monetization platforms",
      "sam": "$50M — community-driven artist platforms",
      "som": "$5M — initial blockchain reward users"
    },
    {
      "id": "visual-art-permacolor-archive-0",
      "theme": "visual-art",
      "title": "PermaColor Archive",
      "pitch": "Secure and share your unique color palettes as permanent IPFS references.",
      "subDiscipline": "color palette curation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata JWT ensures immutable storage and easy retrieval of palette metadata on IPFS.",
      "megaPrompt": "Build \"PermaColor Archive\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecure and share your unique color palettes as permanent IPFS references.\nDiscipline: Visual Art (color palette curation).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata JWT ensures immutable storage and easy retrieval of palette metadata on IPFS.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PermaColorArchive.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogPermaColorArchive\n/// @notice Secure and share your unique color palettes as permanent IPFS references.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogPermaColorArchive {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the color palette curation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global digital artist tools market",
      "sam": "$250M — color-centric art resource platforms",
      "som": "$50M — paid subscriptions for curated palette archives"
    },
    {
      "id": "visual-art-generative-art-vault-1",
      "theme": "visual-art",
      "title": "Generative Art Vault",
      "pitch": "Pin generative art scripts and outputs permanently to preserve creative provenance.",
      "subDiscipline": "algorithmic artwork storage",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "IPFS via Pinata guarantees unalterable storage of code and images with CID verification.",
      "megaPrompt": "Build \"Generative Art Vault\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin generative art scripts and outputs permanently to preserve creative provenance.\nDiscipline: Visual Art (algorithmic artwork storage).\nOnchain primitive: IPFS via Pinata. Why this primitive: IPFS via Pinata guarantees unalterable storage of code and images with CID verification.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GenerativeArtVault.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogGenerativeArtVault\n/// @notice Pin generative art scripts and outputs permanently to preserve creative provenance.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogGenerativeArtVault {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the algorithmic artwork storage artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global generative art market",
      "sam": "$700M — platforms hosting generative art",
      "som": "$150M — premium archival and licensing services"
    },
    {
      "id": "visual-art-gallery-manifest-hub-2",
      "theme": "visual-art",
      "title": "Gallery Manifest Hub",
      "pitch": "Create and pin permanent digital manifests for art gallery exhibitions with immutable records.",
      "subDiscipline": "exhibition cataloging",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata enables reliable, permanent IPFS pinning of JSON manifests for galleries’ provenance needs.",
      "megaPrompt": "Build \"Gallery Manifest Hub\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate and pin permanent digital manifests for art gallery exhibitions with immutable records.\nDiscipline: Visual Art (exhibition cataloging).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata enables reliable, permanent IPFS pinning of JSON manifests for galleries’ provenance needs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GalleryManifestHub.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogGalleryManifestHub\n/// @notice Create and pin permanent digital manifests for art gallery exhibitions with immutable records.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogGalleryManifestHub {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the exhibition cataloging artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — global gallery management software",
      "sam": "$1.2B — digital exhibition catalog platforms",
      "som": "$300M — galleries adopting blockchain provenance"
    },
    {
      "id": "visual-art-illustrator-portfolio-lock-3",
      "theme": "visual-art",
      "title": "Illustrator Portfolio Lock",
      "pitch": "Permanently store and share your illustrator portfolios with verifiable IPFS links.",
      "subDiscipline": "portfolio archiving",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata JWT simplifies secure uploading and pinning with permanent CID references.",
      "megaPrompt": "Build \"Illustrator Portfolio Lock\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPermanently store and share your illustrator portfolios with verifiable IPFS links.\nDiscipline: Visual Art (portfolio archiving).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata JWT simplifies secure uploading and pinning with permanent CID references.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/IllustratorPortfolioLock.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogIllustratorPortfolioLock\n/// @notice Permanently store and share your illustrator portfolios with verifiable IPFS links.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogIllustratorPortfolioLock {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the portfolio archiving artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — digital portfolio platforms",
      "sam": "$400M — illustrator-specific portfolio tools",
      "som": "$80M — paid portfolio permanence services"
    },
    {
      "id": "visual-art-digital-canvas-ledger-4",
      "theme": "visual-art",
      "title": "Digital Canvas Ledger",
      "pitch": "Record and pin painted artwork metadata to IPFS for permanent, tamper-proof provenance.",
      "subDiscipline": "painting provenance tracking",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata’s IPFS pinning ensures metadata immutability and easy retrieval for provenance.",
      "megaPrompt": "Build \"Digital Canvas Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nRecord and pin painted artwork metadata to IPFS for permanent, tamper-proof provenance.\nDiscipline: Visual Art (painting provenance tracking).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata’s IPFS pinning ensures metadata immutability and easy retrieval for provenance.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DigitalCanvasLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogDigitalCanvasLedger\n/// @notice Record and pin painted artwork metadata to IPFS for permanent, tamper-proof provenance.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogDigitalCanvasLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the painting provenance tracking artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — global art provenance solutions",
      "sam": "$2.7B — provenance platforms for painters",
      "som": "$500M — subscription provenance verification"
    },
    {
      "id": "visual-art-nft-manifest-generator-5",
      "theme": "visual-art",
      "title": "NFT Manifest Generator",
      "pitch": "Bundle images and metadata into permanent IPFS manifests to guarantee NFT authenticity.",
      "subDiscipline": "digital asset packaging",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata JWT provides seamless and permanent IPFS pinning ideal for NFT data integrity.",
      "megaPrompt": "Build \"NFT Manifest Generator\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nBundle images and metadata into permanent IPFS manifests to guarantee NFT authenticity.\nDiscipline: Visual Art (digital asset packaging).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata JWT provides seamless and permanent IPFS pinning ideal for NFT data integrity.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/NFTManifestGenerator.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogNFTManifestGenerator\n/// @notice Bundle images and metadata into permanent IPFS manifests to guarantee NFT authenticity.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogNFTManifestGenerator {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the digital asset packaging artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$7B — NFT marketplace infrastructure",
      "sam": "$1.6B — NFT metadata management tools",
      "som": "$350M — NFT authenticity services"
    },
    {
      "id": "visual-art-interactive-art-pinning-6",
      "theme": "visual-art",
      "title": "Interactive Art Pinning",
      "pitch": "Pin interactive art assets as JSON and media files, preserving experience forever on IPFS.",
      "subDiscipline": "multimedia art preservation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata’s JWT upload supports complex JSON and media pinning for interaction fidelity.",
      "megaPrompt": "Build \"Interactive Art Pinning\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin interactive art assets as JSON and media files, preserving experience forever on IPFS.\nDiscipline: Visual Art (multimedia art preservation).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata’s JWT upload supports complex JSON and media pinning for interaction fidelity.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/InteractiveArtPinning.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogInteractiveArtPinning\n/// @notice Pin interactive art assets as JSON and media files, preserving experience forever on IPFS.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogInteractiveArtPinning {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the multimedia art preservation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$800M — multimedia art platforms",
      "sam": "$200M — interactive digital art tools",
      "som": "$45M — premium preservation subscriptions"
    },
    {
      "id": "visual-art-exhibit-provenance-chain-7",
      "theme": "visual-art",
      "title": "Exhibit Provenance Chain",
      "pitch": "Pin exhibit documents and artwork proofs to guarantee authenticity for global exhibitions.",
      "subDiscipline": "art exposition verification",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata IPFS storage offers permanent, censorship-resistant verification of exhibit data.",
      "megaPrompt": "Build \"Exhibit Provenance Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin exhibit documents and artwork proofs to guarantee authenticity for global exhibitions.\nDiscipline: Visual Art (art exposition verification).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata IPFS storage offers permanent, censorship-resistant verification of exhibit data.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ExhibitProvenanceChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogExhibitProvenanceChain\n/// @notice Pin exhibit documents and artwork proofs to guarantee authenticity for global exhibitions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogExhibitProvenanceChain {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the art exposition verification artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global exhibition services market",
      "sam": "$1B — digital exhibit verification tools",
      "som": "$250M — paid provenance verification services"
    },
    {
      "id": "visual-art-painter-s-time-capsule-8",
      "theme": "visual-art",
      "title": "Painter’s Time Capsule",
      "pitch": "Permanently archive each painting stage as images and notes pinned immutably on IPFS.",
      "subDiscipline": "artistic process documentation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata JWT enables secure sequential uploads to preserve process documentation forever.",
      "megaPrompt": "Build \"Painter’s Time Capsule\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPermanently archive each painting stage as images and notes pinned immutably on IPFS.\nDiscipline: Visual Art (artistic process documentation).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata JWT enables secure sequential uploads to preserve process documentation forever.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PaintersTimeCapsule.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogPaintersTimeCapsule\n/// @notice Permanently archive each painting stage as images and notes pinned immutably on IPFS.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogPaintersTimeCapsule {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the artistic process documentation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — artist education and documentation",
      "sam": "$150M — process archiving platforms",
      "som": "$30M — paid archival service usage"
    },
    {
      "id": "visual-art-visual-art-supply-chain-9",
      "theme": "visual-art",
      "title": "Visual Art Supply Chain",
      "pitch": "Pin supplier certificates and provenance of materials to IPFS for buyer transparency.",
      "subDiscipline": "art materials tracking",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata’s permanent storage provides unalterable proof of authenticity for art supplies.",
      "megaPrompt": "Build \"Visual Art Supply Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin supplier certificates and provenance of materials to IPFS for buyer transparency.\nDiscipline: Visual Art (art materials tracking).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata’s permanent storage provides unalterable proof of authenticity for art supplies.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/VisualArtSupplyChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogVisualArtSupplyChain\n/// @notice Pin supplier certificates and provenance of materials to IPFS for buyer transparency.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogVisualArtSupplyChain {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the art materials tracking artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$850M — art supplier certification market",
      "sam": "$210M — digital supply chain traceability",
      "som": "$40M — transparent certification subscriptions"
    },
    {
      "id": "visual-art-auto-manifest-builder-10",
      "theme": "visual-art",
      "title": "Auto-Manifest Builder",
      "pitch": "Automatically generate and pin complete IPFS manifests for artists’ works with minimal effort.",
      "subDiscipline": "metadata automation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata JWT API allows easy automated pins for bulk metadata and image uploads.",
      "megaPrompt": "Build \"Auto-Manifest Builder\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAutomatically generate and pin complete IPFS manifests for artists’ works with minimal effort.\nDiscipline: Visual Art (metadata automation).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata JWT API allows easy automated pins for bulk metadata and image uploads.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/AutoManifestBuilder.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogAutoManifestBuilder\n/// @notice Automatically generate and pin complete IPFS manifests for artists’ works with minimal effort.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogAutoManifestBuilder {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the metadata automation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — creative automation software",
      "sam": "$500M — metadata management solutions",
      "som": "$100M — paid automation feature adoption"
    },
    {
      "id": "visual-art-collaborative-art-archive-11",
      "theme": "visual-art",
      "title": "Collaborative Art Archive",
      "pitch": "Pin shared artworks and process files to IPFS for permanent collaborative history storage.",
      "subDiscipline": "group project documentation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata JWT supports multi-user authenticated uploads securing collaboration records.",
      "megaPrompt": "Build \"Collaborative Art Archive\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin shared artworks and process files to IPFS for permanent collaborative history storage.\nDiscipline: Visual Art (group project documentation).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata JWT supports multi-user authenticated uploads securing collaboration records.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CollaborativeArtArchive.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogCollaborativeArtArchive\n/// @notice Pin shared artworks and process files to IPFS for permanent collaborative history storage.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogCollaborativeArtArchive {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the group project documentation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — collaborative art tool market",
      "sam": "$350M — team project archiving platforms",
      "som": "$75M — subscription for collaborative archiving"
    },
    {
      "id": "visual-art-style-transfer-keeper-12",
      "theme": "visual-art",
      "title": "Style Transfer Keeper",
      "pitch": "Pin AI-generated style files and outputs permanently to safeguard unique artistic styles.",
      "subDiscipline": "AI style preservation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata supports permanent pinning of JSON styles and image outputs ensuring longevity.",
      "megaPrompt": "Build \"Style Transfer Keeper\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin AI-generated style files and outputs permanently to safeguard unique artistic styles.\nDiscipline: Visual Art (AI style preservation).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata supports permanent pinning of JSON styles and image outputs ensuring longevity.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StyleTransferKeeper.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogStyleTransferKeeper\n/// @notice Pin AI-generated style files and outputs permanently to safeguard unique artistic styles.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogStyleTransferKeeper {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the AI style preservation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$900M — AI art tools market",
      "sam": "$220M — style transfer application users",
      "som": "$45M — paid style preservation services"
    },
    {
      "id": "visual-art-art-fair-digital-catalog-13",
      "theme": "visual-art",
      "title": "Art Fair Digital Catalog",
      "pitch": "Create and pin permanent digital catalogs for art fairs accessible globally.",
      "subDiscipline": "event art listings",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata JWT pinning guarantees immutable, decentralized access to fair catalogs and data.",
      "megaPrompt": "Build \"Art Fair Digital Catalog\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate and pin permanent digital catalogs for art fairs accessible globally.\nDiscipline: Visual Art (event art listings).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata JWT pinning guarantees immutable, decentralized access to fair catalogs and data.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ArtFairDigitalCatalog.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogArtFairDigitalCatalog\n/// @notice Create and pin permanent digital catalogs for art fairs accessible globally.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogArtFairDigitalCatalog {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the event art listings artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — global art event management",
      "sam": "$750M — digital catalog solutions",
      "som": "$160M — event catalog subscription usage"
    },
    {
      "id": "visual-art-generative-token-gallery-14",
      "theme": "visual-art",
      "title": "Generative Token Gallery",
      "pitch": "Pin generative art manifests and images for permanent token-backed gallery display.",
      "subDiscipline": "tokenized generative art",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata IPFS storage ensures consistent art data linked to blockchain tokens forever.",
      "megaPrompt": "Build \"Generative Token Gallery\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin generative art manifests and images for permanent token-backed gallery display.\nDiscipline: Visual Art (tokenized generative art).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata IPFS storage ensures consistent art data linked to blockchain tokens forever.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GenerativeTokenGallery.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogGenerativeTokenGallery\n/// @notice Pin generative art manifests and images for permanent token-backed gallery display.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogGenerativeTokenGallery {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the tokenized generative art artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — tokenized art market",
      "sam": "$1B — token gallery platforms",
      "som": "$210M — paid generative token services"
    },
    {
      "id": "visual-art-illustration-rights-ledger-15",
      "theme": "visual-art",
      "title": "Illustration Rights Ledger",
      "pitch": "Pin illustration licenses and usage terms permanently for transparent rights management.",
      "subDiscipline": "digital rights management",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata’s immutable IPFS pins secure rights documents for easy verification.",
      "megaPrompt": "Build \"Illustration Rights Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin illustration licenses and usage terms permanently for transparent rights management.\nDiscipline: Visual Art (digital rights management).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata’s immutable IPFS pins secure rights documents for easy verification.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/IllustrationRightsLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogIllustrationRightsLedger\n/// @notice Pin illustration licenses and usage terms permanently for transparent rights management.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogIllustrationRightsLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the digital rights management artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.8B — digital rights platforms",
      "sam": "$450M — illustration rights management",
      "som": "$90M — subscription licensing verification"
    },
    {
      "id": "visual-art-augmented-art-archive-16",
      "theme": "visual-art",
      "title": "Augmented Art Archive",
      "pitch": "Pin AR textures, markers, and metadata permanently to IPFS for reliable augmented experiences.",
      "subDiscipline": "AR art content storage",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata supports permanent pinning of large AR asset files with consistent CID referencing.",
      "megaPrompt": "Build \"Augmented Art Archive\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin AR textures, markers, and metadata permanently to IPFS for reliable augmented experiences.\nDiscipline: Visual Art (AR art content storage).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata supports permanent pinning of large AR asset files with consistent CID referencing.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/AugmentedArtArchive.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogAugmentedArtArchive\n/// @notice Pin AR textures, markers, and metadata permanently to IPFS for reliable augmented experiences.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogAugmentedArtArchive {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the AR art content storage artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.5B — AR content creation market",
      "sam": "$600M — AR art publishing",
      "som": "$130M — paid AR archival"
    },
    {
      "id": "visual-art-permanent-sketchbook-hub-17",
      "theme": "visual-art",
      "title": "Permanent Sketchbook Hub",
      "pitch": "Pin your sketchbook pages as immutable IPFS entries to build a lifelong visual diary.",
      "subDiscipline": "digital sketch archiving",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata JWT allows easy permanent uploads ensuring sketches remain unchanged and accessible.",
      "megaPrompt": "Build \"Permanent Sketchbook Hub\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin your sketchbook pages as immutable IPFS entries to build a lifelong visual diary.\nDiscipline: Visual Art (digital sketch archiving).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata JWT allows easy permanent uploads ensuring sketches remain unchanged and accessible.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PermanentSketchbookHub.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogPermanentSketchbookHub\n/// @notice Pin your sketchbook pages as immutable IPFS entries to build a lifelong visual diary.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogPermanentSketchbookHub {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the digital sketch archiving artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — digital artist journaling tools",
      "sam": "$270M — sketch archiving apps",
      "som": "$60M — paid archival storage"
    },
    {
      "id": "visual-art-visual-storyboard-chain-18",
      "theme": "visual-art",
      "title": "Visual Storyboard Chain",
      "pitch": "Pin storyboards and frame metadata permanently for narrative visual art projects.",
      "subDiscipline": "narrative art sequencing",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata’s JSON and image pinning preserves sequence integrity on IPFS.",
      "megaPrompt": "Build \"Visual Storyboard Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin storyboards and frame metadata permanently for narrative visual art projects.\nDiscipline: Visual Art (narrative art sequencing).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata’s JSON and image pinning preserves sequence integrity on IPFS.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/VisualStoryboardChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogVisualStoryboardChain\n/// @notice Pin storyboards and frame metadata permanently for narrative visual art projects.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogVisualStoryboardChain {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the narrative art sequencing artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$850M — visual storytelling tools",
      "sam": "$210M — storyboard digital platforms",
      "som": "$45M — subscriptions for sequence permanence"
    },
    {
      "id": "visual-art-curator-s-immutable-log-19",
      "theme": "visual-art",
      "title": "Curator’s Immutable Log",
      "pitch": "Pin curator notes, selections, and art metadata immutably to IPFS ensuring exhibition integrity.",
      "subDiscipline": "art exhibition curation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata JWT ensures curator data permanence and tamper resistance on-chain.",
      "megaPrompt": "Build \"Curator’s Immutable Log\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin curator notes, selections, and art metadata immutably to IPFS ensuring exhibition integrity.\nDiscipline: Visual Art (art exhibition curation).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata JWT ensures curator data permanence and tamper resistance on-chain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CuratorsImmutableLog.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogCuratorsImmutableLog\n/// @notice Pin curator notes, selections, and art metadata immutably to IPFS ensuring exhibition integrity.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogCuratorsImmutableLog {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the art exhibition curation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.2B — digital curation software",
      "sam": "$320M — online exhibition tools",
      "som": "$70M — curator service subscriptions"
    },
    {
      "id": "visual-art-visual-remix-repository-20",
      "theme": "visual-art",
      "title": "Visual Remix Repository",
      "pitch": "Pin remixed artworks and source attribution information permanently for remix transparency.",
      "subDiscipline": "art remix documentation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata enables permanent IPFS storage linking source and new art metadata.",
      "megaPrompt": "Build \"Visual Remix Repository\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin remixed artworks and source attribution information permanently for remix transparency.\nDiscipline: Visual Art (art remix documentation).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata enables permanent IPFS storage linking source and new art metadata.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/VisualRemixRepository.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogVisualRemixRepository\n/// @notice Pin remixed artworks and source attribution information permanently for remix transparency.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogVisualRemixRepository {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the art remix documentation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.1B — remix art marketplaces",
      "sam": "$280M — remix attribution tools",
      "som": "$55M — paid transparency services"
    },
    {
      "id": "visual-art-unique-print-provenance-21",
      "theme": "visual-art",
      "title": "Unique Print Provenance",
      "pitch": "Pin limited print metadata and certificates immutably to prove authenticity for collectors.",
      "subDiscipline": "limited edition prints",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata IPFS pinning offers permanent certificate storage verified by CID.",
      "megaPrompt": "Build \"Unique Print Provenance\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin limited print metadata and certificates immutably to prove authenticity for collectors.\nDiscipline: Visual Art (limited edition prints).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata IPFS pinning offers permanent certificate storage verified by CID.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/UniquePrintProvenance.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogUniquePrintProvenance\n/// @notice Pin limited print metadata and certificates immutably to prove authenticity for collectors.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogUniquePrintProvenance {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the limited edition prints artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$6B — global print art market",
      "sam": "$1.5B — print provenance platforms",
      "som": "$350M — certification and archival subscriptions"
    },
    {
      "id": "visual-art-artistic-collaboration-chain-22",
      "theme": "visual-art",
      "title": "Artistic Collaboration Chain",
      "pitch": "Pin and permanently store co-created artworks and contribution metadata for fair crediting.",
      "subDiscipline": "joint creation records",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata JWT multi-upload supports joint data pinning with immutable records.",
      "megaPrompt": "Build \"Artistic Collaboration Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin and permanently store co-created artworks and contribution metadata for fair crediting.\nDiscipline: Visual Art (joint creation records).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata JWT multi-upload supports joint data pinning with immutable records.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ArtisticCollaborationChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogArtisticCollaborationChain\n/// @notice Pin and permanently store co-created artworks and contribution metadata for fair crediting.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogArtisticCollaborationChain {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the joint creation records artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.4B — collaborative art tools",
      "sam": "$360M — joint creative project platforms",
      "som": "$75M — paid collaboration archiving"
    },
    {
      "id": "visual-art-visual-rights-archive-23",
      "theme": "visual-art",
      "title": "Visual Rights Archive",
      "pitch": "Pin copyright registration data and proofs permanently for artists’ legal protection.",
      "subDiscipline": "copyright preservation",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata’s permanent IPFS pinning guarantees copyright data immutability and accessibility.",
      "megaPrompt": "Build \"Visual Rights Archive\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin copyright registration data and proofs permanently for artists’ legal protection.\nDiscipline: Visual Art (copyright preservation).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata’s permanent IPFS pinning guarantees copyright data immutability and accessibility.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/VisualRightsArchive.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogVisualRightsArchive\n/// @notice Pin copyright registration data and proofs permanently for artists’ legal protection.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogVisualRightsArchive {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the copyright preservation artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — copyright registration market",
      "sam": "$800M — digital copyright services",
      "som": "$180M — subscription copyright storage"
    },
    {
      "id": "visual-art-dynamic-exhibit-snapshot-24",
      "theme": "visual-art",
      "title": "Dynamic Exhibit Snapshot",
      "pitch": "Pin snapshots of changing exhibitions and arrangements permanently for historical records.",
      "subDiscipline": "exhibit state recording",
      "quantumHook": "IPFS via Pinata",
      "quantumHookId": "ipfs-pinata",
      "quantumTag": "decentralized storage",
      "quantumRationale": "Pinata JWT allows frequent permanent pins of evolving JSON/image data on IPFS.",
      "megaPrompt": "Build \"Dynamic Exhibit Snapshot\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPin snapshots of changing exhibitions and arrangements permanently for historical records.\nDiscipline: Visual Art (exhibit state recording).\nOnchain primitive: IPFS via Pinata. Why this primitive: Pinata JWT allows frequent permanent pins of evolving JSON/image data on IPFS.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/DynamicExhibitSnapshot.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title CIDLogDynamicExhibitSnapshot\n/// @notice Pin snapshots of changing exhibitions and arrangements permanently for historical records.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CIDLogDynamicExhibitSnapshot {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. On submit, pin the exhibit state recording artefact to Pinata, then call `log(cid)` on the contract via Privy sponsored tx. Render the CID, IPFS gateway preview, and Etherscan tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — exhibit documentation solutions",
      "sam": "$260M — art show documentation tools",
      "som": "$55M — paid exhibit snapshot services"
    },
    {
      "id": "visual-art-canvas-collaborate-0",
      "theme": "visual-art",
      "title": "Canvas Collaborate",
      "pitch": "Enable painters to co-create artworks with seamless onchain identity and gasless contributions.",
      "subDiscipline": "collaborative painting",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social plus sponsored transactions allow smooth collaboration without gas friction or complex wallets.",
      "megaPrompt": "Build \"Canvas Collaborate\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnable painters to co-create artworks with seamless onchain identity and gasless contributions.\nDiscipline: Visual Art (collaborative painting).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social plus sponsored transactions allow smooth collaboration without gas friction or complex wallets.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CanvasCollaborate.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogCanvasCollaborate\n/// @notice Enable painters to co-create artworks with seamless onchain identity and gasless contributions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogCanvasCollaborate {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every collaborative painting action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$8B — global collaborative art platforms",
      "sam": "$2B — digital collaboration tools for painters",
      "som": "$100M — early adopters in gasless art collaboration"
    },
    {
      "id": "visual-art-illustrator-guild-1",
      "theme": "visual-art",
      "title": "Illustrator Guild",
      "pitch": "Create a hosted network for illustrators to share, sell, and verify work with easy login and zero gas fees.",
      "subDiscipline": "illustrator community",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet with Google sign-in removes blockchain barriers for illustrators expanding their market.",
      "megaPrompt": "Build \"Illustrator Guild\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate a hosted network for illustrators to share, sell, and verify work with easy login and zero gas fees.\nDiscipline: Visual Art (illustrator community).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet with Google sign-in removes blockchain barriers for illustrators expanding their market.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/IllustratorGuild.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogIllustratorGuild\n/// @notice Create a hosted network for illustrators to share, sell, and verify work with easy login and zero gas fees.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogIllustratorGuild {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every illustrator community action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$4B — global illustration market",
      "sam": "$1B — digital sales of illustrations",
      "som": "$50M — networks enabling gasless art transactions"
    },
    {
      "id": "visual-art-generative-canvas-2",
      "theme": "visual-art",
      "title": "Generative Canvas",
      "pitch": "Let generative artists deploy and monetize code-driven art with instant gasless onboarding and transactions.",
      "subDiscipline": "generative art",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Sponsored tx plus embedded wallet offer frictionless deployment and sales for generative artists.",
      "megaPrompt": "Build \"Generative Canvas\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nLet generative artists deploy and monetize code-driven art with instant gasless onboarding and transactions.\nDiscipline: Visual Art (generative art).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Sponsored tx plus embedded wallet offer frictionless deployment and sales for generative artists.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GenerativeCanvas.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogGenerativeCanvas\n/// @notice Let generative artists deploy and monetize code-driven art with instant gasless onboarding and transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogGenerativeCanvas {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every generative art action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3B — generative digital art sales",
      "sam": "$800M — code-based art platforms",
      "som": "$40M — gasless onboarding for generative artists"
    },
    {
      "id": "visual-art-gallery-ledger-3",
      "theme": "visual-art",
      "title": "Gallery Ledger",
      "pitch": "Provide gallerists with gasless, onchain tools to prove artwork provenance and authenticity effortlessly.",
      "subDiscipline": "art provenance tracking",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy-enabled social login and sponsored tx simplify provenance updates without gas costs.",
      "megaPrompt": "Build \"Gallery Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nProvide gallerists with gasless, onchain tools to prove artwork provenance and authenticity effortlessly.\nDiscipline: Visual Art (art provenance tracking).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy-enabled social login and sponsored tx simplify provenance updates without gas costs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GalleryLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogGalleryLedger\n/// @notice Provide gallerists with gasless, onchain tools to prove artwork provenance and authenticity effortlessly.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogGalleryLedger {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every art provenance tracking action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$15B — global art provenance solutions",
      "sam": "$4B — provenance tools for galleries",
      "som": "$200M — gasless provenance management users"
    },
    {
      "id": "visual-art-palette-swap-4",
      "theme": "visual-art",
      "title": "Palette Swap",
      "pitch": "Allow painters and illustrators to exchange and license color palettes with transparent, gasless transactions.",
      "subDiscipline": "color exchange",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social plus sponsored transactions enable easy palette licensing with zero gas hurdles.",
      "megaPrompt": "Build \"Palette Swap\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAllow painters and illustrators to exchange and license color palettes with transparent, gasless transactions.\nDiscipline: Visual Art (color exchange).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social plus sponsored transactions enable easy palette licensing with zero gas hurdles.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PaletteSwap.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogPaletteSwap\n/// @notice Allow painters and illustrators to exchange and license color palettes with transparent, gasless transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogPaletteSwap {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every color exchange action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$500M — digital color licensing market",
      "sam": "$120M — palette trading platforms",
      "som": "$10M — early palette exchange adopters"
    },
    {
      "id": "visual-art-sketchstream-5",
      "theme": "visual-art",
      "title": "SketchStream",
      "pitch": "Stream live sketches with integrated onchain tips and gasless wallet logins for artist-audience engagement.",
      "subDiscipline": "live drawing streams",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet and sponsored tx allow instant, gas-free microtransactions during live art streams.",
      "megaPrompt": "Build \"SketchStream\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nStream live sketches with integrated onchain tips and gasless wallet logins for artist-audience engagement.\nDiscipline: Visual Art (live drawing streams).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet and sponsored tx allow instant, gas-free microtransactions during live art streams.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SketchStream.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogSketchStream\n/// @notice Stream live sketches with integrated onchain tips and gasless wallet logins for artist-audience engagement.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogSketchStream {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every live drawing streams action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — live streaming art markets",
      "sam": "$400M — live tip-enabled drawing platforms",
      "som": "$25M — gasless tipping users"
    },
    {
      "id": "visual-art-nft-splitter-6",
      "theme": "visual-art",
      "title": "NFT Splitter",
      "pitch": "Enable painters and collectors to split artwork ownership onchain with easy social login and no gas fees.",
      "subDiscipline": "fractional art ownership",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social plus sponsored tx make fractional ownership seamless without transaction costs.",
      "megaPrompt": "Build \"NFT Splitter\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnable painters and collectors to split artwork ownership onchain with easy social login and no gas fees.\nDiscipline: Visual Art (fractional art ownership).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social plus sponsored tx make fractional ownership seamless without transaction costs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/NFTSplitter.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogNFTSplitter\n/// @notice Enable painters and collectors to split artwork ownership onchain with easy social login and no gas fees.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogNFTSplitter {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every fractional art ownership action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$10B — art fractionalization market",
      "sam": "$3B — digital sharing of artwork equity",
      "som": "$150M — gasless split ownership users"
    },
    {
      "id": "visual-art-artswap-social-7",
      "theme": "visual-art",
      "title": "ArtSwap Social",
      "pitch": "Build a social marketplace for illustrators to swap artworks and services without gas concerns.",
      "subDiscipline": "art barter communities",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy embedded wallet and sponsored tx facilitate frictionless, gasless peer-to-peer swaps.",
      "megaPrompt": "Build \"ArtSwap Social\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nBuild a social marketplace for illustrators to swap artworks and services without gas concerns.\nDiscipline: Visual Art (art barter communities).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy embedded wallet and sponsored tx facilitate frictionless, gasless peer-to-peer swaps.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ArtSwapSocial.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogArtSwapSocial\n/// @notice Build a social marketplace for illustrators to swap artworks and services without gas concerns.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogArtSwapSocial {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every art barter communities action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — global art barter economy",
      "sam": "$350M — illustrator barter platforms",
      "som": "$20M — gasless transaction swap users"
    },
    {
      "id": "visual-art-sketchvault-8",
      "theme": "visual-art",
      "title": "SketchVault",
      "pitch": "Offer painters a gasless, onchain-secured vault for storing and sharing preliminary sketches with social login.",
      "subDiscipline": "secure sketch storage",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet plus sponsored tx enable secure, cost-free storage of sensitive creative drafts.",
      "megaPrompt": "Build \"SketchVault\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nOffer painters a gasless, onchain-secured vault for storing and sharing preliminary sketches with social login.\nDiscipline: Visual Art (secure sketch storage).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet plus sponsored tx enable secure, cost-free storage of sensitive creative drafts.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SketchVault.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogSketchVault\n/// @notice Offer painters a gasless, onchain-secured vault for storing and sharing preliminary sketches with social login.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogSketchVault {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every secure sketch storage action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — secure digital art storage",
      "sam": "$200M — sketch archival platforms",
      "som": "$12M — gasless storage adopters"
    },
    {
      "id": "visual-art-brushstroke-rights-9",
      "theme": "visual-art",
      "title": "Brushstroke Rights",
      "pitch": "Help illustrators manage and transfer licenses onchain with simple Google sign-in and zero gas.",
      "subDiscipline": "digital rights management",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy primitive ensures easy rights transfers without user knowledge of gas or keys.",
      "megaPrompt": "Build \"Brushstroke Rights\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nHelp illustrators manage and transfer licenses onchain with simple Google sign-in and zero gas.\nDiscipline: Visual Art (digital rights management).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy primitive ensures easy rights transfers without user knowledge of gas or keys.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/BrushstrokeRights.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogBrushstrokeRights\n/// @notice Help illustrators manage and transfer licenses onchain with simple Google sign-in and zero gas.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogBrushstrokeRights {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every digital rights management action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — digital art licensing market",
      "sam": "$1.5B — licensing platforms for illustrators",
      "som": "$80M — gasless DRM users"
    },
    {
      "id": "visual-art-artchain-auctions-10",
      "theme": "visual-art",
      "title": "ArtChain Auctions",
      "pitch": "Create gasless onchain auctions for visual artists with fast wallet onboarding and sponsored bids.",
      "subDiscipline": "auction facilitation",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social plus sponsored transactions prevent bidding friction from gas fees.",
      "megaPrompt": "Build \"ArtChain Auctions\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate gasless onchain auctions for visual artists with fast wallet onboarding and sponsored bids.\nDiscipline: Visual Art (auction facilitation).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social plus sponsored transactions prevent bidding friction from gas fees.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ArtChainAuctions.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogArtChainAuctions\n/// @notice Create gasless onchain auctions for visual artists with fast wallet onboarding and sponsored bids.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogArtChainAuctions {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every auction facilitation action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$12B — global art auctions",
      "sam": "$3.5B — digital art auction platforms",
      "som": "$180M — gasless auction users"
    },
    {
      "id": "visual-art-framefi-marketplace-11",
      "theme": "visual-art",
      "title": "FrameFi Marketplace",
      "pitch": "Enable illustrators to sell artworks with embedded gasless framing add-ons and easy wallet login.",
      "subDiscipline": "digital framing services",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy embedded wallet plus sponsored tx streamline gasless purchase of framing options.",
      "megaPrompt": "Build \"FrameFi Marketplace\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnable illustrators to sell artworks with embedded gasless framing add-ons and easy wallet login.\nDiscipline: Visual Art (digital framing services).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy embedded wallet plus sponsored tx streamline gasless purchase of framing options.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/FrameFiMarketplace.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogFrameFiMarketplace\n/// @notice Enable illustrators to sell artworks with embedded gasless framing add-ons and easy wallet login.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogFrameFiMarketplace {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every digital framing services action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$600M — digital art framing market",
      "sam": "$150M — framing add-on buyers",
      "som": "$10M — gasless framing service users"
    },
    {
      "id": "visual-art-mural-mesh-12",
      "theme": "visual-art",
      "title": "Mural Mesh",
      "pitch": "Coordinate large-scale public murals with painters via gasless onchain collaboration and social login.",
      "subDiscipline": "public mural collaboration",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy sponsored transactions ease multi-party approval without gas costs.",
      "megaPrompt": "Build \"Mural Mesh\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCoordinate large-scale public murals with painters via gasless onchain collaboration and social login.\nDiscipline: Visual Art (public mural collaboration).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy sponsored transactions ease multi-party approval without gas costs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MuralMesh.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogMuralMesh\n/// @notice Coordinate large-scale public murals with painters via gasless onchain collaboration and social login.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogMuralMesh {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every public mural collaboration action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — public mural funding",
      "sam": "$300M — collaborative mural initiatives",
      "som": "$18M — gasless onchain mural participants"
    },
    {
      "id": "visual-art-colorstory-13",
      "theme": "visual-art",
      "title": "ColorStory",
      "pitch": "Illustrators share evolving color-based stories secured onchain with gasless updates and Google sign-in.",
      "subDiscipline": "color narrative art",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet and sponsored tx allow smooth story versioning without transaction costs.",
      "megaPrompt": "Build \"ColorStory\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nIllustrators share evolving color-based stories secured onchain with gasless updates and Google sign-in.\nDiscipline: Visual Art (color narrative art).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet and sponsored tx allow smooth story versioning without transaction costs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ColorStory.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogColorStory\n/// @notice Illustrators share evolving color-based stories secured onchain with gasless updates and Google sign-in.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogColorStory {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every color narrative art action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$350M — digital narrative art market",
      "sam": "$90M — color story platforms",
      "som": "$7M — gasless narrative update users"
    },
    {
      "id": "visual-art-token-tapestry-14",
      "theme": "visual-art",
      "title": "Token Tapestry",
      "pitch": "Artists tokenize and sell unique textile patterns with gasless wallet integration and sponsored transactions.",
      "subDiscipline": "textile pattern art",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social plus sponsored tx ensure seamless gasless sales of tokenized patterns.",
      "megaPrompt": "Build \"Token Tapestry\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nArtists tokenize and sell unique textile patterns with gasless wallet integration and sponsored transactions.\nDiscipline: Visual Art (textile pattern art).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social plus sponsored tx ensure seamless gasless sales of tokenized patterns.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/TokenTapestry.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogTokenTapestry\n/// @notice Artists tokenize and sell unique textile patterns with gasless wallet integration and sponsored transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogTokenTapestry {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every textile pattern art action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — digital textile art market",
      "sam": "$500M — pattern tokenization",
      "som": "$30M — gasless textile pattern buyers"
    },
    {
      "id": "visual-art-gallery-ghost-15",
      "theme": "visual-art",
      "title": "Gallery Ghost",
      "pitch": "Gallerists create virtual galleries with gasless onchain ticketing and Google-based wallet onboarding.",
      "subDiscipline": "virtual gallery hosting",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy embedded wallet plus sponsored tx enable cost-free event participation.",
      "megaPrompt": "Build \"Gallery Ghost\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nGallerists create virtual galleries with gasless onchain ticketing and Google-based wallet onboarding.\nDiscipline: Visual Art (virtual gallery hosting).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy embedded wallet plus sponsored tx enable cost-free event participation.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GalleryGhost.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogGalleryGhost\n/// @notice Gallerists create virtual galleries with gasless onchain ticketing and Google-based wallet onboarding.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogGalleryGhost {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every virtual gallery hosting action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$5B — virtual gallery ticket sales",
      "sam": "$1.2B — digital gallery hosting platforms",
      "som": "$70M — gasless ticket user base"
    },
    {
      "id": "visual-art-brushbot-16",
      "theme": "visual-art",
      "title": "BrushBot",
      "pitch": "Integrate AI painting tools powered by gasless onchain user verification and transactions.",
      "subDiscipline": "AI-assisted painting",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet and sponsored tx provide smooth tool access without blockchain complexity.",
      "megaPrompt": "Build \"BrushBot\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nIntegrate AI painting tools powered by gasless onchain user verification and transactions.\nDiscipline: Visual Art (AI-assisted painting).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet and sponsored tx provide smooth tool access without blockchain complexity.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/BrushBot.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogBrushBot\n/// @notice Integrate AI painting tools powered by gasless onchain user verification and transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogBrushBot {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every AI-assisted painting action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$3.5B — AI art tool market",
      "sam": "$900M — AI painter subscriptions",
      "som": "$45M — gasless AI tool users"
    },
    {
      "id": "visual-art-sketchset-swap-17",
      "theme": "visual-art",
      "title": "SketchSet Swap",
      "pitch": "Illustrators trade and license brush presets with zero gas fees and easy Google sign-in wallets.",
      "subDiscipline": "brush preset exchange",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social plus sponsored transactions eliminate friction in preset licensing.",
      "megaPrompt": "Build \"SketchSet Swap\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nIllustrators trade and license brush presets with zero gas fees and easy Google sign-in wallets.\nDiscipline: Visual Art (brush preset exchange).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social plus sponsored transactions eliminate friction in preset licensing.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SketchSetSwap.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogSketchSetSwap\n/// @notice Illustrators trade and license brush presets with zero gas fees and easy Google sign-in wallets.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogSketchSetSwap {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every brush preset exchange action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$400M — digital brush market",
      "sam": "$100M — brush preset trading",
      "som": "$8M — gasless brush exchange users"
    },
    {
      "id": "visual-art-artchain-critique-18",
      "theme": "visual-art",
      "title": "ArtChain Critique",
      "pitch": "Painters share work for peer feedback with onchain reputation and gasless login/transactions.",
      "subDiscipline": "peer review platform",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet plus sponsored tx enable frictionless participation and reputation building.",
      "megaPrompt": "Build \"ArtChain Critique\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPainters share work for peer feedback with onchain reputation and gasless login/transactions.\nDiscipline: Visual Art (peer review platform).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet plus sponsored tx enable frictionless participation and reputation building.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ArtChainCritique.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogArtChainCritique\n/// @notice Painters share work for peer feedback with onchain reputation and gasless login/transactions.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogArtChainCritique {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every peer review platform action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$700M — digital art critique services",
      "sam": "$180M — peer review platforms for artists",
      "som": "$12M — gasless critique users"
    },
    {
      "id": "visual-art-illustrate-impact-19",
      "theme": "visual-art",
      "title": "Illustrate Impact",
      "pitch": "Facilitate gasless donations and sponsorships for illustrators supporting social causes via easy login.",
      "subDiscipline": "art philanthropy",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social and sponsored tx ensure smooth, zero-fee charitable transactions.",
      "megaPrompt": "Build \"Illustrate Impact\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nFacilitate gasless donations and sponsorships for illustrators supporting social causes via easy login.\nDiscipline: Visual Art (art philanthropy).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social and sponsored tx ensure smooth, zero-fee charitable transactions.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/IllustrateImpact.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogIllustrateImpact\n/// @notice Facilitate gasless donations and sponsorships for illustrators supporting social causes via easy login.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogIllustrateImpact {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every art philanthropy action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2.5B — art philanthropy market",
      "sam": "$600M — donations to illustrators",
      "som": "$35M — gasless charity transaction users"
    },
    {
      "id": "visual-art-colorchain-auctions-20",
      "theme": "visual-art",
      "title": "Colorchain Auctions",
      "pitch": "Host auctions of color-driven artworks with gasless bidding and quick wallet onboarding.",
      "subDiscipline": "color-themed art auctions",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet and sponsored transactions remove gas fee barriers in niche auctions.",
      "megaPrompt": "Build \"Colorchain Auctions\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nHost auctions of color-driven artworks with gasless bidding and quick wallet onboarding.\nDiscipline: Visual Art (color-themed art auctions).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet and sponsored transactions remove gas fee barriers in niche auctions.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ColorchainAuctions.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogColorchainAuctions\n/// @notice Host auctions of color-driven artworks with gasless bidding and quick wallet onboarding.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogColorchainAuctions {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every color-themed art auctions action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1B — color art auction market",
      "sam": "$250M — color themed auction platforms",
      "som": "$15M — gasless bidders"
    },
    {
      "id": "visual-art-illustrator-ink-21",
      "theme": "visual-art",
      "title": "Illustrator Ink",
      "pitch": "Support ink artists selling limited editions with gasless minting and sponsored transaction flows.",
      "subDiscipline": "digital ink art",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy embedded wallet plus sponsored tx create seamless gasless mint experiences.",
      "megaPrompt": "Build \"Illustrator Ink\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSupport ink artists selling limited editions with gasless minting and sponsored transaction flows.\nDiscipline: Visual Art (digital ink art).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy embedded wallet plus sponsored tx create seamless gasless mint experiences.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/IllustratorInk.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogIllustratorInk\n/// @notice Support ink artists selling limited editions with gasless minting and sponsored transaction flows.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogIllustratorInk {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every digital ink art action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.5B — digital ink art market",
      "sam": "$400M — limited edition ink artworks",
      "som": "$22M — gasless minting users"
    },
    {
      "id": "visual-art-generative-gallery-22",
      "theme": "visual-art",
      "title": "Generative Gallery",
      "pitch": "Curate generative art exhibits featuring gasless ticketing and social login onboarding.",
      "subDiscipline": "algorithmic art exhibition",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy wallet and sponsored transactions facilitate easy event access without fees.",
      "megaPrompt": "Build \"Generative Gallery\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCurate generative art exhibits featuring gasless ticketing and social login onboarding.\nDiscipline: Visual Art (algorithmic art exhibition).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy wallet and sponsored transactions facilitate easy event access without fees.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GenerativeGallery.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogGenerativeGallery\n/// @notice Curate generative art exhibits featuring gasless ticketing and social login onboarding.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogGenerativeGallery {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every algorithmic art exhibition action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$1.8B — digital generative art events",
      "sam": "$500M — ticketing for generative exhibits",
      "som": "$28M — gasless event participants"
    },
    {
      "id": "visual-art-artswap-network-23",
      "theme": "visual-art",
      "title": "ArtSwap Network",
      "pitch": "Enable painters and illustrators to swap artworks onchain with gasless wallet bootstrapped by social login.",
      "subDiscipline": "visual art trades",
      "quantumHook": "Privy social + sponsored tx",
      "quantumHookId": "privy-social",
      "quantumTag": "wallet UX",
      "quantumRationale": "Privy social plus sponsored tx guarantee frictionless, cost-free art trades.",
      "megaPrompt": "Build \"ArtSwap Network\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnable painters and illustrators to swap artworks onchain with gasless wallet bootstrapped by social login.\nDiscipline: Visual Art (visual art trades).\nOnchain primitive: Privy social + sponsored tx. Why this primitive: Privy social plus sponsored tx guarantee frictionless, cost-free art trades.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ArtSwapNetwork.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\n/// @title SocialLogArtSwapNetwork\n/// @notice Enable painters and illustrators to swap artworks onchain with gasless wallet bootstrapped by social login.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SocialLogArtSwapNetwork {\n    event Logged(address indexed author, string cid, uint256 at);\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function log(string calldata cid) external {\n        emit Logged(msg.sender, cid, block.timestamp);\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. Every visual art trades action the user performs is sent as a sponsored Sepolia tx (`log(payload)`) and displayed with an Etherscan link. No wallet popups.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$2B — global art swap market",
      "sam": "$550M — digital artwork trade platforms",
      "som": "$33M — gasless swap users"
    },
    {
      "id": "visual-art-canvas-chronicles-0",
      "theme": "visual-art",
      "title": "Canvas Chronicles",
      "pitch": "Securely mint and track authentic painting portfolios with verifiable creation history.",
      "subDiscipline": "painting portfolios",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT provenance mint ensures immutable creator ownership and art authenticity on-chain.",
      "megaPrompt": "Build \"Canvas Chronicles\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecurely mint and track authentic painting portfolios with verifiable creation history.\nDiscipline: Visual Art (painting portfolios).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT provenance mint ensures immutable creator ownership and art authenticity on-chain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/CanvasChronicles.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title CanvasChronicles\n/// @notice ERC-721 provenance for: Securely mint and track authentic painting portfolios with verifiable creation history.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract CanvasChronicles is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"CanvasChronicles\", \"CANVAS\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a painting portfolios artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$2.5B — online artist portfolio services",
      "som": "$50M — NFT-based artist portfolio adopters"
    },
    {
      "id": "visual-art-sketch-stamp-1",
      "theme": "visual-art",
      "title": "Sketch Stamp",
      "pitch": "Authenticate and timestamp early illustration drafts to prove originality and creative process.",
      "subDiscipline": "illustration drafts",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 mint links creators to IPFS-stored drafts ensuring provenance auditability.",
      "megaPrompt": "Build \"Sketch Stamp\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAuthenticate and timestamp early illustration drafts to prove originality and creative process.\nDiscipline: Visual Art (illustration drafts).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 mint links creators to IPFS-stored drafts ensuring provenance auditability.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SketchStamp.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title SketchStamp\n/// @notice ERC-721 provenance for: Authenticate and timestamp early illustration drafts to prove originality and creative process.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SketchStamp is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"SketchStamp\", \"SKETCH\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a illustration drafts artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$800M — digital illustration sector",
      "som": "$10M — provenance-tracked illustration sales"
    },
    {
      "id": "visual-art-generative-genesis-2",
      "theme": "visual-art",
      "title": "Generative Genesis",
      "pitch": "Track and mint each generative art iteration as unique collectible tokens.",
      "subDiscipline": "generative art iterations",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "Onchain NFTs securely link generative code outputs with IPFS CIDs for true provenance.",
      "megaPrompt": "Build \"Generative Genesis\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTrack and mint each generative art iteration as unique collectible tokens.\nDiscipline: Visual Art (generative art iterations).\nOnchain primitive: NFT provenance mint. Why this primitive: Onchain NFTs securely link generative code outputs with IPFS CIDs for true provenance.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GenerativeGenesis.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title GenerativeGenesis\n/// @notice ERC-721 provenance for: Track and mint each generative art iteration as unique collectible tokens.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract GenerativeGenesis is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"GenerativeGenesis\", \"GENERA\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a generative art iterations artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$400M — generative art niche",
      "som": "$5M — NFT generative art collectors"
    },
    {
      "id": "visual-art-gallery-gatekeeper-3",
      "theme": "visual-art",
      "title": "Gallery Gatekeeper",
      "pitch": "Enable galleries to mint and verify provenance of physical artwork displayed or sold.",
      "subDiscipline": "gallery asset curation",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 tokens provide immutable proof of artwork origin and gallery authentication.",
      "megaPrompt": "Build \"Gallery Gatekeeper\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nEnable galleries to mint and verify provenance of physical artwork displayed or sold.\nDiscipline: Visual Art (gallery asset curation).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 tokens provide immutable proof of artwork origin and gallery authentication.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GalleryGatekeeper.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title GalleryGatekeeper\n/// @notice ERC-721 provenance for: Enable galleries to mint and verify provenance of physical artwork displayed or sold.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract GalleryGatekeeper is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"GalleryGatekeeper\", \"GALLER\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a gallery asset curation artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$7B — global gallery sales",
      "som": "$100M — digital provenance certified artworks"
    },
    {
      "id": "visual-art-color-ledger-4",
      "theme": "visual-art",
      "title": "Color Ledger",
      "pitch": "Mint unique NFTs capturing and securing artist-specific color palettes and schemes.",
      "subDiscipline": "color palette preservation",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT provenance mint anchors palette data immutably linked to the creator’s token.",
      "megaPrompt": "Build \"Color Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint unique NFTs capturing and securing artist-specific color palettes and schemes.\nDiscipline: Visual Art (color palette preservation).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT provenance mint anchors palette data immutably linked to the creator’s token.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ColorLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ColorLedger\n/// @notice ERC-721 provenance for: Mint unique NFTs capturing and securing artist-specific color palettes and schemes.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ColorLedger is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ColorLedger\", \"COLORL\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a color palette preservation artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$300M — digital art toolsets",
      "som": "$4M — color-palette licensing via NFTs"
    },
    {
      "id": "visual-art-brushstroke-chain-5",
      "theme": "visual-art",
      "title": "Brushstroke Chain",
      "pitch": "Record and mint signature brushstroke styles as NFTs for artist branding and licensing.",
      "subDiscipline": "brush technique archives",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 tokens verify authentic brushstroke styles with permanent on-chain IPFS proof.",
      "megaPrompt": "Build \"Brushstroke Chain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nRecord and mint signature brushstroke styles as NFTs for artist branding and licensing.\nDiscipline: Visual Art (brush technique archives).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 tokens verify authentic brushstroke styles with permanent on-chain IPFS proof.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/BrushstrokeChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title BrushstrokeChain\n/// @notice ERC-721 provenance for: Record and mint signature brushstroke styles as NFTs for artist branding and licensing.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract BrushstrokeChain is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"BrushstrokeChain\", \"BRUSHS\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a brush technique archives artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$150M — visual art teaching content",
      "som": "$3M — brushstroke NFT sales"
    },
    {
      "id": "visual-art-minted-murals-6",
      "theme": "visual-art",
      "title": "Minted Murals",
      "pitch": "Authenticate and immortalize public murals with creator-owned NFT provenance tokens.",
      "subDiscipline": "public mural documentation",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "Immutable onchain minting guarantees provenance and long-term mural authenticity.",
      "megaPrompt": "Build \"Minted Murals\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAuthenticate and immortalize public murals with creator-owned NFT provenance tokens.\nDiscipline: Visual Art (public mural documentation).\nOnchain primitive: NFT provenance mint. Why this primitive: Immutable onchain minting guarantees provenance and long-term mural authenticity.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MintedMurals.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title MintedMurals\n/// @notice ERC-721 provenance for: Authenticate and immortalize public murals with creator-owned NFT provenance tokens.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract MintedMurals is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"MintedMurals\", \"MINTED\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a public mural documentation artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$500M — public art projects",
      "som": "$8M — NFT mural provenance fees"
    },
    {
      "id": "visual-art-framechain-7",
      "theme": "visual-art",
      "title": "FrameChain",
      "pitch": "Control and verify reproduction rights of artworks via mintable provenance NFTs.",
      "subDiscipline": "art reproduction control",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 provenance tokens enable robust reproduction right tracking onchain.",
      "megaPrompt": "Build \"FrameChain\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nControl and verify reproduction rights of artworks via mintable provenance NFTs.\nDiscipline: Visual Art (art reproduction control).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 provenance tokens enable robust reproduction right tracking onchain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/FrameChain.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title FrameChain\n/// @notice ERC-721 provenance for: Control and verify reproduction rights of artworks via mintable provenance NFTs.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract FrameChain is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"FrameChain\", \"FRAMEC\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a art reproduction control artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$3B — art reproduction licensing",
      "som": "$50M — provenance NFT reproduction licenses"
    },
    {
      "id": "visual-art-illustrator-s-imprint-8",
      "theme": "visual-art",
      "title": "Illustrator’s Imprint",
      "pitch": "Mint signed digital prints with verifiable creator provenance directly on the blockchain.",
      "subDiscipline": "signed digital prints",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 tokens link signed print IPFS CIDs to creator ownership proofs.",
      "megaPrompt": "Build \"Illustrator’s Imprint\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint signed digital prints with verifiable creator provenance directly on the blockchain.\nDiscipline: Visual Art (signed digital prints).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 tokens link signed print IPFS CIDs to creator ownership proofs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/IllustratorsImprint.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title IllustratorsImprint\n/// @notice ERC-721 provenance for: Mint signed digital prints with verifiable creator provenance directly on the blockchain.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract IllustratorsImprint is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"IllustratorsImprint\", \"ILLUST\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a signed digital prints artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$1B — digital print sales",
      "som": "$15M — NFT print provenance market"
    },
    {
      "id": "visual-art-provenance-palette-9",
      "theme": "visual-art",
      "title": "Provenance Palette",
      "pitch": "Digitally certify and mint provenance tokens for unique physical artist palettes.",
      "subDiscipline": "artist palette provenance",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT minting onchain ensures unalterable provenance linked to physical artifacts.",
      "megaPrompt": "Build \"Provenance Palette\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nDigitally certify and mint provenance tokens for unique physical artist palettes.\nDiscipline: Visual Art (artist palette provenance).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT minting onchain ensures unalterable provenance linked to physical artifacts.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ProvenancePalette.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ProvenancePalette\n/// @notice ERC-721 provenance for: Digitally certify and mint provenance tokens for unique physical artist palettes.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ProvenancePalette is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ProvenancePalette\", \"PROVEN\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a artist palette provenance artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$50M — artist tools & collectibles",
      "som": "$1.5M — NFT-verified palette auctions"
    },
    {
      "id": "visual-art-pixel-provenance-10",
      "theme": "visual-art",
      "title": "Pixel Provenance",
      "pitch": "Securely mint pixel art pieces with immutable creator provenance and IPFS metadata.",
      "subDiscipline": "pixel art authentication",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 on Sepolia anchors pixel art provenance for collectors and artists alike.",
      "megaPrompt": "Build \"Pixel Provenance\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecurely mint pixel art pieces with immutable creator provenance and IPFS metadata.\nDiscipline: Visual Art (pixel art authentication).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 on Sepolia anchors pixel art provenance for collectors and artists alike.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/PixelProvenance.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title PixelProvenance\n/// @notice ERC-721 provenance for: Securely mint pixel art pieces with immutable creator provenance and IPFS metadata.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract PixelProvenance is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"PixelProvenance\", \"PIXELP\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a pixel art authentication artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$250M — pixel art market",
      "som": "$5M — NFT pixel art provenance"
    },
    {
      "id": "visual-art-exhibit-echo-11",
      "theme": "visual-art",
      "title": "Exhibit Echo",
      "pitch": "Preserve virtual gallery exhibits with creator-owned NFT provenance linked to IPFS.",
      "subDiscipline": "virtual exhibition records",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "Immutable NFT minting captures and certifies virtual exhibit provenance onchain.",
      "megaPrompt": "Build \"Exhibit Echo\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nPreserve virtual gallery exhibits with creator-owned NFT provenance linked to IPFS.\nDiscipline: Visual Art (virtual exhibition records).\nOnchain primitive: NFT provenance mint. Why this primitive: Immutable NFT minting captures and certifies virtual exhibit provenance onchain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ExhibitEcho.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ExhibitEcho\n/// @notice ERC-721 provenance for: Preserve virtual gallery exhibits with creator-owned NFT provenance linked to IPFS.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ExhibitEcho is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ExhibitEcho\", \"EXHIBI\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a virtual exhibition records artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$1B — virtual exhibition revenues",
      "som": "$12M — NFT exhibit provenance buyers"
    },
    {
      "id": "visual-art-storyboard-stamp-12",
      "theme": "visual-art",
      "title": "Storyboard Stamp",
      "pitch": "Mint sequential NFTs to authenticate evolving concept art storyboards and ideas.",
      "subDiscipline": "concept art progression",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 tokens create immutable chains of concept art evolution on the blockchain.",
      "megaPrompt": "Build \"Storyboard Stamp\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint sequential NFTs to authenticate evolving concept art storyboards and ideas.\nDiscipline: Visual Art (concept art progression).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 tokens create immutable chains of concept art evolution on the blockchain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/StoryboardStamp.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title StoryboardStamp\n/// @notice ERC-721 provenance for: Mint sequential NFTs to authenticate evolving concept art storyboards and ideas.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract StoryboardStamp is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"StoryboardStamp\", \"STORYB\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a concept art progression artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$350M — concept art services",
      "som": "$6M — provenance NFT storyboards"
    },
    {
      "id": "visual-art-masterstroke-mint-13",
      "theme": "visual-art",
      "title": "Masterstroke Mint",
      "pitch": "Create immutable certificates of authenticity for signature artworks via NFT provenance mint.",
      "subDiscipline": "signature artwork certs",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT provenance ensures unforgeable certification linked to IPFS-stored artwork data.",
      "megaPrompt": "Build \"Masterstroke Mint\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate immutable certificates of authenticity for signature artworks via NFT provenance mint.\nDiscipline: Visual Art (signature artwork certs).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT provenance ensures unforgeable certification linked to IPFS-stored artwork data.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MasterstrokeMint.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title MasterstrokeMint\n/// @notice ERC-721 provenance for: Create immutable certificates of authenticity for signature artworks via NFT provenance mint.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract MasterstrokeMint is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"MasterstrokeMint\", \"MASTER\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a signature artwork certs artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$9B — art certification services",
      "som": "$100M — NFT-based authenticity certificates"
    },
    {
      "id": "visual-art-layer-ledger-14",
      "theme": "visual-art",
      "title": "Layer Ledger",
      "pitch": "Track and mint individual painting layers proving creative process and ownership.",
      "subDiscipline": "digital painting layers",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 minting uniquely links layered IPFS CIDs with artist ownership proofs.",
      "megaPrompt": "Build \"Layer Ledger\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nTrack and mint individual painting layers proving creative process and ownership.\nDiscipline: Visual Art (digital painting layers).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 minting uniquely links layered IPFS CIDs with artist ownership proofs.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/LayerLedger.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title LayerLedger\n/// @notice ERC-721 provenance for: Track and mint individual painting layers proving creative process and ownership.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract LayerLedger is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"LayerLedger\", \"LAYERL\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a digital painting layers artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$400M — digital art creation tools",
      "som": "$7M — provenance NFTs of layers"
    },
    {
      "id": "visual-art-artchain-auction-15",
      "theme": "visual-art",
      "title": "Artchain Auction",
      "pitch": "Integrate provenance minting into art auctions to verify authenticity and creator royalties.",
      "subDiscipline": "secondary art sales",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "Onchain NFT provenance mints enable transparent ownership and royalty enforcement.",
      "megaPrompt": "Build \"Artchain Auction\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nIntegrate provenance minting into art auctions to verify authenticity and creator royalties.\nDiscipline: Visual Art (secondary art sales).\nOnchain primitive: NFT provenance mint. Why this primitive: Onchain NFT provenance mints enable transparent ownership and royalty enforcement.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ArtchainAuction.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ArtchainAuction\n/// @notice ERC-721 provenance for: Integrate provenance minting into art auctions to verify authenticity and creator royalties.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ArtchainAuction is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ArtchainAuction\", \"ARTCHA\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a secondary art sales artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$15B — secondary art market",
      "som": "$300M — NFT provenance auction fees"
    },
    {
      "id": "visual-art-color-code-vault-16",
      "theme": "visual-art",
      "title": "Color Code Vault",
      "pitch": "Securely store and prove origin of proprietary digital color codes with mintable NFTs.",
      "subDiscipline": "digital color codes",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 provenance mint guarantees creator ownership and IPFS hash immutability.",
      "megaPrompt": "Build \"Color Code Vault\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nSecurely store and prove origin of proprietary digital color codes with mintable NFTs.\nDiscipline: Visual Art (digital color codes).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 provenance mint guarantees creator ownership and IPFS hash immutability.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ColorCodeVault.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ColorCodeVault\n/// @notice ERC-721 provenance for: Securely store and prove origin of proprietary digital color codes with mintable NFTs.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ColorCodeVault is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ColorCodeVault\", \"COLORC\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a digital color codes artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$200M — digital color tool market",
      "som": "$3M — NFT color code licenses"
    },
    {
      "id": "visual-art-art-mentor-mark-17",
      "theme": "visual-art",
      "title": "Art Mentor Mark",
      "pitch": "Mint NFTs to certify mentorship lineage and creative influence among artists.",
      "subDiscipline": "art mentorship proof",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 tokens create verifiable mentorship provenance anchored on the blockchain.",
      "megaPrompt": "Build \"Art Mentor Mark\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint NFTs to certify mentorship lineage and creative influence among artists.\nDiscipline: Visual Art (art mentorship proof).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 tokens create verifiable mentorship provenance anchored on the blockchain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ArtMentorMark.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ArtMentorMark\n/// @notice ERC-721 provenance for: Mint NFTs to certify mentorship lineage and creative influence among artists.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ArtMentorMark is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ArtMentorMark\", \"ARTMEN\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a art mentorship proof artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$500M — art education platforms",
      "som": "$8M — NFT mentorship certificates"
    },
    {
      "id": "visual-art-signature-seal-18",
      "theme": "visual-art",
      "title": "Signature Seal",
      "pitch": "Allow artists to mint unique digital signatures as NFT proof of authorship and authenticity.",
      "subDiscipline": "digital signature minting",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "Immutable NFT mint links digital signature to IPFS asset verifiably onchain.",
      "megaPrompt": "Build \"Signature Seal\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nAllow artists to mint unique digital signatures as NFT proof of authorship and authenticity.\nDiscipline: Visual Art (digital signature minting).\nOnchain primitive: NFT provenance mint. Why this primitive: Immutable NFT mint links digital signature to IPFS asset verifiably onchain.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/SignatureSeal.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title SignatureSeal\n/// @notice ERC-721 provenance for: Allow artists to mint unique digital signatures as NFT proof of authorship and authenticity.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract SignatureSeal is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"SignatureSeal\", \"SIGNAT\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a digital signature minting artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$1B — digital signature services",
      "som": "$12M — NFT signature deployments"
    },
    {
      "id": "visual-art-nft-mirror-19",
      "theme": "visual-art",
      "title": "NFT Mirror",
      "pitch": "Mint paired NFTs representing original and reflected artworks to showcase creative duality.",
      "subDiscipline": "artistic reflection sets",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 tokens prove paired provenance anchored to immutable IPFS data sets.",
      "megaPrompt": "Build \"NFT Mirror\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint paired NFTs representing original and reflected artworks to showcase creative duality.\nDiscipline: Visual Art (artistic reflection sets).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 tokens prove paired provenance anchored to immutable IPFS data sets.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/NFTMirror.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title NFTMirror\n/// @notice ERC-721 provenance for: Mint paired NFTs representing original and reflected artworks to showcase creative duality.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract NFTMirror is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"NFTMirror\", \"NFTMIR\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a artistic reflection sets artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$250M — contemporary art niche",
      "som": "$4M — NFT paired artwork sales"
    },
    {
      "id": "visual-art-art-rewind-20",
      "theme": "visual-art",
      "title": "Art Rewind",
      "pitch": "Mint stepwise NFTs documenting artwork creation stages for replay and provenance.",
      "subDiscipline": "creative process playback",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 minting onchain links IPFS CIDs capturing each creative moment forever.",
      "megaPrompt": "Build \"Art Rewind\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint stepwise NFTs documenting artwork creation stages for replay and provenance.\nDiscipline: Visual Art (creative process playback).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 minting onchain links IPFS CIDs capturing each creative moment forever.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ArtRewind.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ArtRewind\n/// @notice ERC-721 provenance for: Mint stepwise NFTs documenting artwork creation stages for replay and provenance.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ArtRewind is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ArtRewind\", \"ARTREW\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a creative process playback artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$350M — digital art tutorials",
      "som": "$6M — NFT creative process records"
    },
    {
      "id": "visual-art-gallery-provenance-21",
      "theme": "visual-art",
      "title": "Gallery Provenance",
      "pitch": "Digitally certify artwork origin and gallery exhibition history onchain with NFTs.",
      "subDiscipline": "physical gallery tracking",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "NFT provenance mint ensures transparent gallery provenance authenticated on Sepolia.",
      "megaPrompt": "Build \"Gallery Provenance\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nDigitally certify artwork origin and gallery exhibition history onchain with NFTs.\nDiscipline: Visual Art (physical gallery tracking).\nOnchain primitive: NFT provenance mint. Why this primitive: NFT provenance mint ensures transparent gallery provenance authenticated on Sepolia.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/GalleryProvenance.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title GalleryProvenance\n/// @notice ERC-721 provenance for: Digitally certify artwork origin and gallery exhibition history onchain with NFTs.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract GalleryProvenance is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"GalleryProvenance\", \"GALLER\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a physical gallery tracking artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$6B — gallery exhibited artwork",
      "som": "$90M — NFT gallery provenance usage"
    },
    {
      "id": "visual-art-nft-colorgram-22",
      "theme": "visual-art",
      "title": "NFT Colorgram",
      "pitch": "Create NFTs representing exclusive color-themed artworks with immutable provenance.",
      "subDiscipline": "chromatic art NFTs",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 mint links color-centric art stored on IPFS with verified creator proof.",
      "megaPrompt": "Build \"NFT Colorgram\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nCreate NFTs representing exclusive color-themed artworks with immutable provenance.\nDiscipline: Visual Art (chromatic art NFTs).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 mint links color-centric art stored on IPFS with verified creator proof.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/NFTColorgram.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title NFTColorgram\n/// @notice ERC-721 provenance for: Create NFTs representing exclusive color-themed artworks with immutable provenance.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract NFTColorgram is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"NFTColorgram\", \"NFTCOL\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a chromatic art NFTs artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$500M — color-focused art collectors",
      "som": "$7M — NFT color art trading"
    },
    {
      "id": "visual-art-motion-mint-23",
      "theme": "visual-art",
      "title": "Motion Mint",
      "pitch": "Mint creator-owned NFTs for animated illustrations backed by IPFS-stored assets.",
      "subDiscipline": "animated illustration ownership",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "ERC-721 provenance mint guarantees creator control and provenance of animation files.",
      "megaPrompt": "Build \"Motion Mint\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nMint creator-owned NFTs for animated illustrations backed by IPFS-stored assets.\nDiscipline: Visual Art (animated illustration ownership).\nOnchain primitive: NFT provenance mint. Why this primitive: ERC-721 provenance mint guarantees creator control and provenance of animation files.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/MotionMint.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title MotionMint\n/// @notice ERC-721 provenance for: Mint creator-owned NFTs for animated illustrations backed by IPFS-stored assets.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract MotionMint is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"MotionMint\", \"MOTION\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a animated illustration ownership artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$600M — animated illustration market",
      "som": "$9M — NFT animated art sales"
    },
    {
      "id": "visual-art-provenance-palette-24",
      "theme": "visual-art",
      "title": "Provenance Palette",
      "pitch": "Digitize and mint physical palette images as NFTs preserving unique artist color mixes.",
      "subDiscipline": "physical palette digitization",
      "quantumHook": "NFT provenance mint",
      "quantumHookId": "nft-provenance",
      "quantumTag": "onchain authorship",
      "quantumRationale": "Immutable NFT mint authenticates physical palette provenance via IPFS hashes.",
      "megaPrompt": "Build \"Provenance Palette\" in ONE Lovable message. Single-page demo.\n\nCONCEPT\nDigitize and mint physical palette images as NFTs preserving unique artist color mixes.\nDiscipline: Visual Art (physical palette digitization).\nOnchain primitive: NFT provenance mint. Why this primitive: Immutable NFT mint authenticates physical palette provenance via IPFS hashes.\n\n5-CREDIT BUDGET (HARD LIMIT):\n- ONE single-page app. No router, no Lovable Cloud, no database, no auth flows beyond Privy drop-in.\n- ONE Solidity contract, <=80 lines, deployed to Sepolia, verified on Etherscan.\n- Privy is always the auth + sponsored-tx layer (Google login, embedded wallet).\n- Pinata/IPFS only if the idea genuinely needs to store a file or metadata.\n- At most ONE AI call per user action (use Lovable AI Gateway with LOVABLE_API_KEY if AI is part of the idea).\n- Skip tests, skip CI, skip docs pages. Ship the demo, nothing else.\n\nSTACK\n- React + Vite single page (the index route).\n- SSR-safe Privy mount is mandatory. Never import @privy-io/react-auth at\n  module scope of a route file — it crashes SSR. Use\n  lazy(() => import('./privy-client-entry')) inside <ClientOnly> + <Suspense>,\n  and put <PrivyProvider> only inside privy-client-entry.tsx.\n- PrivyProvider config (do NOT stub defaultChain as { id, name } — omit it\n  or pass viem's `sepolia`; chainId is passed per-call):\n    <PrivyProvider appId={import.meta.env.VITE_PRIVY_APP_ID}\n      config={{ loginMethods:['google','email'],\n                embeddedWallets:{ ethereum:{ createOnLogin:'users-without-wallets' } },\n                appearance:{ theme:'dark' } }}>\n- Read the embedded wallet from useWallets, not user.wallet:\n    const embedded = wallets.find(w => w.walletClientType === 'privy');\n- Every send goes through Privy `useSendTransaction` with BOTH `address`\n  and `sponsor`, wrapped in a 45s Promise.race timeout whose reject message\n  names the exact dashboard toggles:\n    await Promise.race([\n      sendTransaction(\n        { to, data, chainId: 11155111 },\n        { address: embedded.address, sponsor: true }\n      ),\n      new Promise((_, r) => setTimeout(() => r(new Error(\n        \"Privy sendTransaction timed out after 45s. Enable Gas sponsorship -> App pays -> Ethereum Sepolia -> Allow transactions from the client.\"\n      )), 45_000)),\n    ]);\n- Do NOT pass uiOptions:{ showWalletUIs:false } — it aborts with\n  \"signal is aborted without reason\". The approval sheet still shows on\n  the embedded-EOA path; the fee reads US$0.00.\n- Do NOT add ZeroDev / SmartWalletsProvider / a paymaster URL. Native\n  Privy sponsorship on Sepolia works with the toggles above and nothing else.\n- DASHBOARD PREREQUISITE (one-time): Privy dashboard -> Gas sponsorship\n  -> App pays -> add \"Ethereum Sepolia\" -> toggle \"Allow transactions\n  from the client\" ON. Without this, sendTransaction hangs silently.\n- src/lib/pinata.ts uploads via `fetch('https://api.pinata.cloud/pinning/pinFileToIPFS', { method:'POST', headers:{ Authorization: `Bearer ${import.meta.env.VITE_PINATA_JWT}` }, body: fd })`.\n- Hardhat in /contracts (kept outside the Vite bundle). Install\n  `@nomicfoundation/hardhat-toolbox` AND `@nomicfoundation/hardhat-verify@latest`\n  (>=3.x — older versions still hit Etherscan v1 and fail with\n  \"You are using a deprecated V1 endpoint, switch to Etherscan API V2\").\n- hardhat.config.cjs MUST use the Etherscan v2 single-key shape (NOT the per-network map):\n    require(\"@nomicfoundation/hardhat-toolbox\");\n    require(\"@nomicfoundation/hardhat-verify\");\n    module.exports = {\n      solidity: { version: \"0.8.24\", settings: { optimizer: { enabled: true, runs: 200 } } },\n      networks: { sepolia: {\n        url: process.env.SEPOLIA_RPC_URL || \"https://ethereum-sepolia-rpc.publicnode.com\",\n        accounts: [process.env.METAMASK_PRIVATE_KEY.startsWith(\"0x\")\n          ? process.env.METAMASK_PRIVATE_KEY : \"0x\" + process.env.METAMASK_PRIVATE_KEY],\n        chainId: 11155111,\n      } },\n      etherscan: { apiKey: process.env.ETHERSCAN_API_KEY },   // single string, NOT { sepolia: ... }\n      sourcify: { enabled: false },                            // silences the v2.x prompt\n    };\n- Deploy: `npx hardhat run scripts/deploy.cjs --network sepolia`.\n- Verify (run RIGHT AFTER deploy, no constructor args for these contracts):\n  `npx hardhat verify --network sepolia <address>`\n  On success Etherscan returns \"Successfully verified contract … on the block explorer\"\n  and the source becomes readable at\n  `https://sepolia.etherscan.io/address/<address>#code`.\n- Write the deployed address to `src/data/contract.json` so the UI links to\n  `https://sepolia.etherscan.io/address/<address>`.\n\nCONTRACT (contracts/ProvenancePalette.sol):\n```solidity\n// SPDX-License-Identifier: MIT\npragma solidity ^0.8.24;\nimport \"@openzeppelin/contracts/token/ERC721/ERC721.sol\";\n/// @title ProvenancePalette\n/// @notice ERC-721 provenance for: Digitize and mint physical palette images as NFTs preserving unique artist color mixes.\n/// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\ncontract ProvenancePalette is ERC721 {\n    uint256 public nextId;\n    mapping(uint256 => string) public cidOf;\n    constructor() ERC721(\"ProvenancePalette\", \"PROVEN\") {}\n    /// @notice Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n    function mint(string calldata cid) external returns (uint256 id) {\n        id = ++nextId; cidOf[id] = cid; _safeMint(msg.sender, id);\n    }\n    function tokenURI(uint256 id) public view override returns (string memory) {\n        return string(abi.encodePacked(\"ipfs://\", cidOf[id]));\n    }\n}\n```\n\nUSER FLOW\n1. Land on page -> 'Sign in with Google' (Privy) -> embedded wallet auto-provisioned.\n2. After the user creates a physical palette digitization artefact, pin the file to IPFS via Pinata, then call `mint(cid)` on the deployed contract through Privy's sponsored transaction. Show tokenId, IPFS preview (`https://gateway.pinata.cloud/ipfs/<cid>`), and Etherscan mint-tx link.\n3. Footer renders: \"Built during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\"\n\nREQUIRED SECRETS (Lovable -> Project Settings -> Secrets):\n- METAMASK_PRIVATE_KEY  Sepolia deployer key. Fund it: https://cloud.google.com/application/web3/faucet/ethereum/sepolia\n- SEPOLIA_RPC_URL       Alchemy Sepolia HTTPS endpoint (https://eth-sepolia.g.alchemy.com/v2/<key>). Create a free app at https://dashboard.alchemy.com/ -> copy the HTTPS URL. Public RPCs throttle/fail under hackathon load — Alchemy is required.\n- ETHERSCAN_API_KEY     For `npx hardhat verify`. Get: https://etherscan.io/myapikey\n- PRIVY_APP_ID          Google sign-in + sponsored tx. Docs: https://docs.privy.io/llms-full.txt\n- PINATA_JWT            IPFS uploads (only if app pins media). Docs: https://docs.pinata.cloud/llms-full.txt\n\nCREDIT (must appear in UI footer AND as NatSpec on every deployed contract):\nBuilt during the Creative AI & Quantum Hackathon organised by StreetKode Fam during Indian Krump Festival 14\n",
      "tam": "$65B — global visual art market",
      "sam": "$50M — art collector memorabilia",
      "som": "$1.2M — NFT palette collector sales"
    }
  ]
}